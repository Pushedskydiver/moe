# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 4)

Updated 2026-07-16 end-Session-4 — **Stage 2's Sarah loop is live and verified against real Slack infrastructure: chunks 2.2 and 2.3 both shipped, plus a process retrospective that formalized real gaps in the review-gate docs themselves.** Picked up exactly where Session 3's loading instructions left off (chunk 2.2). This entry is written at Alex's prompt, for the same reason as Session 3's own retroactive entry: `CLAUDE.md`'s phase-boundary handoff trigger fired three times this session (PRs #22/#23/#24 merging) and wasn't acted on until asked — see "Major novel patterns" below, this is not a new problem, it's the same one recurring.

**Side-fix — VISION §10 Sonnet-5 model-tier note**: [PR #22](https://github.com/Pushedskydiver/moe/pull/22) — Alex asked to verify moe's model strategy given Sonnet 5's release before continuing. Deep-research pass verified Sonnet 5 facts against primary sources (Anthropic's own docs + System Card, cross-checked via independent re-fetch); one fabricated third-party benchmark-ranking claim was caught by the workflow's own adversarial verify stage and excluded. Added an informational note to VISION §10 (not a policy change) flagging Riley/Dom as personas to validate against Opus-tier with real data once those chunks exist.

**Chunk 2.2 — Persona process skeleton**: [PR #23](https://github.com/Pushedskydiver/moe/pull/23), merged — `apps/server` boots for real: env-sourced `PersonaConfig` (packages/agents), structured JSON logger with recursive secret redaction, `GET /health`, Dockerfile + fly.toml. Verified with real `docker build`/`run`/`curl` round trips, not just unit tests. Review gate caught 2 MATERIAL bugs (a logger field-spread order bug letting caller fields clobber `level`/`message`/`timestamp`; a missing `server.on('error', ...)` handler that let a bind failure bypass structured logging entirely) plus a self-review-caught test-spy leak risk and `xoxb`-shaped fixtures that could trip a secret scanner.

**Chunk 2.3 — Slack Socket Mode inbound events + hardcoded ack reply**: [PR #24](https://github.com/Pushedskydiver/moe/pull/24), merged — `packages/slack` gets real Socket Mode connectivity: event schema/normalization, bot-authored-message filtering (stops a reply-loop), `handleSocketModeEvent` orchestration, `postMessage` wrapper. `apps/server` replies to every inbound message with a hardcoded, non-persona-voiced ack. Review gate found 1 BLOCKING + 4 MATERIAL on the first DA pass (verified against real `@slack/socket-mode`/`@slack/web-api` source, not inference) — including a live-Docker-caught bug where `exit(1)` never actually terminated the process because a listening HTTP server keeps Node's event loop alive on its own. **Verified end-to-end against Sarah's real Slack app** (live DM in, live hardcoded ack out) — hit one real Slack config gotcha along the way (App Home "Messages Tab" toggle, not a code issue).

**Process retrospective**: [PR #25](https://github.com/Pushedskydiver/moe/pull/25), merged — Alex asked why `copilot-surrogate` was skipped on PR #24's original open (a real gap — its retroactive run found a genuine MATERIAL bug: `postMessage`'s TSDoc claimed `@slack/web-api` could resolve `{ok:false}`; the real SDK always throws) and asked for an audit of other protocols with similar risk. Found three more instances of the same failure shape (see "Major novel patterns"). `docs/DEVELOPMENT.md` formalized Round-2 verification as applying to any review-round tool, not DA-only (previously private-memory-only); two new `docs/RATIONALIZATIONS.md` entries plus two "Caught in: PR #24" citations on existing ones; `eslint.config.ts` excludes `.claude/worktrees/**` (was polluting full-repo lint runs with ~490 unrelated errors from leftover review-agent worktrees). PR #25 itself merged with a DA dispatch still in flight — its result arrived afterward with a real MATERIAL finding on `main` (the R2 discipline wasn't actually wired into the numbered Review Gate steps, only stated in a floating paragraph).

**Process retrospective follow-up — R2 wiring**: [PR #26](https://github.com/Pushedskydiver/moe/pull/26), merged — folded DA's post-merge finding into the numbered Review Gate steps (2, 3, 5, 6, 8) plus the R2-detail section's own disambiguation clause. Two review tools ran on this PR, each R2-confirmed where required: DA found 3 (1 MATERIAL + 2 LOW), all fixed and R2-confirmed as folded. `copilot-surrogate`'s mandatory dispatch (triggered by its own `fix(docs)` commits) ran across three rounds and found 6 total, all LOW after the first: 3 on the first full-file pass (1 MATERIAL + 2 LOW); a misquoted `CLAUDE.md` merge-policy phrase surfaced as an out-of-scope observation during that pass's R2 verification and was folded in directly; 2 more LOW findings (an incomplete `SYNC_TABLE` enumeration, a wrong `docs/CONVENTIONS.md` citation) on a follow-up full-file pass after the merge-policy fix landed. All pre-existing drift unrelated to the PR's own diff was folded into this same PR per `docs/GIT.md`'s drift-fix predicate #1 ("if you have an open feature branch, commit the drift-fix there instead"), since `docs/DEVELOPMENT.md` is also on the blast-radius list regardless. Every review dispatch on this PR completed and was confirmed clean before Alex merged — unlike PR #25.

**Major novel patterns this session:**

1. **The `copilot-surrogate` miss was one instance of a class, not a one-off** — auditing the other protocols found three more: chunk 2.3's self-review pass reused chunk 2.2's remembered checklist instead of re-reading `docs/SELF-REVIEW.md`; the R2-verification discipline existed only in private Claude Code memory, never in the shared repo docs, which is exactly why it fired for DA findings (a practiced habit) but not for a `copilot-surrogate` finding; and the session-handoff trigger recurred (see #5). All four are the same shape: a judgment-based trigger silently skipped under momentum, while mechanical ones (branch discipline, BUILD_PLAN checkbox convention) held up fine. Formalized into `docs/DEVELOPMENT.md` (R2 now applies to any review-round tool, not DA-only) and `docs/RATIONALIZATIONS.md` (two new entries, two citations added to existing ones).
2. **Live Docker/Slack verification caught bugs unit tests alone wouldn't have, twice.** Chunk 2.3's `exit(1)` was verified working in tests, but a real Docker run showed the process never actually terminated — a listening HTTP server keeps Node's event loop alive regardless of `process.exitCode`. Separately, a real Docker run showed `@slack/socket-mode`'s own internal logger writing raw, unredacted lines straight to console, bypassing the redaction chunk 2.2 built. Both fixed and re-verified live, not just re-tested.
3. **Dispatching `copilot-surrogate` properly this time immediately proved the point of dispatching it** — run for real (not skipped) on this retrospective's own PR, it caught a real misattribution in the retrospective's own text: a citation credited a `copilot-surrogate` finding with a mutation-testing verification method that actually belonged to a different (DA-chain) finding. Fixed before this PR opened.
4. **A forward-looking memory's predicted failure mode actually happened.** `secret-pattern-mirror-locations` (written before moe v3 had any secret-redaction code) warned that secret-recognition logic accumulates as independent copies that must be updated together. Chunk 2.3 built a second, independent redaction mechanism (value-based, for the Slack SDK's own free-text logging) alongside chunk 2.2's key-based one — and DA caught exactly the predicted partial-update trap (one client wired, the other one silently wasn't). Memory updated to reflect this is now confirmed, not hypothetical.
5. **The handoff trigger recurred — again, and even a same-session fix didn't stop it.** Session 3's own retrospective named "treat every merged PR as a trigger-evaluation moment" as a lesson after it fired repeatedly and got ignored. It recurred anyway in Session 4 (PRs #22/#23/#24, zero handoffs) — and the `docs/RATIONALIZATIONS.md` entry written _earlier in this same session_ to formalize that exact lesson still didn't prevent Alex from having to prompt for this handoff directly. Two data points now say a written note isn't sufficient on its own. Worth Alex's own call on whether this justifies revisiting `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s declined-automation position — not decided here, flagged for Session 5.

**Earlier Session 3 status, compressed** — Stage 1 shipped in full: ticket types (1.1), the topology/DB ADR reversing single-machine+SQLite to N machines + Neon Postgres on evidence (1.2a), database layer (1.2b), atomic claim (1.3), the anti-fabrication `StatusClaim` schema (1.4), the track-record ADR at N=5 (1.5), the risk-tier classifier (1.6) — plus chunk 2.1, the cast-redline gate (kept the 7-role roster, Sarah confirmed first). Two side-fixes (branch-prefix doc drift, a stale VISION citation). [#12](https://github.com/Pushedskydiver/moe/pull/12)–[#21](https://github.com/Pushedskydiver/moe/pull/21). Durable patterns: a deep-research workflow's own adversarial vote can still pass a fabricated statistic (spot-check primary sources before trusting a specific number); R2 verification caught two real residual issues neither single fix pass had; the handoff trigger fired repeatedly and wasn't acted on (named as a lesson here — see Session 4's pattern #5 above for how that actually went).

**Earlier Session 2 status, compressed** — deep-research test of chief-clancy's auto-handoff conclusion (decline-with-updated-reasons position adopted, `docs/DEVELOPMENT.md` §Session handoff), then chunks 0.6c (reference docs + doc-port decision) and 0.7 (generate:agents-md TS port + CI freshness check), closing Stage 0 entirely. [#9](https://github.com/Pushedskydiver/moe/pull/9)–[#11](https://github.com/Pushedskydiver/moe/pull/11).

**Earlier Session 1 status, compressed** — first working session of the v3 rebuild: repo bootstrap through Stage-0 process docs, chunks 0.1–0.6b shipped 2026-07-04–07-09 ([#1](https://github.com/Pushedskydiver/moe/pull/1)–[#8](https://github.com/Pushedskydiver/moe/pull/8)). Durable patterns: Volta/pnpm Node-24 PATH gotcha (documented for real, no longer memory-only); fixture-testing catches silently-inert config; a stated intention needs the same verification as any other claim.

### Session 5 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect PR #26's squash at HEAD), `git status` clean. `BUILD_PLAN.md` shows chunk 2.2 `[x]` and 2.3 `[~]` — 2.3 is merged and content-complete (PR #24), flip to `[x]` is chunk 2.4a's own first edit, matching established precedent.
- **Primary workstream, part 1 — DONE.** The process-hardening PR landed as two PRs (#25, then a post-merge DA finding's own fix as #26 — see the two entries above), both merged. PR #26 itself is fully review-verified (DA + `copilot-surrogate`, R2-confirmed for every BLOCKING/MATERIAL finding it raised) — PR #25's own review gap (a DA MATERIAL finding that merged before R2 could confirm it) is exactly what PR #26 exists to close, not something retroactively fixed on #25 itself. No further action needed here.
- **Primary workstream, part 2 — run the deep-research pass on protocol/workflow reliability Alex asked for.** Scope: (a) practitioner sources — Matt Pocock, Addy Osmani, Simon Willison, Armin Ronacher, Anthropic's own published Claude Code engineering guidance — specifically on how they structure multi-agent review/verification pipelines and whether they've written on gate reliability (do optional-but-recommended checks actually get run consistently); (b) academic sources — LLM self-monitoring/rule-adherence literature, the closest match to what actually happened this session (a discretionary check silently not run, not a gamed check — related to but distinct from the SpecBench self-verification-gaming finding already cited in `docs/VISION.md` §8). Evaluate against three candidate structural fixes already named in conversation (not yet decided): always-on `copilot-surrogate` (removes the judgment call, costs more per PR); a mechanical CI gate detecting blast-radius/doc-touching diffs and requiring evidence of a `copilot-surrogate` run; a dedicated small "gate-checker" step separate from the context doing the actual work. Present findings + a recommendation to Alex, don't just implement one unilaterally — this is exactly the kind of decision `docs/decisions/` exists for.
- **Primary workstream, part 3 — resume BUILD_PLAN Stage 2, chunk 2.4a** ("LLM reply loop, stateless") once the above two are settled. Wires the raw Anthropic Messages API (per `docs/VISION.md` §11's verified model-client choice) for a single-turn DM reply in a deliberately generic, non-persona placeholder voice — explicitly not the real persona voice, which stays behind the Stage-5 do-not-touch gate. Not `[GATE]`.
- **Decision branches:**
  - A. If the process-hardening PR's research (part 2) concludes a structural fix is worth building (e.g. a CI gate), that itself becomes a new BUILD_PLAN chunk with Alex's sign-off, not a rider on other work — matching how `docs/decisions/SESSION-HANDOFF-AUTOMATION.md` already handles this class of decision.
  - B. Stage 2's exit criterion (one persona on Fly, her own Slack App, responds to a DM, every status statement carries §7.6 evidence) isn't met until 2.4a/2.4b land — 2.2/2.3 are done, 2.4a/2.4b are the remaining load-bearing sequence, don't skip ahead.
  - C. If a research-backed decision comes up again, independently spot-check any specific statistic before it lands in a permanent doc — see the `deep-research-numeric-claims-need-spot-check` memory.
- **Carry-overs:**
  - **The handoff trigger has now been missed twice in a row** (Session 3, then Session 4 despite a same-session doc fix) — see Session 4's pattern #5 above. Treat this session's own PR merges as real trigger-evaluation moments; don't wait for a third prompt.
  - `docs/decisions/` holds four ADRs, none trimmed to the ~50-line "decisions-only" budget yet (`docs/decisions/README.md`'s lifecycle step 3) — flagged three times now across review gates without being acted on. Three of four are already close to the target size (37–58 lines); this is a content-judgment task on historical records, not something to do unilaterally — check with Alex first.
  - Sarah's real Slack App exists and is verified working end-to-end (live DM → live ack, confirmed Session 4) — credentials are in a gitignored `.env.local`, template at `.env.example`.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - Branch discipline: create the dedicated branch as the literal first action after syncing `main`, before any edit, every single chunk transition.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite.
  - Local dev needs a real Postgres for `packages/core`'s tests (`DATABASE_URL`) — CI provisions its own via a `postgres:17-alpine` service container; locally, a throwaway Docker container works (`docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=... -e POSTGRES_DB=... postgres:17-alpine`).
- **Fallback:** if Alex redirects on load, follow that; otherwise default to the primary workstream above, in order (PR first, then research, then 2.4a).

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk | Status | Shipped    | Headline                                                                                            |
| ----- | ------ | ---------- | --------------------------------------------------------------------------------------------------- |
| 0.1   | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                |
| 0.2   | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                            |
| 0.3   | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                     |
| 0.4   | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                |
| 0.5   | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                 |
| 0.6a  | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved      |
| 0.6b  | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)      |
| 0.6c  | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision    |
| 0.7   | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**             |
| 1.1   | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                           |
| 1.2a  | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)           |
| 1.2b  | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                       |
| 1.3   | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                    |
| 1.4   | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                             |
| 1.5   | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`) |
| 1.6   | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**         |
| 2.1   | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)         |
| 2.2   | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config    |
| 2.3   | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app     |
