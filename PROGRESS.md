# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 9)

Updated 2026-07-18 end-Session-9 — **Stage 2 finished (core-hours guard + away-detection), and Stage 3's intake cascade now has both halves of its High-band action built: composing a draft (3.4a-i) and the reaction-gate that would resolve one (3.4a-ii) — neither wired to a live consumer yet, both waiting on 3.4a-iii's situational-appropriateness gate.** Seven PRs shipped this session, each with a real review arc — 2.7a and 2.7b closed out Stage 2 (both had open BUILD_PLAN parameters Alex confirmed via `AskUserQuestion` before any code moved, per the established pattern), chunk 3.1 opened Stage 3 with the actual gate conversation BUILD_PLAN flagged (surfacing a genuine disagreement between two independent review passes on a load-bearing external fact, resolved only by an independent live re-check), chunk 3.2 shipped Stage 0's primitive (the first chunk run under Ultracode, review-gate dispatches through the Workflow tool), chunk 3.3 wired the classifier gate in — the deepest review arc of the session at the time (two DA rounds, a split `copilot-surrogate` pass after it hit its own 20-file scope ceiling for the first time) and a real production behavior change (ambient channel messages stop getting an auto-chat-reply, replaced by silent classification) made and confirmed with Alex before any code moved — chunk 3.4a-i wired the first High-band action (an LLM-composed ticket draft), shadow/log-only, into the cascade, and chunk 3.4a-ii built the reaction-gate mechanism that would resolve a draft (commit/redo/park), deliberately scoped to a tested-but-not-live-wired primitive, same shape as 2.7a/2.7b/3.2's own precedent.

**Chunk 2.7a — core-hours + bank-holiday guard** ([PR #37](https://github.com/Pushedskydiver/moe/pull/37)): `evaluateOperatingRhythm` (`packages/core/src/core-hours/`) — a pure DST-safe weekday/clock check (`Intl`-based against Europe/London, no date library dependency) composed with a GOV.UK bank-holidays fetch cached via a new `Cached<T,E>` class, the first real implementation of `docs/CONVENTIONS.md`'s previously-hypothetical "cache-with-serve-stale-on-failure" pattern. Fails CLOSED (treats the day as non-core-hours) on a cold-boot cache miss, matching VISION §14's hard "never operate on a bank holiday" rule. Three parameters BUILD_PLAN flagged as needing Alex's confirmation before code moved — asked via `AskUserQuestion`, not defaulted: core hours **08:30–17:00 Europe/London Mon–Fri**; holiday source **GOV.UK bank-holidays API (england-and-wales)**, cache + serve-stale-on-failure; **direct DM replies exempt entirely** (always reply regardless of core hours — reactive engagement, not Moe acting unprompted, so §14's rest rule doesn't reach it — meaning no change to the existing reply path at all). No consumer wired yet; intake (3.4a-i), the pull loop (6.1a-i), and the ceremony scheduler (7.2a) build on this later. Review arc: DA review found 1 Critical (an unguarded `Response.json()` on a malformed GOV.UK response body would crash a long-running persona process on a transient upstream hiccup — reviewer live-reproduced it) + 3 Low (DST transition-day test coverage was summer/winter-only, not the actual clock-change days; an explicit documented decision on why `Cached<T,E>` isn't publicly exported yet — one consumer today, promote at the second; `evaluate*` added to the verb-vocabulary list). R2 verification confirmed the fold and caught one more Low inside it (the new `evaluate*` wording didn't actually hold for `evaluateOperatingRhythm`, one of its own two cited precedents — it calls an injected dependency). Self-review then turned an uncommented `as T` cast into a real design fix — collapsed `Cached`'s two independently-nullable fields into one, removing the cast structurally rather than commenting around it. `copilot-surrogate`'s mandatory pass (diff >50 LOC, external-API claims) found 1 MATERIAL (`docs/REVIEW-PATTERNS.md`'s own intro paragraph still called the guard module "not existing yet" — directly contradicted by this same PR's edit to that pattern's status two paragraphs below) + 1 Low (`docs/CONVENTIONS.md`'s `Cached<T>` bullet said the refresh flag goes "to the fetch function" when it actually goes to the cache's own `get()` accessor) — both folded and R2-confirmed.

**Live verification** — no Slack UI exists yet for this chunk (no consumer wired), so verification ran directly against the real GOV.UK API instead: a live fetch pulled all 83 real holiday dates; `evaluateOperatingRhythm` correctly classified real "now" as outside-window, correctly classified 2026-12-25 (Christmas Day) as a bank holiday, and correctly failed closed against a simulated total-failure cache.

**Chunk 2.7b — away-detection** ([PR #38](https://github.com/Pushedskydiver/moe/pull/38)): `isAway`/`fetchSlackStatus` (`packages/slack/src/away-detection/`) — resolves VISION Appendix A's "Slack-status-away parsing conventions" question, same in-place-resolution precedent chunk 2.1 set for "Designer-role timing" in that same paragraph. Alex confirmed the keyword list via `AskUserQuestion`: text keywords `away`/`holiday`/`OOO` (BUILD_PLAN's own illustrative list, kept as-is, whole-word case-insensitive match — a bare substring check would false-positive inside `giveaway`/`runaway`); emoji shortcodes widened past BUILD_PLAN's two named examples (🌴🏖️) to also cover travel/illness signals (✈️🤒🌡️🏥), deliberately excluding ambiguous momentary-busy signals (🔴 DND, 🏠 WFH). Every shortcode was verified against the real `iamcal/emoji-data` database rather than guessed — caught a second live instance of the "Slack emoji shortcode gotcha" (🏖️ is `beach_with_umbrella`, not the more obvious-sounding `beach_umbrella`). No consumer wired yet; first real consumer is 7.2b. Review arc: DA review found 2 MATERIAL (`fetchSlackStatus` had zero runtime schema validation on the external Slack response, unlike `packages/core`'s own model example — added a Zod schema + `safeParse`; `BUILD_PLAN.md` prematurely claimed a still-in-progress OAuth-scope fix as a completed past action — reworded to reflect the real pending state) + 3 Low (type duplication, missing regex-metacharacter test coverage — which self-discovered and documented a real, non-obvious `\b`-boundary limitation along the way — misplaced TSDoc). R2 verification independently re-derived every claim against ground truth (ran the actual Zod schema against 9 malformed payloads, mutation-tested the knip fix) rather than trusting the fold commit's own description — confirmed clean. `copilot-surrogate`'s mandatory pass (diff >50 LOC, touches the blast-radius `docs/VISION.md`) came back with **zero findings** — the first fully clean mandatory pass this session, ~27 claims independently verified (live-refetched the emoji database, read the actual installed `@slack/web-api` source to confirm a throw-behavior claim, reproduced the `\b`-boundary claim with a real Node one-liner).

**A real environment gap surfaced mid-chunk, not a code bug:** live-verifying `fetchSlackStatus` hit `missing_scope` — Sarah's Slack App didn't have `users.profile:read`. Alex added the scope and reinstalled out-of-band; once confirmed, live verification against the real API passed on all fronts — a real fetch of Alex's actual (empty) status round-tripped correctly through the Zod schema, `isAway` correctly classified both the real status and fabricated away statuses (without mutating Alex's real Slack profile), and a real invalid-user-ID call correctly hit the genuine `user_not_found` error path.

**Chunk 3.1 — Stage-1 classifier scoring method + thresholds** ([PR #39](https://github.com/Pushedskydiver/moe/pull/39), [`STAGE-1-CLASSIFIER.md`](docs/decisions/STAGE-1-CLASSIFIER.md)): resolves VISION §5.2's open question — scoring method and High/Mid/Low thresholds — via a real 24-message synthetic eval (8 clear work requests, 8 clear banter, 8 deliberately ambiguous/borderline) run against both Claude Haiku 4.5 and Claude Sonnet 5, not decided from theory alone. A `[GATE]` chunk per BUILD_PLAN's own flag — opened with a real design conversation with Alex rather than a quick-confirm; two sub-decisions inside that conversation (bundled vs. separate scoring call; synthetic vs. sourced eval messages) were each then confirmed via `AskUserQuestion` once the shape of the decision was clear. Decision: one bundled structured-output call per message (`output_config: {format: {type: "json_schema", schema}}`) returning a single 0–100 integer confidence score, on **Claude Haiku 4.5** — the eval showed Haiku separating work-signal (72–95, two sub-clusters: 85–95 pure work, 72–85 ambiguous-but-real) from non-actionable (≤35) with a clean 36-point empty band between them, while Sonnet 5's identical eval showed a messier spread (its own "coffee machine" outlier landed at 55, inside its own ambiguous cluster) for 2–3× the cost. Thresholds: **High ≥ 70, Mid 35–69, Low < 35** — the High floor sits 2 points below Haiku's lowest genuine-signal score (72); the strict `< 35` Low ceiling deliberately lands the eval's own "coffee machine is broken again" message (scored exactly 35) in Mid rather than Low, matching the ADR's own Decision 4 that a broken-facilities complaint is more actionable than pure banter. `docs/VISION.md` §5.2's open-question paragraph resolved in place (same precedent as chunks 2.1 and 2.7b), `docs/GLOSSARY.md` gained a new "Intake & triage (VISION §5)" section, `BUILD_PLAN.md`'s 3.1 checkbox flipped. Explicitly a spike per BUILD_PLAN's own scope — throwaway eval code, no production wiring; the classifier gate itself is chunk 3.3.

**This chunk's review arc ran deeper than a normal ADR-only chunk, catching three genuinely distinct classes of problem.** Self-review caught a real factual error in the draft before DA even ran — the original text claimed no eval message landed in the 35–70 threshold gap, but the coffee-machine message scored exactly 35 and (under the adopted strict `<35` formula) falls in Mid, not Low; rewritten to describe this as the threshold working as intended, per the ADR's own Decision 4 reasoning. DA review then found the ADR blended two distinct score clusters under one ambiguous claim (Decision 2's "scored ≥72" language conflicting with a later "Haiku's 85" comparison in the same sentence, without stating these described different subpopulations) — fixed by explicitly separating the pure-work (85–95) and ambiguous-but-real (72–85) clusters. `copilot-surrogate`'s mandatory pass (diff touches the blast-radius `docs/VISION.md`, external-API claims) then found the DA-fold itself had introduced a fresh inaccuracy in the opposite direction from the original: the ADR claimed prefill was "dead on every current-generation model," which is false for Claude Haiku 4.5 — the ADR's own adopted model, not mentioned in any prefill-restriction list, live or bundled. A prior DA-fold on a related LOW finding had already softened a Fable-5-specific claim to "not supported, status not stated," but a live WebFetch of the current Anthropic migration guide showed the guide explicitly states Fable 5 _does_ return a 400 — meaning that softened "fix" was itself wrong. With DA's fold and `copilot-surrogate`'s finding directly disagreeing on a load-bearing external fact, resolved it by independently re-deriving both points from a fresh live fetch (not trusting either account) before correcting all four locations repeating the claim (the ADR's Context/Decision-1/References sections, `docs/VISION.md`, `docs/GLOSSARY.md`, `BUILD_PLAN.md`) — a second R2 pass then independently re-fetched the same guide and confirmed the corrected version accurate. New memory lesson saved (`reviewer-disagreement-resolution`): when two independent review passes disagree on a load-bearing external fact, resolve it with a fresh independent check, don't defer to the more confident-sounding account.

**Chunk 3.2 — Channel scoping (Stage 0)** ([PR #40](https://github.com/Pushedskydiver/moe/pull/40)): `isSurfaceInScope`/`MessageSurface`/`ChannelScopeConfig` (`packages/core/src/channel-scoping/`) — resolves VISION §5.2's Stage 0 rule ("a message only enters the pipeline if it's in a channel/DM the team already treats as work-relevant"). A `dm` surface is always in scope (§5.3 — a DM to a named persona is already unambiguous, handled by that persona's own app without Sarah in the loop; this predicate short-circuits Stage 0 for that case rather than gating on it, since §5.3 itself doesn't speak to Stage 0 specifically); a `channel` surface is in scope only if its ID is in a configured allow-list (§6.1's roster: `#moe-team`/`#moe-incidents`/`#moe-research` in scope, `#moe-random` out). Unlike 2.7a/2.7b, no `AskUserQuestion` was needed before code moved — both the channel roster and the DM-bypass rule were already settled in VISION, not open parameters this "trivial on purpose" chunk (BUILD_PLAN's own framing) had to resolve. Deliberately no `DEFAULT_ChannelScopeConfig` constant, unlike 2.7a/2.7b's `DEFAULT_*` values — real Slack channel IDs are workspace-specific artifacts with no meaningful code-level default; that resolution is deferred to whichever chunk wires a real listener against this (same "no consumer wired yet, primitive only" pattern as 2.7a/2.7b). Review arc, run for the first time this session via the Workflow tool rather than direct Agent dispatches (Alex turned on Ultracode mid-session): DA review came back "clean" (1 Low — a missing empty-allowlist edge-case test, folded as a 4th unit test); `copilot-surrogate`'s mandatory pass (diff >50 LOC) also came back "clean" (1 Low — a citation over-attributing "Stage 0" to VISION §5.3, which only says "without Sarah in the loop"; §5.3 never mentions Stage 0 at all, that's a §5.2 term — fixed in all three places the phrasing was repeated: the code comment, `docs/GLOSSARY.md`, `BUILD_PLAN.md`). Neither pass surfaced anything BLOCKING/MATERIAL, so no R2 was needed either time — the first chunk this session where R2 was never triggered at all. Live verification not applicable: this chunk's code is a pure predicate with zero I/O, nothing beyond the automated test suite (4 unit tests) to check against a real system.

**Chunk 3.3 — Classifier gate (Stage 1)** ([PR #41](https://github.com/Pushedskydiver/moe/pull/41)): `classifyMessageConfidence` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`) wired into `apps/server`'s inbound-message handler — resolves BUILD_PLAN's own scope: run the classifier silently in prod, logged not acted on, for a few days of real-traffic threshold calibration before 3.4a-i. A real design gap surfaced mid-chunk while tracing the _existing_ handler's actual behavior, not from BUILD_PLAN's own text: the pre-existing reply handler generated a full conversational LLM reply for **every** inbound message — DM or ambient channel/group alike — with no Stage-0/1 distinction at all, a leftover from before Stage 3 existed. Resolved with Alex via a real design conversation, not a quick `AskUserQuestion` default: **DMs-only** — ambient channel/group messages now run Stage 0 (`isSurfaceInScope`, 3.2) then, if in scope, Stage 1 (this chunk), and the score is logged; no reply posted for that surface, ever. Direct, confirmed consequence (confirmed with Alex before deleting, not unilateral): `root-candidate-buffer.ts` (2.4b) and `handleUnthreadedMessage` became entirely unreachable once ambient messages never touch the chat-reply path — removed rather than left dead.

**This chunk had the deepest review arc of the session.** DA review (first pass) found 1 Critical — the classifier's own real, billed Anthropic call shipped completely uncapped/unaccounted-for, bypassing 2.6a/2.6b's cost-cap/hard-halt mechanism on a call site that fires on every in-scope ambient message, not just addressed ones — fixed by adding `usage` to `classifyMessageConfidence`'s result, a new `haikuCostUsdMicros` pricing function (flat $1/$5 per MTok, matching the ADR's own cited rate), and wiring `checkCostCapAndAlert`/`recordUsageLogged` into the ambient path exactly as the DM path already does. Also found 1 Material — a TSDoc claim about when `parsed_output` comes back `null` was wrong per the installed SDK's actual source (verified by reading the real `@anthropic-ai/sdk` code): a schema/JSON-parse failure throws a bare `AnthropicError`, not `null` — fixed with a corrected TSDoc and a new `invalid-classification-output` error kind, discriminated from a genuine `APIError` network/rate-limit failure by class (`APIError extends AnthropicError`, so the `instanceof` check order matters). R2 independently re-verified both fixes: re-read the installed SDK source itself, traced the cost-cap write into the actual same `persona_cost_daily` bucket the cap-check reads (not a parallel total), re-ran the full toolchain against the checked-out commit.

`copilot-surrogate`'s mandatory pass (blast-radius doc `docs/CONVENTIONS.md` touched, diff far exceeds 50 LOC) hit its own 20-file scope-escalation ceiling for the first time this session (22 files post-filter) and correctly halted rather than skip verification, per its own documented mechanism. Split into two independent scoped passes (new-logic files vs. `apps/server` wiring files) rather than escalating a mechanical scoping decision to Alex — found 1 Material (`BUILD_PLAN.md`'s chunk 3.2 bullet still said "no consumer wired yet," directly contradicted by chunk 3.3's own neighboring bullet — the "footnote one bullet, leave neighbors stale" pattern recurring a further time) + 3 Low (an off-by-one in a code comment citing the ADR's own "36-71" band as "36-70"; `BUILD_PLAN.md`'s 2.4b bullet not footnoting the root-candidate-buffer removal; `resolve-thread-key.ts`'s TSDoc overgeneralizing which messages return `undefined`). R2-confirmed all four fixed.

**Live verification against Sarah's real Slack app**: a real ambient message in an in-scope channel (`#moe-team`, no @mention) was classified and logged with a real confidence score and no reply; a real message in a channel Sarah's a member of but _not_ in the configured scope (`#moe-ops`) produced zero activity — Stage 0 correctly filtered it before the classifier ever ran; a real DM produced a normal conversational reply, unchanged. Checked the database directly and confirmed the classifier's Haiku cost and the DM's Sonnet cost both accumulated into the _same_ `persona_cost_daily` row — the exact Critical fix, now confirmed working end-to-end, not just in code review. A real environment gap surfaced during this live pass, unrelated to this chunk's own code: the long-lived `moe-postgres` container had never had migrations applied at all (zero tables), causing every DB call to fail on the first live-test round — though the classifier's fail-open design meant classification still succeeded correctly even under that failure. Migrations applied; the second round came back fully clean.

**Chunk 3.4a-i — High-band auto-draft, shadow/log-only** ([PR #42](https://github.com/Pushedskydiver/moe/pull/42)): `classifyConfidenceBand` (`packages/core`, a pure `number → 'high'|'mid'|'low'` predicate on the 3.1 ADR's thresholds) and `composeTicketDraft` (`packages/agents`, Claude Sonnet 5, `zodOutputFormat` + `.messages.parse()`, a deliberately link-only draft — no enrichment beyond the source message, since Stage 3 has no GitHub client yet) wired into a new `apps/server/src/handle-ambient-channel-message.ts` (extracted from `handle-inbound-message.ts` to stay under `max-lines` once this chunk's logic landed). A genuine scope ambiguity surfaced before any code moved, resolved with Alex via `AskUserQuestion` rather than assumed: BUILD_PLAN's own 3.4a-i text reads like a real Slack post ("posted in-thread with... reaction legend"), but neither the reaction-listener/ticket-commit path (3.4a-ii) nor VISION §9's situational-appropriateness gate (3.4a-iii) exist yet — resolved as **shadow/log-only**, same precedent as chunk 3.3 set for this class of risk: the draft and its intended 📦/🔁/✅ reaction legend are logged (`'would post high-band ticket draft'`), never actually posted. A second gap surfaced mid-chunk, self-caught during pre-PR docs sync rather than left for review: BUILD_PLAN's own text also names the 2.7a operating-rhythm guard as a requirement, which the first implementation pass hadn't wired in — fixed by gating `composeAndLogDraft` specifically (not the classification step, which 2.7a's own settled policy doesn't cover) on `evaluateOperatingRhythm`, consuming a `bankHolidaysCache` now constructed once at process startup (`start-slack-listener.ts`) and threaded through `HandlerDeps`.

DA review found 2 Material, both R2-confirmed: a chronology-inverted precedent citation (three sites cited the still-unbuilt chunk 6.5a-i as precedent for the shadow/log-only decision, when 6.5a-i's own text actually cites chunk 3.3 as _its_ precedent — backwards; fixed to cite 3.3 directly) and `docs/REVIEW-PATTERNS.md`'s pre-seeded Core-hours-guard pattern going un-graduated despite this chunk being the exact predicted failure firing for real on the guard's first wired consumer (fixed with a real `Caught:` citation). Plus 2 Low, both deferred rather than fixed: a minor cost-cap-before-operating-rhythm check ordering (fixing it would require pinning wall-clock time into an existing test that currently doesn't need it), and a missing co-located test file for the new `handle-ambient-channel-message.ts` (flagged as a follow-up task, not restructured mid-review).

`copilot-surrogate`'s mandatory pass (blast-radius docs touched, diff far over 50 LOC) then found a **further two Material findings the diff-scoped DA/R2 pass had structurally no way to catch**: both were kept-prose sentences _outside_ the diff that the diff's own edits had just made false. Fixing 2.7a's `evaluateOperatingRhythm` guard's own kept-prose "no consumer wired yet" line — never touched by this PR, since only the 3.4a-i checkbox two lines below it was edited — needed the same "wired for real as of that chunk" follow-up parenthetical the sibling 3.2 entry already carries. And `docs/REVIEW-PATTERNS.md`'s own intro paragraph (also untouched by this PR) still called the Core-hours-guard pattern's predicted failure "unobserved," directly contradicted by this same PR's own edit to that pattern's status line two sections later in the same file — the DA-review fold had corrected the entry's own status line but not the file's separate intro paragraph that introduces it. Plus 1 Low (a citation misattributing a quoted BUILD_PLAN passage to the 3.4a-i bullet when it actually comes from Stage 3's exit criterion). All three R2-confirmed and folded.

**Live verification against Sarah's real Slack app**, run on a Saturday: a real ambient message ("the CLI hangs when I run it against large repos...") classified at confidence 85 (High band) and correctly triggered `evaluateOperatingRhythm`, which deferred draft composition with `reason: 'outside-window'` — no draft composed, no second Anthropic call, confirming the new guard's negative path live (the positive "compose during business hours" path stays covered only by the automated tests with pinned fake timers, not live-tested this session). A real environment gap recurred mid-verification, same class as 3.3's own live-test gap: the local dev Postgres container's tables had been dropped again by the last automated test run's own `afterEach` teardown, causing the classify step's own cost-cap check and usage-recording to fail — `checkCostCapAndAlert` correctly failed open (classification/deferral still completed correctly) rather than blocking. Migrations re-applied; a second message confirmed cost accounting now persists correctly end-to-end (664 micro-USD for the Haiku classify call, matching the pricing formula exactly). A separate CI gotcha recurred a third time this session's history: the PR title's scope (`3.4a-i`, a BUILD_PLAN chunk identifier) failed the title-format check's `[a-zA-Z0-9_-]+` regex on the literal dot — fixed by renaming the scope to a package name (`server`), matching every other merged PR's own convention; memory note `gitmoji-type-pairing-ci` updated with this third, distinct manifestation (using a chunk ID as the scope at all, not just mis-punctuating one).

**Chunk 3.4a-ii — Reaction listener + outcome paths, no live consumer** ([PR #43](https://github.com/Pushedskydiver/moe/pull/43)): `classifyReactionOutcome` (`packages/slack`, a pure reaction-short-name → outcome mapping, short-names verified against the real `iamcal/emoji-data` database — ✅/🔁/📦 are `white_check_mark`/`repeat`/`package`, all unambiguous this time), `rawSlackReactionEventSchema`/`isProcessableReactionEvent`/`normalizeInboundReaction` (`packages/slack`, mirroring `raw-message-event.ts`'s own raw-schema/type-guard/normalize shape for the `reaction_added` event type), a new `pending_ticket_drafts` table + repository (`packages/core`'s `create`/`getByMessage`/`resolve`/`updateContent` — `resolve`'s atomic CAS mirrors chunk 1.3's `claimTicket` exactly, the "parent-message state" this chunk's own text names), and three outcome-path functions plus a dispatching `handleReactionAdded` (`apps/server`): ✅ commits a real `Brief` ticket, 📦 parks a real `Backlog` ticket (both `projectKey: 'chief-clancy'`, VISION §3.4's settled single-project scope; `severity: 'Medium'`, a fixed placeholder Alex confirmed via `AskUserQuestion` since VISION §5.4's trust-erosion rule keeps severity off the LLM layer too), and 🔁 recomposes from the _original_ source message via `composeTicketDraft`, its own fresh cost-cap check and accounting. A genuine chunk-sequencing conflict surfaced before any code moved, resolved with Alex via `AskUserQuestion`: this chunk's own BUILD_PLAN text describes a real reaction listener reacting to a real posted message, but 3.4a-i shipped shadow/log-only and VISION §9 requires 3.4a-iii's situational-appropriateness gate to exist first — resolved as **build the primitive, no live consumer yet**, same shape as 2.7a/2.7b/3.2's own precedent: no Socket Mode `reaction_added` listener is registered, and 3.4a-i's own draft composition still doesn't persist a pending-draft row.

DA review found 3 Material, all R2-confirmed: (1) `classifyReactionOutcome`'s lookup was a plain object literal keyed by an external Slack reaction short-name — a custom workspace emoji named `constructor`/`__proto__`/`toString`/`hasOwnProperty` resolved to a truthy `Object.prototype` member instead of `undefined`, misdispatching as a real, billed 🔁 redo; fixed by switching to a `Map`, with regression tests for all four names. (2) A stale "`start-slack-listener.ts` untouched" claim repeated in the code's own comment, `BUILD_PLAN.md`, and (R2's own independent find) `docs/GLOSSARY.md` — `start-slack-listener.ts` does carry this chunk's `ticketStore`/`draftStore` wiring, it just never calls the dispatcher; corrected in all three. (3) No self-authored-reaction filter exists anywhere in the pipeline, unlike `raw-message-event.ts`'s sibling `bot_id` filter — once 3.4a-iii's own `reactions.add` call seeds the legend onto the persona's own posted message, that action would emit a `reaction_added` event this handler would misdispatch against; documented as an explicit known gap 3.4a-iii must close, rather than half-built now (DA's own suggested fix offered this as an acceptable resolution given this chunk's confirmed no-live-wiring scope). Plus 2 Low: a missing `resolvedAt` guard on the redo write (left as a documented, cost-cap-bounded trade-off) and a redundant type intersection (fixed, trivial). Self-review caught a further real bug before DA even ran: `commitAsTicket` used the caller's possibly-stale draft parameter's title instead of the fresh value the atomic claim returns, so a concurrent 🔁 racing a ✅/📦 commit could have created a ticket with stale content — fixed with a regression test; self-review also caught a folder-structure violation (a single-file `reaction-outcome/` subfolder, flattened per `docs/SELF-REVIEW.md`'s own "single-file concepts stay flat" rule). `copilot-surrogate`, split into 2 parallel passes (27 files, over the 20-file ceiling), came back fully clean — zero Material/Critical findings, only 1 trivial Low (a misquoted code comment, fixed).

**Live verification**: no real Socket Mode listener exists to trigger this chunk's code (by design), so verification ran as a manual integration script against real infrastructure instead — real Postgres, a real Anthropic API call — rather than being skipped. All 5 scenarios passed: 🔁 redo genuinely regenerated the draft's wording via a real Sonnet-5 call; ✅ and 📦 each created a real `Ticket` row with the correct status; a second ✅ on an already-committed draft was correctly ignored (the double-processing guard); an out-of-legend reaction (`thumbsup`) was silently ignored without even a draft lookup. Real cost accounting showed exactly one billed call (the redo), confirming commit/park stay deterministic with zero LLM cost, per VISION §5.4. A familiar environment gap recurred a third time: the shared local Postgres container's tables had been dropped again since the last automated test run — migrations re-applied before verifying. The verification script's own synthetic rows (2 tickets, cost-daily accumulation) were then cleaned up, but the cost-daily row was missed on the first pass and briefly polluted `packages/core`'s "zero total" test the same way it did at 3.4a-i — caught immediately by `pnpm test` itself (the polluted row's own test failure), not a separate check; the failing test's own `afterEach` had already dropped the tables by the time this was investigated, so no manual cleanup was even needed the second time.

**Major novel patterns this session:**

1. **A `da-review`/`copilot-surrogate` dispatch with `isolation: 'worktree'` still touched the primary worktree twice (2.7b's R2 and `copilot-surrogate` passes)** — not because the isolation flag failed, but because the agent's own Bash `cd` invocations wandered into the main checkout when the assigned worktree wasn't checked out the way expected. No data was lost either time (confirmed via `git status`/`git reflog` after both) — the same class of gap the existing `review-agent-isolation` memory note names, recurring on a different mechanism.
2. **Live-verification discovered a real infrastructure gap, not a code bug, for the first time this session:** 2.7b's `fetchSlackStatus` needed a `users.profile:read` OAuth scope Sarah's Slack App didn't have — caught by the live call itself (`missing_scope`), not by any review pass, since no review pass can see a Slack app's actual granted scopes. Alex fixed it out-of-band; `BUILD_PLAN.md` was written to reflect the real pending state rather than a premature completion claim.
3. **The first fully clean mandatory `copilot-surrogate` pass this session** (2.7b, PR #38) — zero findings across ~27 independently-verified claims, contrasted against every other mandatory pass this session finding something real. A useful reminder that "zero findings" is itself real, checkable signal when the reviewer's own report shows independent verification work (live re-fetches, reading actual installed package source, reproducing claims with real code), not just a rubber stamp.
4. **Self-review found three structurally different classes of problem across three chunks, not the same bug repeated:** 2.7a's uncommented `as` cast was a design smell (fixed by reshaping the type, not commenting around it); 3.1's "no message landed in the gap" claim was a plain factual error about the eval's own data, caught by re-reading the ADR against the actual eval output rather than by re-running any tool.
5. **The deepest and most novel process finding this session: a genuine disagreement between two independent review passes, resolved by independent re-verification rather than trusting either account** (3.1, PR #39). DA's own fold on a LOW finding softened a Fable 5 prefill claim; `copilot-surrogate`'s live WebFetch found the softened version itself wrong. Given two fresh-context reviews disagreeing on a load-bearing external fact, resolved it with an independent live re-fetch — saved to memory (`reviewer-disagreement-resolution`) as a new discipline distinct from ordinary R2 verification: disagreement between reviewers is itself a signal to re-derive the fact, not to average or defer to the more confident-sounding account.
6. **A bundled skill's cached snapshot lagging live docs caused two earlier fresh-context review passes (DA and its own R2) to both validate an inaccurate claim** (3.1) — the `claude-api` skill's bundled `model-migration.md` lacked a detail the live docs had since gained (an explicit Fable 5 prefill-400 statement); only `copilot-surrogate`'s live WebFetch caught the gap. Bundled skill content is a snapshot, not ground truth, when a permanent doc makes a claim about current external API behavior — a live fetch is the tiebreaker.
7. **The first chunk this session (3.2, PR #40) run under Ultracode: review-gate dispatches (DA review, `copilot-surrogate`) went through the Workflow tool as background tasks with schema-validated structured output, instead of direct foreground Agent calls.** Same review disciplines underneath (fresh context, `isolation: 'worktree'`, the same mandatory triggers) — the schema made severity triage (BLOCKING/MATERIAL/Low) mechanical rather than free-text parsing, and running in the background let doc/self-review prep happen in parallel while each review ran. Also the first chunk where "overall verdict: clean" from _both_ review layers still each carried one real, actionable Low finding — worth remembering "clean" means no BLOCKING/MATERIAL surfaced, not zero findings; Low findings still get folded regardless of the overall verdict.
8. **`copilot-surrogate`'s own 20-file scope-escalation ceiling fired for the first time this session (22 files, PR #41, chunk 3.3)** — the agent correctly halted per its own documented mechanism rather than skip verification past the cost envelope it's calibrated for. Resolved by splitting into two independent scoped passes over disjoint file sets (new-logic files vs. `apps/server` wiring files), run in parallel via the Workflow tool, rather than escalating a mechanical scoping decision to Alex — a judgment call distinct from the genuine product/design questions this session did escalate (DMs-only, dead-code removal).
9. **Live verification caught a real environment gap a second time this session, in a new form** (3.3): the long-lived `moe-postgres` container had never had migrations applied at all (zero tables), surfacing as multiple DB-call failures on the first live-test round. Not a bug in the new code — the classifier's fail-open design correctly degraded (classification still succeeded) rather than crashing or silently blocking. Migrations applied; confirmed fully clean on a second live round, including the cost-cap fix actually accumulating into the real DB.
10. **Chunk 3.3 had the deepest review arc of the whole session: two DA rounds (1 Critical + 1 Material, R2-confirmed) plus a split `copilot-surrogate` pass (1 Material + 3 Low, R2-confirmed)** — four independent, real findings across two fully separate review dispatches, none missed on re-review. The Critical finding (an uncapped, unaccounted-for second LLM call site) is this session's most consequential catch — a real gap that would have let a busy channel rack up unbounded, invisible spend.
11. **`copilot-surrogate`'s whole-file-at-HEAD read caught real drift a diff-scoped DA/R2 pass structurally cannot see (3.4a-i, PR #42):** the PR's own new text was internally correct, but two _neighboring, untouched_ sentences — 2.7a's "no consumer wired yet" line and `docs/REVIEW-PATTERNS.md`'s own intro paragraph — went stale the moment this PR's edits made them false, in files the diff didn't otherwise touch at those specific lines. A distinct failure mode from the standing "fold commits need a prose-matches-code self-check" carry-over (now a 5th confirmed instance counting this one) — this isn't about a fold commit's own edit being wrong, it's about a correct edit leaving an untouched sibling sentence behind. Worth a specific check going forward: after editing one sentence in a file with cross-referencing sentences elsewhere (an intro paragraph, a sibling BUILD_PLAN entry), grep the same file for other sentences making the same kind of claim before considering the edit complete.
12. **DA review caught a genuine security-adjacent bug for the first time this session: a prototype-pollution-style lookup vulnerability (3.4a-ii, PR #43).** `classifyReactionOutcome`'s `{ white_check_mark: ..., repeat: ..., package: ... }` object-literal lookup, keyed by an external Slack reaction short-name a custom workspace emoji can name almost anything, answered `constructor`/`__proto__`/`toString`/`hasOwnProperty` with a truthy `Object.prototype` member instead of `undefined` — live-reproduced by the reviewer with a real Node one-liner, not just asserted. Would have silently misdispatched a real, billed Anthropic call on a reaction outside the intended legend, the moment 3.4a-iii wires this live. Fixed with a `Map` (no prototype-chain fallback) plus regression tests for all four names — a durable lesson for any future external-string → fixed-set lookup in this codebase: default to a `Map`, not a plain object literal, whenever the key comes from outside the process.

**Earlier Session 8 status, compressed** — Stage 2's cost-tracking arc shipped in full across three PRs: metering substrate (chunk 2.6a, [PR #34](https://github.com/Pushedskydiver/moe/pull/34)) — `persona_cost_daily` table with atomic per-persona-per-day accumulation, integer micro-USD pricing against VISION §10's dated Sonnet-5 rates, live-verified against Sarah's real Slack app to an exact cost match; spend-cap alert ladder with hard-halt enforcement (chunk 2.6b, [PR #35](https://github.com/Pushedskydiver/moe/pull/35)) — an atomic threshold-claim reusing `ticket-lifecycle/claim.ts`'s `claimTicket` pattern for a second, structurally-unrelated race, Alex-confirmed hard-halt + direct-DM-alert design points, three real DA-caught concurrency/correctness bugs (a lost-alert bug, a duplicate-alert race, a silently-diverging-history bug) plus two more from `copilot-surrogate`'s mandatory pass, live-verified through a full 50/80/100%-of-cap arc with `MOE_COST_CAP_MONTHLY` temporarily lowered to make thresholds trivial to cross; and a long-deferred type-safety fix (`PersonaId` widened from bare `string`, [PR #36](https://github.com/Pushedskydiver/moe/pull/36)) where DA caught a real missed third site even at 12 lines. Durable pattern: fold commits need the same prose-matches-code self-check as the original PR — this session's version was sharper than Session 7's, since the fold commit itself introduced a fresh doc-accuracy bug while fixing the code DA flagged, not just missing a pre-existing sibling instance.

**Earlier Session 7 status, compressed** — Chunk 2.5 shipped ([PR #33](https://github.com/Pushedskydiver/moe/pull/33)): status claims route through a real anti-fabrication gate (VISION §7.6) via a new `report_status` tool call, composed through the existing 1.4 gate. The deepest review arc at the time — five independent passes (spec-grill R1+R2 before code moved, DA, a fresh-context R2 on DA's own fold, self-review, `copilot-surrogate`), five distinct real findings, none re-missed twice. Live verification against Sarah's real Slack app produced a genuinely different kind of result: across three attempts, including one deliberately coercive, the model never once fabricated a completion claim — the anti-fabrication property held even though the mechanical tool-call path itself was only exercised by the automated test, not observed live.

**Earlier Session 6 status, compressed** — Thread-scoped conversation state shipped (chunk 2.4b, [PR #32](https://github.com/Pushedskydiver/moe/pull/32)): `conversation_turns` table + repository, DM-vs-thread keying, a root-candidate backfill buffer for Slack's own `thread_ts` timing gap, per-thread serialization queue. Fullest single-chunk process arc at the time (3-round spec-grill; DA found 2 real MATERIAL bugs — a thread-queue collapsing every DM into one lane, an unclosed Postgres pool on exit; R2 mutation-tested both fixes) plus a live-verification-driven persona-naming fix (the placeholder prompt denied being "Sarah"). First clean, fully unprompted handoff-trigger firing after three consecutive misses (Sessions 3/4/5).

**Earlier Session 5 status, compressed** — Process-hardening arc (PRs [#25](https://github.com/Pushedskydiver/moe/pull/25), [#26](https://github.com/Pushedskydiver/moe/pull/26)) formalized Round-2 verification as applying to any review-round tool, not DA-only. Deep-research pass ([PR #28](https://github.com/Pushedskydiver/moe/pull/28)) evaluated three structural fixes for the PR #24/#25 failure shapes; Alex approved narrowing `copilot-surrogate`'s discretionary trigger to mandatory-on-three-conditions over building heavier gate-checker machinery (`docs/decisions/REVIEW-GATE-DISCRETION.md`). Chunk 2.4a shipped ([PR #30](https://github.com/Pushedskydiver/moe/pull/30)): the stateless LLM reply loop, live-verified with a real Anthropic completion. Three PROGRESS.md syncs (#27/#29/#31). Durable pattern: the handoff trigger recurred a third time across the longest unplanned continuous stretch yet (~10 PRs, #25–#31) — sharper diagnosis was that the declined-automation ADR's concern was LLM-summary quality, not whether anything checks the trigger at all; the ADR's own named fix (a deterministic pointer-injection hook) was flagged as the concrete next step, not decided unilaterally.

**Session 1–4** archived to `docs/history/SESSIONS.md` (detail band was growing past budget) — full retrospectives survive in `git log -p PROGRESS.md`.

### Session 10 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect the PR #43 squash, `9d7e62a`, at HEAD), `git status` clean. `BUILD_PLAN.md` shows chunk 3.4a-ii `[x]` — content-complete, live-verified — with 3.4a-iii next. **Stage 2 is fully complete; Stage 3's intake cascade has both halves of its High-band action built (composing a draft, and the reaction-gate that would resolve one) but neither wired to a live consumer — that's exactly what 3.4a-iii unlocks.**
- **Primary workstream — DONE, seven chunks this session.** Chunk 2.7a ([PR #37](https://github.com/Pushedskydiver/moe/pull/37)), chunk 2.7b ([PR #38](https://github.com/Pushedskydiver/moe/pull/38)), chunk 3.1 ([PR #39](https://github.com/Pushedskydiver/moe/pull/39)), chunk 3.2 ([PR #40](https://github.com/Pushedskydiver/moe/pull/40)), chunk 3.3 ([PR #41](https://github.com/Pushedskydiver/moe/pull/41)), chunk 3.4a-i ([PR #42](https://github.com/Pushedskydiver/moe/pull/42)), and chunk 3.4a-ii ([PR #43](https://github.com/Pushedskydiver/moe/pull/43)) all shipped, review-verified and live-verified (2.7a/2.7b/3.3/3.4a-i/3.4a-ii). See the entries above.
- **Next up — BUILD_PLAN Stage 3, chunk 3.4a-iii — "Minimal situational-appropriateness gate."** Its own testable module, consulted before any auto-post — VISION §9 requires the check before the _first_ standing-proactive action, not at Stage 8; 8.5 extends this gate later, it doesn't introduce it. Wiring it in front of 3.4a-i "completes the acceptance-test path" per BUILD_PLAN's own text — meaning this is also the chunk that needs to (a) flip 3.4a-i's `composeAndLogDraft` from shadow/log-only to a real Slack post + real `pending_ticket_drafts` persistence, and (b) register a real Socket Mode `reaction_added` listener calling 3.4a-ii's `handleReactionAdded`. Two known prerequisites 3.4a-ii's own DA review already surfaced, not yet resolved: no self-authored-reaction filter exists anywhere in the pipeline (a `botUserId`-equivalent value needs threading through `PersonaConfig` → `HandlerDeps` → `InboundReaction` before wiring `reactions.add` for real, or the persona's own legend-seeding reaction will misdispatch against itself), and `updatePendingTicketDraftContent`'s redo write has no `resolvedAt` guard (a narrow, already-bounded race, left as a deferred trade-off). Check BUILD_PLAN's own chunk text for full scope before starting.
- **Decision branches:**
  - A. `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision keeps holding up on real evidence — 16 PRs since (#30 through #43) have all either found real errors or come back clean with independent verification shown. PR #43 gave it a new kind of data point: DA review caught a genuine security-adjacent bug (a prototype-pollution-style lookup vulnerability), not just a doc-staleness or cost-cap gap — the mandatory-trigger design keeps earning its keep across a widening range of defect classes.
  - B. If a research-backed decision comes up again, independently spot-check any specific statistic before it lands in a permanent doc — see the `deep-research-numeric-claims-need-spot-check` memory.
  - C. When two independent review passes disagree on a load-bearing external fact, resolve it with a fresh independent check of your own rather than deferring to the more confident-sounding account — see the `reviewer-disagreement-resolution` memory. First instance at 3.1 (PR #39); no second recurrence yet.
  - D. When `copilot-surrogate` hits its own 20-file scope-escalation ceiling, split into independent scoped passes over disjoint file sets rather than escalating a mechanical scoping decision to Alex — reserve escalation for genuine product/design questions (DMs-only, dead-code removal at 3.3 were rightly escalated; file-count scoping wasn't). Recurred a second time at 3.4a-ii (27 files, split proactively before dispatching rather than waiting for the agent to hit the ceiling itself, unlike 3.3's reactive split) — both splits used the same disjoint-file-set shape without incident.
  - E. After fixing a stale sentence a review pass flagged, grep the same file for _other_ sentences making the same kind of claim before considering the fix complete — `copilot-surrogate` found two cases at 3.4a-i (an intro paragraph, a sibling BUILD_PLAN bullet) and R2 independently found a third site at 3.4a-ii (`docs/GLOSSARY.md`) for the identical stale "untouched" claim DA's own R1 pass had already found in two other files. Three sites, one claim, two different chunks — sweep is not optional once a stale claim is confirmed repeated anywhere.
  - F. **New at 3.4a-ii:** default to a `Map`, not a plain object literal, for any lookup keyed by an external string a caller (or an attacker) partly controls — a Slack reaction/emoji short-name, a webhook field, anything outside this process's own control. DA review live-reproduced `{}['constructor']`/`{}['__proto__']` returning truthy `Object.prototype` members instead of `undefined`. Check any future external-string → fixed-set classifier in this codebase (there's now three: `classifyReactionOutcome`, `isAway`'s emoji-shortcode match, `classifyConfidenceBand`'s numeric bands) for the same object-literal-lookup shape.
- **Carry-overs:**
  - A genuinely non-obvious design gap can hide in a chunk's own BUILD_PLAN text, not just in untraced existing code (3.4a-i) — this chunk's operating-rhythm-guard requirement was named in BUILD_PLAN's own 3.4a-i bullet the whole time, but the first implementation pass missed it until a docs-sync re-read caught it. Re-read the chunk's own BUILD_PLAN bullet once more right before considering a chunk done, not just once at the start.
  - Don't use a BUILD_PLAN chunk identifier (`3.4a-i`) as a PR title's scope — the title-format CI check's scope regex rejects the literal dot, and every actually-merged PR in this repo uses a package name as scope instead, putting any chunk reference in the description text. See the `gitmoji-type-pairing-ci` memory's third instance; held cleanly at 3.4a-ii (`feat(server): ...`) with no recurrence.
  - A chunk's own "trace the existing code's actual behavior" step can surface a real design gap BUILD_PLAN's own text never named — 3.3's DMs-only decision came from reading `handle-inbound-message.ts` closely, not from anything BUILD_PLAN flagged. When wiring a new gate/consumer into existing code, check what the existing code _actually does_ today before assuming the new behavior simply layers on top.
  - A new real, billed LLM call site needs the exact same cost-cap gate and accounting as the first, from the start — DA caught 3.3's classifier call shipping completely uncapped; both 3.4a-i's `composeTicketDraft` and 3.4a-ii's `regenerateTicketDraft` call sites applied the lesson proactively (their own fresh cap check, not a stale one from an earlier step) and needed no fold for it. Four call sites now correctly gated on the first try after the one that wasn't.
  - Live verification keeps finding a real environment gap in the same form, now three times (3.3, 3.4a-i, 3.4a-ii): a long-lived local Postgres container can go a long time without migrations ever being (re-)applied, or can have its tables dropped by the last automated test run's own `afterEach` teardown. Check table existence directly (or just re-run migrations) before trusting a "clean" live-test result. **New at 3.4a-ii:** the inverse direction of the same root cause also bites — a manual live-verification script's own leftover row (this time in `persona_cost_daily`, not a missing table) polluted the next `pnpm test` run's "zero total" expectation, exactly as it did at 3.4a-i. Clean up every table a verification script writes to, not just the obviously-synthetic ones (tickets, drafts) — cost-accounting rows are just as real to the next test run. Also new at 3.4a-ii: when a chunk has no live Slack trigger to verify against by design, a manual integration script (real DB, real Anthropic API, synthetic trigger data) is a legitimate, more-thorough alternative to declaring "not live-testable" — worth reaching for again whenever a future chunk lands in the same "primitive built, no consumer wired" shape.
  - `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision keeps holding up on real evidence — 16 PRs since (#30 through #43) have all either found real errors or come back clean with independent verification shown.
  - Two nuances on established patterns from Session 7 — full detail in the `autonomous-run-handoff` memory note, not restated here: (1) a live test that never exercises the literal code path under review can still be a sufficient result if it confirms the deeper property that path protects; (2) fixing a cross-referenced claim in one file doesn't guarantee a sibling instance of the same issue in another file gets caught in the same pass — sweep all citing locations, don't assume one fix is complete. 3.4a-ii's own R2 pass gave this its sharpest instance yet: independently found a _third_ site (`docs/GLOSSARY.md`) repeating a claim R1 had already fixed in two others.
  - A fold commit needs the same "does this diff's prose still match this diff's code" self-check as the original PR, not just the code-correctness fixes DA asked for — recurred at 2.6b, 3.1, 3.3's own `copilot-surrogate` fold, and 3.4a-i's own DA fold. 3.4a-ii's own DA fold got this right on the first try (all three "untouched" sites fixed together, no follow-up needed) — five instances total, the streak of misses broken at least once.
  - The session-handoff trigger's track record is still mixed, not a clean streak: fired unprompted at Session 6's own PR merge, on the user's explicit cue at Sessions 7, 8, and 9. Keep checking at every PR merge regardless of how the last one went. `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s own already-identified-but-deferred "deterministic pointer-injection hook" is still the concrete next step if this keeps needing prompting — Alex's call to scope as a real chunk, not something to build unilaterally.
  - `docs/decisions/` holds six ADRs (`STAGE-1-CLASSIFIER.md` added at 3.1), none trimmed to the ~50-line "decisions-only" budget yet (`docs/decisions/README.md`'s lifecycle step 3) — flagged repeatedly across review gates without being acted on. Content-judgment task on historical records — check with Alex first.
  - Sarah's real Slack App is verified working end-to-end: thread-scoped conversation memory, persona-aware naming, the status-claim gate, per-turn cost accounting (now across **four** call sites/two models — Sonnet 5 for DM chat replies, High-band ticket-draft composition, and 🔁 redo regeneration; Haiku 4.5 for the Stage-1 classifier — all four into the same per-persona/day bucket), cost caps + spend alerts, ambient-channel classify-and-log for in-scope channels (3.3), High-band ticket-draft composition gated by the 2.7a operating-rhythm guard (3.4a-i), and the reaction-gate outcome paths (3.4a-ii) — all real, tested, but the reaction side has no live trigger yet (no Socket Mode `reaction_added` registration, no persisted pending-draft row from 3.4a-i's own shadow mode). DMs keep the full conversational reply path; ambient channel/group messages never get a reply at all anymore (3.3's DMs-only decision), and a High-band draft is only ever logged, never posted (3.4a-i's shadow-mode decision, still true until 3.4a-iii). Credentials in gitignored `.env.local`, template at `.env.example`. `MOE_WORK_RELEVANT_CHANNEL_IDS` is now populated with real channel IDs (moe-team/moe-incidents/moe-research, looked up live via `conversations.list` — Sarah's app needed a `channels:read` scope add, same "missing_scope caught live" pattern as 2.7b's `users.profile:read`). Local live-testing needs `packages/core` migrations applied to whatever Postgres `DATABASE_URL` in `.env.local` actually points at — **confirmed the hard way three times now (3.3, 3.4a-i, 3.4a-ii)**: a long-lived container can still have zero tables; check directly or just re-run `pnpm build && node scripts/migrate.ts` (from `packages/core`) before trusting a live-test result. `0006_create_pending_ticket_drafts.sql` (3.4a-ii) is the newest migration; `0004_create_persona_cost_daily.sql`/`0005_create_persona_cost_alerts.sql` are unchanged since 2.6b. `.env.local`'s `MOE_COST_CAP_MONTHLY` is `50`; `MOE_COST_ALERT_SLACK_USER_ID` is populated with Alex's real Slack user ID.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - Branch discipline: create the dedicated branch as the literal first action after syncing `main`, before any edit, every single chunk transition.
  - `ScheduleWakeup` is `/loop`-dynamic-mode-scoped, not a general "wait passively" tool — when something else (Monitor, a task notification) is already the wake signal, do nothing further; don't layer a second wakeup mechanism on top. Full detail in the `autonomous-run-handoff` memory note.
  - `pnpm`'s own binary bundles an internal Node runtime and overrides `PATH` for child processes it spawns — the documented Node-24 PATH-prepend fix (below) doesn't reach scripts pnpm shells out to internally (`packages/core`'s `migrate` script specifically, which needs Node 24's native TS execution). Workaround: invoke the Volta-pinned `node` binary directly for that one command, bypassing `pnpm run`/`pnpm --filter`. `pnpm build`/`test`/`lint`/`typecheck` are unaffected. Full detail in the `autonomous-run-handoff` memory note.
  - Volta itself (not just pnpm) resolves the wrong pinned Node version when invoked from a package subdirectory lacking its own `"volta"` key in `package.json` — even by the Volta binary's own absolute path. Run Node-24-dependent one-off commands from the monorepo root by relative path, not after `cd`-ing into a package first. Full detail in the `autonomous-run-handoff` memory note.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm directly (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite.
  - Local dev needs a real Postgres for `packages/core`'s tests (`DATABASE_URL`) — CI provisions its own via a `postgres:17-alpine` service container; locally, a throwaway Docker container works (`docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=... -e POSTGRES_DB=... postgres:17-alpine`). Check `docker ps` before starting a duplicate container.
- **Fallback:** if Alex redirects on load, follow that; otherwise chunk 3.4a-iii is next per BUILD_PLAN sequence — the situational-appropriateness gate, and the chunk that finally wires 3.4a-i's draft composition and 3.4a-ii's reaction handling into a real, live end-to-end loop. Check BUILD_PLAN's own chunk text for its full scope before assuming a shape from any prior chunk this session — this one has real Slack-posting and OAuth-scope implications the earlier "build the primitive only" chunks didn't.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk   | Status | Shipped    | Headline                                                                                                                      |
| ------- | ------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 0.1     | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                                          |
| 0.2     | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                                                      |
| 0.3     | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                                               |
| 0.4     | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                                          |
| 0.5     | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                                           |
| 0.6a    | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved                                |
| 0.6b    | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)                                |
| 0.6c    | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision                              |
| 0.7     | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**                                       |
| 1.1     | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                                                     |
| 1.2a    | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)                                     |
| 1.2b    | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                                                 |
| 1.3     | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                                              |
| 1.4     | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                                                       |
| 1.5     | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`)                           |
| 1.6     | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**                                   |
| 2.1     | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)                                   |
| 2.2     | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config                              |
| 2.3     | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app                               |
| 2.4a    | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app                            |
| 2.4b    | Merged | 2026-07-17 | Thread-scoped conversation state + persona-naming fix — live-verified context-aware multi-turn chat                           |
| 2.5     | Merged | 2026-07-17 | Status claims through the `report_status` tool + 1.4 gate — VISION §7.6 anti-fabrication, wired                               |
| 2.6a    | Merged | 2026-07-17 | Cost metering substrate — `persona_cost_daily` table, atomic accumulation, live-verified turn cost                            |
| 2.6b    | Merged | 2026-07-17 | Cost caps + spend alert ladder — atomic claim, hard halt, live-verified full 50/80/100% arc                                   |
| 2.7a    | Merged | 2026-07-18 | Core-hours + bank-holiday guard — DST-safe check, Cached\<T,E\> class, fails closed, no consumer yet                          |
| 2.7b    | Merged | 2026-07-18 | Away-detection — isAway/fetchSlackStatus, verified emoji shortcodes, live-verified, no consumer yet — **Stage 2 complete**    |
| 3.1     | Merged | 2026-07-18 | [GATE] Stage-1 classifier scoring method + thresholds — bundled call, Haiku 4.5, High≥70/Mid35-69/Low<35 — **Stage 3 opened** |
| 3.2     | Merged | 2026-07-18 | Channel scoping (Stage 0) — isSurfaceInScope allow-list + DM bypass, no consumer yet, first Workflow-dispatched review arc    |
| 3.3     | Merged | 2026-07-18 | Classifier gate (Stage 1) — Haiku 4.5 wired live, DMs-only chat replies, cost-cap fix, live-verified end-to-end               |
| 3.4a-i  | Merged | 2026-07-18 | High-band auto-draft — Sonnet-5 ticket draft, shadow/log-only, 2.7a guard wired, live-verified end-to-end                     |
| 3.4a-ii | Merged | 2026-07-18 | Reaction listener + outcome paths — commit/redo/park, no live consumer, prototype-pollution fix, live-verified via real infra |
