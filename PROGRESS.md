# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 8)

Updated 2026-07-18 end-Session-8 — **Stage 2's cost-tracking arc shipped in full: metering substrate through a spend-cap alert ladder with hard-halt enforcement, plus a long-overdue type-safety cleanup — three PRs, each surfacing its own real review-arc findings, all live-verified against Sarah's real Slack app.** Opened on the standing instruction to continue BUILD_PLAN in sequence from Session 7's own handoff (chunk 2.6a next); Alex confirmed 2.6b's two open design points (hard halt vs. alert-only, alert destination) via `AskUserQuestion` before any code moved, per BUILD_PLAN's own explicit note that this needed deciding inside the chunk; at session-end handoff, offered the choice on the long-deferred `PersonaId` typing gap and chose to spin it off as its own tiny PR rather than defer it a fifth time.

**Chunk 2.6a — cost metering substrate** ([PR #34](https://github.com/Pushedskydiver/moe/pull/34)): `persona_cost_daily` table + migration in `packages/core`, accumulated atomically via a real `INSERT ... ON CONFLICT (persona_id, day) DO UPDATE` — race-tested with concurrent `recordUsage` calls, not just sequential ones. `generateReply` (`packages/agents`) now surfaces each turn's real token usage; priced against VISION §10's dated Sonnet-5 rates ($2/$10 per MTok through 2026-08-31, then $3/$15) as an integer micro-USD value via a new `sonnetCostUsdMicros` — no floating-point step anywhere in the pipeline. `apps/server` wires an accounting hook into the reply path, mirroring the existing `HistoryStore` DI seam (a new `CostStore` seam, bound to the real repository in `start-slack-listener.ts`). Accumulates and exposes the numbers only, per BUILD_PLAN's own scope line — no caps, no alerts (that's 2.6b). Review arc: architectural self-check found no new dependency edges; DA review found 1 MATERIAL (`computeCostUsdMicros` violated `docs/CONVENTIONS.md`'s dropped `compute*`/`calculate*` verb prefix — renamed to `sonnetCostUsdMicros`) + 4 LOW (tightened `toUtcDay`'s TSDoc, fixed a comment citing the wrong `CONVENTIONS.md` section, flagged the missing `model` column as deliberate at both the table and schema layers, added a concurrent-write test proving the atomic upsert holds under a real race); self-review caught a stale `schema.ts` comment still describing the schema as `z.number().int()` after the code had already moved to `z.coerce.number().int()`; `copilot-surrogate`'s mandatory factual-claim pass (diff >50 LOC, external-library + VISION claims) found 2 MATERIAL — a misleading claim that choosing an integer column over `NUMERIC` avoids `pg`'s string-typed deserialization (it doesn't; both come back as strings, the real benefit is avoiding any floating-point step) and this PR's own diff never flipping `BUILD_PLAN.md`'s 2.6a checkbox despite its own doc comments citing it as done — both fixed.

**Live verification against Sarah's real Slack app** confirmed the full pipeline end-to-end: a real DM ("Hi Sarah 👋") produced a real Anthropic reply, persisted conversation history exactly as before, and a `persona_cost_daily` row recorded `persona_id=sarah, day=2026-07-17, input_tokens=783, output_tokens=17, cost_usd_micros=1736` — an exact match for `783*2 + 17*10` under introductory pricing. No errors logged anywhere in the pipeline.

**Chunk 2.6b — cost caps + spend alert ladder** ([PR #35](https://github.com/Pushedskydiver/moe/pull/35)): `MOE_COST_CAP_MONTHLY`/`MOE_COST_ALERT_SLACK_USER_ID` config (`packages/agents`'s `parseCostCapConfig`), a pure `evaluateCostCap` decision (exact integer-arithmetic threshold comparisons, no float percentage division), and threshold-crossing dedup state in a new `persona_cost_alerts` table. `apps/server`'s `generateAndPost` checks the cap before calling `generateReply` at all — a halted turn never reaches the Anthropic API. Two design points BUILD_PLAN flagged as needing Alex's confirmation before code moved — asked via `AskUserQuestion`, not inferred: hard halt (not alert-only) at 100%, a visible halt message to the user (not silence), and alerts DM Alex directly rather than a shared channel. Review arc found three real bugs, not nits: DA review caught (1) an alert-send failure that still advanced the watermark, permanently dropping that rung's alert for the month — contradicted this code's own TSDoc; (2) a genuine check-then-act race letting two concurrent turns for the same persona (`threadQueue` only serializes per-thread) both send a duplicate alert — fixed by making the watermark advance an atomic conditional claim (`claimAlertThreshold`, same `WHERE`-guarded mechanism as `ticket-lifecycle/claim.ts`'s `claimTicket`), gating the Slack post on the claim actually winning; (3) a halted turn's `HALT_TEXT` reply was never persisted to conversation history, silently diverging a full month's history from what the user actually saw in Slack for anything longer than a one-off `FALLBACK_TEXT` blip — fixed by returning `ok: true` on halt, matching the existing "ok reflects real reply content, independent of delivery success" precedent. `copilot-surrogate`'s mandatory pass then caught 2 more: `BUILD_PLAN.md` still described the pre-fold `GREATEST`-upsert mechanism after the DA fold replaced it, and the fail-open justification's own stated reasoning didn't actually hold (both fail-open and a hypothetical fail-closed self-heal at the same per-turn granularity, since `halt` is never cached) — both fixed.

**Live verification against Sarah's real Slack app**, `MOE_COST_CAP_MONTHLY=0.01` to make crossing thresholds trivial: a first real turn landed at 19.7% of cap, correctly quiet. Spend was then seeded to 95% via direct SQL (the token/cost pricing math itself was already proven exact in 2.6a's own live run, so this avoided ~5 redundant real Anthropic calls just to reach that level organically). A second real turn crossed 50% and 80% together in one check — both alert DMs landed in Alex's Slack DM with correct text, the turn itself proceeded normally. A third real turn crossed 100% (alert DM landed) and hard-halted — `HALT_TEXT` posted visibly in the same channel, and `persona_cost_daily.cost_usd_micros` stayed exactly flat afterward, confirming `generateReply` was genuinely never called, not just its output discarded.

**Side-fix — `PersonaId` typing closed** ([PR #36](https://github.com/Pushedskydiver/moe/pull/36)): `buildPersonaSystemPrompt` and `HandlerDeps.personaId` both widened from a bare `string` to `packages/agents`'s own validated `PersonaId` union — a DA finding first flagged at chunk 2.4b, missed (touched again without fixing) at 2.5, 2.6a, and 2.6b. Spun off as its own tiny PR rather than deferred a fifth time. Pure type-level change, no runtime behavior differs. DA review caught a real gap even in a 12-line diff: the PR's own cited history named a third site (`check-cost-cap.ts`'s `costAlertText`) the first commit had left untouched — folded before merge.

**Chunk 2.7a — core-hours + bank-holiday guard** ([PR #37](https://github.com/Pushedskydiver/moe/pull/37)): `evaluateOperatingRhythm` (`packages/core/src/core-hours/`) — a pure DST-safe weekday/clock check (`Intl`-based against Europe/London, no date library dependency) composed with a GOV.UK bank-holidays fetch cached via a new `Cached<T,E>` class, the first real implementation of `docs/CONVENTIONS.md`'s previously-hypothetical "cache-with-serve-stale-on-failure" pattern. Fails CLOSED (treats the day as non-core-hours) on a cold-boot cache miss, matching VISION §14's hard "never operate on a bank holiday" rule. Three parameters BUILD_PLAN flagged as needing Alex's confirmation before code moved — asked via `AskUserQuestion`, not defaulted: core hours **08:30–17:00 Europe/London Mon–Fri**; holiday source **GOV.UK bank-holidays API (england-and-wales)**, cache + serve-stale-on-failure; **direct DM replies exempt entirely** (always reply regardless of core hours — reactive engagement, not Moe acting unprompted, so §14's rest rule doesn't reach it — meaning no change to the existing reply path at all). No consumer wired yet; intake (3.4a-i), the pull loop (6.1a-i), and the ceremony scheduler (7.2a) build on this later. Review arc: DA review found 1 Critical (an unguarded `Response.json()` on a malformed GOV.UK response body would crash a long-running persona process on a transient upstream hiccup — reviewer live-reproduced it) + 3 Low (DST transition-day test coverage was summer/winter-only, not the actual clock-change days; an explicit documented decision on why `Cached<T,E>` isn't publicly exported yet — one consumer today, promote at the second; `evaluate*` added to the verb-vocabulary list). R2 verification confirmed the fold and caught one more Low inside it (the new `evaluate*` wording didn't actually hold for `evaluateOperatingRhythm`, one of its own two cited precedents — it calls an injected dependency). Self-review then turned an uncommented `as T` cast into a real design fix — collapsed `Cached`'s two independently-nullable fields into one, removing the cast structurally rather than commenting around it. `copilot-surrogate`'s mandatory pass (diff >50 LOC, external-API claims) found 1 MATERIAL (`docs/REVIEW-PATTERNS.md`'s own intro paragraph still called the guard module "not existing yet" — directly contradicted by this same PR's edit to that pattern's status two paragraphs below) + 1 Low (`docs/CONVENTIONS.md`'s `Cached<T>` bullet said the refresh flag goes "to the fetch function" when it actually goes to the cache's own `get()` accessor) — both folded and R2-confirmed.

**Live verification** — no Slack UI exists yet for this chunk (no consumer wired), so verification ran directly against the real GOV.UK API instead: a live fetch pulled all 83 real holiday dates; `evaluateOperatingRhythm` correctly classified real "now" as outside-window, correctly classified 2026-12-25 (Christmas Day) as a bank holiday, and correctly failed closed against a simulated total-failure cache.

**Chunk 2.7b — away-detection** ([PR #38](https://github.com/Pushedskydiver/moe/pull/38)): `isAway`/`fetchSlackStatus` (`packages/slack/src/away-detection/`) — resolves VISION Appendix A's "Slack-status-away parsing conventions" question, same in-place-resolution precedent chunk 2.1 set for "Designer-role timing" in that same paragraph. Alex confirmed the keyword list via `AskUserQuestion`: text keywords `away`/`holiday`/`OOO` (BUILD_PLAN's own illustrative list, kept as-is, whole-word case-insensitive match — a bare substring check would false-positive inside `giveaway`/`runaway`); emoji shortcodes widened past BUILD_PLAN's two named examples (🌴🏖️) to also cover travel/illness signals (✈️🤒🌡️🏥), deliberately excluding ambiguous momentary-busy signals (🔴 DND, 🏠 WFH). Every shortcode was verified against the real `iamcal/emoji-data` database rather than guessed — caught a second live instance of the "Slack emoji shortcode gotcha" (🏖️ is `beach_with_umbrella`, not the more obvious-sounding `beach_umbrella`). No consumer wired yet; first real consumer is 7.2b. Review arc: DA review found 2 MATERIAL (`fetchSlackStatus` had zero runtime schema validation on the external Slack response, unlike `packages/core`'s own model example — added a Zod schema + `safeParse`; `BUILD_PLAN.md` prematurely claimed a still-in-progress OAuth-scope fix as a completed past action — reworded to reflect the real pending state) + 3 Low (type duplication, missing regex-metacharacter test coverage — which self-discovered and documented a real, non-obvious `\b`-boundary limitation along the way — misplaced TSDoc). R2 verification independently re-derived every claim against ground truth (ran the actual Zod schema against 9 malformed payloads, mutation-tested the knip fix) rather than trusting the fold commit's own description — confirmed clean. `copilot-surrogate`'s mandatory pass (diff >50 LOC, touches the blast-radius `docs/VISION.md`) came back with **zero findings** — the first fully clean mandatory pass this session, ~27 claims independently verified (live-refetched the emoji database, read the actual installed `@slack/web-api` source to confirm a throw-behavior claim, reproduced the `\b`-boundary claim with a real Node one-liner).

**A real environment gap surfaced mid-chunk, not a code bug:** live-verifying `fetchSlackStatus` hit `missing_scope` — Sarah's Slack App didn't have `users.profile:read`. Alex added the scope and reinstalled out-of-band; once confirmed, live verification against the real API passed on all fronts — a real fetch of Alex's actual (empty) status round-tripped correctly through the Zod schema, `isAway` correctly classified both the real status and fabricated away statuses (without mutating Alex's real Slack profile), and a real invalid-user-ID call correctly hit the genuine `user_not_found` error path.

**Major novel patterns this session:**

1. **DA review and `copilot-surrogate` kept finding real, distinct problems across all three PRs, including inside a fold commit itself.** 2.6a: DA caught a naming-convention violation, `copilot-surrogate` caught a missed `BUILD_PLAN.md` checkbox flip. 2.6b: DA alone found 3 MATERIAL concurrency/correctness bugs in one pass (a lost-alert bug, a genuine duplicate-send race, a silently-diverging-history bug); `copilot-surrogate` then found 2 more factual-claim issues **inside the fix commit that had just landed** — a stale `BUILD_PLAN.md` description of a mechanism the fold had already replaced, and a fail-open justification whose own stated reasoning didn't hold. PR #36 (12 lines): DA still caught a real missed third site. No pass this session was redundant, at any diff size.
2. **A fold commit needs the same "does this diff's prose match this diff's code" self-check as the original PR — this recurred with a sharper edge than Session 7's version of the same lesson.** Session 7 caught one review layer missing a _pre-existing_ sibling instance of an issue; this session, the fold commit itself **introduced a fresh doc-accuracy bug** while fixing the code DA flagged. The self-check has to run on every commit that touches prose-about-code, not just the original PR.
3. **The atomic-claim pattern (`WHERE`-guarded conditional update, `ticket-lifecycle/claim.ts`'s `claimTicket`) generalized cleanly to a second, structurally-unrelated problem** — alert-threshold racing (2.6b). A real concurrency bug, not a hypothetical (`threadQueue` only serializes per-thread, so two concurrent turns for the same persona genuinely can race), fixed by reusing an established primitive rather than inventing a new one.
4. **Two new harness/environment gotchas, saved to memory:** `ScheduleWakeup` is `/loop`-scoped, not a general wait-passively tool (mis-reached-for twice this session while a `Monitor` was already the real wake signal); Volta resolves the wrong pinned Node version from a package subdirectory lacking its own `"volta"` key, even by the shim's own absolute binary path — run Node-24-dependent one-off commands from the monorepo root instead.
5. **A `da-review`/`copilot-surrogate` dispatch with `isolation: 'worktree'` still touched the primary worktree twice this session (2.7b's R2 and `copilot-surrogate` passes), not because the isolation flag failed but because the agent's own Bash `cd` invocations wandered into the main checkout when the assigned worktree wasn't checked out the way expected. No data was lost either time (confirmed via `git status`/`git reflog` after both) — but it's the same class of gap the existing `review-agent-isolation` memory note already names, recurring on a different mechanism than the one that note originally described.
6. **Live-verification discovered a real infrastructure gap, not a code bug, for the first time this session:** 2.7b's `fetchSlackStatus` needed a `users.profile:read` OAuth scope Sarah's Slack App didn't have — caught by the live call itself (`missing_scope`), not by any review pass, since no review pass can see a Slack app's actual granted scopes. Alex fixed it out-of-band (added the scope, reinstalled); `BUILD_PLAN.md` was written to reflect the real pending state rather than a premature completion claim, and DA review's own R2 pass separately caught and confirmed that phrasing was accurate once fixed.
7. **The first fully clean mandatory `copilot-surrogate` pass this session** (2.7b, PR #38) — zero findings across ~27 independently-verified claims, contrasted against every other mandatory pass this session (and most of last) finding something real. A useful reminder that "zero findings" is itself real, checkable signal when the reviewer's own report shows independent verification work (live re-fetches, reading actual installed package source, reproducing claims with real code), not just a rubber stamp.

**Earlier Session 7 status, compressed** — Chunk 2.5 shipped ([PR #33](https://github.com/Pushedskydiver/moe/pull/33)): status claims route through a real anti-fabrication gate (VISION §7.6) via a new `report_status` tool call, composed through the existing 1.4 gate. The deepest review arc at the time — five independent passes (spec-grill R1+R2 before code moved, DA, a fresh-context R2 on DA's own fold, self-review, `copilot-surrogate`), five distinct real findings, none re-missed twice. Live verification against Sarah's real Slack app produced a genuinely different kind of result: across three attempts, including one deliberately coercive, the model never once fabricated a completion claim — the anti-fabrication property held even though the mechanical tool-call path itself was only exercised by the automated test, not observed live.

**Earlier Session 6 status, compressed** — Thread-scoped conversation state shipped (chunk 2.4b, [PR #32](https://github.com/Pushedskydiver/moe/pull/32)): `conversation_turns` table + repository, DM-vs-thread keying, a root-candidate backfill buffer for Slack's own `thread_ts` timing gap, per-thread serialization queue. Fullest single-chunk process arc at the time (3-round spec-grill; DA found 2 real MATERIAL bugs — a thread-queue collapsing every DM into one lane, an unclosed Postgres pool on exit; R2 mutation-tested both fixes) plus a live-verification-driven persona-naming fix (the placeholder prompt denied being "Sarah"). First clean, fully unprompted handoff-trigger firing after three consecutive misses (Sessions 3/4/5) — see pattern #4 above for this session's own partial recurrence.

**Earlier Session 5 status, compressed** — Process-hardening arc (PRs [#25](https://github.com/Pushedskydiver/moe/pull/25), [#26](https://github.com/Pushedskydiver/moe/pull/26)) formalized Round-2 verification as applying to any review-round tool, not DA-only. Deep-research pass ([PR #28](https://github.com/Pushedskydiver/moe/pull/28)) evaluated three structural fixes for the PR #24/#25 failure shapes; Alex approved narrowing `copilot-surrogate`'s discretionary trigger to mandatory-on-three-conditions over building heavier gate-checker machinery (`docs/decisions/REVIEW-GATE-DISCRETION.md`). Chunk 2.4a shipped ([PR #30](https://github.com/Pushedskydiver/moe/pull/30)): the stateless LLM reply loop, live-verified with a real Anthropic completion. Three PROGRESS.md syncs (#27/#29/#31). Durable pattern: the handoff trigger recurred a third time across the longest unplanned continuous stretch yet (~10 PRs, #25–#31) — sharper diagnosis was that the declined-automation ADR's concern was LLM-summary quality, not whether anything checks the trigger at all; the ADR's own named fix (a deterministic pointer-injection hook) was flagged as the concrete next step, not decided unilaterally.

**Earlier Session 4 status, compressed** — `apps/server` first boots for real (chunk 2.2, [PR #23](https://github.com/Pushedskydiver/moe/pull/23)): env-sourced `PersonaConfig`, structured JSON logging with secret redaction, health check, Fly deploy config — verified with real `docker build`/`run`/`curl`. Then real Slack Socket Mode connectivity (chunk 2.3, [PR #24](https://github.com/Pushedskydiver/moe/pull/24)): event schema/normalization, bot-loop prevention, a hardcoded non-persona ack reply — verified end-to-end against Sarah's real Slack app, plus a live-Docker-caught bug where `exit(1)` never actually terminated the process (a listening HTTP server keeps Node's event loop alive regardless of `process.exitCode`). A side-fix ([PR #22](https://github.com/Pushedskydiver/moe/pull/22)) verified moe's model strategy against Sonnet 5's real release data, catching one fabricated third-party benchmark claim along the way.

**Session 1–3** archived to `docs/history/SESSIONS.md` (detail band was growing past budget) — full retrospectives survive in `git log -p PROGRESS.md`.

### Session 9 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect the PR #38 squash, `e6272a1`, at HEAD), `git status` clean. `BUILD_PLAN.md` shows chunk 2.7b `[x]` — content-complete and live-verified (PR #38) — with 3.1 next. **Stage 2 is now fully complete.**
- **Primary workstream — DONE, both chunks.** Chunk 2.7a ([PR #37](https://github.com/Pushedskydiver/moe/pull/37)) and chunk 2.7b ([PR #38](https://github.com/Pushedskydiver/moe/pull/38)) both shipped, review-verified and live-verified. See the entries above.
- **Next up — BUILD_PLAN Stage 3, chunk 3.1 — `[GATE]`, not a quick-confirm chunk like the last two.** "Stage-1-classifier spike": resolves VISION §5.2's open question (scoring method — model probability vs. a scoring call vs. log-probs — plus the High/Mid/Low confidence thresholds) via a small offline eval set of real-ish messages. Output is an ADR + the answer written back into VISION §5.2 itself. **This needs a real conversation with Alex, not an `AskUserQuestion` confirm-and-go** — it's a genuine open design/methodology question (which scoring approach, what thresholds, per BUILD_PLAN's own framing), not a parameter with an offered default. Explicitly a spike chunk: throwaway code allowed, no production wiring. Don't start building without that conversation happening first.
- **Decision branches:**
  - A. `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision keeps holding up on real evidence — 10 PRs since have exercised it (#30 through #38 found real errors or were confirmed clean, #29 came back clean). PR #38 gave it a new data point: the first fully clean mandatory `copilot-surrogate` pass this session (zero findings, ~27 independently-verified claims) — "the review pipeline earns its keep" doesn't mean "always finds something," a clean pass with real verification work shown is itself the discipline working, not evidence to start skipping it.
  - B. If a research-backed decision comes up again, independently spot-check any specific statistic before it lands in a permanent doc — see the `deep-research-numeric-claims-need-spot-check` memory.
- **Carry-overs:**
  - **New at Session 9 (2.7b):** a `da-review`/`copilot-surrogate` agent dispatched with `isolation: 'worktree'` can still end up doing verification work in the primary worktree if its assigned worktree isn't checked out as expected — happened twice this session (2.7b's R2 and `copilot-surrogate` passes), confirmed harmless both times (`git status`/`reflog` clean afterward) but worth re-verifying the primary worktree's state after any isolated dispatch returns, not just trusting the isolation flag did its job silently.
  - **New at Session 9 (2.7a):** self-review turning an uncommented `as` cast into a full design fix (collapsing two independently-nullable fields into one, removing the cast structurally) rather than just adding a justification comment — worth treating "does this cast need a comment, or does the type actually need reshaping" as the real self-review question, not just "is there a comment present."
  - Two nuances on established patterns from Session 7 — full detail in the `autonomous-run-handoff` memory note, not restated here: (1) a live test that never exercises the literal code path under review can still be a sufficient result if it confirms the deeper property that path protects; (2) fixing a cross-referenced claim in one file doesn't guarantee a sibling instance of the same issue in another file gets caught in the same pass — sweep all citing locations, don't assume one fix is complete.
  - From 2.6a: a PR's own diff can fail to apply a rule the PR is itself full of evidence for (BUILD_PLAN checkbox flips specifically) — self-review should check this explicitly per-PR now, not just rely on remembering.
  - **New at Session 8 (2.6b):** the same "PR's own diff doesn't apply its own evidence" failure mode recurred a second time, one layer deeper — this time the DA-fold _commit itself_ left `BUILD_PLAN.md` describing the mechanism the fold had just replaced. A fold commit needs the same "does this diff's prose still match this diff's code" self-check as the original PR, not just the code-correctness fixes DA asked for.
  - The session-handoff trigger's track record is still mixed, not a clean streak: fired unprompted at Session 6's own PR merge, on the user's explicit cue at Sessions 7 and 8 (both #34 and #35). Keep checking at every PR merge regardless of how the last one went. `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s own already-identified-but-deferred "deterministic pointer-injection hook" is still the concrete next step if this keeps needing prompting — Alex's call to scope as a real chunk, not something to build unilaterally.
  - `docs/decisions/` holds five ADRs, none trimmed to the ~50-line "decisions-only" budget yet (`docs/decisions/README.md`'s lifecycle step 3) — flagged repeatedly across review gates without being acted on. Content-judgment task on historical records — check with Alex first.
  - Sarah's real Slack App is verified working end-to-end: thread-scoped conversation memory, persona-aware naming, the status-claim gate, per-turn cost accounting, and now cost caps + spend alerts (confirmed live Session 8 — see the entries above). Credentials in gitignored `.env.local`, template at `.env.example`. Local live-testing needs `packages/core` migrations applied to whatever Postgres `DATABASE_URL` in `.env.local` actually points at — a fresh throwaway Docker container has no tables until `pnpm build && node scripts/migrate.ts` (from `packages/core`) runs against it; don't assume a prior session's local DB state carries over. `0004_create_persona_cost_daily.sql` and `0005_create_persona_cost_alerts.sql` are both now part of that migration set. `.env.local`'s `MOE_COST_CAP_MONTHLY` is reset to `50` post-verification (was temporarily `0.01` mid-session to make thresholds trivial to cross); `MOE_COST_ALERT_SLACK_USER_ID` is populated with Alex's real Slack user ID, looked up live via the Slack API rather than guessed.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - **Resolved at Session 8:** the long-deferred `buildPersonaSystemPrompt`/`HandlerDeps.personaId` string-vs-`PersonaId` typing gap ([PR #36](https://github.com/Pushedskydiver/moe/pull/36)) — flagged at 2.4b, missed at 2.5/2.6a/2.6b, finally proposed to and confirmed by Alex as its own tiny chunk rather than deferred a fifth time. No longer a standing carry-over.
  - Branch discipline: create the dedicated branch as the literal first action after syncing `main`, before any edit, every single chunk transition.
  - `ScheduleWakeup` is `/loop`-dynamic-mode-scoped, not a general "wait passively" tool — mis-reached-for twice at Session 8 while a `Monitor` was already doing the actual waiting on a background log tail. When something else (Monitor, a task notification) is already the wake signal, do nothing further; don't layer a second wakeup mechanism on top. Full detail in the `autonomous-run-handoff` memory note.
  - `pnpm`'s own binary bundles an internal Node runtime and overrides `PATH` for child processes it spawns — the documented Node-24 PATH-prepend fix (below) doesn't reach scripts pnpm shells out to internally (bit `packages/core`'s `migrate` script specifically, which needs Node 24's native TS execution). Workaround: invoke the Volta-pinned `node` binary directly for that one command, bypassing `pnpm run`/`pnpm --filter`. `pnpm build`/`test`/`lint`/`typecheck` are unaffected. Full detail in the `autonomous-run-handoff` memory note.
  - **New at Session 8:** Volta itself (not just pnpm) resolves the wrong pinned Node version when invoked from a package subdirectory lacking its own `"volta"` key in `package.json` — even by the Volta binary's own absolute path. Run Node-24-dependent one-off commands from the monorepo root by relative path, not after `cd`-ing into a package first. Full detail in the `autonomous-run-handoff` memory note.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm directly (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite.
  - Local dev needs a real Postgres for `packages/core`'s tests (`DATABASE_URL`) — CI provisions its own via a `postgres:17-alpine` service container; locally, a throwaway Docker container works (`docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=... -e POSTGRES_DB=... postgres:17-alpine`). One from Session 7 (`moe-postgres`) was still running and reused through Session 8 — check `docker ps` before starting a duplicate.
- **Fallback:** if Alex redirects on load, follow that; otherwise chunk 3.1 is next per BUILD_PLAN sequence — but it's a `[GATE]`, so open with the real conversation (scoring method, thresholds) before any code, not a quick-confirm question.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk | Status | Shipped    | Headline                                                                                                                   |
| ----- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------- |
| 0.1   | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                                       |
| 0.2   | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                                                   |
| 0.3   | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                                            |
| 0.4   | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                                       |
| 0.5   | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                                        |
| 0.6a  | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved                             |
| 0.6b  | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)                             |
| 0.6c  | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision                           |
| 0.7   | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**                                    |
| 1.1   | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                                                  |
| 1.2a  | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)                                  |
| 1.2b  | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                                              |
| 1.3   | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                                           |
| 1.4   | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                                                    |
| 1.5   | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`)                        |
| 1.6   | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**                                |
| 2.1   | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)                                |
| 2.2   | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config                           |
| 2.3   | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app                            |
| 2.4a  | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app                         |
| 2.4b  | Merged | 2026-07-17 | Thread-scoped conversation state + persona-naming fix — live-verified context-aware multi-turn chat                        |
| 2.5   | Merged | 2026-07-17 | Status claims through the `report_status` tool + 1.4 gate — VISION §7.6 anti-fabrication, wired                            |
| 2.6a  | Merged | 2026-07-17 | Cost metering substrate — `persona_cost_daily` table, atomic accumulation, live-verified turn cost                         |
| 2.6b  | Merged | 2026-07-17 | Cost caps + spend alert ladder — atomic claim, hard halt, live-verified full 50/80/100% arc                                |
| 2.7a  | Merged | 2026-07-18 | Core-hours + bank-holiday guard — DST-safe check, Cached\<T,E\> class, fails closed, no consumer yet                       |
| 2.7b  | Merged | 2026-07-18 | Away-detection — isAway/fetchSlackStatus, verified emoji shortcodes, live-verified, no consumer yet — **Stage 2 complete** |
