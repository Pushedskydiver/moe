# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 21)

Updated 2026-07-24 end-Session-21 — **Both Session 20 follow-ups closed out**: answered Alex's own-usage model question (Sonnet 5 → Opus 5, no code change) and shipped a `docs/VISION.md` §10 model-tier note refresh for Opus 5's release ([PR #70](https://github.com/Pushedskydiver/moe/pull/70)). **Stage 5's next real build chunk (5.2) is still ahead** — nothing shipped this session was itself a BUILD_PLAN chunk.

**Opus 5 own-usage question** (no PR — chat-only, `claude-api` skill loaded + live `WebFetch` of Alex's linked announcement): recommended switching to Opus 5 for Claude Code usage, citing Anthropic's own published figures (Opus 5 more than doubles Opus 4.8's Frontier-Bench v0.1 score, scores highest on "first-turn redlines," within 0.5% of Fable 5's coding peak at half the cost) and noting Claude Code's harness already auto-applies `xhigh` effort on supported models — there's no separate effort dial to set in the product itself, only the unrelated `/fast` speed toggle.

**`docs/VISION.md` §10 model-tier note refresh** ([PR #70](https://github.com/Pushedskydiver/moe/pull/70)): re-verified the note's SWE-bench Pro and cybersecurity citations directly against both system cards' own PDFs (`curl` + `pdftotext`, not a secondary aggregator) rather than trusting the original 2026-07-15 text. Found and corrected a real pre-existing misattribution in the process: the 69.2% Opus 4.8 figure had been cited to the Claude Sonnet 5 System Card, but that document's own SWE-bench Pro table never includes an Opus-tier model at all — the correct source is the Claude Opus 5 System Card's Table 8.1.A. Updated the headline comparison to Sonnet 5 vs. the newly-released Opus 5 (79.2%) — a **wider** 16.0-point gap than the original 6.0-point Opus-4.8 comparison, reinforcing rather than softening the note's own "treat Opus-tier as the working assumption for Riley's chunk 6.2" conclusion. Left the CodeRabbit bug-recall citation untouched (never Opus-anchored). Explicitly declined to extend the cybersecurity-gap claim onto Opus 5, since no primary source directly tests that pairing.

**Review saga — DA clean modulo 2 LOW + 1 nit (all fixed inline), then `copilot-surrogate` caught a second, independent pre-existing bug in the very sentence next to the one being edited.** DA (isolated worktree) independently re-downloaded both system-card PDFs and confirmed every number/quote in the rewrite, flagging only a wording-clarity nit ("narrows... widened" reading as a near-contradiction on a literal skim — fixed with an explicit reference-frame clause) and a note that `PROGRESS.md`'s own Session 21 loading instructions pointed at the wrong VISION section (§11 instead of the note's actual home, §10 — corrected in this same sync). The mandatory `copilot-surrogate` pass (VISION.md is blast-radius) then found a real MATERIAL of its own, in prose immediately upstream of the edited text but not introduced by it: the paragraph's opening sentence claimed "no model ID is hardcoded anywhere in the codebase yet" — false since BUILD_PLAN chunks 2.6a/3.1, which hardcode `claude-sonnet-5` (`generate-reply.ts`, `compose-ticket-draft.ts`) and `claude-haiku-4-5` (`classify-message-confidence.ts`, `evaluate-situational-appropriateness.ts`). Fixed to name the actual four call sites; R2 (fresh, isolated) independently re-grepped the whole of `packages/`/`apps/` for any Claude model-ID string to confirm the fix's claim was actually the complete set, not just internally consistent — confirmed clean.

**Novel patterns this session:**

1. **A "just update a citation" docs task surfaced two independent, pre-existing factual bugs it wasn't looking for.** Neither the wrong-system-card misattribution nor the stale "no model hardcoded" claim was what this task set out to find — both surfaced purely because the review gate (self-directed primary-source re-verification, then DA, then `copilot-surrogate`) was applied to the paragraph anyway, on a change that could easily have been treated as "too small to need the full gate."
2. **A citation can cite the right fact from the wrong document, and only re-fetching the actual cited source (not a secondary aggregator, not the number itself) catches it.** The 69.2% figure was numerically correct and appeared in a real Anthropic system card — just not the one named. Verifying "is this number true" is a different check from "does this document actually say this," and only the second one caught the bug.
3. **`copilot-surrogate`'s HEAD-scoped (not diff-scoped) reading found a defect in kept prose immediately adjacent to new prose, for the second time this project's history** (first instance: chunk 4.1, Session 13-era). Worth treating as a confirmed pattern, not a one-off: a small diff to one sentence in a paragraph is a good moment for a reviewer to reread the _whole_ paragraph, not just the changed clause.

**Earlier Session 20 status preserved below for context** — **Chunk 5.1 (multi-app Slack credential config + Slack App Manifest provisioning script) shipped** ([PR #69](https://github.com/Pushedskydiver/moe/pull/69)) — **Stage 5's first real build chunk done** (5.0 was decision-only); DA + a re-scoped `copilot-surrogate` pass each found real findings, all fixed and R2-confirmed, plus a PR-title CI fix and a self-caught mid-session process slip (an unconfirmed `git push`, now a durable memory rule).

**Chunk 5.1 — Multi-app credential config + provisioning script** ([PR #69](https://github.com/Pushedskydiver/moe/pull/69)): **First prerequisite found and fixed**: chunk 5.0 was decision-only (docs), but the actual roster _code_ (`packages/core/src/persona-roster.ts`) still only listed 7 personas — two existing tests actively asserted `'maya'` should be _rejected_. Fixed as this chunk's own first slice, confirming a previously forward-looking memory note ("a consumer package resolving a dependency from stale built output rather than live source") as a real, live instance again: `@moe/core`'s stale `dist/` initially made the new manifest tests fail with `Cannot read properties of undefined`, until rebuilt. **Live-verified against Slack's current API docs** (2026-07-24, no banked research existed for this): `apps.manifest.create`/`update` are Tier 1 (~1/min), `validate`/`export` are Tier 3 (~50/min); the response nests `signing_secret`/`client_id` inside a `credentials` object; no bot token or `xapp-` app-level token is ever returned — both remain Alex's own manual tail. The app-configuration token itself (distinct from a bot/app token) is manually generated at api.slack.com/apps and expires in 12 hours, refreshable via `tooling.tokens.rotate`. Two design forks resolved with Alex via `AskUserQuestion` before code moved: the new schema is additive (a separate multi-app type, not a reshape of the existing single-active-persona `PersonaConfig`); captured credentials print to stdout for manual copy rather than auto-writing to disk — no new secrets-at-rest surface.

**Built:** `buildPersonaSlackManifest` (`packages/slack`) — the shared manifest template, its OAuth-scope/event-subscription list reconstructed from every real Slack API call in the codebase plus config-only-reasoned scopes with no direct call site; only `users.profile:read` has a documented live-discovery trail (a `missing_scope` error on Sarah's real app, chunk 2.7b) — its own TSDoc flags the rest of the list as unverified against Sarah's real exported manifest, recommending an `apps.manifest.export` cross-check before actually provisioning the other 7 apps. `createManifestClient` + `provisionPersonaSlackApp` (`apps.manifest.validate` → `create`) + `parseManifestProvisioningConfig` + `parseProvisionPersonaIds` (safe narrower-retry targeting after a partial-batch failure, `MOE_SLACK_PROVISION_PERSONA_IDS`) round out `packages/slack`. `provisionSlackApps` (`apps/server`) orchestrates the batch: waits the Tier-1 rate limit between `create` calls, logs-and-continues past a single persona's failure, prints each captured credential set to stdout. The thin CLI (`scripts/provision-slack-apps.ts`, `pnpm --filter @moe/server provision:slack-apps`) defaults to every roster member except Sarah (already live). **No live execution against Slack yet** — every piece is built and fully tested against a mocked manifest client; actually creating the 7 real apps needs Alex to generate the app-configuration token first, a deliberate later step outside this chunk's own scope.

**Review saga — DA review (3 MATERIAL + 2 LOW, all fixed + R2-confirmed clean) followed by a real `copilot-surrogate` scope-ceiling escalation, correctly re-scoped rather than overridden.** DA found: a genuine security-relevant gap — `apps.manifest.create` has no natural-key dedup, so blindly re-running the full default persona list after a partial-batch failure would create duplicate real Slack apps for every persona that already succeeded, fixed via a real design addition (`parseProvisionPersonaIds` + the env override), not just a doc correction; a correctness bug where a `validate`-stage error (Tier 3, cheap, no app created) could get mislabeled `creation-failed` (Tier 1, the scarce budget), fixed via a dedicated `runValidation` helper with its own try/catch; and a secrets-hygiene gap — the config token lacked key-based redaction, matching an already-documented, twice-previously-bitten `docs/CONVENTIONS.md` anti-pattern. `copilot-surrogate`'s first dispatch hit its own 20-file scope ceiling on the PR's real 22-file diff and correctly stopped without reading anything, per its own protocol — re-dispatched (not overridden) to the 7 files actually carrying external/cross-doc claims, mirroring chunk 4.6's own precedent for the identical situation. That re-scoped pass, having independently live-re-verified every Slack API claim against docs.slack.dev and the real SDK source first, found 1 MATERIAL (`build-persona-slack-manifest.ts`'s TSDoc — duplicated in `BUILD_PLAN.md` — claimed 3 scopes were "only ever discovered live... (BUILD_PLAN 3.4a-iii, 3.3)," but neither cited chunk mentions a `missing_scope` discovery at all, and the claim silently omitted `users.profile:read`, the one scope chunk 2.7b _does_ document that way) + 3 LOW (a citation saying "Sarah's app" when the actual source comment says "Alex's app"; `docs/ARCHITECTURE.md`'s `packages/slack` row omitting the new `parseProvisionPersonaIds` export; a `docs/CONVENTIONS.md` wording ambiguity that read as claiming `provision*` carries no data on success, like `validate*`, when it plainly does). All 4 fixed and R2-confirmed resolved — R2 independently re-read chunk 2.7b's own text rather than trusting the fix's restated claim. Separately, CI's "Validate PR title format" check failed on the PR's original title, which used a comma-separated multi-package scope (`(core,slack,server)`) — exactly the failure shape `docs/GIT.md` §Types already names by example (`(core,agents,server)`) as a known CI-failure cause. Renamed (Alex's explicit go-ahead via `AskUserQuestion`) to drop the scope entirely, matching established multi-package-PR precedent (e.g. chunk 4.5's own scopeless title).

**Novel patterns this session:**

1. **`copilot-surrogate`'s 20-file scope ceiling fired a second real time** (first was chunk 4.6) — re-scoped to the 7 files carrying external/factual claims, the same shape as last time, reinforcing the "trust the ceiling, don't override" precedent with a second live instance rather than a hypothetical one.
2. **A documented anti-pattern can still land in the very codebase that documents it.** `docs/GIT.md` §Types already names comma-joined multi-package scopes as a known CI-failure shape, with a near-identical example (`(core,agents,server)`) — yet this chunk's own first commit used exactly that shape and failed CI on it. A second live instance of the "a written lesson doesn't self-apply to a new occurrence" meta-pattern this project already tracks.
3. **A genuine own-process mistake, caught and self-corrected mid-session.** Pushed a review-fix commit without the explicit confirmation this session had itself established as required before any `git push`. Disclosed transparently to Alex rather than continuing silently, and turned into a durable, no-size-exception memory rule rather than a one-off apology.
4. **R2 verification's rigor held under a second real MATERIAL finding, on unrelated subject matter.** Independently re-read the cited primary source (`BUILD_PLAN.md` chunk 2.7b's actual text) rather than trusting the fix commit's own restated claim — the same discipline Session 19's Impeccable-rule-count resolution first established, now confirmed holding on a different kind of claim (provenance/citation, not a raw count).

**Earlier Session 19 status preserved below for context** — **Chunk 5.0 (Cast redline completion — Designer/Maya activated) shipped**, roster now 8, activated ahead of Alex's own future second-project plan (not chief-clancy's own UI growth, which was investigated and found decision-irrelevant); two full DA-review-family rounds including a genuine cross-tool reviewer disagreement (Impeccable's rule count) resolved via direct primary-source verification.

**Chunk 5.0 — Cast redline completion** ([PR #68](https://github.com/Pushedskydiver/moe/pull/68)): the architectural pass found a real, concrete fact that looked at first like the literal re-evaluation trigger `docs/decisions/CAST-ROSTER.md` itself named — chief-clancy had grown a genuine UI codebase (`packages/design`, "Phase F," a React 19 + Vite canvas app) in the 9 days since the original 2026-07-15 deferral. That framing was **directly corrected by Alex**: the real driver wasn't chief-clancy's own surface at all (investigated and found real but immature/dev-tool-facing, not decision-relevant) — it was his own explicit steer to activate Designer proactively, ahead of a second, real end-user-UI project (his own Colour-Contrast-Checker repo) he plans to bring into scope once the single-project loop is proven, rather than defer until that need materializes. Alex asked for a default-to-yes absent strong, real evidence otherwise; a fresh check for such evidence (the three originally-cited papers, competing "AI coworker" product cast-composition disclosures) found none published in the intervening 9 days, so the default-yes held. **Roster is now 8** (Sarah/Marcus/Riley/Priya/Dom/Theo/Nia + **Maya/Designer**). Also settled in the same conversation, at Alex's own explicit request: chunk 5.3 (persona prompts) adopts a per-persona deep-research + adversarial-"grill" methodology — extending the existing `spec-grill` two-phase discipline from specs to prompts — and Designer's own research pass (a genuine multi-agent survey of Impeccable, Claude Design, and the broader AI-design-tool landscape, aimed at avoiding "AI slop") is banked ahead of its own future sub-chunk at `.claude/research/designer-persona-landscape/landscape-survey.md` (gitignored, local-only).

**Review saga — two full DA-review-family rounds, each requiring its own R2, plus a genuine cross-tool reviewer disagreement resolved live.** DA R1 found 2 MATERIAL (stale "7"-persona sibling references left in `BUILD_PLAN.md` outside the primary 5.0 edit — three separate lines describing the same "how many personas" fact the primary edit didn't sweep; a stale `docs/decisions/README.md` ADR-index row) + 3 LOW (a stale `TOPOLOGY-AND-DATABASE.md` footnote; a numeric slip in the Designer research bank; a citation-attribution nit), all fixed. **The first R2 dispatch was itself invalid** — a worktree-isolated agent checked its own stale worktree copy instead of the primary checkout holding the uncommitted diff, concluding (wrongly) that none of the five fixes existed; a corrected re-dispatch with the primary checkout's absolute path spelled out confirmed all five genuinely folded. `copilot-surrogate` (mandatory: `VISION.md`/`PERSONAS.md`/`DEVELOPMENT.md` are all blast-radius docs) then found 3 MATERIAL + 1 LOW of its own that DA's pass had missed entirely: a stale "seven apps" reference in `VISION.md` §6.6 (the same sibling-reference-miss class as DA's own finding, just in a different file DA hadn't swept); Maya missing from Appendix C's per-persona anti-sycophancy layering table (present in `docs/PERSONAS.md`'s parallel table, absent here); and a genuine three-way disagreement on Impeccable's actual rule count (the original research said 59, DA's own fold changed it to 58 per the tool's README, `copilot-surrogate`'s own live count said 59 again) — resolved, per `docs/DEVELOPMENT.md`'s own rule for contradictory review verdicts, by going to the primary source directly: a fresh, independent fetch+count of the actual registry file confirmed 59 is correct and the README is stale, with the file's own cited path also corrected (`cli/engine/registry/antipatterns.mjs`, not the originally-cited `skill/` path, which doesn't exist). All fixed, R2-confirmed via an independent re-fetch of the same file, not just re-reading the fix's own claim.

**Novel patterns this session:**

1. **A worktree-isolated review dispatch can't see the primary session's own uncommitted changes** — `git worktree add` shares committed history, not the working-tree diff, so a dispatch reviewing uncommitted work needs the primary checkout's absolute path spelled out explicitly, or the agent silently reads its own stale copy and reports back as if the changes don't exist at all (indistinguishable from a real, if unlikely, finding). Now codified directly in `docs/DEVELOPMENT.md`, not memory-only, per Alex's standing preference for protocols to live in the project.
2. **A research-backed hypothesis can be evidence-supported and still be wrong about the actual motivating reason.** The session's own architectural pass found a real, checkable fact (chief-clancy's `packages/design` growth) that looked like it satisfied the literal re-evaluation trigger — but Alex's own direct correction revealed the real driver was something the research never touched (his own future project-onboarding plan). The lesson isn't "the research was bad" — it's that a plausible, evidence-backed causal story still needs to be checked against the actual decision-maker's own reasoning before it's treated as settled.
3. **The "fix one sibling reference, miss another" pattern now has a cross-tool instance, not just a cross-location one.** DA's own pass caught and fixed three stale "7"-persona references in `BUILD_PLAN.md` but never checked `VISION.md` for the same underlying fact — `copilot-surrogate`'s later, independently-scoped pass caught VISION's own stale "seven apps" line. Two different review tools, two different blind spots on the identical underlying drift.
4. **A genuine reviewer disagreement on an external fact was resolved by going to the primary source directly, not by picking a side — the first live instance of `docs/DEVELOPMENT.md`'s own already-codified rule for this actually firing.** Three passes disagreed on Impeccable's rule count (59 → 58 → 59) across the session; the resolution wasn't averaging or trusting the "most recent" or "most confident" account, but an independent fourth fetch+count against the actual file, which sided with the two "59" counts over the tool's own (stale) README claim.

**Earlier Session 18 status preserved below for context** — **Chunk 4.6 (DB backup + tested restore) shipped** ([PR #67](https://github.com/Pushedskydiver/moe/pull/67)) — **Stage 4 fully complete**, the project's first chunk against real, newly-provisioned external infrastructure (a real Neon project), taking a record four independent review rounds to reach clean on a security-sensitive connection-string parser.

**Chunk 4.6 — DB backup + tested restore** ([PR #67](https://github.com/Pushedskydiver/moe/pull/67)): the architectural pass ran as 4 parallel research agents (repo-internal state, live Neon-docs verification, ops/conventions precedent, downstream BUILD_PLAN/VISION references) and surfaced a genuine surprise: no production Neon project had ever actually been provisioned — `fly deploy` had never been run for real, `.env.local`/CI both pointed at local/stand-in Postgres, and `docs/decisions/TOPOLOGY-AND-DATABASE.md`'s own PITR claim was explicitly flagged as unverified. Put to Alex via `AskUserQuestion`: he created a real free-tier Neon project on the spot specifically for this chunk, then chose live verification over a docs-only build, and both restore paths (Neon-native PITR **and** a `pg_dump`/`pg_restore` fallback) over PITR alone — the latter matching Neon's own guidance to pair PITR with a portable logical backup, reinforced by Neon's status page showing a real recurring pattern of branch/compute-reliability incidents through 2026. A follow-up clarifying question mid-session ("do I need to enable Neon Auth?") was answered by live-verifying against Neon's current docs rather than from training-data memory — Neon Auth is an unrelated optional add-on, not part of project creation.

**Built:** `packages/core/src/backup/` — pure, tested command-builders wrapping `pg_dump`/`pg_restore` inside a throwaway `postgres:18-alpine` container (version-matched to the real Neon project's actual Postgres 18, confirmed via `neonctl projects list` — a live rehearsal itself caught that local smoke-testing had been using CI's unrelated `postgres:17-alpine` pin until the real project's version was actually checked, which would have made a real production dump silently refuse to run). Thin CLI scripts `packages/core/scripts/backup.ts`/`restore.ts` mirror `migrate.ts`'s own shape. Documented in new `docs/OPERATIONS.md` (added to `CLAUDE.md`'s Key docs). **Live-verified against the real Neon project** ("Moe AI Team", `plain-wildflower-25588697`) twice each (once per major fix round that changed the parsing mechanism) — not just read from Neon's docs: a PITR rehearsal inserted a marker row, restored the branch to a pre-marker timestamp via `neonctl branches restore`, and confirmed the marker was gone while `schema_migrations` still showed all 16 rows (proving the actual rewind, not just that the command returned); a `pg_dump`/`pg_restore` rehearsal dumped the real project and restored it into an unrelated local Postgres database, confirming full schema + a marker row survived the round-trip.

**Review saga — four DA-review-family rounds on one PR, a new project record.** R1 (DA): 1 Critical — an unsanitized `BACKUP_FILE_PATH` reached a shell-embedded restore command unsanitized (real command injection, fixed via a new `isShellSafeFileName` allowlist check) — plus 2 Material (the original design put the plaintext connection string in `pg_dump`/`pg_restore`'s own argv via `--dbname="$CONN"`, visible via `docker top` even though never in `docker`'s own argv, fixed by switching to discrete `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE`/`PGSSLMODE` env vars; and the destructive-restore confirmation gate never showed the operator what it would destroy, fixed via a redacted-connection-string confirmation phrase) plus 2 Low. **R2** found the confirmation-gate fix only partial — a malformed `DATABASE_URL` still crashed both scripts via an unguarded `new URL()` call. **A third independent pass** found THAT fix (`isValidConnectionString`) only proved URL-shape, not that percent-encoding was decodable — `decodeURIComponent` remained a separate unguarded throw site, reachable in `restore.ts` even after the destructive-action confirmation had already been shown — plus a genuinely new class of bug neither R1 nor R2 caught: a decoded control character (`%0A`) could inject an arbitrary extra line into the generated `--env-file`, the same injection _class_ as R1's Critical at a different boundary. Fixed by consolidating everything into one `Result`-returning `parsePgEnvFromConnectionString` that never throws. **A fourth, tightly-scoped pass** (walk every `return`, run 5 adversarial inputs against real compiled output) confirmed that consolidation genuinely clean, with one further Low (an implicit, untested safety argument about why the hostname field alone is never decoded) closed with a regression test + comment. `copilot-surrogate` (scope-escalated once — the 28-file diff exceeded its own 20-file ceiling, cleanly re-scoped to the 7 files carrying external/factual claims per its own recommendation) then found 1 Material of its own: `BUILD_PLAN.md`'s own review-history paragraph said "three rounds" while narrating four. Self-review caught a stale TSDoc in `docker-run-command.ts` still describing the since-removed `$CONN` mechanism, and a control-character regex duplicated verbatim across two files rather than sharing one constant.

**Novel patterns this session:**

1. **Fixing a review finding can uncover — or itself introduce — the next gap in the same call chain, repeatedly.** Three consecutive rounds (R1→R2→R3) each found the previous commit's fix only partially closed the underlying "malformed/adversarial `DATABASE_URL` crashes or leaks" class, via a different specific code path each time (a bare `new URL()`, then `decodeURIComponent`, then an un-escaped control character in the generated env file). The lesson isn't "the fixes were bad" — each one genuinely closed what it targeted — it's that a security-sensitive function accepting untrusted input deserves one adversarial-input-enumeration pass up front (or a fresh reviewer explicitly told to hunt broadly, not just verify the stated claim) rather than fixing exactly the reported symptom each round and hoping nothing adjacent got missed.
2. **A background review-agent dispatch can fail on pure infrastructure, not agent judgment — retry before assuming the task itself is unworkable.** The fourth-round verification dispatch hit "Connection closed mid-response" three times in a row via the plain Agent tool, then hung entirely when tried via the Workflow tool. What actually worked was narrowing the prompt from open-ended ("hunt broadly for a fourth category of bug") to a fixed 4-step numbered checklist — worth trying a tighter scope before concluding a dispatch is structurally too heavy, but also worth just retrying 2-3 times first since this looked like transient infra flakiness, not a scope problem, until the narrower prompt actually confirmed it.
3. **A review agent's own self-imposed scope ceiling firing is a signal to work with, not around.** `copilot-surrogate`'s 20-file cap correctly fired on this PR's real 28-file diff and returned a well-reasoned narrower recommendation (the 7 files actually carrying external/cross-doc claims) rather than either silently truncating or refusing outright — re-dispatching on exactly that recommended scope was the right move, not overriding the ceiling.
4. **A doc narrating "here's what was fixed and confirmed" needs the exact same scrutiny as the code it describes — and can fail the same way twice in one PR.** `BUILD_PLAN.md`'s chunk 4.6 entry asserted "R2-confirmed" in the same commit that made the fix, before an independent pass had actually run (caught by the real R2); after correcting that, the same paragraph's own review-round _count_ was still wrong (said three, narrated four) — caught only by `copilot-surrogate`'s dedicated claim-extraction pass. Two independent instances of the same underlying failure (a completeness claim written before the thing it claims has actually happened) inside one PR's own review-history narrative.

**Earlier Session 17 status preserved below for context** — **Chunk 4.5 (WIP limits + classes of service enforcement) shipped** ([PR #65](https://github.com/Pushedskydiver/moe/pull/65)), followed by a same-session cleanup of a repeatedly-flagged, never-actioned content-judgment task (ADR trimming, [PR #66](https://github.com/Pushedskydiver/moe/pull/66), Alex asked directly).

**Chunk 4.5 — WIP limits + classes of service enforcement** ([PR #65](https://github.com/Pushedskydiver/moe/pull/65)): architectural pass run as 4 parallel research agents (VISION/BUILD_PLAN requirements, the ticket schema/migration conventions, the claim mechanism/guard-shape precedents, build conventions) rather than a solo read — three genuinely open questions then put to Alex via `AskUserQuestion`, who delegated the final call on all three to "strong, real evidence" rather than picking an option directly (a new interaction shape this session, distinct from every prior chunk's explicit-pick pattern): **the Expedite queue-_ordering_ gap** — the ADR/GLOSSARY both promise Expedite "jumps ahead of Standard work within its board status," but no BUILD_PLAN chunk (4.5's own WIP-cap guard, or the future 6.1a-i pull loop) claimed that ordering logic — resolved by flagging it as a new BUILD_PLAN 6.1a-i follow-up note rather than inventing an untested primitive with no real consumer to validate its shape against; **the guard's return shape** — a decision+reason object (`{allowed, reason}`), matching `evaluateOperatingRhythm`'s own shape and `docs/CONVENTIONS.md`'s `evaluate*` verb convention, over a bare boolean or a `claim.ts`-style Result union; **`classOfService`'s default mechanism** — resolved twice, not once: the first-pass recommendation (DB-level `DEFAULT 'Standard'` only, zero application-code changes) turned out to be technically infeasible once actually traced through `createTicket`'s pre-insert `ticketSchema.safeParse` call, which validates a candidate row _before_ it ever reaches the database's own default — the real established precedent (migration `0013`'s `origin` column, traced through `createPendingTicketDraft`) already resolved this the other way: the DB default exists solely for backfilling pre-existing rows, and every new insert supplies the value explicitly. `NewTicket`'s Pick was widened accordingly, and `reaction-outcome-actions.ts`'s `commitAsTicket` got a new `DEFAULT_CLASS_OF_SERVICE` constant alongside its existing `DEFAULT_SEVERITY`.

Built as the `classOfService` migration (`packages/core/migrations/0016_add_ticket_class_of_service.sql`, following `0013`'s exact `ALTER TABLE ... DEFAULT ... CHECK` shape) and `evaluateWipLimit` (`packages/core/src/capacity/`, pure and synchronous like `classifyRiskTier` — not async like `evaluateOperatingRhythm`, since nothing before the not-yet-built BUILD_PLAN 6.1a-i pull loop can produce a live per-status count) enforcing `DEFAULT_WIP_LIMITS` (Brief 3, Plan 2, Build 2, Review 2; Backlog/Done/Cancelled uncapped — every `BoardStatus` listed explicitly so a future status fails to compile here until decided). **No live call site yet** — BUILD_PLAN 6.1a-ii wires it in, same shape chunk 4.4a's `composeExternalPostBody` had before 4.4b.

**DA review found 1 MATERIAL, both R2-confirmed fixed**: two new prose sites (`BUILD_PLAN.md`'s new 6.1a-i note, `wip-limit-guard.ts`'s own TSDoc) quoted the ADR's "jumps ahead of Standard work within its board status" as "...Standard within its board status," dropping "work" — `docs/DA-REVIEW.md`'s own named recurring-misquote failure shape. Plus 2 LOW, 1 fixed (an `index.ts` export-placement drift from the file's established append-at-end-by-chunk convention — a repeat of a Session-13-era defect this repo had already corrected once). R2's own sibling-location sweep then found a _third_, pre-existing instance of the identical dropped-word error in `docs/GLOSSARY.md` (chunk 4.3, unrelated to this diff) — Alex asked to fold that fix into this same PR too, along with updating both GLOSSARY entries' stale "not yet built" language now that this PR actually ships. `copilot-surrogate` (mandatory: diff over 50 LOC, new prose citing the ADR/BUILD_PLAN/GLOSSARY) then found its own LOW: `schema.ts`'s `classOfService` TSDoc pointed at `PendingTicketDraftsTable`'s own TSDoc for a backfill caveat that TSDoc doesn't actually contain — the caveat language lives only in migration `0013`'s SQL comment, a different file — fixed to point at the right source.

**Side arc — ADR trim** ([PR #66](https://github.com/Pushedskydiver/moe/pull/66)): `docs/decisions/`'s four over-budget ADRs (`CAST-ROSTER.md`, `REVIEW-GATE-DISCRETION.md`, `STAGE-1-CLASSIFIER.md`, `TOPOLOGY-AND-DATABASE.md`) had been flagged repeatedly across sessions without action — Alex asked to sort it out directly. Read all four in full before touching anything: none were bloated leftovers from an un-trimmed brief stage — every section was genuinely decision-relevant (real citations, real eval numbers, real incident data), so mechanical cutting would have traded away the "positions considered and rejected" content the doc type exists to preserve. Trimmed only what was genuinely cuttable (one tangential citation-fidelity aside, bullets/points arguing the same underlying point from adjacent angles merged into one, one stale forward-reference to an already-shipped chunk) — three files reached exactly 50 lines, one (`REVIEW-GATE-DISCRETION.md`) settled at 52 rather than forcing distinct rationale threads together further, reported honestly rather than hidden. **Two full DA-review-family rounds surfaced real defects a purely mechanical trim wouldn't have caught**: DA (R1) found 3 MATERIAL — a Sonnet score silently decoupled from its named example during a merge, a citation left orphaned once its explaining footnote was cut, a production-code comment (`classify-message-confidence.ts`) left pointing at a renumbered "Decision 4" that no longer existed — all fixed, R2-confirmed clean. The mandatory `copilot-surrogate` pass then found a 4th MATERIAL entirely on its own: `CAST-ROSTER.md`'s MAST/79% citation-fidelity footnote had pointed at the wrong VISION section (§13.1, unrelated content) since the ADR's original 2026-07-15 draft — a pre-existing bug nobody had caught in over a week, surfaced only because the trim's own fold moved that sentence into a more prominent spot. Fixed, R2-confirmed, R2 explicitly ran a repo-wide sweep for the same wrong-section defect recurring elsewhere (clean).

**Novel patterns this session:**

1. **A recommendation given to Alex mid-session can be wrong even when evidence-backed, if the evidence used was too shallow.** The DB-default-only design was recommended with a real citation (migration `0013`'s own file), but only tracing the actual insert _code path_ (not just the migration file) surfaced that `createTicket`'s pre-insert Zod validation would reject a candidate missing a required field regardless of the DB's own default. The eventual correct answer was still evidence-based (the same `origin` precedent, traced one level deeper via `createPendingTicketDraft`) — the lesson is "trace the full call path before committing to a design claim," not "don't trust evidence."
2. **A misquote fixed in two sibling locations within one PR can still have an unrelated third instance elsewhere in the repo.** This is the project's fourth-ish instance of "a defect caught and fixed once recurs in a sibling location," but the new wrinkle is that R2's sibling-sweep (dispatched only to verify the _known_ two fixes) is what surfaced a completely separate, pre-existing instance in a different chunk's doc — the sweep's own thoroughness found something outside its assigned scope, which Alex then explicitly asked to fold in rather than leave as a background-task chip.
3. **Alex delegating a design decision to "your own steer, based on strong, real evidence" is a distinct interaction shape from every prior chunk's explicit multiple-choice pick.** Worth recognizing as its own pattern: it raises the bar for justifying the choice — each of the three decisions here cited a specific, checkable precedent (an existing function's TSDoc, an existing migration file, a specific codebase convention doc) rather than presenting options and waiting to be told which one.
4. **A doc-freshness pattern already flagged twice recurred a third time, on this very PR.** `docs/ARCHITECTURE.md`'s `packages/core` row didn't mention `classOfService`/`evaluateWipLimit` post-merge — fixed as a direct-to-main `fix(docs)` drift commit (all five of `docs/GIT.md`'s predicates held: no open branch, grep-falsifiable, low-LOC, not blast-radius, not executable markdown) rather than left for a future session to rediscover.
5. **A task repeatedly flagged and deferred as "content judgment, check with Alex first" turned out to hide real, unrelated defects once someone actually did it.** The ADR trim's own stated goal was a line-count budget, but the review gate it went through anyway caught four genuine factual/structural bugs — one of them (the wrong VISION §13.1 pointer) had been silently wrong for over a week across multiple intervening sessions that touched the same file. The lesson isn't specific to line-count budgets: a standing "flagged but deferred, needs judgment" item is not the same as "low-value," and deferring it repeatedly means whatever's actually wrong underneath stays wrong for exactly as long as it keeps getting deferred.

**Session 16 archived** — compressed to a one-line row in `docs/history/SESSIONS.md` (chunk 4.4c, reconciliation poll, [PR #64](https://github.com/Pushedskydiver/moe/pull/64)); full retrospective survives in `git log -p PROGRESS.md` at this session's compression commit.

### Session 22 loading instructions

- **Verify state before picking anything up:** `git log --oneline -8` on `main` (expect this sync commit at HEAD, then PR #70's squash, then the PR #69 sync commit, then PR #69's own squash, then the PR #68 sync commit, then PR #68's squash), `git status` clean. `BUILD_PLAN.md` shows chunk 5.1 `[x]`; **chunk 5.2 (N-process deployment) is the actual next real build chunk** — nothing in Session 21 was itself a chunk. Also check `docker ps -a --filter name=moe-postgres` and `ssh-add -l` before assuming either is fine — both have silently dropped state mid-session before.
- **Primary workstream — DONE this session.** Both Session 20 follow-ups closed: the Opus-5 own-usage answer (chat-only, no PR) and [PR #70](https://github.com/Pushedskydiver/moe/pull/70) (VISION.md §10 model-tier note refresh) — merged. See the entries above for full detail, including the two independent pre-existing bugs `copilot-surrogate` and DA each caught along the way.
- **Next up — BUILD_PLAN 5.2: N-process deployment.** Described as "genuinely mechanical" per its own text (Fly config per the 1.2a ADR, shared DB access as already decided there, per-persona health checks) — the DB engine and atomic-claim tests are already proven against the real topology, so this shouldn't need new engine work.
- **Decision branches:**
  - K/L (Session 19, still live, both already codified in `docs/DEVELOPMENT.md`): a worktree-isolated review dispatch needs the primary checkout's absolute path spelled out explicitly when reviewing uncommitted changes; a genuine cross-tool reviewer disagreement on an external fact resolves via a third independent primary-source check, not by picking a side.
  - H/I/J/E (Session 18, still live): adversarial-input enumeration pass on security-sensitive input handling; trust a review agent's own scope-ceiling recommendation, re-scope rather than override; scrutinize a "here's what's fixed" narrative against a fresh pass's actual output, not belief; `isolation: 'worktree'` on every DA/R2/`copilot-surrogate` dispatch, no exceptions.
  - M (Session 20, still live, held cleanly this session): confirm with Alex before every `git push`, no size/risk exception — asked before both pushes on the VISION.md PR this session, no lapse.
  - N. **New this session: a small diff to one sentence in an already-reviewed paragraph is still worth a full-paragraph reread, not just a diff-scoped one.** `copilot-surrogate`'s own HEAD-scoped (not diff-scoped) reading is what caught the stale "no model hardcoded" claim sitting one sentence away from the actual edit — a diff-scoped reviewer would have missed it entirely. Second confirmed instance of this pattern (first: chunk 4.1, Session 13-era).
- **Carry-overs:**
  - **Roster is now 8**: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master, **Maya/Designer**. `docs/VISION.md` §4.1, `docs/PERSONAS.md`'s roster table, and `docs/decisions/CAST-ROSTER.md`'s Status update are the authoritative records.
  - **Designer's own research bank exists** at `.claude/research/designer-persona-landscape/landscape-survey.md` (gitignored, local-only) — read it before drafting Maya's actual prompt at her future 5.3 sub-chunk.
  - **`packages/slack`'s manifest-provisioning surface has never been run live against Slack** — every piece is built and tested against a mocked client only. Actually creating the other 7 personas' real Slack apps needs Alex to generate a fresh app-configuration token at api.slack.com/apps first (12-hour expiry) — a deliberate later step, not yet scheduled to a chunk.
  - **Model-tier reality as of Session 21**: the codebase hardcodes exactly two models across four call sites — `claude-sonnet-5` (`generate-reply.ts`, `compose-ticket-draft.ts`) and `claude-haiku-4-5` (`classify-message-confidence.ts`, `evaluate-situational-appropriateness.ts`, the Stage-1 classifier gate). `docs/VISION.md` §10's Model-tier note now recommends Opus-tier (Opus 5, `claude-opus-5`) as the working assumption for Riley's heavyweight chunk 6.2 coding work specifically — not a blanket swap of the two existing hardcodes, which remain untouched until real eval data says otherwise, per the note's own "informational, not a pin" framing.
  - **A real Neon project exists**: "Moe AI Team", id `plain-wildflower-25588697`, Postgres 18, region `aws-eu-west-2`. `NEON_DATABASE_URL`/`NEON_API_KEY` in `.env.local` (gitignored). Treat as real production-adjacent infrastructure, not a throwaway.
  - `packages/core/src/backup/` + `docs/OPERATIONS.md` — read the latter before touching production data.
  - Newest migration is still `0016_add_ticket_class_of_service.sql`. `packages/core/src/capacity/` (`evaluateWipLimit`) still has **no live call site**, waiting on 6.1a-ii. `docs/decisions/BOARD-AND-CAPACITY-MODEL.md`'s Expedite queue-ordering promise is still unbuilt (6.1a-i follow-up). `Pushedskydiver/chief-clancy#489` remains deliberately open as a live-verification artifact.
  - Node-version resolution (Volta vs. pnpm's own child-process check disagreeing on the reported version) is still an open, unresolved minor discrepancy — verify fresh each session rather than trusting either the old "PATH prepend needed" note or last session's "seems fine" observation. `DATABASE_URL` still needs exporting per Bash invocation for any `packages/core`/`apps/server` DB-backed test run.
  - `docs/history/SESSIONS.md` now runs through Session 16 (just archived this sync); `PROGRESS.md`'s detail band holds Sessions 17-21 (5 entries) — **at, not yet past, the archival threshold** again. The next session to add its own entry will need to archive Session 17 first.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk    | Status | Shipped    | Headline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------- | ------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1      | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 0.2      | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 0.3      | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 0.4      | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 0.5      | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 0.6a     | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 0.6b     | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 0.6c     | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 0.7      | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1.1      | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.2a     | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.2b     | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1.3      | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 1.4      | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1.5      | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.6      | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.1      | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.2      | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.3      | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.4a     | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2.4b     | Merged | 2026-07-17 | Thread-scoped conversation state + persona-naming fix — live-verified context-aware multi-turn chat                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.5      | Merged | 2026-07-17 | Status claims through the `report_status` tool + 1.4 gate — VISION §7.6 anti-fabrication, wired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.6a     | Merged | 2026-07-17 | Cost metering substrate — `persona_cost_daily` table, atomic accumulation, live-verified turn cost                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2.6b     | Merged | 2026-07-17 | Cost caps + spend alert ladder — atomic claim, hard halt, live-verified full 50/80/100% arc                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.7a     | Merged | 2026-07-18 | Core-hours + bank-holiday guard — DST-safe check, Cached\<T,E\> class, fails closed, no consumer yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.7b     | Merged | 2026-07-18 | Away-detection — isAway/fetchSlackStatus, verified emoji shortcodes, live-verified, no consumer yet — **Stage 2 complete**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 3.1      | Merged | 2026-07-18 | [GATE] Stage-1 classifier scoring method + thresholds — bundled call, Haiku 4.5, High≥70/Mid35-69/Low<35 — **Stage 3 opened**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 3.2      | Merged | 2026-07-18 | Channel scoping (Stage 0) — isSurfaceInScope allow-list + DM bypass, no consumer yet, first Workflow-dispatched review arc                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 3.3      | Merged | 2026-07-18 | Classifier gate (Stage 1) — Haiku 4.5 wired live, DMs-only chat replies, cost-cap fix, live-verified end-to-end                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3.4a-i   | Merged | 2026-07-18 | High-band auto-draft — Sonnet-5 ticket draft, shadow/log-only, 2.7a guard wired, live-verified end-to-end                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3.4a-ii  | Merged | 2026-07-18 | Reaction listener + outcome paths — commit/redo/park, no live consumer, prototype-pollution fix, live-verified via real infra                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 3.4a-iii | Merged | 2026-07-19 | Situational-appropriateness gate + real end-to-end wiring — real post, self-authored filter, live dispatch — **Stage 3 exit criterion passes live**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 3.4c     | Merged | 2026-07-19 | Review-queue table + Low-band silent log — built ahead of 3.4b (sequencing resolved via `AskUserQuestion`), `outcomeReason` column anticipates 3.4b, live-verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3.4b-i   | Merged | 2026-07-19 | Mid-band confirming question (ask-side) — split from 3.4b-ii via `AskUserQuestion`, shared guards extracted, DA MATERIAL fixed + R2-confirmed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 3.4b-ii  | Merged | 2026-07-19 | Mid-band confirming question (answer-side) — 👍/👎 dispatch, `postAndPersistDraft` reused, DA+`copilot-surrogate` MATERIAL fixed + R2, live-verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 3.5      | Merged | 2026-07-19 | Review-queue sweep — manual CLI script, `sweep_state` table, dual DA+`copilot-surrogate` R2 arc — **Stage 3 complete** (3.6 may trail into Stage 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 3.6      | Merged | 2026-07-20 | Draft-outcome tracking — committed/redone/ignored counts in the sweep digest, DA+`copilot-surrogate` dual-fold arc scoped the metric to High-band only — **Stage 3 fully complete**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 4.1      | Merged | 2026-07-21 | GitHub App auth + client — `@octokit/auth-app` installation-token auth, live-verified against a real App on `Pushedskydiver/chief-clancy`, DA MATERIAL fixed + R2 — **Stage 4 opened**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4.2      | Merged | 2026-07-22 | Issue discovery (poll) — `github_issue_triage` table, live-verified read-only, bundled 44-site logger-collision fix + `check-cost-cap.ts` coverage gap, DA + `copilot-surrogate` MATERIAL fixed + dual R2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 4.3      | Merged | 2026-07-22 | [GATE] Board home + capacity model — moe's DB canonical, Standard/Expedite CoS, `classOfService` field named (not built), WIP limits 3/2/2/2 — decision-only, DA + `copilot-surrogate` MATERIAL fixed + dual R2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 4.4a     | Merged | 2026-07-22 | External-post attribution — `composeExternalPostBody`, persona roster promoted to `packages/core`, `@Pushedskydiver` footer escape-hatch, no live consumer yet — DA Low findings fixed, `copilot-surrogate` clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4.4b     | Merged | 2026-07-23 | Outbound issue creation — `createGithubIssue`, `ticket_github_issue_links` claim-first idempotency guard, live-verified real issue `chief-clancy#489` — DA MATERIAL (incl. a fabricated fix claim) fixed + R2, `copilot-surrogate` Low fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4.4c     | Merged | 2026-07-23 | Reconciliation poll — `getGithubIssueState` per-issue lookup, `listResolvedTicketGithubIssueLinks`, closure→`Cancelled` (any reason, `Done` skipped, `Build`/`Review` still cancels loudly, reopened stays `Cancelled`) — DA MATERIAL (async conversion, real outcome logging) fixed, live-verified read-only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4.5      | Merged | 2026-07-23 | WIP limits + classes of service — `classOfService` migration (0016) mirrors `origin`'s backfill-only-default precedent, `evaluateWipLimit` pure/sync decision+reason guard (Brief 3/Plan 2/Build 2/Review 2), no live call site yet — DA MATERIAL (dropped-word misquote, R2-confirmed) + `copilot-surrogate` LOW fixed, Expedite ordering flagged as 6.1a-i follow-up                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4.6      | Merged | 2026-07-24 | DB backup + tested restore — first real Neon project provisioned (`plain-wildflower-25588697`), `packages/core/src/backup/` command-builders + `docs/OPERATIONS.md`, both PITR + `pg_dump`/`pg_restore` fallback live-verified twice against the real project — DA Critical (command injection) + 2 Material fixed, 3 further independent passes each closed a gap the last one's fix left open, `copilot-surrogate` Material (miscounted review-round header) fixed — **Stage 4 complete**                                                                                                                                                                                                                                                                                                     |
| 5.0      | Merged | 2026-07-24 | [GATE] Cast redline completion — Designer/Maya activated, roster now 8 (not chief-clancy's own `packages/design` growth, investigated and found immature/dev-tool-facing — Alex's own proactive-activation steer ahead of a future second project); settles chunk 5.3's per-persona deep-research + grill methodology, Designer's own research banked at `.claude/research/designer-persona-landscape/landscape-survey.md` — DA (2 Material) + `copilot-surrogate` (3 Material) fixed + dual R2, incl. a genuine cross-tool reviewer disagreement (Impeccable rule count, 58 vs. 59) resolved via direct primary-source verification — **Stage 5 opened**                                                                                                                                       |
| 5.1      | Merged | 2026-07-24 | Multi-app Slack credential config + provisioning script — `buildPersonaSlackManifest`/`provisionPersonaSlackApp`/`parseProvisionPersonaIds` (`packages/slack`), `provisionSlackApps` orchestrator + thin CLI (`apps/server`), live-verified against Slack's current API docs (manifest tiers, response shape, token expiry); found & fixed a prerequisite gap (roster code still 7 personas post-5.0's docs-only decision) as its own first slice; no live execution against Slack yet — DA (3 Material) fixed + R2, `copilot-surrogate` scope-ceiling escalation correctly re-scoped to 7 files (2nd live instance of that precedent) then found 1 Material + 3 Low fixed + R2, plus a PR-title CI fix (comma-separated scope, the exact anti-pattern `docs/GIT.md` already documents by name) |
