# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 14)

Updated 2026-07-22 end-Session-14 — **Chunk 4.2 (Issue discovery poll) shipped** ([PR #57](https://github.com/Pushedskydiver/moe/pull/57)), bundled with a real cross-cutting logging bug found live mid-session and a pre-existing test-coverage gap Alex asked to fold in — one PR, three distinct concerns, all through the full review gate. **Same session, continued: chunk 4.3 (the board home + capacity-model GATE) also shipped** ([PR #58](https://github.com/Pushedskydiver/moe/pull/58)) — a decision-only, docs-only PR that still needed two full DA-review-family fold cycles.

**Chunk 4.3 — Board home + capacity-model decision** ([PR #58](https://github.com/Pushedskydiver/moe/pull/58)): a `[GATE]` chunk, no application code. Architectural research first (VISION §3.1/§3.4, `packages/core`'s existing `tickets`/`severity`/`board-status` schemas, what `packages/github` chunk 4.1/4.2 actually built), then four questions resolved with Alex via `AskUserQuestion`, all recommendations accepted: **board home** — moe's own DB stays canonical (the `tickets` table is already load-bearing since chunk 1.2b; GitHub Projects v2, cited in BUILD_PLAN's own original framing as "the old VISION's answer," has zero code and that prior design isn't retained anywhere checkable in this repo); **classes of service** — two, Standard + Expedite (Expedite = `#moe-incidents`-sourced or `severity: 'Critical'`); **a new explicit `classOfService` ticket-schema field**, not derived from `severity` (which is itself still a hardcoded placeholder everywhere a ticket gets created) — named as chunk 4.5's own follow-up migration, not built in this decision-only chunk; **WIP limits** — small starting caps (Brief 3, Plan 2, Build 2, Review 2; Backlog/Done uncapped), the same "reasoned starting number, no calibration data yet" logic the track-record N=5 ADR used, since no persona pull-loop beyond Sarah's intake exists to generate throughput data. Full reasoning: `docs/decisions/BOARD-AND-CAPACITY-MODEL.md`. Also resolved VISION §3.1/§3.4's own long-open question (append-only "Resolved" paragraphs, not rewrites), updated `docs/GLOSSARY.md`'s Classes-of-Service entry, and fixed a stale `docs/ARCHITECTURE.md` `packages/github` row that still said "Scaffolded (one fn, one test)" despite chunks 4.1/4.2 shipping real code.

**DA review (R1) found 2 MATERIAL in the new ADR's own prose**: a misattributed citation (the "previous design not retained... checkable" quote was sourced to `CLAUDE.md` instead of its real home, `docs/VISION.md`'s own front matter); and an evidentiary-overreach sentence citing the review-queue sweep and draft-outcome tracking as proof the `tickets` table specifically is load-bearing, when both actually operate on sibling tables (`review_queue`/`sweep_state`, `pending_ticket_drafts`) — the underlying decision wasn't wrong (moe's DB as a whole _is_ heavily load-bearing), but the paragraph overclaimed which examples proved which specific fact. Plus 2 LOW (a dropped/added "the" in a quoted phrase; a follow-up chunk retarget from BUILD_PLAN 4.3's original "1.1/1.2b" wording to the actually-sensible chunk 4.5, presented without acknowledging the retarget). All fixed, R2-confirmed — R2 also ran an exhaustive sibling-occurrence sweep for both bug patterns and found no further instances.

**Mandatory `copilot-surrogate` (VISION.md is blast-radius) then found its own MATERIAL**: the ADR twice cited "chunk 6.1b hasn't shipped" as evidence no personas beyond Sarah exist yet — but chunk 6.1b is Sarah's own Brief-stage handler (converting triage entries into tickets), not the chunk that brings the rest of the cast online; that's Stage 5. Both citations fixed to "Stage 5." Plus 1 LOW (`ARCHITECTURE.md`'s `packages/core` row never named the `tickets` table the ADR leans on as its central evidence — added). Both fixed, R2-confirmed, including a second exhaustive sibling sweep for the same 6.1b-persona-existence pattern (clean — the other 6.1b references in the repo are all correctly about the triage-to-ticket conversion, a different claim entirely).

**Chunk 4.2 — Issue discovery (poll)** ([PR #57](https://github.com/Pushedskydiver/moe/pull/57)): a manually-triggered CLI poll (`pnpm --filter @moe/server discover:github-issues`) lists every open issue in the configured repo via chunk 4.1's `createGithubClient`, filters out pull requests (GitHub's Issues API returns both), and upserts a minimal pointer row (title/url/state/GitHub's own `updated_at`) per issue into a new `github_issue_triage` table (migration `0014`) — BUILD_PLAN 6.1b's future consumer, unread by anything yet. Architectural pass first, four open questions resolved with Alex via `AskUserQuestion`: read-only App permissions for now (narrowed by Alex mid-session, confirmed live); manual CLI trigger, not a background timer (no scheduled-job infra exists yet, same posture as 3.5's sweep); a minimal-pointer table shape, not the full issue body (a future persona fetches fresh rather than risking a stale cache, per BUILD_PLAN 6.1b); and a single `MOE_GITHUB_REPO="owner/name"` env var, matching the App installation's own single-repo scope. Live-verified twice against the real `chief-clancy` repo with permissions narrowed to read-only — once to confirm the fresh-insert path, once more to confirm the upsert-on-repoll path (`firstSeenAt` fixed, `lastSeenAt` advances).

**Bundled fix — logger `message:` field collision, found live, not by a test.** Inspecting real JSON log output during chunk 4.2's own live verification surfaced that `apps/server/src/logger.ts`'s `writeLine` deliberately lets its own `level`/`message`/`timestamp` win over a same-named caller field (a real, intentional, already-tested anti-spoofing behavior) — but 44 call sites across `apps/server` and `packages/slack` had unknowingly named their own error-detail field `message`, silently discarding it on every failed log line, undetected until now. Flagged directly to Alex rather than silently fixed or deferred, given the severity; his answer: **"Let's fix it now as part of this PR."** All 44 renamed to `errorMessage`, ~30 test assertions updated across 8 files, a permanent TSDoc warning added to `logger.ts` and a matching bullet to `docs/CONVENTIONS.md`.

**DA review (R1) found the fix wasn't actually complete** — 3 more collisions in `packages/slack/src/socket-mode-listener.ts` a first-pass grep scoped to `apps/server` had missed, plus a fabricated "~12 call sites" quantifier in two permanent docs (the real count: 44). Both fixed, plus 6 LOW findings (a genuinely false `upsert*`/`record*` naming claim was NOT one of these — that came later from `copilot-surrogate`): 4 instances of a paraphrase presented in quote-marks as if verbatim (one found independently by the author, not R1, in a location R1's grep hadn't reached), an undocumented `upsert*` verb, missing whitespace-rejection on `MOE_GITHUB_REPO`, and two untested error branches (one needing an out-of-Postgres-`INTEGER`-range value to force a genuine DB-level throw distinct from Zod's own upstream validation). A fresh R2 pass confirmed every fold — including independently recounting the "44" claim from the diff itself rather than trusting the prose, and running the new out-of-range-integer test live against the real database.

**Alex then asked to fold a previously-flagged, out-of-scope coverage gap into this same PR** — `check-cost-cap.ts` had two of its four error-logging branches never covered by any test, before or after this PR's field rename. A follow-up task had already been spawned for it; withdrawn once Alex said to just do it here. Nine new tests added (`check-cost-cap.test.ts`), covering the full claim-then-alert recursion including the "claim legitimately loses the race" (silent) vs. "claim fails for a real reason" (logged) branches. `copilot-surrogate` (mandatory: blast-radius doc touched, diff far over 50 LOC) then found one genuine MATERIAL issue of its own: the new `upsert*` vocabulary entry's claimed distinction from `record*` ("bookkeeping-only... returning the resulting row") was factually false — the two existing `record*` repository functions are themselves upserts that also return the resulting row. Corrected to the real distinction (`record*` tracks this app's own internal bookkeeping; `upsert*` mirrors an external entity's current state) and R2-confirmed against every `record*`/`upsert*` function in the repo, not just the two originally cited.

**Novel patterns this session:**

1. **A real, previously-undetected bug can hide behind a passing test suite indefinitely** — the logger collision had zero test failures (the anti-spoofing behavior it exploited was itself correctly tested), and surfaced only from directly reading live JSON log output during unrelated manual verification. 883+ green tests didn't catch it because no test asserted on the _content_ of a field that was silently being overwritten with something else — the assertion was checking the wrong thing entirely, not failing to check.
2. **A repo-wide grep run once isn't the same as a repo-wide grep run correctly** — the original ~12-site fix scoped itself to `apps/server` (where the bug was first spotted) without verifying that scope was actually exhaustive; the same `AppLogger` type flows into `packages/slack` via DI, and nobody checked. DA's own R1 pass is what caught the gap, not the original fix's own stated confidence.
3. **A review agent's corrective fix can introduce the exact same class of defect it's fixing** — the DA-fold's own new prose (`docs/CONVENTIONS.md`'s `upsert*` entry) invented a plausible-sounding but false factual claim, caught only by the next review layer (`copilot-surrogate`), not by the self-review pass that immediately followed the DA fold. Reinforces Session 13's own Novel Pattern #3 (a correction pass isn't self-exempting) with a new instance: this time the defect was in freshly-authored doc prose, not a code fix.
4. **When Alex authorizes folding an already-spawned follow-up task into the current PR, withdraw the spawned chip rather than leaving both live** — `dismiss_task` immediately once the scope-widening request lands, so the user isn't left with a stale suggestion for work already in flight.
5. **A decision-only, zero-code PR still needs the full review gate, and still finds real defects** — chunk 4.3 was pure prose (an ADR plus doc updates), and both DA review and `copilot-surrogate` still found genuine MATERIAL factual errors in freshly-written argumentation (a misattributed citation, an evidentiary-overreach paragraph, a wrong chunk-number citation for a real fact). "It's just docs" is exactly the rationalization `docs/RATIONALIZATIONS.md` already names and warns against — this session is a second, concrete confirmation it doesn't hold up, not a new lesson.
6. **An ADR's own central evidentiary paragraph deserves the same "does this example actually prove this specific claim" scrutiny as a numeric quantifier** — DA's finding wasn't that the cited files didn't exist or didn't do what was claimed; it's that two of four cited examples proved a _broader_ claim (moe's DB as a whole is load-bearing) than the _narrower_ one the sentence was actually making (the `tickets` table specifically). Precision about which evidence supports which exact claim matters even when the underlying conclusion is correct.
7. **A citation two sections apart in the same new file can carry the identical wrong-chunk-number bug twice** — the "6.1b vs. Stage 5" mistake appeared in both the ADR's Decision 4 and its Triggers-for-re-evaluation section, written minutes apart by the same author under the same (wrong) mental model. A single grep-and-fix in one location doesn't imply the other's clean — R2's own sibling-occurrence sweep is what confirmed both were caught, not an assumption that fixing one meant fixing the pattern.

**Earlier Session 13 status preserved below for context** — **Stage 4 opened: chunk 4.1 (GitHub App auth + client) shipped, plus both LOW findings its own DA review deferred, closed out the same session as their own separate PRs.** Three PRs: chunk 4.1 itself ([PR #54](https://github.com/Pushedskydiver/moe/pull/54)), a `.reduce()`-to-recursion cleanup ([PR #55](https://github.com/Pushedskydiver/moe/pull/55)), and an `AppLogger`-type promotion to `@moe/core` ([PR #56](https://github.com/Pushedskydiver/moe/pull/56)) — the latter two both spawned via background-task chips Alex clicked mid-session, not planned upfront.

**Chunk 4.1 — GitHub App auth + client** ([PR #54](https://github.com/Pushedskydiver/moe/pull/54)): `packages/github`'s first real code (previously a toolchain-proof scaffold). Architectural pass first (VISION §11/§6.7, CONVENTIONS §External API Integration Patterns), four genuinely open questions resolved with Alex via `AskUserQuestion` before any code moved: token/installation-token caching delegated to `@octokit/auth-app`'s own built-in mechanism (not a hand-rolled `Cached<T>`); boot-time validation as a live installation-token exchange (`auth({type:'installation'})`) rather than local-only — Alex explicitly deferred to "your recommendation as long as you have strong, real evidence," and a stronger precedent than the original framing surfaced mid-research: `start-slack-listener.ts`'s own `fetchBotUserId`/`auth.test` boot check is _already_ a live round-trip purpose-built to validate the credential, not just an incidental side effect of Socket Mode's persistent connection — the recommendation was revised on that evidence and reported back before implementing; the private key read as a single-line env var with escaped `\n` (verified via direct `WebFetch` against `@octokit/auth-app`'s real README, which auto-converts escaped newlines itself — no custom decode code needed); and a real GitHub App needed creating, which Alex did mid-session (App ID `3814883`, installed on `Pushedskydiver/chief-clancy`, installation ID `134711865` — credentials now live in `.env.local`). Live-verified end to end: the real installation-token exchange succeeds against the real App.

DA review found 2 MATERIAL: a stale `main()` TSDoc (still named 5 of the 6 now-parsed configs), and a genuine redaction gap — `createGithubSdkLoggerAdapter`'s static `secretValues` list only ever covers the private key (known at construction time), never the installation access token the client actually sends on every real request (minted per-call, unknown at construction) — fixed with a new pattern-based `GITHUB_TOKEN_PATTERN` regex redaction layered on top, both confirmed via a fresh R2 pass. Self-review caught an over-exported barrel symbol (`createGithubSdkLoggerAdapter` shouldn't have been in the public package entry, inconsistent with how `packages/slack`/`packages/agents` keep their own logger adapters internal-only) and a test-fixture token realistic enough to risk tripping GitHub's own push-time secret scanning — confirmed the fix mattered when the real PR's GitGuardian CI check passed clean. `copilot-surrogate` (mandatory: blast-radius doc touched, >50 LOC, external-library factual claims) found 3 LOW precision nits, all folded, no R2 needed since nothing was BLOCKING/MATERIAL.

**Two deferred-LOW follow-ups, both same session:** DA review's own LOW findings on PR #54 spawned two background-task chips; Alex clicked both later in the session.

- [PR #55](https://github.com/Pushedskydiver/moe/pull/55) — `docs/CONVENTIONS.md` bans `.reduce()` but `eslint-plugin-functional` isn't configured to catch it, so it was a silent violation copied into a third sibling package's logger adapter (`packages/github`, from chunk 4.1). Replaced with the codebase's own established recursion pattern (`[head, ...rest]`, base case, recurse — matching `check-cost-cap.ts`'s `sendCostAlerts`/`migrate.ts`'s `applyPending`) across all three files. Added the multi-secret-redaction test coverage that never existed before (no prior test ever passed more than one secret in `secretValues` at once — exactly the property a buggy recursive rewrite could most easily break). DA review found a MATERIAL: a mid-fix `replace_all: true` rename (correcting an initial naming collision) corrupted a _second_, unrelated sentence in the same file referencing a different real cross-file function — caught, fixed, R2-confirmed.
- [PR #56](https://github.com/Pushedskydiver/moe/pull/56) — an identical 3-method `AppLogger` type duplicated across sibling packages, promoted to `packages/core/src/app-logger.ts`. DA review's own whole-repo _structural-shape_ grep (not just name-matching) caught two more identical duplicates the task's own description had missed (`packages/slack/src/socket-mode-listener.ts`'s private `ListenerLogger`, and `apps/server/src/logger.ts`'s own exported `Logger` — the actual origin of the shape, fixed via a type alias rather than renaming its 3 existing consumers) — both fixed, R2-confirmed. R2 itself then caught two fresh Low-severity doc-accuracy defects in the exact TSDoc it had just re-verified (an unmarked-elision paraphrase reading as verbatim, and a stated "eight" total that didn't reconcile with its own breakdown) — fixed in the same pass. `copilot-surrogate` found zero BLOCKING/MATERIAL and 3 LOW: one (a new export landing at the top of `index.ts` instead of the file's established append-at-end convention) folded immediately; two (pre-existing, unrelated citation errors surfaced only because `copilot-surrogate` reads touched files at HEAD, not diff-scoped — a stale `BUILD_PLAN` chunk citation, a misattributed VISION section) were initially spun off as separate follow-up tasks per `docs/SELF-REVIEW.md`'s "NOTICED BUT NOT TOUCHING" convention, then folded into PR #56 directly per Alex's own explicit request once he pointed out both target files were already in its diff — no fresh full review cycle re-dispatched for those two single-line prose corrections, since both had already been independently discovered _and_ grep-verified by `copilot-surrogate` itself (not self-verified), re-confirmed against primary sources directly before editing.

**Novel patterns this session:**

1. **Alex's "real evidence" bar changed a recommendation mid-research, not just confirmed one** — asked to defer to my own judgment "as long as you have strong, real evidence," direct `WebFetch` verification against the real `@octokit/auth-app` docs surfaced a stronger, more specific precedent already living in this exact codebase (`start-slack-listener.ts`'s own live credential check) than my first-pass reasoning had used, and the recommendation was revised and reported back with the new evidence before any code moved — the `vision-is-north-star` discipline applied to a self-generated recommendation, not just an inherited claim.
2. **A background-task chip, once clicked, delivers its own prompt text as a fresh user turn — and picking it up needs the same branch-from-trunk-after-escalated-PR discipline as any planned chunk transition.** This recurred twice (once per spawned-task PR): each time, the target branch had to sync `main` first since the chip's own target files sometimes only existed on a still-open sibling PR — a new trigger shape for an already-known lesson (`docs/GIT.md` §Branch Strategy Rules), not a new lesson itself.
3. **A review agent's own re-verification pass (R2) can introduce fresh low-severity defects in the exact prose it's fixing — twice in one PR.** PR #56's own naming-collision fix missed a second occurrence in the same file (caught by DA); DA's own suggested TSDoc rewrite (to resolve an earlier LOW quoting issue) then introduced a fresh unmarked-elision-plus-arithmetic-mismatch pair, caught by R2. Reinforces that a correction pass isn't self-exempting — it gets the same scrutiny as the code it's fixing, not a pass because it's itself a fix.
4. **`copilot-surrogate`'s HEAD-scope (not diff-scoped) reading is a real, recurring source of unrelated-but-genuine findings**, not a one-off — surfaced two pre-existing factual errors (a stale `BUILD_PLAN` chunk citation, a misattributed VISION section) in files a PR only lightly touched. Default handling is `docs/SELF-REVIEW.md`'s "NOTICED BUT NOT TOUCHING" (flag, don't fix) — but when Alex explicitly asks to fold an already-flagged, already-verified finding into the open PR anyway, that's a legitimate scope-widening call to honor, not a default to reach for unprompted.
5. **A throwaway script calling the real function directly (not a full server boot) was sufficient, faithful live verification** for the boot-time credential guard — didn't need Slack/DB running too, since the guard's own real code path (`parseGithubConfig` → `validateGithubCredentials`) doesn't touch either.

**Earlier Session 12 status preserved below for context** — Stage 3 finished for real at chunk 3.6 (draft-outcome tracking, [PR #52](https://github.com/Pushedskydiver/moe/pull/52)), closing the last chunk BUILD_PLAN's own gating rule had allowed to trail into Stage 4; a same-session follow-up ([PR #53](https://github.com/Pushedskydiver/moe/pull/53)) ported seven process lessons out of Claude's private memory into the actual project docs, per Alex's own direct request. Both PRs needed DA's own fold to need R2, then `copilot-surrogate`'s own fold to need a second R2 on top of that.

**Chunk 3.6 — Draft-outcome tracking** ([PR #52](https://github.com/Pushedskydiver/moe/pull/52)): counts `pending_ticket_drafts` by outcome (`'committed'` folds ✅+📦, `'redone'` is still-open+regenerated, `'ignored'` is still-open+never-redone+past a 24h threshold) and surfaces the rate as a `📊 Draft outcomes` line in the 3.5 sweep digest — VISION §5.4's named production metric for the whole intake cascade. Three genuinely open bucket-definition questions (plus the windowed-vs-lifetime-cumulative framing) were resolved with Alex via `AskUserQuestion` before any code moved. Built via TDD: a `redo_count` column (migration `0012`), `getDraftOutcomeCounts` (a single aggregate query using Kysely's `filterWhere`/Postgres `FILTER (WHERE ...)`), and digest-wiring into `review-queue-sweep.ts` (rides along with an existing post, not an independent trigger, since a lifetime-cumulative count is almost never zero).

DA review found a genuine MATERIAL bug: `pending_ticket_drafts` is a shared table for both High-band auto-drafts and Mid-band 👍-confirmed drafts (via `postAndPersistDraft`), so the new counting function was silently mixing both populations despite every doc claiming "High-band" — skewing the reported acceptance rate. **Alex did not just pick DA's recommended fix — he asked whether it was worth verifying the docs directly, or whether there was strong real evidence behind the recommendation.** Re-reading VISION §5.2 directly (not paraphrased) confirmed "ignored/corrected draft" is textually tied to the High-confidence bullet specifically, not Mid-confidence's separate confirming-question action — genuine grounding, reported back with the exact quotes before implementing. Fixed with a new `origin: 'high-band' | 'mid-band-confirmed'` column (migration `0013`, backfilled `'high-band'` for pre-migration rows as a documented, accepted historical-data caveat), threaded through both `postAndPersistDraft` callers and filtered into `getDraftOutcomeCounts`. R2 (fresh context, isolated worktree) independently re-verified every write/read path, mutation-tested the new regression pin by temporarily removing the filter and confirming the test failed as predicted, and manually confirmed the CHECK constraint against real Postgres — found 1 further MATERIAL gap (no CHECK-constraint regression test, breaking an established per-column-constraint testing convention this file otherwise holds everywhere) plus 2 nits, all fixed.

`copilot-surrogate`'s mandatory pass (blast-radius docs touched) found 4 findings on its own first pass: 2 MATERIAL (a stray blank line in `docs/GLOSSARY.md` would have broken the new row out of the GFM table entirely, rendering as a literal-pipe-containing orphan paragraph instead of a glossary entry; a "VISION §5.4 framing" quote in three separate locations — `BUILD_PLAN.md`, `GLOSSARY.md`, and a TSDoc — turned out to be leftover BUILD_PLAN stub text misattributed as a direct VISION quote, verified via a clean `grep` miss against the real `VISION.md`), 1 MATERIAL (an internal contradiction: two docs said "four" `AskUserQuestion`-resolved bucket-definition questions while the TSDoc said "three," reconciled to the TSDoc's own more-contemporaneous account), 1 LOW (a paraphrase presented in quote-marks as if verbatim). All fixed; a second fresh-context R2 pass independently re-verified all four fixes (including re-grepping VISION directly rather than trusting the first pass's own account) and caught a **fifth** instance of the same misquote pattern surviving in a location the original fix hadn't touched — the DA-review-fold paragraph in `BUILD_PLAN.md` itself, added later than the three files the first copilot-surrogate pass covered. Self-review then independently caught a sixth, unrelated staleness neither review agent had flagged: `pending-ticket-draft.ts`'s own module TSDoc still called the type "a High-band ticket draft" even though `origin` now makes it explicitly serve both bands.

**Docs-port PR** ([PR #53](https://github.com/Pushedskydiver/moe/pull/53)): Alex asked directly, after PR #52 merged, whether any of the durable lessons Claude had been saving to its own private cross-session memory ought to live in the project's own docs instead — "I'd rather protocols we add or improve live within the project rather than memory." Seven candidates were surfaced and confirmed via `AskUserQuestion`: secret-pattern mirror locations (→ `docs/CONVENTIONS.md`), review-agent isolation and reviewer-disagreement resolution (→ `docs/DEVELOPMENT.md`), gitmoji↔type CI strictness and the branch-first discipline (→ `docs/GIT.md`), the typecheck-vs-build gotcha (→ `docs/DEVELOPMENT.md`), and a new quoted-text-verbatim-quote discipline this same session's chunk 3.6 review arc had just surfaced (→ `docs/DA-REVIEW.md`, as a new 7th claim-extraction bucket). All 4 target docs are on `docs/GIT.md`'s own Blast-Radius list, so this 15-line diff went through the full review gate. DA review found the ported gitmoji-variation-selector claim was itself backwards for 2 of 11 types (`refactor`/`perf` actually need the trailing Unicode selector the original memory said to strip) plus a LOW framing issue implying a local commit-msg check exists when none does — both fixed, R2-confirmed by independently decoding the CI workflow's own emoji codepoints. `copilot-surrogate` then found the PR's own new "Seven buckets" addition had gone stale in the very same sentence it was introduced in (still said "Six"), plus the same drift in `.claude/agents/copilot-surrogate.md`'s own inline duplicate of the bucket list — both fixed, R2-confirmed. Self-review caught one more: the isolation guidance had been embedded only inside DA's own dispatch step, under-scoping it — moved to its own paragraph applying to every review-agent dispatch (DA, R2, `copilot-surrogate`) rather than just DA's. All 7 ported memory files were then trimmed to short pointers at their new doc homes, keeping only genuinely cross-project-general content (or none, where the lesson was purely moe-specific).

**Side task:** Alex also confirmed `moe-dev-postgres` (an empty, unreferenced Docker container, 6 days older than the `moe-postgres` container `.env.local` actually points at) was safe to delete — removed. `moe-postgres` itself was kept; it's what local `packages/core`/`apps/server` test runs actually use.

**Novel patterns this session:**

### Session 15 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect PR #58's squash at HEAD, then #57's, then #56's, then #55's), `git status` clean. `BUILD_PLAN.md` shows chunks 4.2 and 4.3 both `[x]`.
- **Primary workstream — DONE this session.** [PR #57](https://github.com/Pushedskydiver/moe/pull/57) (chunk 4.2 + the bundled logger fix + the `check-cost-cap.ts` coverage gap) and [PR #58](https://github.com/Pushedskydiver/moe/pull/58) (chunk 4.3, the board home + capacity-model gate) — both merged. See the entries above for full detail.
- **Next up — BUILD_PLAN Stage 4, chunk 4.4a — "External-post attribution (AI transparency)."** Every externally-visible artifact moe writes (GitHub issues now; comments/PRs/reviews at Stage 6) carries VISION §6.7's three layers: bot identity, persona attribution in the message body, and a footer escape-hatch to Alex. Non-negotiable for external humans, so this lands _before_ the first GitHub write — chunk 4.4b (outbound ticket → issue creation/link, the plan's first GitHub write) depends on it and can't skip ahead. Do the architectural pass first (read VISION §6.7 directly, don't assume) and raise genuinely open questions via `AskUserQuestion` before any code moves, same discipline as every prior chunk this stage.
- **Decision branches:**
  - A. `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision keeps holding up — every dispatch this session (2 PRs, 4 DA-family dispatches, 2 of them R2 confirmations, one `copilot-surrogate`) either found something real or came back clean with independent verification shown, no wasted dispatch. Chunk 4.3 in particular reconfirmed this holds even for decision-only, zero-code PRs — "it's just docs" isn't a reason to skip the gate (Novel Patterns #5).
  - B. A repo-wide grep needs to actually be repo-wide, not scoped to "the package/file where the bug was first spotted" — recurred twice this session in different shapes: the ~12-site logger fix missed 3 sites in a sibling package (Novel Patterns #2), and the same discipline applies to citation/quote accuracy in prose, not just code (Novel Patterns #6/#7).
  - C. A review agent's own corrective fix isn't self-exempt from the next review layer catching a fresh defect in it — recurred a third time this session, now in an ADR's own prose (a misattributed citation, an evidentiary-overreach paragraph) rather than code or a doc bullet (Novel Patterns #3, #6).
  - D. When Alex authorizes folding an already-spawned follow-up chip into the current PR, `dismiss_task` it immediately — don't leave a stale suggestion live for work already in flight (Novel Patterns #4).
- **Carry-overs:**
  - Board/capacity model is now decided (`docs/decisions/BOARD-AND-CAPACITY-MODEL.md`): moe's own DB is the canonical board; two classes of service (Standard/Expedite); a `classOfService` ticket-schema field is named but _not yet built_ — its migration is chunk 4.5's own job, don't assume it exists; WIP limits are Brief 3/Plan 2/Build 2/Review 2 (Backlog/Done uncapped), also not yet enforced (chunk 4.5 again).
  - Newest migration is still `0014_create_github_issue_triage.sql` — chunk 4.3 shipped no migration (decision-only). Check `packages/core/migrations/` directly rather than trusting any single "newest migration" claim in this file.
  - `apps/server/src/logger.ts`'s `writeLine` reserves `message`/`level`/`timestamp` as fields-object keys — a caller field with any of those three names is always silently discarded, not an accident, tested intentionally. Use `errorMessage` (or any other non-reserved name) for a caller's own error-detail field. Documented in `logger.ts`'s own TSDoc and `docs/CONVENTIONS.md`'s Error Handling section — this is now a codified rule, not just a memory of the incident.
  - `docs/CONVENTIONS.md`'s Function verb vocabulary now includes `upsert*` (mirrors an external entity's current state) alongside the existing `record*` (this app's own internal bookkeeping) — both are upserts under the hood; the naming split is about what's being tracked, not the SQL shape.
  - `.env.local` now also carries `MOE_GITHUB_REPO=Pushedskydiver/chief-clancy`. The real GitHub App's permissions are narrowed to read-only (Alex confirmed mid-session) — chunk 4.4b (the plan's first GitHub _write_) will need this widened again, with its own failure modes handled explicitly, not silently, per that chunk's own BUILD_PLAN description.
  - `packages/core/src/intake/github-issue-triage-repository.ts`'s `upsertGithubIssueTriageEntry` and the `github_issue_triage` table are unconsumed until chunk 6.1b (Brief-stage handler) — don't be surprised nothing reads it yet.
  - `docs/decisions/` now holds seven ADRs, none trimmed to the ~50-line "decisions-only" budget yet — flagged repeatedly without being acted on. Content-judgment task on historical records — check with Alex first.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm directly (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite. `DATABASE_URL` also needs exporting inline in every Bash call that runs `packages/core`/`apps/server` tests directly (`postgres://postgres:password@localhost:5432/moe_dev` against the `moe-postgres` Docker container on port 5432) — it does not persist across separate Bash tool invocations in this harness the way a real shell session would. Don't guess this value — read it from `.env.local` rather than assuming a default password (cost real time earlier this session).
  - `ScheduleWakeup` is `/loop`-dynamic-mode-scoped, not a general "wait passively" tool — when something else (Monitor, a task notification) is already the wake signal, do nothing further.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk    | Status | Shipped    | Headline                                                                                                                                                                                                        |
| -------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1      | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                                                                                                                            |
| 0.2      | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                                                                                                                                        |
| 0.3      | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                                                                                                                                 |
| 0.4      | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                                                                                                                            |
| 0.5      | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                                                                                                                             |
| 0.6a     | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved                                                                                                                  |
| 0.6b     | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)                                                                                                                  |
| 0.6c     | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision                                                                                                                |
| 0.7      | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**                                                                                                                         |
| 1.1      | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                                                                                                                                       |
| 1.2a     | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)                                                                                                                       |
| 1.2b     | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                                                                                                                                   |
| 1.3      | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                                                                                                                                |
| 1.4      | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                                                                                                                                         |
| 1.5      | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`)                                                                                                             |
| 1.6      | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**                                                                                                                     |
| 2.1      | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)                                                                                                                     |
| 2.2      | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config                                                                                                                |
| 2.3      | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app                                                                                                                 |
| 2.4a     | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app                                                                                                              |
| 2.4b     | Merged | 2026-07-17 | Thread-scoped conversation state + persona-naming fix — live-verified context-aware multi-turn chat                                                                                                             |
| 2.5      | Merged | 2026-07-17 | Status claims through the `report_status` tool + 1.4 gate — VISION §7.6 anti-fabrication, wired                                                                                                                 |
| 2.6a     | Merged | 2026-07-17 | Cost metering substrate — `persona_cost_daily` table, atomic accumulation, live-verified turn cost                                                                                                              |
| 2.6b     | Merged | 2026-07-17 | Cost caps + spend alert ladder — atomic claim, hard halt, live-verified full 50/80/100% arc                                                                                                                     |
| 2.7a     | Merged | 2026-07-18 | Core-hours + bank-holiday guard — DST-safe check, Cached\<T,E\> class, fails closed, no consumer yet                                                                                                            |
| 2.7b     | Merged | 2026-07-18 | Away-detection — isAway/fetchSlackStatus, verified emoji shortcodes, live-verified, no consumer yet — **Stage 2 complete**                                                                                      |
| 3.1      | Merged | 2026-07-18 | [GATE] Stage-1 classifier scoring method + thresholds — bundled call, Haiku 4.5, High≥70/Mid35-69/Low<35 — **Stage 3 opened**                                                                                   |
| 3.2      | Merged | 2026-07-18 | Channel scoping (Stage 0) — isSurfaceInScope allow-list + DM bypass, no consumer yet, first Workflow-dispatched review arc                                                                                      |
| 3.3      | Merged | 2026-07-18 | Classifier gate (Stage 1) — Haiku 4.5 wired live, DMs-only chat replies, cost-cap fix, live-verified end-to-end                                                                                                 |
| 3.4a-i   | Merged | 2026-07-18 | High-band auto-draft — Sonnet-5 ticket draft, shadow/log-only, 2.7a guard wired, live-verified end-to-end                                                                                                       |
| 3.4a-ii  | Merged | 2026-07-18 | Reaction listener + outcome paths — commit/redo/park, no live consumer, prototype-pollution fix, live-verified via real infra                                                                                   |
| 3.4a-iii | Merged | 2026-07-19 | Situational-appropriateness gate + real end-to-end wiring — real post, self-authored filter, live dispatch — **Stage 3 exit criterion passes live**                                                             |
| 3.4c     | Merged | 2026-07-19 | Review-queue table + Low-band silent log — built ahead of 3.4b (sequencing resolved via `AskUserQuestion`), `outcomeReason` column anticipates 3.4b, live-verified                                              |
| 3.4b-i   | Merged | 2026-07-19 | Mid-band confirming question (ask-side) — split from 3.4b-ii via `AskUserQuestion`, shared guards extracted, DA MATERIAL fixed + R2-confirmed                                                                   |
| 3.4b-ii  | Merged | 2026-07-19 | Mid-band confirming question (answer-side) — 👍/👎 dispatch, `postAndPersistDraft` reused, DA+`copilot-surrogate` MATERIAL fixed + R2, live-verified                                                            |
| 3.5      | Merged | 2026-07-19 | Review-queue sweep — manual CLI script, `sweep_state` table, dual DA+`copilot-surrogate` R2 arc — **Stage 3 complete** (3.6 may trail into Stage 4)                                                             |
| 3.6      | Merged | 2026-07-20 | Draft-outcome tracking — committed/redone/ignored counts in the sweep digest, DA+`copilot-surrogate` dual-fold arc scoped the metric to High-band only — **Stage 3 fully complete**                             |
| 4.1      | Merged | 2026-07-21 | GitHub App auth + client — `@octokit/auth-app` installation-token auth, live-verified against a real App on `Pushedskydiver/chief-clancy`, DA MATERIAL fixed + R2 — **Stage 4 opened**                          |
| 4.2      | Merged | 2026-07-22 | Issue discovery (poll) — `github_issue_triage` table, live-verified read-only, bundled 44-site logger-collision fix + `check-cost-cap.ts` coverage gap, DA + `copilot-surrogate` MATERIAL fixed + dual R2       |
| 4.3      | Merged | 2026-07-22 | [GATE] Board home + capacity model — moe's DB canonical, Standard/Expedite CoS, `classOfService` field named (not built), WIP limits 3/2/2/2 — decision-only, DA + `copilot-surrogate` MATERIAL fixed + dual R2 |
