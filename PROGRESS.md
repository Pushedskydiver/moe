# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 10)

Updated 2026-07-19 end-Session-10 — **Chunk 3.4a-iii shipped, completing Stage 3's own acceptance-test path: a plain Slack message now produces a real posted ticket draft with a real reaction gate, and reacting to it now produces a real ticket in the database — the exact live test Stage 3's exit criterion names, now passing for real, no longer shadow/log-only.** One chunk this session, but the largest single-PR diff of the project so far (26 files) — it closes 3.4a-i's shadow-mode draft composition and 3.4a-ii's reaction-outcome primitives into one real, live end-to-end loop: VISION §9's situational-appropriateness gate (the last piece Stage 3 had been building toward since 3.4a-i), real Slack posting, real `pending_ticket_drafts` persistence, real `reactions.add` seeding, the self-authored-reaction filter 3.4a-ii's own DA review had flagged as a known gap, and a real Socket Mode `reaction_added` listener registration. Stage 3 itself isn't fully done — 3.4b (Mid-band confirming question), 3.4c (review-queue + Low-band), 3.5 (sweep), 3.6 (draft-outcome tracking) remain — but its own exit criterion is now a real, passing live test, not a projection.

**Chunk 3.4a-iii — Minimal situational-appropriateness gate + real end-to-end wiring** ([PR #44](https://github.com/Pushedskydiver/moe/pull/44)): `evaluateSituationalAppropriateness` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`, `{appropriate: boolean, reasoning: string}`) is VISION §9's minimal gate, consulted before any auto-post — named after the Viktor incident (a competitor bot reacting to a layoff announcement with a skull emoji). Three genuinely open questions were resolved with Alex via `AskUserQuestion` before any code moved, not guessed or defaulted: the gate mechanism (an LLM content classifier, not a keyword list or external toggle — the failure mode is about message _content_, which a fixed vocabulary can't reliably catch); its scope (gates 3.4a-i's draft-posting only, not 3.4a-ii's reaction-outcome dispatch — a human's own reaction is a response to the bot, not the bot acting unprompted, same reactive/proactive distinction 2.7a's core-hours guard already draws for DM replies); and how much of this chunk's terse BUILD_PLAN text to build in one PR — confirmed doing all seven pieces in one chunk (the gate itself, wiring it into draft composition, flipping 3.4a-i's shadow mode to a real Slack post, persisting `pending_ticket_drafts` on the real post, seeding real `reactions.add` calls, the self-authored-reaction filter, and the real Socket Mode `reaction_added` listener) rather than splitting further. **Fails CLOSED** on any gate failure (an API error, not just `appropriate: false`) — the deliberate opposite of `checkCostCapAndAlert`'s fail-open design, since an uncertain safety signal should block the risky action, same "better to wrongly rest once" reasoning as 2.7a's bank-holiday guard.

`postMessage`'s own result type (`packages/slack`) was extended to require `ts` on success — a genuinely necessary, backward-compatible change (verified all three other real call sites — the DM-reply path's `postHaltReply`/`generateAndPost`, and `check-cost-cap.ts`'s own cost-alert DM — never read it) needed to key the persisted draft row and seed reactions against the _posted_ message's own identity, not the source message's. The self-authored-reaction filter 3.4a-ii's own DA review flagged as a known gap was built one layer down from `apps/server`, inside `@moe/slack` itself: `fetchBotUserId` (a thin `auth.test` wrapper, same pattern as `postMessage`/`addReaction`) is fetched once at process startup, and a new `handleSocketModeReactionEvent` (mirroring the existing `message`-event handler's ack→validate→filter→normalize→dispatch shape) compares each real `reaction_added` event's `user` against it before ever calling `onReactionAdded` — `apps/server`'s own reaction-outcome code never needs to know about bot identity at all, a deliberate architectural choice.

DA review (fresh context, isolated worktree) found no Critical/Blocking findings but 2 Material + 1 Low + 1 Nit, all fixed: `seedReactionLegend` used `.reduce()` where `docs/CONVENTIONS.md`'s Code Style section bans it outright — fixed first to a `for...of` loop with a scoped `eslint-disable`, then (after Alex directly questioned mid-review whether disabling a lint rule was really the best option, and asked where else in the codebase disables a rule) refactored a second time to a genuinely loop-free recursive shape matching `check-cost-cap.ts`'s own `sendCostAlerts` precedent for sequential-by-design async work over a short list — zero `eslint-disable` comments in the final file. A stale post-rename TSDoc reference (`composeAndLogDraft`, the pre-3.4a-i-rename name) and a boolean-naming-vocabulary miss (`passes*` instead of the documented `is*/has*/can*/should*` prefixes) rounded out the fold. R2 verification (fresh context) caught a real regression the fold itself introduced: fixing the stale-name reference landed _before_ the boolean-rename fix, so the "fixed" TSDoc referenced the old `passesSituationalGate` name that the second fix had just renamed away — sequencing two fixes within one fold left a fresh stale reference exactly where the first fix had just landed. Fixed and independently grep-confirmed clean.

`copilot-surrogate`'s mandatory pass (diff far over 50 LOC, new prose making factual claims about the rest of the repo) exceeded its own 20-file scope ceiling for a third time this project (26 files) — split into 3 disjoint-file-set passes (`packages/slack`, `apps/server`, `packages/agents`+`packages/core`+docs) run in parallel. Two passes came back fully clean (~24 and ~22 independently-verified claims respectively, including reading the actually-installed `@slack/web-api`/`@slack/socket-mode` package source and Slack's live published API docs to verify the single most consequential claim in the PR — that `@slack/socket-mode`'s client really does emit `event.payload.event.type` as its own event name, which is what makes the new `reaction_added` listener registration correct at all). The third pass, and independently the first pass too (out of its own assigned scope, flagged anyway), both caught the same finding: `BUILD_PLAN.md`'s own chunk summary still described `seedReactionLegend` as "a promise-chain reduce" — stale twice over, from before the loop fix and then again after the recursion refactor above. Fixed, plus a related Low (the doc undercounted `postMessage`'s other call sites — three, not one, though the safety conclusion held regardless). Findings were posted as an actual PR comment this time — a step this specific PR had initially skipped (findings were only relayed in conversation), caught and corrected before merge via a scheduled fallback check firing late, not left as a silent gap.

**Live verification against Sarah's real Slack app produced the fullest end-to-end result of the project so far, and found two genuine environment gaps along the way, neither a code defect.** A real message ("The CLI hangs when I run it against large repos...") classified at confidence 85 and correctly deferred via the pre-existing operating-rhythm guard, since it landed on a real Sunday — confirming that guard's negative path live yet again, this time as _positive_ confirmation that the classification pipeline (Stage 0/1, cost accounting, High-band routing) is fully live and correct, with this chunk's own new pieces genuinely gated behind it exactly as designed. To exercise this chunk's own new behavior without waiting for a real core-hours window, Alex approved a one-off script bypassing only the operating-rhythm clock check — a more surgical variant of 3.4a-ii's own "manual integration script" pattern, bypassing just the one guard genuinely blocking the test rather than the whole trigger path — calling the real production functions (the situational-appropriateness gate, `composeTicketDraft`, `postMessage`, `addReaction`, `createPendingTicketDraft`) against real Anthropic, real Slack, and real Postgres. The gate correctly returned `appropriate: true`; the draft posted and threaded correctly on the real source message; the row persisted correctly, keyed on the _posted_ message's own `ts` — but all three `reactions.add` calls failed with a real `missing_scope` error. Not a code bug: Sarah's Slack App had never been granted `reactions:write`, since no earlier chunk had ever called that API. Alex added the scope and reinstalled the app; a retry succeeded. Reacting to the real posted draft then surfaced a _second_, distinct gap: the app had `reactions:write` but no `reaction_added` bot-event subscription under Event Subscriptions — a separate configuration axis from OAuth scopes entirely, so the live server's Socket Mode connection simply never received the event, with nothing in even the server's own raw, unfiltered log output to show for it. Alex added the missing event subscription and reinstalled again; a fresh reaction immediately dispatched correctly — the self-authored-reaction filter correctly ignored Sarah's own three seed reactions, and only Alex's real 📦 click triggered `commitAsTicket`, producing a real `Backlog`-status, `chief-clancy`-project, `Medium`-severity ticket in Postgres, confirmed by direct query. Both gaps are documented for whichever future persona/chunk next needs reaction-based actions — an OAuth scope and an event subscription are two independently-necessary, easily-conflated configuration steps.

**Chunk 3.4c — Review-queue table + Low-band silent log** ([PR #46](https://github.com/Pushedskydiver/moe/pull/46)), shipped later the same session after [PR #45](https://github.com/Pushedskydiver/moe/pull/45)'s test-reorg: VISION §5.2's "nothing is silently eaten" backstop — a Low-band ambient message now writes a real `review_queue` row (`packages/core`'s `createReviewQueueEntry`) instead of just being logged. **Built out of BUILD_PLAN's own listed order, ahead of 3.4b** — 3.4b's own text names a dependency on this chunk's table for its "no or silence" path, a genuine chunk-sequencing conflict resolved with Alex via `AskUserQuestion` before any code moved (same discipline as 3.4a-ii/3.4a-iii). A second `AskUserQuestion` confirmed adding an `outcomeReason` column (`'low-confidence' | 'mid-no-response'`) now rather than waiting for 3.4b, since the requirement was already known. Deliberately **not** shaped like `pending_ticket_drafts`: a review-queue row is a plain append-only log entry, not a workflow object, so it has no `resolvedAt`/claim state and no uniqueness constraint on `(channelId, messageTs)`. Wired into `handleAmbientChannelMessage`'s existing High/Mid/Low branch via a new `logToReviewQueue` sibling to `composeAndPostDraft`, with a matching `reviewQueueStore` DI seam threaded through `HandlerDeps`/`start-slack-listener.ts`.

DA review (fresh context, isolated worktree) found no Critical/Blocking findings, 3 Low: two internal-consistency nits (3.4b's/3.5's own BUILD_PLAN text not yet reflecting the new column) fixed directly; the third — no idempotency guard exists anywhere in the pipeline against a duplicate Slack `message` event delivery — is a real, pre-existing systemic gap (it would double-post a real ticket draft on the existing High-band path too, not just double-log here) explicitly left out of this PR's scope and spawned as a separate tracked follow-up task rather than fixed inline. `copilot-surrogate`'s mandatory pass (diff >50 LOC) found 3 Low comment-accuracy issues, all fixed: a wrong relative import path in a TSDoc comment (`../confidence-band.ts` instead of `./confidence-band.ts` — `schema.ts` and `confidence-band.ts` are siblings), a cross-reference to a function name that doesn't match the real export (`create` cited instead of `createPendingTicketDraft`), and a "same reasoning as ... above it in this file" citation pointing at a comment that never actually states that reasoning. Both review passes' findings posted/recorded per process, with no BLOCKING/MATERIAL findings from either — no R2 pass needed.

A real bug surfaced while writing the repository's own tests, unrelated to the new table's own design: `packages/core/src/ticket-lifecycle/test-db.ts`'s `resetDatabase` hardcodes its `DROP TABLE` list, and it hadn't been taught about the new `review_queue` table — row counts leaked across tests within the same file until fixed. Every future new-table chunk needs to remember this same manual step; nothing structural currently catches a miss. Live-verified against real Anthropic + real Postgres via a one-off manual script (3.4a-ii's own established pattern, no Slack app involved since Low-band messages never post to Slack): a genuine low-confidence classification (confidence 5, "reads as banter") correctly wrote a real `review_queue` row end to end.

**Novel patterns this session:**

1. **A human directly challenging an already-"compliant" review fix produced a strictly better fix than the documented escape hatch itself.** DA review's own fold used `docs/CONVENTIONS.md`'s documented `functional/no-loop-statements` escape hatch (a `for...of` loop + scoped disable) — technically compliant, but Alex questioned it directly mid-review and asked whether a loop-free option existed. It did: recursion, this codebase's own established pattern (`sendCostAlerts`) for the identical "sequential async work over a short list" shape, with zero disables needed. A review pass finding something "compliant with the documented escape hatch" doesn't mean it's the best available option — worth checking whether the escape hatch is even needed before reaching for it.
2. **Live verification found two distinct, layered Slack-app-configuration gaps in immediate succession** — an OAuth scope (`reactions:write`) and a separate event subscription (`reaction_added`), neither visible to any code-only review pass, each only surfaced by exercising the real app end-to-end one layer at a time. General lesson for any future chunk adding a new Slack event _type_ (not just a new API call): the OAuth scope and the Event Subscriptions entry are two independently-required configuration axes — having one doesn't imply the other, and only a live test can see either.
3. **R2 verification caught a same-fold sequencing regression for the first time** — not staleness left behind by the original diff, but freshly introduced by the fold itself, because two fixes landed in an order where the second fix's rename broke the first fix's own just-written reference. Check: after applying multiple fold fixes within one PR, re-grep for cross-references _after all fixes land_, not just after each one individually.
4. **`copilot-surrogate`'s 20-file ceiling fired for a third time (26 files), now routine in mechanism but notable for built-in cross-validation**: two of the three independent split passes caught the identical `BUILD_PLAN.md` staleness without coordinating — the split itself provides free corroboration when two passes happen to overlap on a claim just outside their own assigned scope.
5. **A stale `ScheduleWakeup` fallback firing late still caught a real process gap**, even though its main trigger condition (three copilot-surrogate passes completing) had already been handled through direct task notifications well before the wakeup fired: the late-firing prompt's own "post findings as a PR comment" instruction turned out not to have actually been done, only relayed in conversation — corrected on the spot. A stale wakeup isn't automatically a no-op; check what it's actually asking before dismissing it as redundant.

**Earlier Session 9 status, compressed** — Stage 2 finished (core-hours + bank-holiday guard, chunk 2.7a, [PR #37](https://github.com/Pushedskydiver/moe/pull/37), `evaluateOperatingRhythm` + `Cached<T,E>`, fails closed; away-detection, chunk 2.7b, [PR #38](https://github.com/Pushedskydiver/moe/pull/38), `isAway`/`fetchSlackStatus`, live-verify hit a real `missing_scope` gap on `users.profile:read`, fixed live — the project's first "code correct, environment wrong" catch) and Stage 3's intake cascade built out through its first five chunks: the Stage-1 classifier scoring-method ADR (chunk 3.1, [PR #39](https://github.com/Pushedskydiver/moe/pull/39), [`STAGE-1-CLASSIFIER.md`](docs/decisions/STAGE-1-CLASSIFIER.md), a real 24-message synthetic eval settling Haiku 4.5 + High≥70/Mid35-69/Low<35 — the session's deepest process finding, two independent review passes disagreeing on a load-bearing external fact, resolved by independent re-verification and saved as the `reviewer-disagreement-resolution` memory lesson); channel scoping (chunk 3.2, [PR #40](https://github.com/Pushedskydiver/moe/pull/40), first Workflow-dispatched review arc, both passes clean); the classifier gate wired live (chunk 3.3, [PR #41](https://github.com/Pushedskydiver/moe/pull/41), DMs-only decision, 1 Critical uncapped-cost-site fix, `copilot-surrogate`'s 20-file ceiling firing for the first time); High-band auto-draft shipped shadow/log-only (chunk 3.4a-i, [PR #42](https://github.com/Pushedskydiver/moe/pull/42), `copilot-surrogate`'s whole-file-at-HEAD read catching drift a diff-scoped pass structurally couldn't see); and the reaction listener + outcome paths with no live consumer yet (chunk 3.4a-ii, [PR #43](https://github.com/Pushedskydiver/moe/pull/43), a real prototype-pollution-style lookup bug fixed with a `Map`, live-verified via a manual integration script against real infra). Seven PRs, each with a full review arc — DA review found something real in six of seven; live verification found real environment gaps three times (the recurring "local Postgres missing migrations" family) plus the `users.profile:read` scope gap. Full session detail (all 12 numbered patterns, every chunk's own review-arc paragraph) survives in `git log -p PROGRESS.md` at this compression commit.

**Earlier Session 8 status, compressed** — Stage 2's cost-tracking arc shipped in full across three PRs: metering substrate (chunk 2.6a, [PR #34](https://github.com/Pushedskydiver/moe/pull/34)) — `persona_cost_daily` table with atomic per-persona-per-day accumulation, integer micro-USD pricing against VISION §10's dated Sonnet-5 rates, live-verified against Sarah's real Slack app to an exact cost match; spend-cap alert ladder with hard-halt enforcement (chunk 2.6b, [PR #35](https://github.com/Pushedskydiver/moe/pull/35)) — an atomic threshold-claim reusing `ticket-lifecycle/claim.ts`'s `claimTicket` pattern for a second, structurally-unrelated race, Alex-confirmed hard-halt + direct-DM-alert design points, three real DA-caught concurrency/correctness bugs (a lost-alert bug, a duplicate-alert race, a silently-diverging-history bug) plus two more from `copilot-surrogate`'s mandatory pass, live-verified through a full 50/80/100%-of-cap arc with `MOE_COST_CAP_MONTHLY` temporarily lowered to make thresholds trivial to cross; and a long-deferred type-safety fix (`PersonaId` widened from bare `string`, [PR #36](https://github.com/Pushedskydiver/moe/pull/36)) where DA caught a real missed third site even at 12 lines. Durable pattern: fold commits need the same prose-matches-code self-check as the original PR — this session's version was sharper than Session 7's, since the fold commit itself introduced a fresh doc-accuracy bug while fixing the code DA flagged, not just missing a pre-existing sibling instance.

**Earlier Session 7 status, compressed** — Chunk 2.5 shipped ([PR #33](https://github.com/Pushedskydiver/moe/pull/33)): status claims route through a real anti-fabrication gate (VISION §7.6) via a new `report_status` tool call, composed through the existing 1.4 gate. The deepest review arc at the time — five independent passes (spec-grill R1+R2 before code moved, DA, a fresh-context R2 on DA's own fold, self-review, `copilot-surrogate`), five distinct real findings, none re-missed twice. Live verification against Sarah's real Slack app produced a genuinely different kind of result: across three attempts, including one deliberately coercive, the model never once fabricated a completion claim — the anti-fabrication property held even though the mechanical tool-call path itself was only exercised by the automated test, not observed live.

**Earlier Session 6 status, compressed** — Thread-scoped conversation state shipped (chunk 2.4b, [PR #32](https://github.com/Pushedskydiver/moe/pull/32)): `conversation_turns` table + repository, DM-vs-thread keying, a root-candidate backfill buffer for Slack's own `thread_ts` timing gap, per-thread serialization queue. Fullest single-chunk process arc at the time (3-round spec-grill; DA found 2 real MATERIAL bugs — a thread-queue collapsing every DM into one lane, an unclosed Postgres pool on exit; R2 mutation-tested both fixes) plus a live-verification-driven persona-naming fix (the placeholder prompt denied being "Sarah"). First clean, fully unprompted handoff-trigger firing after three consecutive misses (Sessions 3/4/5).

**Session 1–5** archived to `docs/history/SESSIONS.md` (detail band exceeded the 5-session budget) — full retrospectives survive in `git log -p PROGRESS.md`.

### Session 11 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect the PR #46 squash, `b85390e`, at HEAD), `git status` clean. `BUILD_PLAN.md` shows chunk 3.4c `[x]` — content-complete, live-verified end-to-end — with 3.4b next (now unblocked, real `review_queue` table exists). **Stage 3's own exit criterion passes as a real live test, and Low-band messages now log to a real table too — but Stage 3 itself isn't fully done: 3.4b (Mid-band confirming question), 3.5 (review-queue sweep), 3.6 (draft-outcome tracking) remain.**
- **Primary workstream — DONE, one chunk this session (the largest single-PR diff of the project so far).** Chunk 3.4a-iii ([PR #44](https://github.com/Pushedskydiver/moe/pull/44)) shipped, review-verified (DA + R2, `copilot-surrogate` 3-way split + fold, findings posted as an actual PR comment) and live-verified fully end-to-end, including two real Slack-app configuration fixes along the way. See the entry above.
- **Two follow-up chunks also shipped this session:** [PR #45](https://github.com/Pushedskydiver/moe/pull/45) — `handle-ambient-channel-message.ts`'s missing co-located test file resolved (pure test reorg, 17 tests extracted, DA Approve + 0 `copilot-surrogate` findings). [PR #46](https://github.com/Pushedskydiver/moe/pull/46) — chunk 3.4c, the review-queue table + Low-band silent log, built out of BUILD_PLAN's own listed order ahead of 3.4b (sequencing + schema questions both resolved with Alex via `AskUserQuestion`), DA + `copilot-surrogate` both Low-only (all fixed), live-verified against real Anthropic + real Postgres. See the entries above for both.
- **Next up — BUILD_PLAN Stage 3, chunk 3.4b — "Mid-band confirming question."** One short confirming question plus handling the answer: yes → draft via the 3.4a-i path (real, not shadow-mode); no or silence → log via the now-real 3.4c `review_queue` table (`outcomeReason: 'mid-no-response'`, the column already exists — no migration needed). DA review at 3.4c flagged a genuinely open question for this chunk to resolve before assuming the existing schema is sufficient: does `'mid-no-response'` need to distinguish an explicit "no" from silence/timeout for 3.5's own human-eyeballing sweep, or is one value fine? Raise with Alex via `AskUserQuestion` if it's still ambiguous once you're actually designing the confirming-question UX, don't assume either direction.
- **Decision branches:**
  - A. `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision keeps holding up on real evidence — 17 PRs since (#30 through #44) have all either found real errors or come back clean with independent verification shown.
  - B. If a research-backed decision comes up again, independently spot-check any specific statistic before it lands in a permanent doc — see the `deep-research-numeric-claims-need-spot-check` memory.
  - C. When two independent review passes disagree on a load-bearing external fact, resolve it with a fresh independent check of your own rather than deferring to the more confident-sounding account — see the `reviewer-disagreement-resolution` memory. First instance at 3.1 (PR #39); no second recurrence yet.
  - D. When `copilot-surrogate` hits its own 20-file scope-escalation ceiling, split into independent scoped passes over disjoint file sets rather than escalating a mechanical scoping decision to Alex. Now recurred a third time at 3.4a-iii (26 files, proactive 3-way split) — all three splits (3.3's reactive 2-way, 3.4a-ii's proactive 2-way, 3.4a-iii's proactive 3-way) used the same disjoint-file-set shape without incident, and 3.4a-iii's own split incidentally showed two independent passes can catch the identical finding without coordinating — free cross-validation, not just parallelism.
  - E. After fixing a stale sentence a review pass flagged, grep the same file for _other_ sentences making the same kind of claim before considering the fix complete — recurred at 3.4a-i, 3.4a-ii, and 3.4a-iii (`BUILD_PLAN.md`'s "promise-chain reduce" text, stale twice over from two sequential code changes). **New nuance at 3.4a-iii:** the same discipline applies _within_ a single fold, not just across separate review rounds — R2 caught a fold that fixed one stale reference but, by fixing a second finding (a rename) afterward, left the first fix's own fresh text referencing the pre-rename name. Re-grep after _all_ fixes in a fold land, not just after each one individually.
  - F. Default to a `Map`, not a plain object literal, for any lookup keyed by an external string a caller (or an attacker) partly controls (3.4a-ii's prototype-pollution finding). Three instances in this codebase now use this shape correctly: `classifyReactionOutcome`, `isAway`'s emoji-shortcode match, `classifyConfidenceBand`'s numeric bands.
  - G. **New at 3.4a-iii:** recursion (destructure `[head, ...rest]`, base-case-then-recurse) is this codebase's own established pattern for sequential-by-design async work over a short list — reach for it before a `for...of` loop + `eslint-disable` when `docs/CONVENTIONS.md`'s `.reduce()` ban applies and a loop's own warning would otherwise need a disable comment. Two confirmed instances: `check-cost-cap.ts`'s `sendCostAlerts` (pre-existing) and `handle-ambient-channel-message.ts`'s `seedReactionLegend` (3.4a-iii, added after Alex directly questioned an already-"compliant" eslint-disable fix mid-review).
  - H. **New at 3.4a-iii:** a new Slack event _type_ subscription (Event Subscriptions → Subscribe to Bot Events) is a distinct, separately-required configuration axis from an OAuth _scope_ grant — don't assume one implies the other. Any future chunk adding a new Slack event type (not just a new API method call) should expect to need both checked, and only live-verification can catch a missing one.
  - I. **New at 3.4a-iii:** `copilot-surrogate`'s findings need to actually land as a posted PR comment, not just get relayed in conversation — this session caught and corrected a miss on this specific step (via a stale `ScheduleWakeup` firing late, itself a reminder that a late wakeup can still be doing real work, not just noise). Check this explicitly as its own step when dispatching `copilot-surrogate`, not just "did I read the findings."
- **Carry-overs:**
  - A genuinely non-obvious design gap can hide in a chunk's own BUILD_PLAN text, not just in untraced existing code (3.4a-i) — re-read the chunk's own BUILD_PLAN bullet once more right before considering a chunk done, not just once at the start.
  - Don't use a BUILD_PLAN chunk identifier as a PR title's scope — the title-format CI check's scope regex rejects a literal dot; every merged PR in this repo uses a package name as scope instead. Held cleanly at 3.4a-ii and 3.4a-iii with no recurrence.
  - A new real, billed LLM call site needs the exact same cost-cap gate and accounting as the first, from the start (established at 3.3, applied proactively since). 3.4a-iii's own situational-appropriateness gate call is the fifth call site to get this right on the first try.
  - Live verification keeps finding a real environment gap in the same family, now **five** times (3.3, 3.4a-i, 3.4a-ii, 3.4a-iii, 3.4c): a long-lived local Postgres container needing a full from-scratch migration re-apply despite real uptime — the pattern isn't about container restarts specifically. **Root cause finally identified at 3.4c**, not just worked around again: `packages/core`'s own `resetDatabase` (`ticket-lifecycle/test-db.ts`) `DROP TABLE`s everything in its `afterEach`, and it reads `DATABASE_URL` the same way the "live dev" DB connection does — running the local test suite (`pnpm --filter @moe/core test` or the repo-wide `pnpm test`) against the same `DATABASE_URL` used for live verification wipes the schema as a side effect of ordinary test cleanup, every time. Not a bug to fix (tests correctly clean up after themselves against a real DB, per `docs/TESTING.md`) — just: re-run migrations after running tests, not only "sometimes," whenever a live check follows a test run against the same DB.
  - **New at 3.4c:** `test-db.ts`'s `resetDatabase` hardcodes its own `DROP TABLE` list — every new table a chunk adds needs a matching manual addition there, or its own tests leak state across each other within the same file (caught live while writing `review-queue-repository.test.ts`'s own tests). Nothing structural currently flags a miss; check this explicitly whenever a chunk adds a new table, the same way `schema.ts`'s `Database` type and a new migration file both already need touching.
  - The manual-integration-script live-verification pattern (established at 3.4a-ii for chunks with no live trigger by design) has a more surgical variant as of 3.4a-iii: when only _one specific guard_ is the actual live-test blocker (e.g. a clock-dependent guard on a weekend, not the whole trigger path being unbuildable), bypass only that one guard in the script and call every other real production function against real APIs — a narrower, more faithful test than bypassing the entire pipeline.
  - A fold commit needs the same "does this diff's prose still match this diff's code" self-check as the original PR — recurred across 2.6b, 3.1, 3.3, 3.4a-i, and (in the sharper same-fold-sequencing form) 3.4a-iii. 3.4a-ii's own fold is still the one clean exception.
  - The session-handoff trigger's track record is still mixed, not a clean streak. Keep checking at every PR merge regardless of how the last one went. `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s own already-identified "deterministic pointer-injection hook" is still the concrete next step if this keeps needing prompting — Alex's call to scope as a real chunk, not something to build unilaterally.
  - `docs/decisions/` holds six ADRs, none trimmed to the ~50-line "decisions-only" budget yet (`docs/decisions/README.md`'s lifecycle step 3) — flagged repeatedly without being acted on. Content-judgment task on historical records — check with Alex first.
  - Sarah's real Slack App is verified working fully end-to-end as of this session, the first time every layer of the intake cascade has been live-confirmed at once: thread-scoped conversation memory, persona-aware naming, the status-claim gate, per-turn cost accounting (**five** call sites/two models — Sonnet 5 for DM replies, High-band draft composition, and 🔁 redo; Haiku 4.5 for the Stage-1 classifier and the situational-appropriateness gate — all into the same per-persona/day bucket), cost caps + spend alerts, ambient-channel classify-and-log, High-band ticket-draft composition (real Slack post, gated by both the 2.7a operating-rhythm guard and the new situational-appropriateness gate), and the full reaction-gate outcome loop (real `reactions.add` seeding, real self-authored-reaction filtering, real commit/redo/park dispatch). DMs keep the full conversational reply path; ambient channel/group messages never get a reply at all (3.3's DMs-only decision). Credentials in gitignored `.env.local`, template at `.env.example`. Sarah's Slack App now has `reactions:write` (OAuth scope) and `reaction_added` (Event Subscriptions bot event) — both added live this session, same "missing_scope/missing-subscription caught live" pattern as 2.7b's `users.profile:read` and 3.3's `channels:read`. `0006_create_pending_ticket_drafts.sql` is still the newest migration (no new migration this chunk). `.env.local`'s `MOE_COST_CAP_MONTHLY` is `50`; `MOE_COST_ALERT_SLACK_USER_ID` is populated with Alex's real Slack user ID.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - Branch discipline: create the dedicated branch as the literal first action after syncing `main`, before any edit, every single chunk transition.
  - `ScheduleWakeup` is `/loop`-dynamic-mode-scoped, not a general "wait passively" tool — when something else (Monitor, a task notification) is already the wake signal, do nothing further; don't layer a second wakeup mechanism on top. That said, a stale wakeup that fires anyway can still surface a real gap (3.4a-iii) — check what it's actually asking before dismissing it as redundant noise. Full detail in the `autonomous-run-handoff` memory note.
  - `pnpm`'s own binary bundles an internal Node runtime and overrides `PATH` for child processes it spawns — the documented Node-24 PATH-prepend fix doesn't reach scripts pnpm shells out to internally (`packages/core`'s `migrate` script specifically). Workaround: invoke the Volta-pinned `node` binary directly for that one command, bypassing `pnpm run`/`pnpm --filter`. Full detail in the `autonomous-run-handoff` memory note.
  - Volta itself (not just pnpm) resolves the wrong pinned Node version when invoked from a package subdirectory lacking its own `"volta"` key in `package.json`. Run Node-24-dependent one-off commands from the monorepo root by relative path, not after `cd`-ing into a package first. Full detail in the `autonomous-run-handoff` memory note.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm directly (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite.
  - Local dev needs a real Postgres for `packages/core`'s tests (`DATABASE_URL`) — CI provisions its own via a `postgres:17-alpine` service container; locally, a throwaway Docker container works. Check `docker ps` before starting a duplicate container.
  - A real, running local dev server can be driven directly for live verification (`node apps/server/dist/index.js` with `.env.local`'s real credentials sourced into the environment) — the `Monitor` tool is the right mechanism to watch its structured JSON-lines stdout for specific lifecycle events without polling; pipe through `tee` to a raw log file (not just a `grep` filter) when debugging something unexpected, since a narrow filter can make "nothing matched" indistinguishable from "nothing happened."
- **Fallback:** if Alex redirects on load, follow that; otherwise chunk 3.4b is next — do an architectural pass against `docs/VISION.md`/`docs/GLOSSARY.md`/`BUILD_PLAN.md`'s own 3.4b+3.4c text together first and raise any genuinely open confirming-question-UX design questions with Alex via `AskUserQuestion` before code moves, same discipline every prior chunk at this level of BUILD_PLAN-text ambiguity has followed.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk    | Status | Shipped    | Headline                                                                                                                                                           |
| -------- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0.1      | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                                                                               |
| 0.2      | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                                                                                           |
| 0.3      | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                                                                                    |
| 0.4      | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                                                                               |
| 0.5      | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                                                                                |
| 0.6a     | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved                                                                     |
| 0.6b     | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)                                                                     |
| 0.6c     | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision                                                                   |
| 0.7      | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**                                                                            |
| 1.1      | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                                                                                          |
| 1.2a     | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)                                                                          |
| 1.2b     | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                                                                                      |
| 1.3      | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                                                                                   |
| 1.4      | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                                                                                            |
| 1.5      | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`)                                                                |
| 1.6      | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**                                                                        |
| 2.1      | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)                                                                        |
| 2.2      | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config                                                                   |
| 2.3      | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app                                                                    |
| 2.4a     | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app                                                                 |
| 2.4b     | Merged | 2026-07-17 | Thread-scoped conversation state + persona-naming fix — live-verified context-aware multi-turn chat                                                                |
| 2.5      | Merged | 2026-07-17 | Status claims through the `report_status` tool + 1.4 gate — VISION §7.6 anti-fabrication, wired                                                                    |
| 2.6a     | Merged | 2026-07-17 | Cost metering substrate — `persona_cost_daily` table, atomic accumulation, live-verified turn cost                                                                 |
| 2.6b     | Merged | 2026-07-17 | Cost caps + spend alert ladder — atomic claim, hard halt, live-verified full 50/80/100% arc                                                                        |
| 2.7a     | Merged | 2026-07-18 | Core-hours + bank-holiday guard — DST-safe check, Cached\<T,E\> class, fails closed, no consumer yet                                                               |
| 2.7b     | Merged | 2026-07-18 | Away-detection — isAway/fetchSlackStatus, verified emoji shortcodes, live-verified, no consumer yet — **Stage 2 complete**                                         |
| 3.1      | Merged | 2026-07-18 | [GATE] Stage-1 classifier scoring method + thresholds — bundled call, Haiku 4.5, High≥70/Mid35-69/Low<35 — **Stage 3 opened**                                      |
| 3.2      | Merged | 2026-07-18 | Channel scoping (Stage 0) — isSurfaceInScope allow-list + DM bypass, no consumer yet, first Workflow-dispatched review arc                                         |
| 3.3      | Merged | 2026-07-18 | Classifier gate (Stage 1) — Haiku 4.5 wired live, DMs-only chat replies, cost-cap fix, live-verified end-to-end                                                    |
| 3.4a-i   | Merged | 2026-07-18 | High-band auto-draft — Sonnet-5 ticket draft, shadow/log-only, 2.7a guard wired, live-verified end-to-end                                                          |
| 3.4a-ii  | Merged | 2026-07-18 | Reaction listener + outcome paths — commit/redo/park, no live consumer, prototype-pollution fix, live-verified via real infra                                      |
| 3.4a-iii | Merged | 2026-07-19 | Situational-appropriateness gate + real end-to-end wiring — real post, self-authored filter, live dispatch — **Stage 3 exit criterion passes live**                |
| 3.4c     | Merged | 2026-07-19 | Review-queue table + Low-band silent log — built ahead of 3.4b (sequencing resolved via `AskUserQuestion`), `outcomeReason` column anticipates 3.4b, live-verified |
