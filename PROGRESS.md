# Progress

Living state document — current state, what's next. Session-by-session detail lives in git history once entries archive out (see `docs/history/SESSIONS.md` and `docs/DEVELOPMENT.md` §Session handoff for the mechanics).

## Next workstreams (after Session 5)

Updated 2026-07-16 end-Session-5 — **Stage 2's Sarah loop is fully live: chunk 2.4a shipped and was verified with a real Anthropic completion, on top of a process-hardening arc that formalized real review-gate gaps and produced a researched decision on how far to harden further.** This entry closes out a single unusually long continuous session (no compaction/handoff fired the entire time — see "Major novel patterns" below, item 1). Seven PRs (#25–#31) landed directly in this entry; the three Session 4 had already covered (#22–#24) are compressed below now that they're historical — ten total shipped across the combined stretch. Written now because Alex had to ask a **third** time — see item 1.

**Process retrospective arc** (PRs [#25](https://github.com/Pushedskydiver/moe/pull/25), [#26](https://github.com/Pushedskydiver/moe/pull/26)): Alex asked why `copilot-surrogate` was skipped on PR #24 — its retroactive run found a genuine MATERIAL bug (`postMessage`'s TSDoc claimed `@slack/web-api` could resolve `{ok:false}`; the real SDK always throws). An audit found three more instances of the same failure shape (a judgment-based trigger silently skipped under momentum — see pattern #2 below). `docs/DEVELOPMENT.md` formalized Round-2 verification as applying to any review-round tool, not DA-only (previously private-memory-only); two new `docs/RATIONALIZATIONS.md` entries. PR #25 itself merged with a DA dispatch still in flight — its post-merge result found the R2 rule wasn't actually wired into the numbered Review Gate steps, only stated in a floating paragraph; PR #26 fixed that, with both review tools R2-confirmed clean before merge this time.

**Deep-research pass + review-gate discretion decision** ([PR #28](https://github.com/Pushedskydiver/moe/pull/28)): a Workflow-orchestrated deep-research pass (6 parallel sources — Matt Pocock, Addy Osmani, Simon Willison, Armin Ronacher, Anthropic's own engineering docs, arXiv literature on LLM rule-adherence; 62 findings, all 11 numeric claims independently verified) evaluated three candidate structural fixes for the PR #24/#25 failure shapes. Alex approved Claude's own recommendation over the research's most-evidenced option: **narrow** `copilot-surrogate`'s discretionary trigger to mandatory on its three (sharpened) conditions, **decline** building a dedicated gate-checker subagent or CI enforcement machinery for now, on proportionality grounds (moe is mid-Stage-2, effectively solo, low PR volume). Full reasoning: [`docs/decisions/REVIEW-GATE-DISCRETION.md`](docs/decisions/REVIEW-GATE-DISCRETION.md); research artefact: `.claude/research/review-gate-reliability/research-2026-07-16.md` (gitignored). Review gate on the implementation PR itself caught 8 real errors in the ADR's own supporting evidence (misread citations, an internal self-contradiction, a wrong project-stage claim) — see pattern #3.

**Chunk 2.4a — LLM reply loop (stateless)** ([PR #30](https://github.com/Pushedskydiver/moe/pull/30)): `apps/server` replaces the hardcoded Slack ack with a real single-turn, stateless call to the raw Anthropic Messages API (`docs/VISION.md` §11's verified model-client choice), in a deliberately generic, non-persona placeholder voice living outside the Stage-5-gated persona-prompt path. New in `packages/agents`: `anthropic-config.ts`, `create-anthropic-client.ts` + `create-anthropic-sdk-logger-adapter.ts`, `generate-reply.ts`, `placeholder-system-prompt.ts`. Model pinned to `claude-sonnet-5` per `docs/VISION.md` §10's resolved answer. Review gate found the Anthropic SDK's own internal logger wasn't routed through the app's redacting logger — the exact same class of secret-leak gap chunk 2.3 had with the Slack SDK, despite a memory written specifically to prevent a repeat (see pattern #4) — plus a wrong `VISION.md` citation and an overclaim about the SDK's default logger behavior, all fixed and R2-confirmed against real installed-package source. Post-review: `MOE_ANTHROPIC_API_KEY` renamed to the bare `ANTHROPIC_API_KEY`, matching `DATABASE_URL`/`PORT`'s unprefixed precedent and the exact env var `@anthropic-ai/sdk` itself defaults to — caught when Alex set the key in `.env.local` under the bare name unprompted. **Verified end-to-end with a real Anthropic completion**, reported directly by Alex in conversation (not GitHub-artifact-corroborated — happened post-merge): a live DM to Sarah got back a real LLM-generated reply.

**Three PROGRESS.md syncs** ([#27](https://github.com/Pushedskydiver/moe/pull/27), [#29](https://github.com/Pushedskydiver/moe/pull/29), [#31](https://github.com/Pushedskydiver/moe/pull/31)): routine status-sync PRs keeping the living-state doc current after each landing (#27 covered #25/#26; #29 covered #27/#28; #31 covered #30). Every single one had real DA and/or `copilot-surrogate` findings folded — see pattern #5.

**Major novel patterns this session:**

1. **The handoff trigger recurred a third time — across the longest unplanned continuous stretch yet.** Session 3 named it as a lesson; a `docs/RATIONALIZATIONS.md` entry written in Session 4 formalized it; neither prevented this session running through ten PRs and multiple natural phase boundaries (every one of #25–#31 merging) without a single handoff check firing on its own — Alex had to ask again. Sharper diagnosis this time: `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s declined-automation position is about LLM-authored _summary quality_ (hallucination/omission risk) — that's not what's failing here. What's failing is nobody/nothing checks the trigger condition at all; a written note in a doc doesn't get re-read mid-session under momentum. The ADR's own text already names the fix that targets _this specific_ gap without touching the declined summary-quality concern: a **deterministic pointer-injection hook** (`SessionStart(compact)`/`PostCompact`, no LLM authorship) — "not prototyped; not scoped into `BUILD_PLAN.md`." Three data points now say the manual-only approach doesn't hold at this trigger. Recommend Alex scope this as its own chunk rather than deferring a fourth time — not decided here, flagged as the strongest carry-over.
2. **The `copilot-surrogate` miss (PR #24) was one instance of a class, not a one-off** — the audit that followed found three more: a self-review pass reusing a remembered checklist instead of re-reading `docs/SELF-REVIEW.md`; the R2-verification discipline living only in private memory, never the shared docs; and pattern #1 above. All four are the same shape: a judgment-based trigger silently skipped under momentum, while mechanical ones (branch discipline, BUILD_PLAN checkbox convention) held up fine.
3. **`copilot-surrogate`'s new mandatory trigger (the PR #28 decision) immediately proved itself in production.** Three PRs since (#29, #30, #31) shipped a `fix(docs)` commit and exercised it; two of those three (#30, #31) found real, previously-uncaught factual errors — including inside moe's own session-record-keeping (pattern #5) — and the third (#29) came back genuinely clean, which is itself useful signal the trigger isn't just always finding something to report. The decision to narrow rather than build heavier machinery is holding up on real evidence, not just the research that justified it.
4. **A documented lesson didn't transfer to a new-but-structurally-identical instance.** `secret-pattern-mirror-locations` (written after chunk 2.3's Slack-SDK-logger gap) exists specifically to prevent this exact bug recurring. Chunk 2.4a's Anthropic client needed the identical fix from scratch — the memory didn't auto-apply itself to a new SDK integration; DA had to catch it fresh. Same shape as pattern #1: a written lesson only holds when something re-surfaces it at the moment it's needed, not just when it's written down once.
5. **PROGRESS.md syncs are themselves error-prone narrative work, not "just docs."** All three pure-sync PRs this session (#27, #29, #31) had real MATERIAL findings folded by DA and/or `copilot-surrogate` — miscounted tallies, self-contradictions within the same file, a claim mis-attributed from one PR's record to another's. Session-record-keeping gets the same review discipline as code specifically because it keeps failing without it.
6. **Live verification remains the actual bar for "done."** Chunk 2.4a's real Anthropic completion round-trip needed Alex's own API key and a live Slack DM — no amount of unit tests or code review could have proven the wiring genuinely worked end-to-end, the same lesson chunks 2.2/2.3 already established for Docker/Slack alone.

**Earlier Session 4 status, compressed** — `apps/server` first boots for real (chunk 2.2, [PR #23](https://github.com/Pushedskydiver/moe/pull/23)): env-sourced `PersonaConfig`, structured JSON logging with secret redaction, health check, Fly deploy config — verified with real `docker build`/`run`/`curl`. Then real Slack Socket Mode connectivity (chunk 2.3, [PR #24](https://github.com/Pushedskydiver/moe/pull/24)): event schema/normalization, bot-loop prevention, a hardcoded non-persona ack reply — verified end-to-end against Sarah's real Slack app, plus a live-Docker-caught bug where `exit(1)` never actually terminated the process (a listening HTTP server keeps Node's event loop alive regardless of `process.exitCode`). A side-fix ([PR #22](https://github.com/Pushedskydiver/moe/pull/22)) verified moe's model strategy against Sonnet 5's real release data, catching one fabricated third-party benchmark claim along the way. Durable pattern: the handoff trigger fired repeatedly (PRs #22/#23/#24 all merging) and wasn't acted on until asked — the first occurrence of what became this session's own pattern #1.

**Earlier Session 3 status, compressed** — Stage 1 shipped in full: ticket types (1.1), the topology/DB ADR reversing single-machine+SQLite to N machines + Neon Postgres on evidence (1.2a), database layer (1.2b), atomic claim (1.3), the anti-fabrication `StatusClaim` schema (1.4), the track-record ADR at N=5 (1.5), the risk-tier classifier (1.6) — plus chunk 2.1, the cast-redline gate (kept the 7-role roster, Sarah confirmed first). Two side-fixes (branch-prefix doc drift, a stale VISION citation). [#12](https://github.com/Pushedskydiver/moe/pull/12)–[#21](https://github.com/Pushedskydiver/moe/pull/21). Durable patterns: a deep-research workflow's own adversarial vote can still pass a fabricated statistic (spot-check primary sources before trusting a specific number); R2 verification caught two real residual issues neither single fix pass had; the handoff trigger fired repeatedly and wasn't acted on (named as a lesson here — see the pattern's own recurrence above, twice more since).

**Earlier Session 2 status, compressed** — deep-research test of chief-clancy's auto-handoff conclusion (decline-with-updated-reasons position adopted, `docs/DEVELOPMENT.md` §Session handoff), then chunks 0.6c (reference docs + doc-port decision) and 0.7 (generate:agents-md TS port + CI freshness check), closing Stage 0 entirely. [#9](https://github.com/Pushedskydiver/moe/pull/9)–[#11](https://github.com/Pushedskydiver/moe/pull/11).

**Session 1** archived to `docs/history/SESSIONS.md` (detail band was growing past budget) — full retrospective survives in `git log -p PROGRESS.md`.

### Session 6 loading instructions

- **Verify state before picking anything up:** `git log --oneline -5` on `main` (expect PR #31's squash at HEAD), `git status` clean. `BUILD_PLAN.md` shows chunk 2.4a `[x]` — content-complete and live-verified (PR #30) — with 2.4b next. Also check `docs/history/SESSIONS.md`'s row count / this file's detail-band size at session start, per the archival trigger — don't wait for it to "come up."
- **Primary workstream — BUILD_PLAN Stage 2, chunk 2.4b** ("Thread-scoped conversation state"). Store and rehydrate per-thread history into the next turn (where it's keyed, how it's trimmed) — the remaining piece of Stage 2's exit criterion. Not `[GATE]`.
- **Decision branches:**
  - A. `docs/decisions/REVIEW-GATE-DISCRETION.md`'s narrowed-trigger decision is holding up on real evidence (3 PRs since have exercised it — #30/#31 found real errors, #29 came back clean) — see its own "Triggers for re-evaluation" section for what would reopen it. Any escalation to the heavier fixes (B/C) still enters `BUILD_PLAN.md` as its own chunk with Alex's sign-off, not a rider on other work.
  - B. If a research-backed decision comes up again, independently spot-check any specific statistic before it lands in a permanent doc — see the `deep-research-numeric-claims-need-spot-check` memory. Confirmed working again this session (PR #28): both DA and `copilot-surrogate` independently re-fetched primary sources and caught real citation errors an earlier pass hadn't — spot-checking numeric/behavioral claims against live sources, not just internal consistency, keeps finding real drift.
- **Carry-overs:**
  - **The handoff trigger has now been missed three times in a row** (Session 3; Session 4 despite a same-session doc fix; Session 5 across the longest unplanned continuous stretch yet, ~10 PRs) — see "Major novel patterns" pattern #1 above for the sharper diagnosis: the declined-automation ADR's concern was LLM-summary quality, not "does anything check the trigger at all." **Recommended concrete action, not just another flag:** scope `docs/decisions/SESSION-HANDOFF-AUTOMATION.md`'s own already-identified-but-deferred "deterministic pointer-injection hook" (`SessionStart(compact)`/`PostCompact`, no LLM authorship — see that ADR's §Deferred work) as a real `BUILD_PLAN.md` chunk with Alex's sign-off, rather than deferring a fourth time. This is Alex's call to make, not something to build unilaterally, but three data points is past "worth a note."
  - `docs/decisions/` holds five ADRs, none trimmed to the ~50-line "decisions-only" budget yet (`docs/decisions/README.md`'s lifecycle step 3) — flagged three times now across review gates without being acted on. This is a content-judgment task on historical records, not something to do unilaterally — check with Alex first.
  - Sarah's real Slack App exists and is verified working end-to-end, including a real Anthropic LLM completion round-trip (live DM → live LLM-generated reply, confirmed Session 5) — credentials are in a gitignored `.env.local`, template at `.env.example`. `ANTHROPIC_API_KEY` (bare name, no `MOE_` prefix) is now part of that file too.
  - `docs/PERSONAS.md` mirrors VISION §4.1 under the same do-not-touch protection; don't edit one without the other. Cast: Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master. Designer deferred to the 5.0 gate.
  - Branch discipline: create the dedicated branch as the literal first action after syncing `main`, before any edit, every single chunk transition.
  - Node-24 `PATH` prepend still needed in every Bash call that runs pnpm (`docs/DEVELOPMENT.md` §Local dev environment). `pnpm build` runs first in the pre-push suite.
  - Local dev needs a real Postgres for `packages/core`'s tests (`DATABASE_URL`) — CI provisions its own via a `postgres:17-alpine` service container; locally, a throwaway Docker container works (`docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=... -e POSTGRES_DB=... postgres:17-alpine`).
- **Fallback:** if Alex redirects on load, follow that; otherwise default to chunk 2.4b next.

## Session archive

Archived sessions are in `docs/history/SESSIONS.md`. Full retrospective for any session survives in `git log -p PROGRESS.md` at that session's compression commit.

## Phase ledger

| Chunk | Status | Shipped    | Headline                                                                                            |
| ----- | ------ | ---------- | --------------------------------------------------------------------------------------------------- |
| 0.1   | Merged | 2026-07-04 | git init + pnpm workspace skeleton, `packages/core` toolchain proven                                |
| 0.2   | Merged | 2026-07-04 | ESLint/Prettier/knip/husky, 6 rule-family fixtures verified then deleted                            |
| 0.3   | Merged | 2026-07-04 | Package graph settled, `eslint-plugin-boundaries` wired (needed a resolver fix)                     |
| 0.4   | Merged | 2026-07-04 | CI pipeline + branch protection live                                                                |
| 0.5   | Merged | 2026-07-04 | Review-gate agents ported, dogfooded, caught real `CLAUDE.md` drift                                 |
| 0.6a  | Merged | 2026-07-04 | Process docs (`docs/DEVELOPMENT.md`, `docs/TESTING.md`) + `PROGRESS.md`/`SESSIONS.md` resolved      |
| 0.6b  | Merged | 2026-07-09 | Review canon (`docs/RATIONALIZATIONS.md`, `docs/REVIEW-PATTERNS.md` pre-seeded per VISION §12)      |
| 0.6c  | Merged | 2026-07-10 | Reference docs (`ARCHITECTURE.md`, `GLOSSARY.md`, `decisions/`) + chief-clancy doc-port decision    |
| 0.7   | Merged | 2026-07-10 | `generate:agents-md` TS port + CI freshness check (now required) — **Stage 0 complete**             |
| 1.1   | Merged | 2026-07-10 | Ticket types + Zod schemas — pure domain model, no DB yet                                           |
| 1.2a  | Merged | 2026-07-11 | [GATE] Process topology × DB ADR — N machines, Neon Postgres (`TOPOLOGY-AND-DATABASE.md`)           |
| 1.2b  | Merged | 2026-07-11 | Database layer + tickets table (Kysely + pg, flat-SQL migrations, plain CRUD)                       |
| 1.3   | Merged | 2026-07-11 | Atomic claim — compare-and-set via `WHERE claimedBy IS NULL`, concurrency-tested                    |
| 1.4   | Merged | 2026-07-11 | StatusClaim schema + composer gate — VISION §7.6's anti-fabrication fix                             |
| 1.5   | Merged | 2026-07-11 | [GATE] Track-record definition ADR — N=5, minimum-across-directories (`TRACK-RECORD-DEFINITION.md`) |
| 1.6   | Merged | 2026-07-15 | Risk-tier classifier (`classifyRiskTier`, VISION §8.1 + the 1.5 ADR) — **Stage 1 complete**         |
| 2.1   | Merged | 2026-07-15 | [GATE] Cast redline — 7-role roster kept, Sarah first, Designer deferred (`CAST-ROSTER.md`)         |
| 2.2   | Merged | 2026-07-15 | Persona process skeleton — boot, health check, structured logging + redaction, Fly deploy config    |
| 2.3   | Merged | 2026-07-16 | Slack Socket Mode inbound events + hardcoded ack — verified live against Sarah's real Slack app     |
| 2.4a  | Merged | 2026-07-16 | LLM reply loop, stateless — real Anthropic completion verified live against Sarah's real Slack app  |
