# BUILD_PLAN.md — moe v3

> **Sizing discipline (Alex's explicit rule):** every chunk below is small, safe, and simple — roughly one PR, one sitting, one reviewable idea. Anything that turned out bigger during drafting got split. If a chunk turns out to be complex _during implementation_, stop and split it rather than pushing through — a chunk that grows past ~300 changed LOC of real logic (excluding lockfiles/scaffolding boilerplate) is a signal to split, not a target to squeeze under.
>
> **Reading order:** `docs/VISION.md` (product truth) → `CLAUDE.md` (engineering rules) → this file (sequence). Where this file conflicts with VISION.md, VISION.md wins.
>
> **Chunk states:** `[ ]` not started · `[~]` in progress · `[x]` merged · `[GATE]` needs an Alex conversation/decision before starting — not buildable until the gate clears.

---

## How the stages ladder up

```
Stage 0  Repo skeleton + engineering spine        (nothing moe-specific yet)
Stage 1  Core domain: tickets, claims, database    (pure logic + one DB table at a time)
Stage 2  One persona process, one Slack app        (prove the loop with N=1 before N=8)
Stage 3  Intake: chat → ticket                     (the #1 failure-mode fix, on N=1)
Stage 4  GitHub + the board                        (tickets become real work on chief-clancy)
Stage 5  The full cast                             (N=1 → N=8, gated on the cast redline)
Stage 6  Team behaviour: handoffs, review, merge   (personas working together)
Stage 7  Ceremonies                                (retro, replenishment, monthly review)
Stage 8  Memory & growth                           (voice consistency over months)
```

Each stage ends with something observable working end-to-end. **The gating rule, precisely:** a stage starts when the previous stage's _exit criterion_ is met — not when every chunk in it is checked. Chunks an exit criterion doesn't cover may float across the boundary, with these hard orderings: 0.6a–0.6c/0.7 land before Stage 1's first PR; 1.5/1.6 land before Stage 5's first 5.3 sub-chunk (VISION §13.1's item 1 puts the risk-tier gate in the foundation, before any persona exists in prose); 3.6 may trail into Stage 4; 7.6 is independent and may be pulled earlier. That's the anti-scope-spiral mechanism, per VISION §13.

**On VISION §13.1's "the full cast stands up together":** this plan runs N=1 through Stages 2–4 because the plumbing those stages build (Slack transport, intake cascade, GitHub + board) is persona-independent and cheaper to debug on one process than eight — none of it is cross-persona dynamics. The cast then stands up together as one stage (Stage 5), not one-persona-at-a-time rollout. **Resolved at chunk 2.1:** this ordering is confirmed, not amended away — VISION §13.1's item 3 now says so explicitly.

---

## Stage 0 — Repo skeleton + engineering spine

**Exit criterion:** `pnpm test && pnpm lint && pnpm typecheck && pnpm format:check && pnpm knip` green in CI on a hello-world monorepo; the review-gate agents exist and have been exercised once.

- [x] **0.1 — git init + pnpm workspace skeleton.** `git init`, root `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, Volta/engines pin (Node 24), `.gitignore`, empty `packages/core` with one exported function and one passing test. Nothing else. _Superseded in part, 2026-07-27: Node is now pinned by `mise.toml` as well. Volta resolves from the **nearest** `package.json` and does not walk **up** a workspace, so every `pnpm --filter` script silently ran Volta's global default; it is also unmaintained. The `volta` field is **retained** in `package.json` for now, not removed — a machine still on Volta gets identical versions. The `engines` pin is unaffected. Historical record kept as-is: the decision was right for what was known then._
- [x] **0.2 — Lint/format config to CONVENTIONS.md spec.** ESLint (complexity caps, functional rules, unicorn, sonarjs, n), Prettier + import-ordering plugin, knip (its own config — the exit criterion requires it green), lint-staged + husky pre-commit. Copy chief-clancy's configs, apply the documented deltas (no path aliases, full Zod). One deliberately-failing fixture per rule-family proves the config bites, then delete the fixtures.
- [x] **0.3 — eslint-plugin-boundaries + the settled package graph.** Decide the real package graph (confirm or amend the first-cut table in `docs/CONVENTIONS.md` §Architecture Enforcement), scaffold the empty packages (`core`, `memory`, `agents`, `slack`, `github`, `apps/server`), wire boundaries enforcement. Update CONVENTIONS.md + GIT.md scope labels from "first-cut" to settled in the same PR.
- [x] **0.4 — CI pipeline.** GitHub Actions: the quality suite on every PR, PR-title gitmoji/type check, branch protection on `main`. No deploy automation (per GIT.md §Deploy Flow — deliberate).
- [x] **0.5 — Review-gate agents ported.** Adapt chief-clancy's `da-review.md`, `spec-grill.md`, `copilot-surrogate.md` into `.claude/agents/`, retargeted at moe's doc set. Port `docs/DA-REVIEW.md` + `docs/SELF-REVIEW.md` (the checklists they cite) in the same chunk — the agents are useless without their checklists.
- [x] **0.6a — Process docs.** `docs/DEVELOPMENT.md` (review gate, session handoff incl. moe's PROGRESS.md/SESSIONS.md equivalents — resolves the open question in CLAUDE.md) + `docs/TESTING.md`.
- [x] **0.6b — Review canon.** `docs/RATIONALIZATIONS.md` + `docs/REVIEW-PATTERNS.md`, pre-seeded per VISION §12. Its own PR because 0.5's review-gate agents cite these as authority — the highest-stakes content in the chunk-0 doc set deserves its own review.
- [x] **0.6c — Reference docs + port decision.** `docs/ARCHITECTURE.md`, `docs/GLOSSARY.md`, `docs/decisions/` with README. Also decide here which of chief-clancy's other docs moe ports (CLAUDE.md's open question — see the parked list). `docs/INDEX.md` is deliberately **deferred past Stage 0** — it needs real PRs to route against (CLAUDE.md already says this).
- [x] **0.7 — `generate:agents-md` script.** Port the AGENTS.md generation script (source-only + literal markers) into `scripts/`, wire as `pnpm generate:agents-md`, add a CI check that AGENTS.md is up-to-date with CLAUDE.md.

## Stage 1 — Core domain: tickets, claims, database

**Exit criterion:** two concurrent fake persona processes race to claim the same ticket in an integration test; exactly one wins, every status message about it carries evidence.

- [x] **1.1 — Ticket types + Zod schemas.** `Ticket`, board statuses (`Backlog → Brief → Plan → Build → Review → Done` + `Cancelled`), severity classes, `projectKey`. Pure types + schemas + unit tests. No DB yet.
- [x] **1.2a — [GATE] Process topology × DB access ADR.** **Decided (2026-07-11): N machines, one per persona, backed by Neon Postgres.** Rejects the single-machine + SQLite-on-volume + Litestream shape this chunk originally scoped as the cheaper default — a live-verified deep-research pass found that framing wrong on three fronts: Litestream has an ongoing pattern of silent-replication-failure bugs (one fix merged then reverted, unfixed for 4+ months since, plus two more open issues in the same class — the worst failure mode for claim-critical state); Fly's own platform incident cadence makes host-level blast radius a real, not just theoretical, risk; and the real cost gap is much narrower than assumed once Neon (not Fly Managed Postgres's $38/mo floor) is the comparison point. Full reasoning, rejected alternatives (including Fly's native multi-container Machines API as a better-than-`overmind` single-machine option — still rejected), and evidence: `docs/decisions/TOPOLOGY-AND-DATABASE.md`. Written ADR with Alex. **Decision-only chunk.**
- [x] **1.2b — Database layer + tickets table.** The DB per the 1.2a ADR (Neon Postgres, N machines — one per persona), migrations tooling, the tickets table, plain CRUD. No claiming logic yet.
- [x] **1.3 — Atomic claim.** Compare-and-set claim/release on a ticket (`claimedBy` + version column). Property/concurrency tests: N racing claimants, exactly one winner, no lost updates. This is the orchestrator's load-bearing primitive (VISION §4.5) — it gets its own chunk so it's tested in isolation.
- [x] **1.4 — StatusClaim schema + composer gate.** The `{claim, toolCallId, toolOutputSnippet, timestamp}` type (VISION §7.6), plus the composer function that refuses to emit a status claim without populated evidence and falls back to "not yet verified". Pure logic, heavily unit-tested. **This is failure-mode-#2's fix and it ships before any persona exists.**
- [x] **1.5 — [GATE] Track-record definition ADR.** **Decided (2026-07-12):** minimum track record across directories for a multi-directory diff; preserve on git-detected rename; no transfer to a brand-new directory (Tier 2 floor); threshold **N = 5** consecutive unreverted merges. Full reasoning and rejected alternatives: `docs/decisions/TRACK-RECORD-DEFINITION.md`. Written ADR with Alex. **Decision-only chunk.**
- [x] **1.6 — Risk-tier classifier.** `classifyRiskTier(diffMeta) → Tier 0–3` as a pure function per the ADR from 1.5 + VISION §8.1's table. Property-based tests. No enforcement wiring yet — just the classifier.

## Stage 2 — One persona process, one Slack app

**Exit criterion:** one persona (Sarah, confirmed at 2.1 as front-door) runs on Fly, has her own Slack App, responds to a DM, and every work-status statement she makes carries §7.6 evidence.

- [x] **2.1 — [GATE] Cast redline conversation.** **Decided (2026-07-15):** the roster itself (not just the first persona) is settled — keep the previous 7-role roster (Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master) as-is (the research found no evidence for or against reworking it, a symmetric silence, not a confirmation) and confirm **Sarah** as the first/front-door persona for Stage 2. Only the 8th role (Designer) takes the deferred branch — pushed to the 5.0 gate, since no evidence supports adding it now and chief-clancy has no real UI/UX surface yet. Also added a new VISION §8.2 safeguard against a real, research-confirmed risk of named-persona framing (reduced human review scrutiny, blame-shifting onto the AI) the research surfaced along the way. Full reasoning and evidence: `docs/decisions/CAST-ROSTER.md`. VISION §4.1 filled in + `docs/PERSONAS.md` skeleton. Written with Alex.
- [x] **2.2 — Persona process skeleton.** `apps/server` boots, loads a persona config (ID, Slack credentials), connects nothing, health-check endpoint, structured logging with secret redaction. Deployable to Fly as one machine.
- [x] **2.3 — Slack app + inbound events.** One Slack App (the first persona's), **Socket Mode** (settled by a 2026-07-04 docs verification: no public endpoint per app, event retries work the same over Socket Mode as HTTP (~6-min retry window either way), Block Kit interactivity fully supported, 10 connections per app with no workspace cap, and Slack's "HTTP for production" advice targets horizontally-scaled apps — not 8 single-instance always-on processes, which is Socket Mode's stated sweet spot). Receives DMs and channel messages, Zod-validates payloads, normalizes to an internal `InboundMessage`. Replies with a hardcoded acknowledgment — no LLM yet.
- [x] **2.4a — LLM reply loop (stateless).** Wire the **raw Anthropic Messages API** (per VISION §11's verified model-client choice — the Agent SDK's per-query subprocess spawn (~12s) and session-file accumulation make it the wrong layer for chat turns; it returns for Riley's agentic work at 6.2): a single-turn DM produces a coherent reply in a deliberately generic, non-persona placeholder voice — explicitly **not** the persona's character, which is Stage 5 behind the do-not-touch gate. The placeholder prompt lives outside `packages/agents/src/personas/*/prompt.md` so no gated path is touched. This chunk proves the client wiring end-to-end (config, turn invocation with inline tool definitions, reply extraction, error/retry/timeout, post back to Slack).
- [x] **2.4b — Thread-scoped conversation state.** Store and rehydrate per-thread history into the next turn. Keying: a DM channel is one continuous conversation; a channel/group message only accumulates history inside an explicit Slack thread, keeping unrelated people/topics in a busy channel from bleeding together — an un-threaded channel/group message stays fully stateless, same as 2.4a. Trimming: last 20 stored turns (~10 exchanges) rehydrated per call, full history retained in Postgres regardless. New `conversation_turns` table + repository in `packages/core`; `apps/server` gains its first `@moe/core`/DB dependency (an edge already legal in the dependency-direction table, now exercised). Also closes a gap Slack's own event shape creates: the message that opens a channel/group thread never carries `thread_ts` itself (only the replies that follow do), so a small in-memory root-candidate buffer backfills the opening message (and the bot's own reply to it) into a newly-formed thread's history on its first reply. **Removed at chunk 3.3**: once ambient channel/group messages stopped going through the chat-reply path at all (DMs-only), this buffer became unreachable and was deleted rather than left as dead code. A per-thread-key serialization queue prevents two concurrent messages in the same thread from racing on history fetch/persist. Migrations stay a manual pre-deploy step, not run at boot.
- [x] **2.5 — Status messages through the claim gate.** Any status claim now routes through a `report_status` tool call rather than free prose, per VISION §7.6's typed-object requirement — resolved as the correct reading of this line's own acceptance test. Composed via the 1.4 gate. Integration test: persona calls `report_status` with a claim but no backing evidence (no real work tool exists yet at Stage 2) → the message that actually reaches Slack, and gets persisted to conversation history, says "Not yet verified." Known limitation of this chunk's own enforcement, not a VISION-sanctioned exception (VISION §7.6 only names the _misgrounded_-claim gap as an accepted Tier 0/1 tradeoff, not this one): a bare free-prose claim with zero tool call at all still bypasses the gate entirely, since enforcement is a system-prompt instruction, not a mechanical block — closing that fully is Stage 5/6 territory (persona prompting, independent verifiers), not this chunk's job.
- [x] **2.6a — Cost metering substrate.** `persona_cost_daily` table + migration (`packages/core`), accumulated atomically via a real `INSERT ... ON CONFLICT DO UPDATE`, not a read-modify-write from application code — race-tested with concurrent `recordUsage` calls, not just sequential ones. `generateReply` surfaces each turn's real token usage (`packages/agents`), priced against VISION §10's dated Sonnet-5 rates ($2/$10 per MTok through 2026-08-31, then $3/$15) as an integer micro-USD value, not a float. `apps/server` wires an accounting hook into the reply path, mirroring the existing `HistoryStore` DI seam. Accumulates and exposes the numbers only — no caps, no alerts (2.6b). Live-verified against Sarah's real Slack app: a real turn recorded `persona_id=sarah, day=2026-07-17, input_tokens=783, output_tokens=17, cost_usd_micros=1736` — an exact match for introductory pricing.
- [x] **2.6b — Caps + alert ladder.** `MOE_COST_CAP_MONTHLY` (a dollar amount, converted once at the config boundary to micro-USD) + `MOE_COST_ALERT_SLACK_USER_ID` (`packages/agents`'s `parseCostCapConfig`), consuming 2.6a's `persona_cost_daily` via a new `getPersonaCostForMonth` monthly-sum query (`packages/core`). The VISION §10 alert ladder (50/80/100%) is a pure `evaluateCostCap` decision (`packages/agents`) — exact integer-arithmetic threshold comparisons, no float percentage division — with threshold-crossing state persisted in a new `persona_cost_alerts` table via `claimAlertThreshold` — an atomic conditional-update claim (`WHERE highestThresholdAlerted < excluded.highestThresholdAlerted`, same mechanism as `ticket-lifecycle/claim.ts`'s `claimTicket`), not a blind `GREATEST` upsert: the Slack DM only sends when a claim actually wins, so two concurrent turns for the same persona racing on the same newly-crossed threshold can't both send it — race-tested with concurrent `claimAlertThreshold` calls, not just sequential ones. Alex confirmed the two open design points before code moved: hard-halt (not alert-only) at 100%, and a visible halt message to the user (not silence) — both per BUILD_PLAN's own note that this needed confirming inside this chunk; the alert ladder DMs Alex directly rather than a shared channel, also confirmed. `apps/server`'s `generateAndPost` checks the cap before calling `generateReply` at all, so a halted turn never reaches the Anthropic API. The remaining §10 buckets — the per-ticket ceiling and tier/track-record-scaled caps — land at 6.8 once tickets and tiers are live: deferred with a home, not dropped. Live-verified against Sarah's real Slack app: seeded spend at 95% of a $0.01 test cap, one real turn crossed 50% and 80% together (both alert DMs landed with correct text), a second real turn crossed 100% (alert DM landed) and hard-halted — the visible halt reply posted with zero new token usage recorded, confirming `generateReply` was genuinely skipped, not just its output discarded.
- [x] **2.7a — Core-hours + bank-holiday guard.** The shared core-hours/weekend/UK-bank-holiday module every persona-initiated behavior consults — VISION §6.4 delegates the concrete parameters here, and §14 makes weekend/bank-holiday rest a hard rule. Parameters settled (Alex confirmed via `AskUserQuestion`, not inferred): core hours 08:30–17:00 Europe/London Mon–Fri (`isWithinCoreHoursWindow`, DST-safe via `Intl`, `packages/core`); holiday source = the GOV.UK bank-holidays API (england-and-wales), cached in-process with a serve-stale-on-failure fallback (`Cached<T,E>`, `createBankHolidaysCache`) — a cold-boot cache with no successful fetch yet fails CLOSED (treats the day as a rest day) rather than guessing, matching §14's hard rule; off-hours policy — proactive sends and intake drafts are **not performed** outside the window (`evaluateOperatingRhythm`). **Corrected at chunk 3.9:** this line originally read "defer to the next core-hours window", which no code has ever done — there is no scheduler, and the blocked action was simply dropped. Since 3.9 a blocked _ambient_ intake action leaves a durable `review_queue` row so the message survives for the 3.5 sweep digest; genuine deferral (composing and posting when the window opens) is 3.9's own step (2), still unbuilt, direct DM replies are exempt and always proceed (a DM is reactive engagement, not Moe acting unprompted, so §14's rest rule doesn't reach it) — no wiring needed on the existing reply path for this. No consumer wired yet; this chunk builds the reusable primitive only. Consumed by intake (3.4a-i, wired for real as of that chunk — see its own entry), the pull loop (6.1a-i), and the ceremony scheduler (7.2a).
- [x] **2.7b — Away-detection.** Alex's Slack status text/emoji against a keyword list — settled (Alex confirmed via `AskUserQuestion`, not inferred): text keywords "away"/"holiday"/"OOO" (whole-word, case-insensitive, `isAway` in `packages/slack`); emoji shortcodes widened past BUILD_PLAN's own two named examples (🌴/🏖️) to `palm_tree`/`beach_with_umbrella`/`airplane`/`face_with_thermometer`/`thermometer`/`hospital` — each verified against the real `iamcal/emoji-data` short-name database, not guessed. `fetchSlackStatus` (`users.profile.get`) is a separate Slack API read from 2.7a's pure calendar logic — required a new `users.profile:read` OAuth scope Sarah's app didn't have (discovered live: the first real call returned `missing_scope`). Alex is adding the scope and reinstalling separately from this code change; live verification against the real API is pending that, not yet confirmed as of this commit. No consumer wired yet; first real consumer is 7.2b.

## Stage 3 — Intake: chat → ticket (failure-mode #1's fix)

**Exit criterion:** VISION §0's failure #1, inverted, now passes as a live test — paste a GitHub issue link (or a plain "this needs looking into") in **a DM or** a scoped channel, no @-mention, no command, and a ticket draft appears with a reaction gate. At this stage the issue-link case produces a **link-only draft** (the URL plus whatever the message said) — issue title/body enrichment arrives with 4.1/4.4b, since Stage 3 has no GitHub client.

> **History of this criterion (resolved 2026-07-25, at chunk 3.7).** It originally read "in a DM or scoped channel". Chunk 3.3 then resolved chat replies as **DMs-only** and, symmetrically, routed the intake cascade to non-DMs only (`handle-inbound-message.ts`: `if (message.channelType !== 'im')` → cascade, then `return`; a DM went to the chat-reply path and never reached Stage 1). That was a deliberate, Alex-confirmed decision, but nobody reconciled it against this criterion, which predates it — so **Stage 3 read as passing a test its own code could not pass, from 3.3 merging until 3.7**. The channel half genuinely passed throughout and was live-verified at 3.4a-iii. The DM half was filed as chunk **3.7** and is now built, restoring the original wording rather than keeping the narrowed one. Worth remembering as a pattern: an exit criterion is not self-maintaining, and a chunk that changes routing needs to be checked against the criterion of the stage it sits in.

- [x] **3.1 — [GATE] Stage-1-classifier spike.** Resolved VISION §5.2's open question (`docs/decisions/STAGE-1-CLASSIFIER.md`): scoring method is a single bundled structured-output call (one 0–100 integer confidence score) on Claude Haiku 4.5 — log-probs don't exist on the Claude API at all (on any model), which alone rules that candidate out regardless of prefill's own model-by-model restrictions, so this was really a choice between "model-reported probability" and "a separate scoring call," which are the same mechanism at different points in the pipeline; bundled wins on VISION §5.2's own "fast, cheap" framing. Thresholds — High ≥ 70, Mid 35–69, Low < 35 — calibrated against a live 24-message synthetic eval (8 work requests, 8 banter, 8 ambiguous) run against the real Claude API on both Haiku 4.5 and Sonnet 5; Haiku showed a cleaner separation gap (scores 36–71 entirely unoccupied) at a fraction of Sonnet's cost, the deciding evidence. Spike chunk — the eval script itself was throwaway (not committed); the ADR + VISION.md edit are the permanent, reviewed output.
- [x] **3.2 — Channel scoping (Stage 0 of the cascade).** Config for which channels/DMs are work-relevant surfaces; messages outside scope never reach the classifier. Trivial on purpose. Built as `isSurfaceInScope`/`MessageSurface`/`ChannelScopeConfig` (`packages/core`): a DM to a named persona is always in scope (VISION §5.3 — already unambiguous, handled without Sarah in the loop; this chunk's own design choice is to short-circuit Stage 0 for that case rather than gate on it, since §5.3 doesn't speak to Stage 0 specifically); a channel is in scope only if its ID is in the configured work-relevant set. No open parameter to confirm with Alex here — VISION §6.1's channel roster (`#moe-team`/`#moe-incidents`/`#moe-research` in scope, `#moe-random` out) and §5.3's DM-bypass rule are both already settled, not open questions this chunk needed to resolve. No default config constant, unlike 2.7a/2.7b's `DEFAULT_*` values — real Slack channel IDs are workspace-specific artifacts with no meaningful code-level default; that resolution happens whenever a real listener is wired against this. No consumer wired yet at this chunk, same as 2.7a/2.7b — this chunk builds the reusable primitive only (consumer wired at 3.3).
- [x] **3.3 — Classifier gate (Stage 1 of the cascade).** The cheap per-message call per the 3.1 ADR, returning a confidence score. Logged, not yet acted on — run it silently in prod for a few days of real traffic to sanity-check the thresholds before 3.4a-i. Built as `classifyMessageConfidence` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`) wired into `apps/server/src/handle-inbound-message.ts`. A real design question surfaced mid-chunk, resolved with Alex before code moved: the existing reply handler generated a full conversational LLM reply to every inbound message, DM or ambient channel/group alike, with no Stage-0/1 distinction — a gap from before Stage 3 existed. Resolved as **DMs-only**: an ambient channel/group message now runs Stage 0 (3.2's `isSurfaceInScope`) then, if in scope, Stage 1 (this chunk) and logs the score — no reply posted, ever, for that surface; a DM is unaffected, unchanged from 2.4a/2.4b. Direct, confirmed consequence: `root-candidate-buffer.ts` (2.4b, backfilling an unthreaded channel message's root once a reply first threaded it) and `handleUnthreadedMessage` became entirely unreachable once ambient messages never reach the chat-reply path at all — removed rather than left as dead code (Alex confirmed before deleting). DA review caught 1 Critical (the classifier's own real, billed Anthropic call shipped completely uncapped and unaccounted-for — bypassing 2.6a/2.6b's cost-cap/hard-halt mechanism entirely, on a call site VISION §5.2 itself notes fires on every in-scope ambient message, not just addressed ones; fixed by adding `usage` to `classifyMessageConfidence`'s result, a new `haikuCostUsdMicros` pricing function, and wiring `checkCostCapAndAlert`/`recordUsageLogged` into the ambient path exactly as the DM path already does) + 1 Material (a TSDoc claim about when the classifier's `parsed_output` comes back `null` was wrong per the installed SDK's actual source — a schema/JSON-parse failure throws a bare `AnthropicError`, not `null`; fixed with a corrected TSDoc and a new `invalid-classification-output` error kind distinguishing that case from a genuine `APIError` network/rate-limit failure) + 1 Low (ambient classification isn't yet scoped to Sarah's process specifically — harmless at N=1 persona, a forward-reference for whichever Stage 5+ chunk adds a second persona sharing a channel).
- [x] **3.4a-i — High-band auto-draft.** High → LLM-composed ticket draft from the thread, posted in-thread with the 📦/🔁/✅ reaction legend (✅ commit the draft as a ticket; 🔁 redo — regenerate from the thread; 📦 park it to Backlog untriaged). Reactions are inert here — logged, not yet acted on — and auto-posting stays in log-only mode until 3.4a-iii's gate is wired in front of it. Respects the 2.7a operating-rhythm guard. Built as `classifyConfidenceBand` (`packages/core`, a pure `number → ConfidenceBand` function, thresholds from `docs/decisions/STAGE-1-CLASSIFIER.md`) and `composeTicketDraft` (`packages/agents`, Claude Sonnet 5, `zodOutputFormat` + `.messages.parse()`, a link-only draft — no enrichment beyond the source message, since Stage 3 has no GitHub client yet), wired into `handleAmbientChannelMessage` (`apps/server`, extracted from `handle-inbound-message.ts` to stay under `max-lines` once this chunk's logic landed). A genuine scope ambiguity surfaced mid-chunk, resolved with Alex before code moved: this chunk's own text reads like a real Slack post ("posted in-thread with... reaction legend"), but no reaction-listener or ticket-commit path exists yet (that's 3.4a-ii) and VISION §9's situational-appropriateness gate doesn't exist yet either (3.4a-iii) — resolved as **shadow/log-only**, same precedent as chunk 3.3 for the same class of risk (chunk 6.5a-i's own plan later follows this same pattern too, per its own text): the draft and its intended reaction legend are logged (`'would post high-band ticket draft'`), never actually posted to Slack. A second design gap surfaced during docs sync, after the shadow-mode implementation otherwise looked complete: this chunk's own text also names the 2.7a operating-rhythm guard as a requirement, which the first implementation pass had not wired in. Fixed before opening a PR, not left for review to catch: `evaluateOperatingRhythm` now gates `composeAndLogDraft` specifically (not the classification step above it, which 2.7a's own settled off-hours policy doesn't name), consuming a `bankHolidaysCache` constructed once via `createBankHolidaysCache()` at process startup (`start-slack-listener.ts`) and threaded through `HandlerDeps`. Gated by its own fresh `checkCostCapAndAlert` call and accounted via `sonnetCostUsdMicros`, same "every real, billed call site needs this from the start" lesson chunk 3.3's own DA review taught, applied proactively here rather than caught in review. DA review caught 2 Material (both R2-confirmed): a chronology-inverted precedent citation — three sites said "same precedent as chunk 6.5a-i," but 6.5a-i is itself unbuilt and its own text cites chunk 3.3 as _its_ precedent, not the other way around; fixed to cite 3.3 directly. And `docs/REVIEW-PATTERNS.md`'s pre-seeded Core-hours-guard pattern went un-graduated despite this chunk being the exact predicted failure firing for real (the first wired consumer's initial pass omitted the guard) — fixed with a real `Caught:` citation. Plus 2 Low, both deferred rather than fixed: `composeAndLogDraft`'s cost-cap check runs before the cheaper operating-rhythm check (a minor DB-round-trip cost during off-hours, not a correctness issue — reordering would also require pinning wall-clock time into an existing cost-cap-only test that currently doesn't need to); and `handleAmbientChannelMessage` (its own new file this chunk) has no co-located test file, tested only via `handle-inbound-message.test.ts` — a file-organization gap, not a coverage gap, flagged as a follow-up rather than restructured mid-review.
- [x] **3.4a-ii — Reaction listener + outcome paths.** The Slack reaction-event handler (short-name mapping, parent-message state) and the three outcomes: ✅'s ticket-commit write path, 🔁's regeneration loop, 📦's park-to-Backlog transition. A genuine chunk-sequencing conflict surfaced before any code moved, resolved with Alex via `AskUserQuestion`: this chunk's own text describes a real reaction listener reacting to a real posted message, but 3.4a-i shipped shadow/log-only (no real post) and VISION §9 requires 3.4a-iii's situational-appropriateness gate to exist before the first real standing-proactive action — resolved as **build the primitive, no live consumer yet**, same shape as 2.7a/2.7b/3.2's own precedent. Built as `classifyReactionOutcome` (`packages/slack`, a pure reaction-short-name → outcome mapping — ✅/🔁/📦 short-names verified against the real `iamcal/emoji-data` database, not guessed: `white_check_mark`/`repeat`/`package`, all three unambiguous, no aliasing gotcha this time), `rawSlackReactionEventSchema`/`isProcessableReactionEvent`/`normalizeInboundReaction` (`packages/slack`, mirroring `raw-message-event.ts`'s own raw-schema/type-guard/normalize-function shape for the `reaction_added` event type), a new `pending_ticket_drafts` table + repository (`packages/core`'s `create`/`getByMessage`/`resolve`/`updateContent`, the "parent-message state" this chunk's own text names — `resolve`'s atomic CAS mirrors chunk 1.3's `claimTicket` exactly, guarding the ✅/📦 outcomes against a double-fired reaction), and three outcome-path functions plus a dispatching `handleReactionAdded` (`apps/server`'s `reaction-outcome-actions.ts`/`handle-reaction-added.ts`): ✅ commits a real `Brief` ticket, 📦 parks a real `Backlog` ticket (both `projectKey: 'chief-clancy'`, VISION §3.4's settled single-project scope — not an open parameter; `severity: 'Medium'`, a fixed placeholder Alex confirmed via `AskUserQuestion` since VISION §5.4's trust-erosion rule keeps severity assignment off the LLM layer too, same reasoning that already keeps `composeTicketDraft` from producing one), and 🔁 recomposes from the _original_ source message (not the previous draft's own text) via `composeTicketDraft`, with its own fresh `checkCostCapAndAlert` check and `sonnetCostUsdMicros` accounting — the fourth real, billed call site this cascade has needed this treatment for, applied proactively from the start. `handleReactionAdded` also ignores any reaction (including 🔁) on an already-resolved draft, a defense-in-depth check in front of `resolve`'s own CAS. No Socket Mode `reaction_added` listener is registered against a real process — `socket-mode-listener.ts` itself is untouched, though `start-slack-listener.ts` DOES gain this chunk's `ticketStore`/`draftStore` construction and threading into `HandlerDeps`, it just never calls `handleReactionAdded` — and 3.4a-i's own draft composition still doesn't persist a `pending_ticket_drafts` row, so both halves of the real end-to-end loop wait for 3.4a-iii, per that chunk's own text ("wiring it in front of 3.4a-i completes the acceptance-test path"). DA review caught 3 Material (all R2-confirmed): (1) `classifyReactionOutcome`'s lookup was a plain object literal keyed by an external Slack reaction short-name — a custom workspace emoji named `constructor`/`__proto__`/`toString`/`hasOwnProperty` would have resolved to a truthy `Object.prototype` member instead of `undefined`, misdispatching as a real, billed 🔁 redo; fixed by switching to a `Map`, with regression tests for all four names. (2) A stale "untouched" claim about `start-slack-listener.ts` repeated verbatim in this bullet, `handle-reaction-added.ts`'s own comment, and `docs/GLOSSARY.md`'s Reaction-gate-outcomes entry (R2 independently found the third site) — corrected in all three. (3) No self-authored-reaction filter exists anywhere in the pipeline, unlike `raw-message-event.ts`'s sibling `bot_id` filter — once 3.4a-iii's own `reactions.add` call seeds the legend onto the persona's own posted message, that action itself emits a `reaction_added` event this handler would (mis)dispatch against; no `botUserId`-equivalent field exists in `PersonaConfig`/`HandlerDeps` to filter it with. Documented as an explicit known gap 3.4a-iii must close (a forward-reference, not a fix, since building it now would mean guessing at a shape 3.4a-iii's own real wiring might not need) rather than built this chunk — DA's own suggested fix offered this as an acceptable resolution alongside building it early. Plus 2 Low: `updatePendingTicketDraftContent`'s redo write has no `resolvedAt` guard, left as a documented trade-off (a narrow, already-bounded-by-the-cost-cap race with a concurrent commit, not a correctness risk); and a redundant type intersection in `handle-reaction-added.ts`, fixed since it was trivial.
- [x] **3.4a-iii — Minimal situational-appropriateness gate.** Its own testable module, consulted before any auto-post — VISION §9 requires the check before the _first_ standing-proactive action, not at Stage 8; 8.5 extends this gate, it doesn't introduce it. Wiring it in front of 3.4a-i completes the acceptance-test path. Alex confirmed via `AskUserQuestion` on two genuinely open parameters BUILD_PLAN's own text left unspecified: the gate mechanism (an LLM content classifier, not a keyword list or external toggle) and its scope (gates 3.4a-i's own draft-posting only, not 3.4a-ii's reaction-outcome dispatch — a human's own reaction is a response to the bot, not the bot acting unprompted, same reactive/proactive distinction 2.7a's core-hours guard already draws for DM replies). A third question — how much of this chunk's terse text to build in one PR — was also put to Alex rather than guessed or silently split: confirmed doing all of it in one chunk (the gate itself, wiring it into draft composition, flipping 3.4a-i's shadow-mode to a real Slack post, persisting the `pending_ticket_drafts` row on the real posted message, seeding real `reactions.add` calls, the self-authored-reaction filter 3.4a-ii's own DA review flagged as a known gap, and registering the real Socket Mode `reaction_added` listener) — completing both halves of the acceptance-test path 3.4a-i/3.4a-ii separately deferred. Built as `evaluateSituationalAppropriateness` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`, `{appropriate: boolean, reasoning: string}`) — **fails CLOSED** on any gate failure (an API error, not just `appropriate: false`), the opposite of `checkCostCapAndAlert`'s fail-open design, since an uncertain safety signal should block the risky action, same "better to wrongly rest once" reasoning as 2.7a's bank-holiday guard. `postMessage`'s own success result (`packages/slack`) was extended to require `ts` on success — a genuinely necessary, backward-compatible API change (verified its three other call sites — the DM-reply path's `postHaltReply`/`generateAndPost` in `handle-inbound-message.ts`, and `check-cost-cap.ts`'s own cost-alert DM — never read it) needed to key the persisted `pending_ticket_drafts` row and seed reactions against the real posted message's own identity, not the source message's. `addReaction` (`packages/slack`, mirroring `postMessage`'s own wrapper pattern) seeds the 📦/🔁/✅ legend sequentially — recursively, not via a loop or `.reduce()` (the latter is banned outright by `docs/CONVENTIONS.md`'s Code Style section), matching `check-cost-cap.ts`'s own `sendCostAlerts` precedent for sequential-by-design async work over a short list — logging (not aborting) a single failed reaction so the rest still get seeded. The self-authored-reaction filter is built one layer down from `apps/server`'s own dispatcher, in `@moe/slack`: `fetchBotUserId` (a thin `auth.test` wrapper) is called once at process startup, and `handleSocketModeReactionEvent` (mirroring `handle-socket-mode-event.ts`'s own `message`-event shape) compares each `reaction_added` event's `user` against it before ever calling `onReactionAdded` — `apps/server`'s `handleReactionAdded`/`reaction-outcome-actions.ts` never need to know about bot identity at all. `createSocketModeListener` gained a real `reaction_added` registration alongside `message`, and `start-slack-listener.ts` sequences `fetchBotUserId` before constructing the listener (an unrecoverable-class failure here — same as a bad Socket Mode token — exits the process rather than starting with no self-filter).
- [x] **3.4b-i — Mid-band confirming question (ask-side).** Posts the real confirming question and persists its own pending state; the yes/no/silence answer dispatch is 3.4b-ii's own separate chunk. A genuine chunk-scope question surfaced before any code moved, resolved with Alex via `AskUserQuestion`: BUILD_PLAN's own original 3.4b text bundled "ask the question" and "handle the answer" into one bullet, but the answer-side needs its own reaction-listener dispatch restructuring (checking both `pending_ticket_drafts` and a new `pending_confirming_questions` table) — resolved as **split into 3.4b-i/3.4b-ii**, the same ask-side/answer-side split chunk 3.4a-i/3.4a-ii already used for the High-band cascade. A second question, on how 3.4b detects "silence" specifically: the codebase has zero scheduled-job/cron infrastructure today (`docs/decisions/` has no ADR for one) — Alex confirmed via `AskUserQuestion` that silence/timeout detection defers entirely to chunk 3.5's own future sweep; 3.4b-i and 3.4b-ii together only build the reactive yes/no paths. A third question — the confirming question's own wording mechanism — was also put to Alex rather than guessed: confirmed a fixed template string, not an LLM-composed one, since it needs no new billed call site and the wording itself is cheap to change later. Built as a new `pending_confirming_questions` table + migration (`0008_create_pending_confirming_questions.sql`) and repository (`packages/core`'s `intake/` folder, mirroring `pending_ticket_drafts`' workflow-object/CAS shape — not `review_queue`'s append-only shape, since a question genuinely resolves once) and `composeAndPostConfirmingQuestion` (`apps/server`): posts a fixed-template question in-thread on the source message, persists the pending row keyed on the real posted message, and seeds a 👍 (yes, draft it) / 👎 (no) reaction legend — deliberately distinct from the High-band 📦/🔁/✅ legend so 3.4b-ii's own dispatch can tell them apart by reaction name alone, no message-type lookup collision to resolve. 👍/👎 short-names verified against Slack's own published `reaction_added` event reference (its own example payload literally shows `"reaction": "thumbsup"`), not guessed from `iamcal/emoji-data`'s primary/alias field split, which would have given the wrong answer (`+1`/`-1`) here. The cost-cap+operating-rhythm guard and the situational-appropriateness gate 3.4a-i/3.4a-iii built for the High-band draft path were extracted to a shared `standing-proactive-guards.ts` this chunk (`isCostAndRhythmGuardSatisfied`/`isSituationallyAppropriate`, an `actionDescription` param parameterizing only the log-message text) — a genuine 2+-consumer case once the Mid-band question needed the exact same two checks, proven behavior-preserving by all of the High-band path's own pre-existing tests staying green after the extraction — with one incidental, deliberate fix along the way: the situational-appropriateness gate's fail-closed log message had said "draft composition" while its three sibling messages all said "ticket-draft composition," a pre-existing wording inconsistency the unified `actionDescription` param fixed as a side effect (the one test assertion this touched was updated to match). A new `confirmingQuestionStore` DI seam threaded through `HandlerDeps`/`start-slack-listener.ts`, matching `ticketStore`/`draftStore`/`reviewQueueStore`'s existing pattern exactly — wired into the DM reply path's own `HandlerDeps` construction only where the ambient path already lives, same as `reviewQueueStore` before it. Same "build the primitive, no live consumer yet" shape 2.7a/2.7b/3.2/3.4a-ii all used — 3.4b-ii wires the real Socket Mode dispatch reading 👍/👎 and resolving `pending_confirming_questions` rows.
- [x] **3.4b-ii — Mid-band confirming question (answer-side).** The 👍/👎 reaction-event dispatch: 👍 → draft via the 3.4a-i posting flow, posted against the original source message (threaded on it for an ambient question; top-level since BUILD_PLAN 3.7 when the question came from a DM, matching how the question itself was posted); 👎 → log via the 3.4c path. Built as `classifyConfirmingQuestionOutcome` (`packages/slack`, a pure `thumbsup`/`thumbsdown` → `yes`/`no` mapping — `Map`-based, not a plain object literal, same external-string-lookup reasoning `classifyReactionOutcome` already established), and `handleReactionAdded`'s dispatch restructured into two sibling functions sharing one lookup → null-check → resolved-check → outcome-switch shape: the pre-existing `dispatchDraftOutcome` (unchanged behavior, just extracted) over `pending_ticket_drafts`, and a new `dispatchConfirmingQuestionOutcome` over `pending_confirming_questions` — a reaction is classified against both legends in order, ignored without any repository lookup if it matches neither (the 📦/🔁/✅ and 👍/👎 short-names are deliberately disjoint, confirmed at 3.4b-i, so no collision to resolve). `draftFromConfirmingQuestion`'s 👍 outcome (`reaction-outcome-actions.ts`) checks the cost cap _before_ the atomic claim, not after — a halted persona leaves the confirming question unresolved, so a later retry (once the cap resets) still has a real question to act on, rather than silently burning the claim on an attempt that never composed anything — then reuses `postAndPersistDraft` (`handle-ambient-channel-message.ts`) _directly_, not reimplemented: that function's own param types were narrowed from the full `HandlerDeps`/`InboundMessage` to minimal structural types (`DraftPostingDeps`/`DraftSourceMessage` — only `channelId`/`ts`/`text`, since a reaction-outcome context has no `channelType`/`userId` to construct a full `InboundMessage` with) and exported, same "only require what's actually used" reasoning `check-cost-cap.ts`'s own `CostCapDeps` already established, proven behavior-preserving by every pre-existing High-band test staying green through the refactor. `logConfirmingQuestionAsNo`'s 👎 outcome (no billed call, so no cost-cap check) writes a real `review_queue` row with the new `outcomeReason: 'mid-no'` value, carrying the classifier's own `confidence`/`reasoning` through — the same context the Low-band path already provides. New migration `0009_widen_review_queue_outcome_reason.sql`: `ALTER TABLE ... DROP CONSTRAINT ... ADD CONSTRAINT`, replacing chunk 3.4c's single placeholder value (`'mid-no-response'`) with the two distinct `'mid-no'`/`'mid-silence'` values this chunk's own resolved design question settled on — verified directly against the real Postgres instance (not guessed) that no code path had ever written the placeholder, so no data migration was needed, and that the auto-generated constraint name really is `review_queue_outcome_reason_check` before writing the `DROP CONSTRAINT` statement. A new `confirmingQuestionStore`/`reviewQueueStore` pairing added to `ReactionOutcomeDeps` and wired into `onReactionAdded`'s `createReactionHandler` deps in `start-slack-listener.ts` (previously only `onMessage` had `confirmingQuestionStore`). `resolvePendingConfirmingQuestion`'s atomic CAS claim (built at 3.4b-i, unused until this chunk) guards both outcomes against a reaction double-firing, mirroring `pending_ticket_drafts`' own `resolve` precedent. Live-verified fully end-to-end via a manual integration script against real Anthropic + real Slack + real Postgres: a real 👍 dispatch composed and posted a real ticket draft, confirmed via a direct `conversations.replies` API call that the posted draft's own `thread_ts` exactly matches the _original_ seed message's `ts` (not the confirming question's own, never-really-posted message) — proving the "threaded on the original source message" design holds live, not just against mocks; a real 👎 dispatch wrote a real `review_queue` row with `outcome_reason: 'mid-no'` against the widened live constraint.
- [x] **3.4c — Review-queue table + Low-band silent log.** The review-queue table (new table + migration, shared with 3.5) and the Low-band silent write. Natural prerequisite for 3.5. **Built out of BUILD_PLAN's own listed order, before 3.4b**: 3.4b's own text names a dependency on this chunk (its "no or silence" path logs via 3.4c) that didn't exist yet — a genuine chunk-sequencing conflict, same class as 3.4a-ii/3.4a-iii's own, resolved with Alex via `AskUserQuestion` before any code moved. Confirmed building 3.4c first rather than a 3.4b-side placeholder/interim log, since 3.4c is also 3.5's own named prerequisite — building it first unblocks two future chunks instead of one. A second question — whether the schema should anticipate 3.4b's own future write now, or stay Low-band-only until 3.4b actually lands — was also put to Alex rather than guessed: confirmed adding the column now (`outcomeReason: 'low-confidence' | 'mid-no-response'`), since the requirement is already known and is the entire reason this chunk was reordered ahead of 3.4b. Built as `review_queue` (new table + migration `0007_create_review_queue.sql`) and `createReviewQueueEntry` (`packages/core`'s `intake/` folder, mirroring `pending_ticket_drafts`' schema/repository shape) — deliberately **not** the same shape throughout: a review-queue row is a plain append-only log entry, not a workflow object, so unlike `pending_ticket_drafts` it has no `resolvedAt`/claim state and no uniqueness constraint on `(channelId, messageTs)` (two entries for the same message are legitimate — nothing ever looks one up by message). Wired into `handleAmbientChannelMessage`'s existing High/Mid/Low branch (`apps/server`, a new `logToReviewQueue` sibling to `composeAndPostDraft`) — a Low-band score now writes a real row carrying the classifier's own confidence/reasoning through, "log, don't throw" on failure, same as `recordUsageLogged`'s own precedent. A new `reviewQueueStore` DI seam threaded through `HandlerDeps`/`start-slack-listener.ts`, matching `ticketStore`/`draftStore`'s existing pattern exactly. No consumer read the table yet at this point in the build — chunk 3.5 (below) later gave it one. A real pre-existing gap surfaced while writing the repository's own tests: `test-db.ts`'s `resetDatabase` hardcodes its `DROP TABLE` list, and every prior new-table chunk had remembered to add itself to it — `review_queue` was added too, but the pattern itself (an easy-to-forget hardcoded list, not something a fresh migration file or `Database` type edit would ever flag) is worth a future chunk's attention if it recurs a second time.
- [x] **3.5 — Review-queue sweep.** A small on-demand job listing every `review_queue` row since the last run for human eyeballing — the "nothing is silently eaten" backstop, plus the real `'mid-silence'` writer 3.4b-i/3.4b-ii both deferred here. Three genuinely open questions resolved with Alex via `AskUserQuestion` before any code moved: the trigger mechanism (BUILD_PLAN's own "on-demand (or daily)" text left this open — the codebase has zero scheduled-job/cron infrastructure, and chunk 7.2a's own future ceremony scheduler is the real home for that, not this chunk — confirmed **manual CLI script only**, mirroring `packages/core/scripts/migrate.ts`'s own precedent, over building a throwaway daily-timer primitive this chunk would only partially need); the sweep's own audience (confirmed **DM Alex directly**, reusing `MOE_COST_ALERT_SLACK_USER_ID`, the same audience the cost-cap alert ladder already uses — not a new config parameter, and not routed through `costCapConfig`'s own bundle, since this script has no cost-cap concern of its own); and the formatting (confirmed **surface the three-way `outcomeReason` distinction**, not one flat list, so Alex can tell "nobody answered" from "the classifier itself wasn't confident" at a glance). A fourth question surfaced during design, not anticipated by BUILD_PLAN's own text: since `review_queue` deliberately has no resolved/claimed state (chunk 3.4c's own choice), the sweep needs some way to avoid re-reporting the same old rows every run — confirmed **a new `sweep_state` table** tracking a persona-scoped "last swept at" timestamp (migration `0010_create_sweep_state.sql`), over a fixed rolling window from `now`, since the latter has real gaps if the script is skipped or run twice in the same window.

Built as `sweep-state-repository.ts`'s `getSweepState`/`recordSweepCompleted` (`packages/core`'s `intake/` folder, an upsert — one row per persona, no CAS guard needed since this is a manually-run, single-operator script, not a concurrent-caller race), two new query functions (`listReviewQueueEntriesSince` on the existing `review-queue-repository.ts`, `findStaleUnresolvedConfirmingQuestions` on the existing `pending-confirming-questions-repository.ts`), and `apps/server/src/review-queue-sweep.ts`'s `runReviewQueueSweep` — a fully DI-testable orchestration function (real infra construction lives only in the thin `scripts/review-queue-sweep.ts` entrypoint, mirroring `migrate.ts`'s own real-vs-testable split): resolves any confirming question unresolved past a 24-hour threshold as `'mid-silence'` (atomically re-claiming each one via the existing `resolvePendingConfirmingQuestion` CAS, so a real 👍/👎 answer racing the sweep always wins), lists every `review_queue` row since the last sweep, DMs Alex a formatted digest grouped by `outcomeReason` (skipped entirely when there's nothing new), then advances `sweep_state` — but only once _both_ listing and, when there's real content to report, the digest post itself succeed, so a listing failure or a failed Slack post each leave the window untouched rather than silently skipping past rows Alex was never shown (DA review: the posting-failure half was originally missed, since a naive "advance after posting is attempted" would defeat this very backstop's own purpose). `apps/server/package.json` gained a `sweep:review-queue` script (`pnpm build && node scripts/review-queue-sweep.ts`), same shape as `packages/core`'s own `migrate` script. Live-verified against real Anthropic-free infra (real Postgres, real Slack): ran the real script once against an empty DB first — correctly reported nothing and advanced `sweep_state` — then seeded two real `review_queue` rows and one real stale `pending_confirming_questions` row (25 hours old, past the threshold) and ran the script a second time, which correctly resolved the stale question, wrote its `'mid-silence'` row, and grouped all three entries into one digest; confirmed via direct Postgres queries that `sweep_state.lastSweptAt` advanced again and the confirming question's own `resolvedAt` was set.

- [x] **3.6 — Draft-outcome tracking.** Counts High-band drafts by outcome — VISION §5.4's named production metric for the whole intake cascade ("the real production metric to watch is the rate of ignored/rejected drafts, not an academic precision score"), surfaced in the 3.5 sweep post. BUILD_PLAN's own terse three-bucket text (✅-committed / 🔁-redone / ignored) doesn't cleanly map onto the real four-way reaction-gate outcome space (✅ commit / 📦 park / 🔁 redo / no reaction), and 🔁 isn't actually terminal — a redone draft can still later be ✅/📦'd, or sit ignored after redoing. Three genuinely open bucket-definition questions were resolved with Alex via `AskUserQuestion` before any code moved: `'committed'` folds in both ✅ and 📦 (both mean "accepted as real work, a ticket now exists," just different triage status — a draft's own row carries no record of _which_ reaction resolved it, only that one did, matching VISION §5.4's own accepted-vs-ignored framing over a four-way split); `'redone'` means still-open **and** regenerated at least once, distinguishing a draft the human has engaged with from one nobody's touched, with no age qualifier of its own since a redo is itself an explicit human action; and `'ignored'` requires never-redone **and** older than a 24-hour threshold, reusing chunk 3.5's own silence-threshold reasoning so a draft posted moments before the sweep runs isn't miscounted before anyone's had a fair chance to react (a still-open, never-redone draft younger than the threshold falls into none of the three buckets, correctly excluded rather than folded into "ignored"). Separately, the counts are lifetime-cumulative, not windowed to "since last sweep" like `review_queue`'s own entries — VISION §5.4 names the ignored/rejected-draft rate as the metric to watch once real usage data exists, which favors the full accumulated trend over a noisy per-window slice, recomputed fresh each sweep call rather than guessed.

  DA review (fresh context, isolated worktree) found a MATERIAL bug: `pending_ticket_drafts` has always been one shared table for both `composeAndPostDraft`'s High-band auto-drafts and `draftFromConfirmingQuestion`'s Mid-band 👍-confirmed drafts (via the shared `postAndPersistDraft`, since chunk 3.4b-ii), with nothing distinguishing the two — so `getDraftOutcomeCounts` was silently counting both populations together despite every doc in this chunk claiming "High-band," skewing the reported acceptance rate upward (a Mid-band draft has already passed a human-confirmation gate before drafting even happens, so it's a rosier population than a High-band auto-draft). Rather than taking DA's own recommended fix at face value, Alex asked whether it was worth verifying the docs directly rather than trusting the finding's framing — re-reading VISION §5.2 directly (not paraphrased) confirmed "ignored/corrected draft" is textually tied to the High-confidence bullet specifically, not Mid-confidence's own separate confirming-question action, so scoping to High-band only (rather than counting both bands and rewriting the docs to say so) is the change VISION's own text actually supports. Fixed with a new `origin: 'high-band' | 'mid-band-confirmed'` column (migration `0013_add_pending_ticket_drafts_origin.sql`, backfilled `'high-band'` for all pre-migration rows — an accepted, documented historical-data caveat, not a correctness guarantee, given moe's current low-volume scale), threaded through both `postAndPersistDraft` callers and filtered into `getDraftOutcomeCounts`'s existing query. A direct regression test (`draft-outcome-counts.test.ts`) pins a `'mid-band-confirmed'`-origin draft as excluded from every bucket.

  Built as a new `redo_count` column on `pending_ticket_drafts` (migration `0012_add_pending_ticket_drafts_redo_count.sql`, `Generated<number>` with a DB-level `DEFAULT 0` — same shape as `TicketsTable.version`, and deliberately excluded from `pendingTicketDraftSchema`'s own pure domain shape for the identical reason `ticketSchema` excludes `version`/`claimedBy`: a tracking/derivation field, not part of the domain shape a caller round-trips through the app), incremented inside `updatePendingTicketDraftContent`'s own existing UPDATE (`sql`` redo_count + 1 ```, matching `claim.ts`'s own `version + 1`precedent) rather than a separate write path.`getDraftOutcomeCounts` (`packages/core`'s new `intake/draft-outcome-counts.ts`) computes all three buckets in one aggregate query using Postgres `FILTER (WHERE ...)`clauses (Kysely's`filterWhere`), not three round-trips, with a small Zod schema (`z.coerce.number()`) coercing Postgres's own `BIGINT` `COUNT(*)`string results back to real numbers — the same`PersonaCostDailyTable`-precedented coercion reasoning, not a new pattern. `apps/server/src/review-queue-sweep.ts`'s digest gained a `📊 Draft outcomes (all time): N committed, M redone (open), K ignored — R% acceptance rate`line, computed over`committed`/`ignored`only (excluding still-open`redone` drafts from the rate's own denominator, since they haven't reached a real accept-or-reject signal yet) and omitted entirely when there's no terminal data. The counts fetch rides along with an existing digest post (`entries.length > 0`), not an independent trigger — a lifetime-cumulative count is almost never zero once any High-band draft has ever existed, so treating it as its own trigger would defeat the sweep's existing "quiet when nothing new" behavior; a counts-fetch failure logs and degrades gracefully (the review-queue digest itself still posts, just without the draft-outcomes line) rather than blocking the sweep's own real purpose.

- [x] **3.7 — Intake cascade on DMs.** Closes the deferred half of Stage 3's own exit criterion (see its history note above). **Stage 3 complete** — its exit criterion is phrased as a _live_ test, and the live-verification line below records the run that met it. Before this a DM got a conversational reply and never entered the cascade: `createInboundMessageHandler` branched `if (message.channelType !== 'im')` into `handleAmbientChannelMessage` and returned, so a work-shaped DM produced no classification, no band, no draft, no reaction gate. VISION §5.2's Stage 0 is explicit that the pipeline covers "a channel/DM the team already treats as work-relevant", and §5.3 says a DM to a named persona "is already unambiguous", which argues for a _higher_ propensity to draft from a DM, not zero. `isSurfaceInScope` already short-circuited DMs as always-in-scope (3.2), so the Stage 0 half existed — what was missing was routing a DM into Stage 1 at all.

  **The real design question, and why this was not a two-line change:** a DM gets a chat reply, and the cascade posts a draft. Doing both naively means every work-shaped DM gets a conversational answer _and_ a ticket draft in the same thread. **Design settled with Alex, 2026-07-25 (before code):** Stage 0 passes `{kind:'dm'}` (`isSurfaceInScope` already always-passes DMs; the `dm` arm had been dead code until now). Stage 1 classifies **every** DM — no length/keyword pre-filter, since any such filter is exactly the keyword heuristic VISION §5.1 rejects. Band decides the outcome: **High** → ticket draft with the 📦/🔁/✅ legend and no chat reply; **Mid** → confirming question, no chat reply; **Low** → the conversational reply exactly as before, and **no** review-queue row (the queue exists so nothing is "silently eaten", and a DM that got a real answer wasn't eaten — logging every "thanks" would bury the sweep digest in chatter).

  **The governing invariant, and the reason this was not a re-pointed branch: the DM path is _never silent_.** It always posts something — a reply, `FALLBACK_TEXT`, or `HALT_TEXT`. The ambient path is silent by construction: every guard and every failure returns without posting. Routing DMs through it as-is would have imported four silences onto the only surface that works in production — a work-shaped DM outside 08:30–17:00 Mon–Fri (or on a cold boot where the bank-holidays cache fails closed), a failed classify, a fail-closed appropriateness gate, and a cost-cap halt would each have produced **nothing at all**, where every one of them produces a reply. **Settled: the cascade may only ever _add_ to the DM response, never remove it.** High/Mid post a draft or question _instead of_ a reply only when everything succeeds; any guard block, classifier failure or post failure falls through to the normal conversational reply. That makes 3.7 strictly additive and structurally unable to regress the working surface.

  **Where a DM draft lands — the sub-question this entry originally asked and the first settlement paragraph did not answer.** The line above asks "does the draft land in the DM thread or somewhere the team can see it"; only the reply-vs-draft half got answered, and the first implementation quietly resolved the rest by reusing the ambient path's threading (DA review caught this). **Settled with Alex, 2026-07-25: top-level in the DM, not threaded.** Threading would leave the DM showing only the user's own message plus a "1 reply" affordance, hiding both the draft and its 📦/🔁/✅ legend behind a click — on the one surface whose defining property is that it is never silent, and where the reaction gate only works if it is actually seen. An ambient post stays threaded, which is what keeps it attached to its source in a busy channel. Implemented as a required `surface` option on both post primitives, so no call site can inherit the wrong placement by omission, plus a persisted `sourceSurface` column on `pending_confirming_questions` (migration `0018`) — the 👍 outcome composes its draft long after the original `InboundMessage` is gone, and without it a DM-originated Mid→👍 draft would thread onto an older message while the question it answers sat top-level. Backfilled `'channel'`, exactly correct rather than merely convenient: no DM had reached the cascade before this chunk.

  **The Stage 1 classifier's own prompt is knowingly left mismatched.** `classify-message-confidence.ts`'s system prompt opens "You are a fast triage classifier for a shared team Slack channel", which is now wrong for half its inputs. **Settled with Alex: leave it and observe first.** Chunk 3.1's High ≥ 70 / Mid 35–69 / Low < 35 thresholds were calibrated against a live eval run on that exact prompt, so editing the prompt puts the calibration back in play on no evidence. VISION §5.3 argues a DM is the _more_ directed surface, so if the channel framing does suppress DM scores the symptom is specific and observable — work-shaped DMs landing Mid or Low — and 3.5's review-queue sweep is the existing instrument for spotting it. Revisit with real data, not from theory.

  **Guards: the operating-rhythm guard does not apply to a DM-triggered draft.** BUILD_PLAN 2.7a already settled that DM replies are exempt because a DM is reactive engagement rather than Moe acting unprompted, and a draft triggered _by_ Alex's own DM is equally reactive. The situational-appropriateness gate is skipped on the same reasoning (it exists to gate _unprompted_ posting). The **cost cap still applies** — that is about spend, not rest — and a halted persona keeps its visible `HALT_TEXT` rather than going mute.

  **Built as `runDmIntakeCascade` (`apps/server/src/run-dm-intake-cascade.ts`)**, returning a two-state `{handled: true, postedText} | {handled: false}` — deliberately collapsing Low band, cost-cap halt, classifier failure and failed post into one shape, since they all mean the same thing to the caller and one shape is what makes the fall-through impossible to get half-right. **A design choice was made here that this entry's own pre-code notes did not anticipate:** rather than adding an options flag to make `composeAndPostDraft`/`composeAndPostConfirmingQuestion`'s proactive guards optional (as the trap list below suggested), the cascade calls the already-ungated `postAndPersistDraft`/`postAndPersistConfirmingQuestion` primitives **directly** — exactly the precedent `reaction-outcome-actions.ts`'s own `draftFromConfirmingQuestion` set at 3.4b-ii for the identical "reactive, so no proactive guards" situation. That leaves the live ambient path completely untouched and makes it structurally impossible for the DM path to acquire the rhythm guard by accident, rather than relying on a flag being passed correctly at every call site.

  Supporting changes: `classifyMessageForIntake` (`classify-message-for-intake.ts`) extracts the shared cap-check → classify → account-for-usage sequence out of `handleAmbientChannelMessage`'s own private `classifyAmbientMessage` — the same genuine 2+-consumer trigger that produced `standing-proactive-guards.ts` at 3.4b-i, and for a sharper reason here: this is a real, billed Anthropic call, and chunk 3.3's own DA review caught exactly this call shipping uncapped, so two copies is two places for that defect to reappear independently. Both post primitives now return their posted text, which the DM path persists as the assistant's `conversation_turns` row (see the traps below). A new `'high-band-dm'` `DraftOrigin` + migration `0017_widen_pending_ticket_drafts_origin.sql` (DROP/ADD CONSTRAINT, same shape as 0009/0011; the auto-generated name `pending_ticket_drafts_origin_check` was verified against the real database before the DROP was written, not guessed) keeps DM drafts out of `getDraftOutcomeCounts`. Purely additive, with no backfill ambiguity of 0013's kind — no pre-existing row can be a DM draft, since no DM had ever reached the cascade. `generate-and-post-reply.ts` is a pure move of the conversational-reply path out of `handle-inbound-message.ts`, needed only to stay under eslint's `max-lines` once the cascade wiring landed. **Deliberately deferred, not dropped — with a home and a re-entry condition, not just a code comment:** surfacing a _separate_ DM acceptance rate in the 3.5 sweep digest. VISION §5.4 names the ignored/rejected-draft rate as the production metric to watch. The ambient half of that metric is live and unaffected — ambient intake has been live in all three work channels since 3.4a-iii — the pre-5.2a restriction was a human convention about _membership_, not anything in code, and Sarah was always a member — so nothing is being lost by keeping DM drafts out of it. What is missing is any DM-side equivalent. **Re-entry condition: the first real DM draft being ✅/📦'd in production**, at which point there is a population worth reporting and the existing `📊 Draft outcomes` line can gain a second, separately-scoped figure. Note the `origin` axis is only half-applied by design — a Mid-band 👍-confirmed draft carries `'mid-band-confirmed'` on either surface, so a DM-only _Mid_ rate would still need a migration; see `pending-ticket-draft.ts`'s own TSDoc for why that is deliberate rather than an oversight.

  **Known traps recorded before code moved, and how each was closed:** (1) reusing `handleAmbientChannelMessage` directly would pass `{kind:'channel'}` with a `D…` id never in `MOE_WORK_RELEVANT_CHANNEL_IDS`, silently dropping **every** DM — closed by passing `{kind:'dm'}`, pinned by two dedicated Stage 0 tests (one with an _empty_ allow-list, where a `channel` surface cannot possibly pass) rather than relying on the fixture's `C123`/`D123` asymmetry, which would have failed as an ordinary scoping miss and masked the real cause. (2) `handleThreadedMessage` is the only writer of `conversation_turns`, so a High/Mid DM bypassing it leaves a hole in the history the next reply is generated from — closed by routing both outcomes through a shared `persistTurns`, storing the exact posted text as the assistant turn, the same "history should match the real transcript" reasoning `generateAndPost` already applies to `HALT_TEXT`. (3) `getDraftOutcomeCounts` filters `origin: 'high-band'` with no DM/channel axis — closed by the new origin value above. All four load-bearing new behaviours were **mutation-checked**, not just asserted: reverting the surface kind, the origin value, the absent rhythm guard, and the absent review-queue write each fails the tests that claim to pin them.

  One defensive addition beyond the settled design: `runDmIntakeCascadeSafely` wraps the cascade in a try/catch that logs and falls through. **No currently reachable path throws** — every `@moe/core` repository and every `@moe/agents` wrapper catches and returns a `Result` — so this is defence-in-depth of the same kind as the deliberately unreachable `threadKey === undefined` narrowing guard beside it. It earns its place on asymmetry, not likelihood: an escaping throw would reach `socket-mode-listener.ts`'s top-level `.catch`, which logs and stops, and that is silence on the one surface never allowed to be silent. Catching makes the cascade additive **by construction rather than by audit**, so a future call site added inside it cannot quietly reintroduce the silence.

  **Live verification.** Migrations `0017`/`0018` applied to the real Neon project and confirmed by direct query (18 applied; the `origin` CHECK now carries `'high-band-dm'`; `pending_confirming_questions.source_surface` exists). `moe-sarah` redeployed on this code and healthy — health check passing, GitHub credentials validated, Socket Mode connected. **Live-verified end to end, 2026-07-26**, against real Slack + real Anthropic + real Postgres: Alex DM'd Sarah "Hi Sarah 👋 The CLI hangs on large repos, can someone take a look" → classified **85** (High), and a real ticket draft posted with the 📦/🔁/✅ legend and **no** conversational reply beside it. Every design point confirmed independently rather than inferred: the posted message's own `thread_ts` is **absent** (`conversations.history`), proving the top-level placement decision holds live and not just against mocks; the persisted row carries `origin: 'high-band-dm'`, keeping it out of the ambient acceptance-rate population; **both** `conversation_turns` rows were written, the assistant turn holding the exact draft text — the "hole in the history" trap, closed and observed; and **zero** `review_queue` rows were written in the window. A real ✅ reaction then committed a real `Brief` ticket (`5d743b2a`), resolving the draft. Stage 3's exit criterion is met on both halves.

  **Sequencing (corrected 2026-07-25): done before further Stage 5 work.** An earlier draft of this line suggested deferring until after 5.3 in case the answer depended on persona voice — it didn't. Whether a work-shaped DM yields a reply, a draft, or both is a pipeline/UX decision; voice only affects how a draft is phrased. Three reasons it went first: the gating rule above says a stage starts when the previous stage's exit criterion is met, and Stage 3's was not, so continuing into 5.3 would have been a _knowing_ violation rather than the unnoticed one 3.3 created; DMs were the one interaction surface with no intake at all — ambient channel intake has been live since 3.4a-iii (the interim rule was a human convention about who may _join_ a channel; 5.2a itself gates the cascade in code, and gates membership not at all), so this closed the actual gap rather than duplicating working coverage; and it is small next to 5.3's eight prompt sub-chunks, which would otherwise all have been built on a stage that never passed.

- [x] **3.8 — Ignore Slackbot's own notification DMs.** **Merged 2026-07-28** ([PR #78](https://github.com/Pushedskydiver/moe/pull/78)), **deployed 2026-07-28**: `8b0e0e9` (this chunk's own merge commit) is an ancestor of `02c9309` (chunk 5.2b's), so it shipped in that same fleet redeploy — confirmed via `git log 8b0e0e9..02c9309`, not a separate dedicated deploy of its own. All eight personas verified single-Machine/healthy at that redeploy. _Found by chunk 3.7's live verification, not by review — the class of defect Session 22's "live deployment is its own verification class" lesson names._ Sarah holds a DM channel with **Slackbot** (`USLACK`, `D0BLKFEU9UY`, confirmed via `conversations.info`), which Slack uses to deliver workspace notifications. One real example already in `conversation_turns`: `"<@U04UQ6CLZ1U> archived the channel <#C0B5NRDF55Y>"`.

  **Why the existing filters miss it.** `isProcessableMessageEvent` (`packages/slack/src/raw-message-event.ts`) already drops bot-authored (`bot_id`) and subtyped events, which is why this looks like it should already be handled. It isn't: Slackbot delivers these as plain messages carrying **neither** field, so they arrive indistinguishable from a human DM.

  **What it costs, and what 3.7 changed about it.** Before 3.7 each notification produced one billed Sonnet reply that then **failed to post** — `restricted_action_read_only_channel`, observed at `2026-07-25T01:59:36Z`, since the Slackbot DM is read-only. That error had been sitting unexplained in the logs. After 3.7 each one additionally gets a billed Haiku classification, because the cascade classifies every DM by design. The governing invariant is not violated — a failed post falls through to the reply path exactly as intended — but the whole exchange is spend on a channel that can never receive it. Worst case is a workspace-notification burst: N notifications, 2N billed calls, 2N failed posts.

  **Likely fix:** drop messages whose author is `USLACK` at the same Stage 0 point `isSurfaceInScope` already occupies, before any billed call — the same shape as 5.2a's fix (1). Worth checking first whether a real human DM can ever have `user: 'USLACK'` (it cannot, but the check belongs in the chunk, not here) and whether any other Slack-internal author id needs the same treatment. Small; the value is that it removes both a recurring cost and a recurring error-log line that currently reads as a real failure.

  **Built:** an exact-match guard on `message.userId === 'USLACK'`, placed as the first thing `createInboundMessageHandler`'s DM branch does — ahead of both the intake-cascade classify (`runDmIntakeCascade`) and the fallback chat-reply generator (`generateAndPost`), not only inside `runDmIntakeCascade` at `isSurfaceInScope`'s own literal spot. That distinction matters: a guard placed only there would stop the 3.7-added Haiku classify but not the pre-existing Sonnet reply, since `runDmIntakeCascade` returning `handled: false` falls straight through to the reply path — reproducing exactly the pre-3.7 failure this chunk's own cost accounting says it closes.

  **Live verification of this chunk's own open question** (Alex-run, 2026-07-28, direct query against production Neon, not assumed): `SELECT persona_id, role, content, created_at FROM conversation_turns WHERE role = 'user' ORDER BY created_at` returned all **5** user-role turns that have ever existed in the real table. Four are ordinary human DMs (three testing exchanges, one real bug report from chunk 3.7's own live verification); the fifth is exactly the one Slackbot-shaped message this entry already cites, `"<@U04UQ6CLZ1U> archived the channel <#C0B5NRDF55Y>"`, and no other Slack-internal sender appears anywhere in the table. This is what DA review's own Claim-extraction pass flagged as needed before merge — a comment asserting a production-data check needs the check's own evidence on record, not just the assertion, since 3.8's own spec text (above) framed this as an open question for whoever built the chunk, not a settled fact. The fix stays scoped to the one confirmed case on that basis.

- [x] **3.9 — Off-hours ambient intake is dropped, not deferred.** **Merged 2026-07-27** ([PR #77](https://github.com/Pushedskydiver/moe/pull/77)), **deployed 2026-07-28**: migration `0019` applied to production Neon (verified — 19 rows in `schema_migrations`, the `CHECK` constraint carries all six `outcomeReason` values) and all eight personas redeployed on the fixed image, each confirmed single-Machine/healthy with a clean boot sequence. _Found live on 2026-07-27, during chunk 5.2a's canary verification — Alex posted a work-shaped message in `#moe-team` at 01:13 BST and nothing happened._ Sarah classified it correctly (confidence **85**, High band) and then logged `deferring ticket-draft composition — outside core hours`. **The word "deferring" is wrong.** `composeAndPostDraft` simply `return`ed when the guard failed; nothing was persisted, nothing retried, and `apps/server` has no scheduler of any kind. The message was gone. A bug report posted at 18:00 on a Friday was never drafted.

  **One correction to this entry's own original diagnosis, made during the chunk's recon: "leaves no trace" was overstated.** `classifyMessageForIntake` already logs the full message text, confidence and reasoning to the structured log stream, and a billed-cost row is written, both _before_ the guard is consulted. The accurate claim — and the one that matters — is that nothing was recoverable **from the database**, and nothing reached any human-facing surface. A log line nobody greps is not a backstop.

  **This is a broken promise, not just a missing feature.** BUILD_PLAN 2.7a's own settled policy **read** "proactive sends and intake drafts defer to the next core-hours window" — deferral implies pickup. (That line is corrected as part of this chunk, so 2.7a no longer says it; the quote is preserved here because it is what made the promise.) And VISION §5.2's "nothing is silently eaten" is exactly what this violates: a High-band ambient message out of hours is eaten more completely than a Low-band one, which at least writes a `review_queue` row.

  **DMs are already correct and must not be "fixed".** The DM path never consults the rhythm guard (2.7a settled DM replies as reactive; 3.7 extended that to DM-triggered drafts), so a DM produces a draft at any hour on any day. Confirmed empirically, not just by reading: 3.7's own live verification posted a real draft at **Sunday 01:34 BST**. Only the ambient channel path has this gap.

  **Settled with Alex, 2026-07-27 — two steps, in this order.**

  **(1) Now, small: don't lose it. — BUILT.** When the operating-rhythm guard blocks an ambient intake action, write a `review_queue` row carrying the classifier's own confidence/reasoning, so the 3.5 sweep digest surfaces it and Alex can act. Uses only existing infrastructure, keeps the §6.4/§14 rest rules fully intact — writing a DB row is not posting to Slack, and the write path touches `reviewQueueStore` and the logger only, never a Slack client — and closes the silent-loss hole immediately.

  **Four things recon found that this spec had wrong or missing, three of which changed the work.**

  1. **Both bands, not just High.** `composeAndPostConfirmingQuestion` ran the byte-identical guard and dropped the message the same way, and this entry never mentioned it. An ambient Mid-band message out of hours was eaten exactly as completely. **Alex settled covering both, with two distinct `outcomeReason` values** (`'high-band-off-hours'`, `'mid-band-off-hours'`) rather than one shared one: the digest groups by reason so a human can tell causes apart, and "a draft was never composed" and "a question was never asked" call for different responses. Migration `0019_widen_review_queue_outcome_reason_off_hours.sql`.
  2. **The suggested name `'high-band-deferred'` re-told the lie this chunk exists to remove.** Step (2) is unbuilt, so nothing picks these rows up; naming them "deferred" would have baked the false promise into a permanent DB `CHECK` constraint, a Zod enum and a digest section label. The shipped names describe the cause, matching every existing value (`'low-confidence'`, `'mid-no'`, `'mid-silence'`, `'mid-yes-failed'`). The spec said "e.g.", so the name was never locked.
  3. **The write was not expressible as specified.** `isCostAndRhythmGuardSatisfied` returned a bare `boolean` and returned `false` for _both_ a cost-cap halt and an off-hours block, so no caller could tell which had fired. It is now `evaluateCostAndRhythmGuard`, returning a `{satisfied, reason}` decision. `docs/CONVENTIONS.md` prescribes the `evaluate*` **verb** (reserving `is*` for boolean predicates); the flat decision **shape** comes from the `evaluateOperatingRhythm`/`evaluateWipLimit` precedent, not from CONVENTIONS, whose `Result`-union rule is scoped to expected domain failures rather than guard decisions. Two review passes disagreed on that distinction and it was settled by reading CONVENTIONS directly. Separately, `composeAndPostDraft` never received the classifier output at all (the Mid-band path already did), so it took a bundled input object; it was already at eslint's `max-params: 3`.
  4. **The wording fix was six occurrences across four files, not two sites.** Beyond the log line and 2.7a: a _second_ misleading "deferring" in the same guard file (the fail-closed appropriateness branch), `docs/GLOSSARY.md` in two separate entries, and `docs/ARCHITECTURE.md`. A seventh, in `PROGRESS.md`, was found by review after this bullet was first written and is also corrected. Counting precisely matters in the bullet whose whole point is that the first count was too low. The second log line's own branch is a genuine remaining silent loss and is **3.10**, not this chunk — but leaving one "deferring" standing beside a corrected one would read as a considered distinction rather than an oversight.

  **Scope boundary, held deliberately and pinned by tests:** only the operating-rhythm block writes a row. A cost-cap halt does not, and neither does the appropriateness gate — see 3.10. **DA review mutation-tested this claim and found it false as first written:** three of four mutations survived, because the High-band cost-cap test drove an already-over-cap persona, which halts inside `classifyMessageForIntake` before the draft guard is ever entered — so its `not.toHaveBeenCalled()` was vacuously true on a function that never ran. Fixed by crossing the cap **mid-turn** (under at classify, over at the guard) and by pinning both fail-closed appropriateness branches. All four mutations are now killed.

  **One consequence worth naming:** the row is written _before_ the situational-appropriateness gate runs, so the off-hours path is marginally more permissive than the in-hours one. Deliberate — that gate is a billed Haiku call whose purpose is deciding whether posting into a shared channel is appropriate, and nothing is posted here. Paying for it to decide whether to write a private row Alex alone reads would be spend for no protection.

  **(2) Later, once a timer exists: genuinely defer. — NOT BUILT, unchanged.** Auto-compose and post the draft at the start of the next core-hours window, which is what 2.7a promised all along. This needs deferred-work infrastructure the repo deliberately does not have — **re-entry condition: whichever of chunk 7.2a (ceremony scheduler) or 6.1a-i (pull loop) lands first**, since either provides the recurring trigger. Design question to settle then, not now: whether a message deferred over a weekend is still worth drafting on Monday, or whether deferral should expire.

  **Deliberately rejected** (Alex, same conversation): replying in-channel with "out of hours, I'll pick this up tomorrow." That post is itself the persona acting outside the operating-rhythm window, which is precisely what §6.4/§14 exist to prevent (§6.4 owns the core-hours window; §14's own text is narrower than this chunk first implied — it names weekends and UK bank holidays specifically, not weekday evenings, so the 01:13 Monday canary is a §6.4 case) — and it would put noise on every off-hours message in a shared channel. Also rejected: dropping the rhythm guard for ambient drafts entirely (treating them as reactive the way 3.7 treats DM drafts). Coherent, and it would need no new infrastructure, but it reopens a §14 question 2.7a settled and §14 is a `CLAUDE.md` do-not-touch surface.

- [x] **3.10 — The ambient guard chain's two remaining silent losses.** **Merged 2026-07-28** ([PR #80](https://github.com/Pushedskydiver/moe/pull/80)), **migrated and deployed 2026-07-29** — migration `0022` run against production Neon (Alex confirmed via chat, separate authorization from the merge) and independently verified: 22 rows in `schema_migrations`, the `review_queue_outcome_reason_check` constraint carries all 10 values. All eight personas redeployed on one built-once-reused image (`fly deploy -c fly.sarah.toml --build-only --push`, then `--image <tag> --ha=false` per persona, Alex confirmed via chat as a separate authorization from the migration), each independently verified — `fly status` shows exactly one Machine per App (no stray `--ha` standby), `fly checks list` shows `health: passing` with the correct `personaId` in the body for all eight. _Found by chunk 3.9's recon, not in production — the same defect class 3.9 itself fixed, in the two sibling branches 3.9 was deliberately scoped away from (Alex settled the narrower scope on 2026-07-27, to keep that chunk on the guard it was filed for)._ After 3.9, an ambient High- or Mid-band message blocked by the **operating-rhythm** guard leaves a `review_queue` row. Two other exits from the same chain still drop it with nothing persisted:

  1. **A cost-cap halt** (`evaluateCostAndRhythmGuard`'s `'cost-cap-reached'` branch). Note this is the _second_ cap check — an already-over-cap persona short-circuits earlier, inside `classifyMessageForIntake`, before a band is even known, so there is nothing classifier-derived to write there. The guard-level halt is the narrower case where spend crosses the cap _between_ the classify and the compose, which chunk 3.7's own DA review established really can happen inside one turn.
  2. **The situational-appropriateness gate failing CLOSED** (`isSituationallyAppropriate`, on an Anthropic error/timeout/unparseable response — _not_ a genuine `appropriate: false` verdict). This is the sharper of the two: an infrastructure blip, not a judgement, and it permanently loses a message that may well have been a real bug report. A true `appropriate: false` should arguably stay silent, since that is a considered decision that the message should not be acted on — **that distinction is the chunk's main design question**, and it is a real one, because the review queue is Alex-only and never posts, so even an "inappropriate" message arguably belongs in it.

  **Design question settled (Alex, via `AskUserQuestion`, 2026-07-28):** only the infra-blip fail-closed case writes a `review_queue` row. A genuine `appropriate: false` verdict is a considered decision, not silent data loss, and stays silent — logging it too would add queue noise for a settled judgement call, not the genuine ambiguity the queue exists for. `isSituationallyAppropriate` (`standing-proactive-guards.ts`) currently returns a bare `boolean` with two separate `return false` branches that already exist — line ~149 for `!appropriateness.ok` (the infra case), line ~170 for `!appropriateness.appropriate` (the genuine-verdict case) — the caller just can't currently tell which happened. Widening it to a discriminated result (mirroring `evaluateCostAndRhythmGuard`'s own `{satisfied, reason}` shape from 3.9) is what makes the two branches distinguishable at the call site; no new mechanism to invent.

  **Why not folded into 3.9:** each needs its own reasoning about what the row _means_ and whether a row is even wanted, and 3.9 already grew from one band to two. The infrastructure is now all in place — `logAmbientIntakeToReviewQueue` takes an `outcomeReason`, and the guard already reports a distinguishable reason — so this is now purely two small branches plus a migration, the design question above being the only piece that needed Alex before code moves. Both branches are pinned negatively by 3.9's tests — two cap tests (mid-turn on the High band; a direct call to `composeAndPostConfirmingQuestion` on the Mid band, which bypasses `classifyMessageForIntake` and so reaches the guard's own cap branch) and two fail-closed tests, one per band — so this chunk must update those four assertions rather than only adding new ones. Do not trust a green suite as evidence the branch is covered: on 3.9 that exact reasoning was wrong, and only mutation testing exposed it.

  **Built:** both guard exits now write a `review_queue` row (`'{high,mid}-band-cost-cap'`, `'{high,mid}-band-appropriateness-check-failed'`, migration `0022`, 10 `outcomeReason` values total) except the genuine `appropriate: false` verdict, which stays silent per the settled design question. `isSituationallyAppropriate` renamed `evaluateSituationalAppropriatenessGuard` (matching `evaluateCostAndRhythmGuard`'s own 3.9 rename) and, along with `CostAndRhythmGuardDecision`, widened from a flat `{satisfied, reason}` object to a **boolean-discriminated union** — the flat shape from 3.9 didn't actually narrow `reason` inside `if (!guard.satisfied)`, so DA review's own fix replaced the cost-cap branch's conditional write (`!== 'cost-cap-reached'`) with an unconditional write choosing only the label, via a new exhaustive-`switch` helper module (`ambient-guard-outcome-reason.ts`, shared by both bands once both needed the identical mapping) rather than a ternary with no compile-time backstop for a hypothetical third reason.

  **Review saga — three rounds, each catching something the last missed.** DA (fresh context, worktree isolation) found 2 Material + 3 Low: a third, structurally identical silent-loss path one guard step upstream (`classifyMessageForIntake`'s own classification-failure branch, not fixed here — filed as **3.11** below); a stale digest-section count in `docs/GLOSSARY.md` ("six sections" left over after this chunk's own four new values, should read ten); a previously-honest TSDoc caveat about `evaluateOperatingRhythm`'s own sub-reasons losing precision that got silently deleted during the rewrite rather than resolved or re-deferred; and the ternary/equality-check exhaustiveness gap above. All fixed. A fresh R2 pass confirmed all five folds — including compiling isolated `tsc --strict` fixtures against both the old flat shape and the new discriminated union to prove the narrowing/exhaustiveness claims were real, not just asserted — and itself found 2 more Material in the fix commit: a stale cross-reference to a type the fix had just inlined away, and two new exported functions that didn't follow `docs/CONVENTIONS.md`'s verb vocabulary (`evaluate*`, not a bare noun phrase). Both fixed. Self-review then caught two smaller issues in that fix: a comment hedge ("not yet filed as its own chunk") that went stale within the same PR once 3.11 was actually filed, and a digest-ordering comment referencing a "blank line" that didn't yet exist in the object literal it described.

- [x] **3.11 — `classifyMessageForIntake`'s own classification-failure silent loss.** **Merged 2026-07-29** ([PR #81](https://github.com/Pushedskydiver/moe/pull/81)), **migrated and deployed 2026-07-29** — migration `0023` run against production Neon and all eight personas redeployed on one built-once-reused image — this time Alex authorized both steps together in a single message ("Feel free to do the migrate and redeploy"), unlike 3.10's separate migrate/redeploy grants; both still independently verified regardless (schema_migrations row count of 23 plus both CHECK constraint definitions confirmed against production; `fly status`/`fly checks list` confirming single-Machine/healthy with correct `personaId` for all 8 personas). _Found by 3.10's own DA review, not in production — the same defect class 3.9/3.10 both fixed, one guard step further upstream._ `classifyMessageForIntake` (`apps/server/src/classify-message-for-intake.ts`) returns `undefined` on **two** distinct conditions: a cost-cap halt (already correctly out of scope — no classifier output exists yet to write, per 3.10's own reasoning for its sibling short-circuit), and the Stage 1 classifier call itself erroring, timing out, or returning an unparseable response. `handleAmbientChannelMessage` treats both identically — a bare `if (classified === undefined) return;` — with no `review_queue` row for either. The classification-failure case is the same infrastructure-blip shape 3.10 just made durable one guard downstream (the situational-appropriateness gate's own `'evaluation-failed'` branch), with the identical stakes: an Anthropic blip permanently drops a message that may have been a real bug report.

  **Not a trivial reuse of 3.10's own fix.** The existing `review_queue` row shape requires non-nullable `confidence`/`reasoning` fields (`reviewQueueEntrySchema`, `packages/core/src/intake/review-queue-entry.ts`) — a classification failure has neither, so writing a row here needs its own design decision before code moves: a sentinel/placeholder value (e.g. `confidence: 0`, a fixed reasoning string), or a schema change making those fields nullable for this one write path, mirroring `pending_ticket_drafts.source_message_ts`'s own nullable-for-one-legacy-case precedent (BUILD_PLAN 5.2b). Whichever fix ships also needs a new `outcomeReason` value distinct from the four 3.10 already added, since "the classifier itself never ran" is a different cause from "a classified High/Mid message's own downstream guard blocked it" — the digest groups by cause specifically so these read differently to Alex.

  **Design question settled (Alex, via `AskUserQuestion`, 2026-07-29):** `confidence` widened to nullable rather than a sentinel value. A sentinel like `0` would collide with a real low score, and an out-of-range sentinel (e.g. `-1`) would still need the same column widened to accept it — buying nothing over `NULL` while being less self-documenting. `reasoning` stays non-null: the real Anthropic error message is genuine, useful content, not a placeholder. The new `outcomeReason` value, `'classification-failed'`, is deliberately not band-prefixed unlike 3.10's four values — no band was ever determined, since the classifier never produced a score at all.

  **DM path unaffected, same reasoning as 3.9/3.10:** the DM cascade (`run-dm-intake-cascade.ts`) falls through to its normal conversational reply on `undefined`, so a classification failure there is never silent — this gap is ambient-only, like every other one in this guard chain.

  **Built:** `classifyMessageForIntake` now returns a `ClassifyForIntakeResult` discriminated union — `ok: true` with the classification, or `ok: false` with `'cost-cap-reached'` (still nothing to persist) or `'classification-failed'` (carries the real Anthropic error message) — mirroring the boolean-discriminated-union pattern `standing-proactive-guards.ts` established at chunk 3.10. `handleAmbientChannelMessage` writes a `review_queue` row (`'classification-failed'`, `confidence: null`) only for the latter reason, via a new `logClassificationFailure` helper (`log-ambient-intake-to-review-queue.ts`) — not a "2+ consumer" extraction like that file's siblings, but justified as keeping `handle-ambient-channel-message.ts` under eslint's `max-lines` while staying in the one module that already owns every `review_queue`-row-shape decision for the ambient cascade. `run-dm-intake-cascade.ts`'s own change is a type-shape update only (`classified === undefined` → `!classified.ok`) — its `DmIntakeCascadeResult` TSDoc already states every failure reason collapses to the identical fall-through regardless of cause. The confidence/outcomeReason "iff" invariant is enforced at both the Zod layer (`reviewQueueEntrySchema`'s own cross-field `.refine`) and the DB layer (migration `0023`'s `confidence_null_iff_classification_failed` CHECK) — not left as a type-level-only guarantee, mirroring the exact gap chunk 5.2b's own DA review found for `sourceMessageTs`.

  **Review saga.** DA (fresh context, worktree isolation) found 1 Material — `review-queue-sweep.ts`'s digest-ordering rule, rewritten to place the new value, claimed a single test ("a genuine negative judgement, or no judgement at all") that didn't actually explain why the Mid-band values (`'mid-no'`/`'mid-silence'`/`'mid-yes-failed'`) belong in the quieter group — plus 3 Low: missing spaces around several backtick-wrapped identifiers in a `docs/ARCHITECTURE.md` cell; no DB-level mirror of the Zod cross-field refine; a missing explicit test pin that the cost-cap-reached branch stays silent. All fixed — the ordering rule restated as the two-part OR it actually is; the DB CHECK added; the test pin added. The `docs/ARCHITECTURE.md` fix turned out to need more than a typo correction: fixing the missing spaces and re-running `prettier --write` on that row **reproducibly re-corrupted it** once the row crossed some length threshold this chunk's own addition pushed it past (`main`'s own version of the row passes `prettier --check` cleanly; adding even a short, correctly-spaced sentence makes it fail, and `--write` then silently strips spaces elsewhere in the same cell). Reverted that doc's own addition entirely rather than fighting the tool — `docs/GLOSSARY.md` and inline TSDoc already carry the same information. A fresh R2 pass confirmed all 4 fixes (including independently reproducing the Prettier bug itself, not just trusting the claim) and found no new Material. Self-review then caught one stale cross-reference the DA-review fix commit's own rename had left behind.

- [x] **3.12 — The Stage 1 classifier treats a question _about_ work as work needing a ticket.** **Found in production 2026-08-01**, on the very first real DM sent to Sarah after 5.3a-ii's fleet deploy — the first live exercise of her real `prompt.md`. Alex DM'd `is the auth work finished?` intending to test her evidence-before-verdict discipline (VISION §7.6 — "not yet verified" as a comfortable, non-penalized answer). The classifier scored it **75** → High band → auto-drafted a ticket titled _"Auth work status check"_ whose entire body was _"Someone asked whether the auth work is finished. No other detail was given in the message."_ — a ticket tracking the fact that a question was asked. The conversational reply, where Sarah's own judgement would have applied, never ran: the High band _replaces_ it.

  **Root cause is the classifier prompt's own category definition, not the thresholds and not the model.** `CLASSIFIER_SYSTEM_PROMPT` (`packages/agents/src/classify-message-confidence.ts`) defines the work category as "a bug report, a feature request, a task, a genuine facilities/ops issue, **or a question that needs someone to act**". A status question genuinely _is_ a question that needs someone to act — by answering it — so Haiku applied the instruction correctly; its own logged reasoning says so almost verbatim ("a direct question asking for a status update... indicating someone needs to provide information or take action"). That clause plainly exists to catch requests phrased as questions ("can someone look at the deploy?"), but nothing in it separates those from questions asking _about_ work someone else owns.

  **Not a 5.3a-ii regression** — the classifier is chunk 3.3 code, untouched by that chunk; 5.3a-ii's own wiring is confirmed working in the same transcript (Sarah's Low-band replies are unmistakably `prompt.md`'s voice: "I'm Sarah, moe's PM. I'm the front door for the team..."). This is a pre-existing defect that only live traffic could surface, and it surfaced within minutes of anyone actually talking to her.

  **Scope note — this is a prompt-semantics fix that needs real re-validation, not a one-line string edit.** `docs/decisions/STAGE-1-CLASSIFIER.md` calibrated the 70/35 thresholds against a real 24-message eval whose work-signal cluster scored ≥72 and whose non-actionable cluster scored ≤35, leaving a deliberately empty 36–71 band. Narrowing the work category shifts where status questions land, so the eval has to be re-run — and **extended**, since it evidently contained no status-question cases at all. Also worth noting for that ADR: its own "Triggers for re-evaluation" anticipate _threshold_ miscalibration ("revisit the specific threshold values, not the scoring method itself") and real-traffic Mid-band validation — neither covers a wrong category _definition_ producing a confidently correct score. The ADR needs an addendum recording that gap alongside whatever fix lands.

  **Design question, settled with Alex before code moved:** a pure status question about already-known/tracked work routes to **Low band** (the ordinary conversational reply) — a confirming question (Mid) would be a non-sequitur with no ticket to confirm, and Low is where evidence-before-verdict and the `report_status` tool already live.

  **Built:** narrowed `CLASSIFIER_SYSTEM_PROMPT`'s category clause from "a question that needs someone to act" to specifically "asking whether an already-identified piece of work is finished, done, or has happened yet," with an explicit carve-out preserving existence/resource-lookup questions ("do we have a billing docs page?") — those stay unaffected because Low has no ticket-creation tool (only `report_status`), so collapsing them there would silently delete their only path to a tracked ticket. The exact wording went through two live-evaluation rounds against the real Claude Haiku 4.5 API, not just reasoning about the prompt text: a first, broader attempt fixed every status-question case but also pulled the billing-docs example from Mid to Low, an unintended regression caught only by actually running the eval. Full score tables, both rounds, and a new ADR trigger ("a category definition can produce a confidently correct score for the wrong reason") are recorded in `docs/decisions/STAGE-1-CLASSIFIER.md`'s new addendum. Thresholds unchanged (High ≥ 70, Mid 35–69, Low < 35 still hold) — this was a category-definition fix, not a recalibration.

  **Review saga.** Six rounds of DA review (R1→R6), each round independently re-verifying the prior round's fixes and re-reading the full diff fresh rather than only checking the prescribed items — real issues surfaced at every layer: an overclaiming code comment ("unaffected" when several rows had moved), a misattributed ADR citation (Decision 3 → should be Decision 2, then a second miscitation found on the same sweep, Decision 1 → should be BUILD_PLAN chunk 3.1), a missing regression test for a prompt-text bug fix (the Prove-It Pattern), a same-document numeric contradiction (Decision 2's original 72–85 range vs. the addendum's own 60, unreconciled), a silently-dropped negative-category clause ("commentary that doesn't need any action") with zero test coverage, and a `toContain` assertion that read as a pin but wouldn't actually have caught its own named regression (the target phrase occurred twice in the prompt; the assertion needed to check both occurrences' absence independently). Converged with no Critical/Medium+ findings remaining. `copilot-surrogate` was dispatched only after Alex asked directly whether it had run — a real process miss, not a judgment call, since the diff had crossed its mandatory 50-LOC/factual-claim threshold from its first commit. Dispatched retroactively, fresh context, all three files read in full at HEAD: found 2 Material (the same misquote of the real incident's ticket body — wrong capitalization and a dropped second sentence — recurring in both the code comment and the test file, and by then also in the PR's own first commit message) + 1 Low (a pseudo-quote blending two different phrasings), all fixed and posted as a PR comment. Merged as [PR #87](https://github.com/Pushedskydiver/moe/pull/87) — prompt-text only, no migration. **Deployed 2026-08-02** — built once (`registry.fly.io/moe-sarah:deployment-01KZ27G0XR93Y6PDE1GT77FVVG`) and reused across all eight Apps via `fly deploy -c fly.<persona>.toml --image <tag> --ha=false`, mirroring 5.3a-ii's own precedent. Every persona independently verified: single Machine, `1 total, 1 passing`, correct image tag, correct `personaId` in its own `/health` body; Sarah's logs confirm the clean boot sequence (`server started` → `github app credentials validated` → `slack socket mode connected`).

## Stage 4 — GitHub + the board

**Exit criterion:** a ticket created from chat exists as a tracked item against chief-clancy, and an externally-opened GitHub issue appears in the triage queue and can be linked to a ticket. Persona-driven conversion of a triage-queue entry into a board ticket is deliberately not a Stage-4 capability — it's Sarah's Brief-stage triage behaviour and lands at 6.1b, where the pull loop lives.

- [x] **4.1 — GitHub App auth + client.** App credentials, installation token flow, boot-time key-validation guard (the v2 outage lesson — fail loudly at startup on a bad key, naming the fix). Read-only endpoints first.
- [x] **4.2 — Issue discovery (poll).** Poll chief-clancy's open issues into a triage queue table. No webhook dependency (optional fast-path later).
- [x] **4.3 — [GATE] Board home + capacity-model decision.** **Decided (2026-07-22):** moe's own DB stays canonical (GitHub issues are the external mirror); two classes of service (Standard, Expedite); a new `classOfService` ticket-schema field (named as chunk 4.5's own follow-up migration, not built here); small starting WIP limits (Brief 3, Plan 2, Build 2, Review 2; Backlog/Done uncapped). Full reasoning and rejected alternatives: `docs/decisions/BOARD-AND-CAPACITY-MODEL.md`.
- [x] **4.4a — External-post attribution (AI transparency).** Every externally-visible artifact moe writes — GitHub issues now, comments/PRs/reviews at Stage 6 — carries VISION §6.7's three layers: bot identity, persona attribution in the message body, and a footer escape-hatch to Alex. §6.7 is non-negotiable for external humans, so this lands **before the first GitHub write** — the write path never exists without it. **Built:** the platform-layer bot identity is the single shared GitHub App from chunk 4.1 (confirmed via `AskUserQuestion`, no per-persona GitHub identity work); `composeExternalPostBody` (`packages/github`) appends a deterministic persona-attribution line + a `@Pushedskydiver` footer escape-hatch to any post body, over a new shared `PERSONA_ROSTER`/`personaIdSchema` (`packages/core`, also now `packages/agents`' own `PersonaId` source). No live consumer yet — 4.4b is the first real call site.
- [x] **4.4b — Outbound ticket → issue creation/link.** Create/link a GitHub issue for a chat-born ticket, per the 4.3 decision. This is the plan's first GitHub _write_: the App-permission/token-scope change from 4.1's read-only posture lands here, with its failure modes, not silently. **Built:** Alex widened the real App's Issues permission to Read & write (`AskUserQuestion`-confirmed sequencing — code built against a mocked client first, permission widened in parallel, then live-verified). A manually-triggered CLI script (`create:github-issues`, matching 3.5/4.2's own no-scheduled-job-infra precedent) scans for tickets with no linked issue and creates one for each via a new `createGithubIssue` (`packages/github`). The ticket↔issue link lives in a new `ticket_github_issue_links` table (`packages/core`, migration `0015`) — a separate table, not columns on `tickets` (keeps VISION's "GitHub issues are an external mirror" framing out of the pure domain schema). A claim-first idempotency guard (`AskUserQuestion`-confirmed over a best-effort-log-only alternative) inserts a `pending` row before ever calling GitHub, so a crash between a successful creation and the follow-up DB write can't trigger a duplicate issue on retry — a still-`pending` row from a prior crashed run is surfaced for manual reconciliation, never auto-retried. Wires chunk 4.4a's `composeExternalPostBody` into every created issue's body. Live-verified end to end against the real App and real repo: a real issue (`Pushedskydiver/chief-clancy#489`) was created and correctly linked, carrying the real AI-transparency attribution/footer.
- [x] **4.4c — Reconciliation poll.** Reflect external issue closure as `Cancelled` — an idempotent background job with its own state mapping. Edge cases decided here: reopened issues, closure of an issue whose ticket is mid-Build. **Built:** three open points resolved with Alex via `AskUserQuestion` before any code moved — any closure maps to `Cancelled` regardless of GitHub's `state_reason` (nothing in moe closes issues via its own workflow today, so every closure this poll sees is genuinely external); a reopened issue leaves an already-`Cancelled` ticket cancelled (VISION §3.1 calls it "a non-flow terminal state," never auto-reversed, only logged for manual re-triage); and closure still cancels a ticket already in `Build`/`Review`, logged at `warn` rather than `info` so it's noticed. A `Done` ticket is skipped outright (no GitHub call at all) as a safeguard against a future closes-on-merge feature. Built as `getGithubIssueState` (`packages/github`, a per-issue `issues.get` lookup rather than `listOpenIssues`'s own bulk `paginate`), `listResolvedTicketGithubIssueLinks` (`packages/core`), and `reconcileGithubIssues` (`apps/server`, manually triggered — `pnpm --filter @moe/server reconcile:github-issues`, same no-scheduled-job-infra precedent as every other Stage 4 poll). Live-verified read-only against the real App/repo (a throwaway ticket linked to the real, still-open `chief-clancy#489` correctly stayed unchanged) — the closure→`Cancelled` path itself is thoroughly unit-tested but not live-verified against a real closed issue, since deliberately closing #489 would conflict with its own documented purpose as a live-verification artifact from chunk 4.4b.
- [x] **4.5 — WIP limits + classes of service enforcement.** Enforce the numbers decided at the 4.3 gate at pull time. Starts with the `classOfService` ticket-schema migration the 4.3 gate named but didn't build (`docs/decisions/BOARD-AND-CAPACITY-MODEL.md`), then small config + one guard function — its call site is the 6.1a-ii transition wiring, which wires it in. **Built:** the `classOfService` migration (`packages/core/migrations/0016_add_ticket_class_of_service.sql`, `ALTER TABLE tickets ADD COLUMN ... DEFAULT 'Standard' CHECK (... IN ('Standard', 'Expedite'))`) follows migration `0013`'s `origin`-column precedent exactly — the DB `DEFAULT` exists solely to backfill pre-existing rows; every new insert supplies the value explicitly (`NewTicket`'s Pick widened, `reaction-outcome-actions.ts`'s `commitAsTicket` gets a new `DEFAULT_CLASS_OF_SERVICE` constant alongside `DEFAULT_SEVERITY`), confirmed by tracing `createTicket`'s pre-insert `ticketSchema` validation rather than assuming the DB default alone would suffice. `evaluateWipLimit` (`packages/core/src/capacity/`, pure/sync like `classifyRiskTier`, not async like `evaluateOperatingRhythm` — nothing before 6.1a-i exists yet to produce a live count) takes an already-known per-status count against `DEFAULT_WIP_LIMITS` (Brief 3, Plan 2, Build 2, Review 2; Backlog/Done/Cancelled uncapped) and returns a decision+reason (`'under-limit' | 'at-limit' | 'uncapped-status'`), mirroring `evaluateOperatingRhythm`'s shape per `docs/CONVENTIONS.md`'s `evaluate*` verb convention. **No live call site yet** — 6.1a-ii wires it in, same shape chunk 4.4a's `composeExternalPostBody` had before 4.4b. Scoped to the WIP cap only, not Expedite's queue-_ordering_ behavior — see 6.1a-i's own updated entry below for that gap.
- [x] **4.6 — DB backup + tested restore.** Neon's own point-in-time-recovery/branching story (or logical `pg_dump` backups, per the 1.2a ADR's shape) for the production DB, plus a rehearsed restore path — 1.2b ships migrations with no rollback story, and by Stage 6 this data is load-bearing (track record, review queue, claims), so durability lands before then, not after the first loss. Extends to the `/data` memory files when Stage 8 adds them. **Decided (2026-07-24, `AskUserQuestion`):** the architectural pass found no production Neon project had ever actually been provisioned (`fly deploy` has never been run for real) — Alex created one for this chunk specifically, then confirmed live verification over a docs-only build, and both restore paths (PITR + a `pg_dump` fallback) rather than PITR alone, given Neon's own docs recommend the pairing and its status page shows a real recurring pattern of branch/compute-reliability incidents through 2026. **Built:** `packages/core/src/backup/` — pure, tested command-builders (`buildPgDumpCommand`, `buildPgRestoreCommand`, `buildDockerRunCommand`, `generateBackupFileName`) wrapping `pg_dump`/`pg_restore` inside a throwaway `postgres:18-alpine` container (version-matched to the real Neon project's own Postgres 18, confirmed via `neonctl projects list` — a different pin from the `postgres:17-alpine` image CI/local dev use as a Neon stand-in, and a real gap the live rehearsal itself caught before it shipped: local smoke-testing had used 17 until the real project's actual version was checked). The connection string is split into discrete `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE`/`PGSSLMODE` values (`parsePgEnvFromConnectionString`) written to an `--env-file` — libpq's own documented env-var mechanism, so no process (not `docker`, not `pg_dump`/`pg_restore` themselves) ever receives the secret via a command-line argument; `pg_restore` alone still needs an explicit `--dbname="$PGDATABASE"` (a name, not a credential), since unlike `pg_dump` it refuses to run without one. Thin CLI scripts `packages/core/scripts/backup.ts`/`restore.ts` mirror `migrate.ts`'s own shape (`pnpm --filter @moe/core run backup`/`run restore`); `restore.ts`'s destructive `pg_restore --clean --if-exists` is gated by two checks, not one — an operator-supplied `BACKUP_FILE_PATH` is validated against a shell-safe character allowlist (`isShellSafeFileName`) before it reaches the container's restore command, and the confirmation phrase is the target's own redacted connection string (`redactConnectionStringForDisplay`, credentials stripped), not a static phrase — printed by the script itself so the operator sees what they're about to destroy, and a confirmation copied for a different database won't match. Documented in a new `docs/OPERATIONS.md` (added to `CLAUDE.md`'s Key docs). **Review history — four rounds, each catching something the last missed, narrated in full rather than only the final state** (this same paragraph overstated confirmation prematurely twice already; see git history for the uncondensed account). **R1 (DA):** 1 Critical (an unsanitized `BACKUP_FILE_PATH` reached a shell-embedded command string — fixed via `isShellSafeFileName`), 2 Material (the original `--dbname="$CONN"` design put the plaintext connection string in `pg_dump`/`pg_restore`'s own argv, visible via `docker top` despite never appearing in `docker`'s own argv — fixed via the discrete `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE`/`PGSSLMODE` redesign; and the original static confirmation phrase never showed the operator what it would destroy and wasn't bound to the specific target — fixed via a redacted-connection-string confirmation, `redactConnectionStringForDisplay`), 2 Low (no cleanup on an operator Ctrl-C/kill mid-run — fixed via `SIGINT`/`SIGTERM` handlers plus `try/finally`; a host path containing `:` could confuse Docker's own volume-spec parsing — left as a genuinely undocumented edge case at the time). **R2** found the confirmation-gate Material fix only partial (a malformed `DATABASE_URL` crashed both scripts with a raw uncaught `TypeError`, since neither `redactConnectionStringForDisplay` nor `parsePgEnvFromConnectionString` guarded its own `new URL(...)` call) and a new Material of its own (this paragraph had asserted "R2-confirmed" in the same commit that made the fixes, before an independent pass had actually run — `docs/RATIONALIZATIONS.md`'s named self-verification-isn't-verification anti-pattern). Fixed via an `isValidConnectionString` boundary check. **A third independent pass found that fix itself only partial, plus one more new Material:** `isValidConnectionString` proved the value was URL-shaped but not that its percent-encoded segments were decodable — `parsePgEnvFromConnectionString`'s own `decodeURIComponent` calls remained a separate unguarded throw site, reachable in `restore.ts` even after the destructive-action confirmation had already been shown; and separately, a decoded field containing `%0A` could inject an arbitrary extra line into the generated `--env-file` (the same injection class as R1's Critical, at a different boundary `formatEnvFileContents` never guarded). Fixed by consolidating validation into one `Result`-returning `parsePgEnvFromConnectionString` (superseding and replacing `isValidConnectionString` entirely) that rejects unparseable URLs, undecodable percent-encoding, and any decoded field containing `\r`/`\n`/`\0`, called once in each script immediately after reading `DATABASE_URL` and before any other step; `formatEnvFileContents` also gained its own defensive check against the same control characters, since it's the function actually writing credentials to disk. The previously-undocumented colon-in-host-path Low finding is now genuinely documented in `docs/OPERATIONS.md`'s "Known limitation" note (this paragraph's own prior claim that it already was documented was itself false — corrected here, not repeated). **A fourth, tightly-scoped independent pass** (walk every `return` in `parsePgEnvFromConnectionString`, confirm zero `throw` statements, run 5 adversarial inputs against the actual compiled `dist/` output rather than reasoning about them) found the consolidated parser genuinely clean — no exceptions on any tested input, both scripts confirmed to call it exactly once before anything else — with exactly one further Low: `url.hostname` is deliberately never run through `tryDecode` (unlike the other four fields), which is _why_ a percent-encoded control byte in the host segment stays literal text rather than decoding to a real `\r`/`\n`, but that safety argument was implicit and untested. Fixed with a regression test pinning the exact behavior (`postgres://user:pass@localhost%0A:5432/db` → `PGHOST: 'localhost%0A'`) and a comment on the function's own hostname line warning against "fixing" the asymmetry without re-deriving why it's there — test/comment only, no parsing-behavior change for a real connection string, so this round didn't need its own rehearsal. Both live rehearsals were re-run against the real Neon project after each of the first three fix rounds, since each of those actually changed the parsing mechanism the rehearsal exercises. **Live-verified against the real Neon project** ("Moe AI Team", `plain-wildflower-25588697`) — not just read from Neon's docs: migrations applied for real; a PITR rehearsal (`neonctl branches restore`) inserted a marker row, restored the branch to a pre-marker timestamp, and confirmed the marker was gone while `schema_migrations` still showed all 16 rows (proving the rewind, not just that the command returned); a `pg_dump`/`pg_restore` rehearsal (repeated after each fix round) dumped the real project and restored it into an unrelated local Postgres database, confirming full schema + a marker row survived the round-trip each time. No automation/scheduling built — matches every other ops script in this repo (manually-triggered); a documented production migration process remains a separate, still-open gap, out of scope here. — **Stage 4 complete.**

## Stage 5 — The full cast

**Exit criterion:** all 8 personas from the cast redline (2.1, completed at 5.0) run as separate processes with separate Slack apps; each has a distinct, Alex-approved voice; a DM to any of them gets an in-character response — a conversational reply, or, when the 3.7 cascade classifies the DM as High/Mid band, a ticket draft or confirming question in its place.

- [x] **5.0 — [GATE] Cast redline completion.** **Decided (2026-07-24):** activate the 8th role, Designer (Maya) — the roster is now 8. Not driven by chief-clancy's own UI surface (its new `packages/design` package was investigated and found real but immature and dev-tool-facing, not the deciding factor) — Alex's own explicit steer was to activate proactively, ahead of a second, real end-user-UI project he plans to bring into moe's scope later, rather than defer until then; checked fresh for evidence against activating now (none found), so the default-yes Alex asked for stands. Also settled in the same conversation: chunk 5.3 adopts a per-persona deep-research + adversarial-grill methodology (extending the existing `spec-grill` discipline) for drafting each prompt. Designer's own research pass (surveying Impeccable, Claude Design, and the broader AI-design-tool landscape) is banked ahead of that chunk at `.claude/research/designer-persona-landscape/landscape-survey.md`. Full reasoning: `docs/decisions/CAST-ROSTER.md`'s Status update.
- [x] **5.1 — Multi-app credential config + provisioning script.** Persona-keyed manifest-provisioning schema, plus a Slack **App Manifest API** provisioning script: one manifest template looped over the persona configs — `apps.manifest.validate` → `create`, capturing `signing_secret`/`client_id`/`oauth_authorize_url` from the response, with the manifest itself kept in-repo as config-as-code. **First prerequisite found and fixed**: chunk 5.0 was decision-only (docs), but the actual roster _code_ (`packages/core/src/persona-roster.ts`) still only listed 7 personas — `personaIdSchema`/`PERSONA_ROSTER` never got `maya` added, and two existing tests actively asserted `'maya'` should be _rejected_. Fixed as this chunk's own first slice, matching the recurring "prompt-edit needs a `pnpm build`" gotcha (memory's own forward-looking note, now a confirmed live instance: `@moe/core`'s stale `dist/` initially made the new manifest tests fail with `Cannot read properties of undefined`, until rebuilt).
      **Live-verified against Slack's current API docs** (2026-07-24, no banked research existed for BUILD_PLAN's own original "verified 2026-07-04" citation, so this chunk re-verified live rather than trusting it): `apps.manifest.create`/`update` are Tier 1 (~1/min, confirming the original citation), `validate`/`export` are Tier 3 (~50/min); the response nests `signing_secret`/`client_id` inside a `credentials` object, not top-level; no bot token or `xapp-` app-level token is ever returned — both remain Alex's own manual tail (OAuth install, then a UI-only token-generation step with no API path), unchanged from the original chunk text. The app-configuration token itself (distinct from a bot/app token, required for every `apps.manifest.*` call) is manually generated at api.slack.com/apps, expires in 12 hours — ample for a one-shot ~7-minute run.
      **Built:** `buildPersonaSlackManifest` (`packages/slack`) — the shared manifest template, its OAuth-scope/event-subscription list reconstructed from every real Slack API call in the codebase plus config-only-reasoned scopes with no direct call site (`reactions:read` for the `reaction_added` subscription, `channels:read` alongside `channels:history`); only `users.profile:read` has a documented live-discovery trail (a `missing_scope` error on Sarah's real app, chunk 2.7b) — the rest rest on that reasoning alone, not a similarly recorded discovery. Its own TSDoc flags this list as unverified against Sarah's real exported manifest and recommends an `apps.manifest.export` cross-check as a hard gate before actually provisioning the other 7 apps. `createManifestClient` (config-token-authenticated `WebClient`, mirroring `create-slack-clients.ts`'s single-builder precedent) and `provisionPersonaSlackApp` (`apps.manifest.validate` → `create`, Result-shaped like `post-message.ts`/`add-reaction.ts`) round out `packages/slack`, alongside `parseProvisionPersonaIds` (see the retry-safety fix below). `provisionSlackApps` (`apps/server`) orchestrates the batch: waits the Tier-1 rate limit between `create` calls (skipped when the previous persona never reached `create` at all), logs-and-continues past a single persona's failure, prints each captured credential set to stdout for manual copy into `.env.local`/Fly secrets (Alex confirmed via `AskUserQuestion`, over auto-writing a new gitignored per-persona file — no new secrets-at-rest surface). The thin CLI (`scripts/provision-slack-apps.ts`, `pnpm --filter @moe/server provision:slack-apps`) defaults to every roster member except Sarah (already live), overridable via `MOE_SLACK_PROVISION_PERSONA_IDS` for a safe, narrower retry after a partial-batch failure. Two design forks resolved with Alex via `AskUserQuestion` before code moved: the new schema is additive (a separate multi-app type, not a reshape of the existing single-active-persona `PersonaConfig`/`parsePersonaConfig`, which stays exactly as chunk 2.2 built it); and captured credentials print to stdout rather than auto-writing to disk.
      **No live execution against Slack yet** — every piece is built and fully tested against a mocked manifest client; actually creating the 7 real apps needs Alex to generate the app-configuration token first, a deliberate later step, not this chunk's own scope.
      **DA review found 3 MATERIAL** (all R2-confirmed): a false retry-safety claim — re-running the default persona list after a partial-batch failure would create duplicate real Slack apps, since `apps.manifest.create` has no natural-key dedup; fixed via `MOE_SLACK_PROVISION_PERSONA_IDS` (a new `parseProvisionPersonaIds`) rather than an automatic guarantee the script can't actually make. A single try/catch wrapping both `validate` and `create` mislabeled every thrown error as `creation-failed`, even from the cheap `validate` stage — misleading about whether the scarce Tier-1 budget was actually spent; fixed by extracting `validate` into its own try/catch (`runValidation`), with a regression test for "validate itself throws." The new CLI script wired `secretKeys: []` despite handling a real credential (the config token) — `docs/CONVENTIONS.md`'s own external-API-integration rule names this exact gap as already having bitten the codebase twice; fixed with a `SECRET_KEYS` list matching every sibling script's own precedent. Plus 2 Low (both fixed): `docs/ARCHITECTURE.md`'s `apps/server` row hadn't been updated alongside its `packages/slack` row; the orchestration function waited the full rate-limit delay even after a persona failed at the cheap `validate` stage, never reaching `create` at all.
- [x] **5.2 — N-process deployment.** The mechanical Fly config per the 1.2a ADR, shared DB access as decided there, per-persona health checks. Genuinely mechanical: the DB engine was chosen and built at 1.2b against the real topology, and 1.3's atomic-claim tests already ran against it — no engine work hides here.
      **The Fly App count turned out to be forced, not chosen** (live-verified against fly.io's own docs, 2026-07-25): Fly secrets are App-scoped — "an app's secrets are available as environment variables at runtime on every Machine belonging to that Fly App" — and `fly secrets set` has no process-group flag; Fly's multi-process guide states it plainly, though as a convenience of the single-app shape rather than as a limitation — "secrets are shared across all process groups in a single app" (fly.io/docs/**app-guides/multiple-processes**/; the similarly-named fly.io/docs/launch/processes/ is a different page and does not contain that sentence, which is how DA review came to report the quote as fabricated — it was verbatim-accurate, on the page actually cited, and both were re-checked against the raw rendered HTML rather than a summarizer). Since every persona process reads the same _unsuffixed_ `MOE_SLACK_BOT_TOKEN`/`MOE_SLACK_SIGNING_SECRET`/`MOE_SLACK_APP_TOKEN` names, one App with eight process groups could only work by giving those variables persona-suffixed names — reversing 5.1's own explicit decision that `PersonaConfig`/`parsePersonaConfig` stays exactly as 2.2 built it. So: **eight Fly Apps, `moe-sarah` … `moe-maya`**. The 1.2a ADR settles the _machine_ count, not the App count, and is satisfied either way; `fly.toml`'s original header comment had already assumed this shape without any mechanism behind it.
      **Built:** `buildFlyAppConfig` (`packages/core/src/deploy/`) — a pure builder returning `{ appName, fileName, toml }` per persona, with its shared settings in `fly-deploy-constants.ts` (same split as `backup/`'s own `backup-constants.ts`). `packages/core/scripts/generate-fly-configs.ts` (`pnpm --filter @moe/core generate:fly-configs`) writes the eight repo-root `fly.<persona>.toml` files, mirroring `backup.ts`/`migrate.ts`'s thin-CLI-over-built-`dist/` shape; a new `fly-configs-freshness` CI job regenerates and diffs them. The generator also **deletes** any root `fly.<name>.toml` that isn't a current persona's before writing — it owns that whole filename namespace, untracked files included, so nothing else may hand-author one. That sweep is what makes the gate's roster-_shrinkage_ coverage real: a write-only generator would leave a retired persona's config untouched, so `git add -A` would find nothing to stage and the gate would pass on genuine drift. It stages before diffing (`git add -A -- 'fly.*.toml'` then `git diff --cached --exit-code`) rather than using `agents-md-freshness`'s bare `git diff --exit-code`, because a ninth persona would emit a brand-new _untracked_ file that a plain `git diff` cannot see — both the drift and new-file cases were verified to actually fail the gate, not assumed to. The single-app root `fly.toml` is deleted.
      **Four decisions Alex made via `AskUserQuestion`** before code moved: uniform `moe-<personaId>` naming (free now, not later — verified against the real Fly account on 2026-07-25: **no App named `moe` has ever existed**, so chunk 2.2's `app = "moe"` pointed at nothing. The one App that does exist, `moe-ai-team`, is the shelved pre-rebuild iteration's — suspended, last deployed 2026-06-23, already in `lhr` — and is deliberately _not_ repurposed, since a name outside the `moe-<personaId>` scheme reintroduces the per-persona special case the uniform naming exists to avoid); a tested generator + CI gate over eight hand-written files (verified first: `fly.toml` sits outside _every_ quality gate — Prettier infers no parser for TOML, and ESLint/knip/Vitest all scope to `src/`); `primary_region` `iad` → **`lhr`**, matching the real Neon project's own `eu-west-2` region and the team's Europe/London core hours, which 2.2's original pin never justified; and scope-of-done as config **plus a live Sarah deploy**.
      **Two config-shape changes beyond a straight port of 2.2's file.** `[http_service]` is dropped for a top-level `[checks]` block: a persona reaches Slack over Socket Mode, an outbound WebSocket, so nothing needs to reach it from the public internet, and `[http_service]` would publish all eight Apps on ports 80/443 — Fly's own reference points at top-level `[checks]` for exactly this case. `createHealthHandler` is unchanged; only its reachability from outside Fly's private network is. And `auto_stop_machines`/`auto_start_machines`/`min_machines_running` are not carried over — all three are `[http_service]`-scoped, and Fly documents `min_machines_running` as having "no effect unless you set `auto_stop_machines` to `"stop"` or `"suspend"`", so it was already a no-op next to 2.2's own `false` (itself the legacy boolean form of the current `"off"`) — that sentence is verbatim on fly.io/docs/launch/autostop-autostart/, not on the configuration reference, the same URL-mismatch shape as above.
      **DA review caught a genuinely inverted rationale, and it was the important finding.** The deploy command carries `--ha=false`, originally justified as stopping `fly deploy` from "... run a second process with the same MOE_PERSONA_ID — two Socket Mode connections, two claim loops." That is false for _these_ configs: having dropped `[http_service]`, the process groups have no services, and Fly documents the default `--ha=true` as creating "one Machine and ... one stopped standby Machine for process groups without services" (fly.io/docs/reference/app-availability/) — a failover that consumes nothing until the primary is unavailable. Alex re-decided with the real behaviour in hand and kept `--ha=false`, on the narrower true reason: a standby that starts while the primary is merely _unreachable_ would put two live processes on one persona's Slack connection, and moe has no cross-process reply dedup (`seen-event-cache.ts` is per-process, in-memory) — ticket claims would stay correct via 1.3's concurrency-tested optimistic lock, but Slack replies could double. The runbook's own `# confirm exactly ONE Machine, not two` line was the sharpest consequence: an operator following it would have destroyed the fleet's only redundancy on sight, so it now explains the `†` standby marker instead.
      **DA also caught the sibling reference the sweep missed.** `claim.test.ts`'s racing-claimants test spawned exactly 10 claimants to match `db.ts`'s old `max: 10`, and its own comment warned that shrinking the pool would leave the assertion green while no longer exercising a race. Lowering `max` to 5 did exactly that. Fixed by exporting `DB_POOL_MAX_CONNECTIONS` and binding the claimant count to it, so the coupling can no longer drift silently — the duplicated literal was the whole defect.
      **`createPool`'s `max` re-derived from 10 to 5** now that N is really 8: a 0.25 CU Neon compute allows 104 direct connections with 7 reserved for its superuser (verified against Neon's own connection-pooling docs), so 97 are usable and the old value put the fleet at 80 — 82% of budget, with no headroom for a rolling restart holding an old and a new pool open at once. `docs/OPERATIONS.md`'s new §Deploying the persona fleet also promotes `db.ts`'s advice-only "use the `-pooler` hostname in production" note into a required deploy step, since the pooled endpoint accepts 10,000 client connections and the direct hostname is a trap that only bites under load.
      Runbook in `docs/OPERATIONS.md` §Deploying the persona fleet, which also closes that doc's own previously-flagged "documented production migration process" gap: migrations are one manual `pnpm --filter @moe/core migrate` against the single shared database _before_ the eight deploys, not a per-App step. `migrate.ts`'s advisory-lock TSDoc claimed "N persona machines can all attempt migrations on boot safely" — true of the lock, but describing a design `main.ts` deliberately doesn't use; reworded to say so.
      **Live-verified end to end (2026-07-25).** Sarah is deployed and running: `fly deploy -c fly.sarah.toml --ha=false` created exactly **one** Machine (`80e9e04a666e78`) in `lhr` — the deploy plan itself said "create 1 \"app\" machine", confirming `--ha=false` suppresses the standby — with the health check passing and returning `{"status":"ok","personaId":"sarah"}` over Fly's private network, no public port published. Boot logs show the full sequence: `server started` (personaId sarah, port 8080) → `github app credentials validated` → `slack socket mode connected`. All 8 generated configs also pass `fly config validate` against Fly's real schema, and the deploy image builds (`docker build`, 519MB local / 85MB pushed).
      **Two things the live run corrected that no amount of config review would have.** Chunk 2.2's `grace_period = "10s"` had never been exercised — it was written into a `fly.toml` that was never deployed. Sarah reached a listening socket ~7s after machine start and the check logged one failure before passing, a ~3s margin; raised to 30s (`fly-deploy-constants.ts`), which costs nothing in steady state since the grace period only bounds the startup window. And the Fly account itself disproved a claim this entry had made from the repo alone: there is **no App named `moe`**, so chunk 2.2's `app = "moe"` pointed at nothing; the App that exists, `moe-ai-team`, is the shelved pre-rebuild iteration's and is deliberately not repurposed.
      **Still blocked, and not part of this chunk:** the other seven personas cannot deploy until `pnpm --filter @moe/server provision:slack-apps` actually runs against Slack — BUILD_PLAN 5.1 built and tested that script but never executed it live — which needs a fresh 12-hour `MOE_SLACK_APP_CONFIG_TOKEN` and the `apps.manifest.export` cross-check `build-persona-slack-manifest.ts` sets as a gate. That is 5.1 tail-work, not 5.2's.
- [x] **5.2a — Ambient intake scoping.** _5.2 itself is complete; this is a separate follow-on its live deploy exposed._ **Shipped 2026-07-27** ([PR #74](https://github.com/Pushedskydiver/moe/pull/74)) and deployed to all eight Fly Apps, which is what retires the membership restriction recorded below. **Discovered 2026-07-25**, immediately after the whole fleet went live: nothing scopes ambient-channel handling to one persona, so every persona that is a member of a work-relevant channel independently classifies every message there. DA already flagged this at chunk 3.3 as "ambient classification isn't yet scoped to Sarah's process specifically — harmless at N=1 persona, a forward-reference for whichever Stage 5+ chunk adds a second persona sharing a channel." That chunk arrived: N is 8.
      **What actually breaks — corrected 2026-07-26, because the original text here was wrong and the truth is worse.** This entry previously claimed the `UNIQUE (channel_id, message_ts)` constraint (migration `0006`) "decides which single writer wins," leaving "K−1 orphaned Slack messages with no DB row" and no reaction legend. **It does not, and they are not.** `postAndPersistDraft` writes `messageTs: posted.ts` — the _posted draft's own_ timestamp, which is different for every persona — so the constraint never collides, `createPendingTicketDraft` is a plain `INSERT` with no `ON CONFLICT`, and there is no winner-picking mechanism at all. All K inserts succeed and all K reaction legends are seeded. The real failure is **K fully-live drafts, each with a working ✅/🔁/📦 legend, each independently committable into its own separate `Brief` ticket** — duplicate tickets, not inert messages.

  The duplication is also wider than the High band. **Mid-band** (`postAndPersistConfirmingQuestion`) has the identical post-then-persist shape and the same posted-ts key, so K personas post K real confirming questions, each with a real row and legend — a human 👍 on any of them fires a real draft. **Low-band** (`logToReviewQueue`, since 3.9 the shared `logAmbientIntakeToReviewQueue`) keys on the source message's ts and `review_queue` has no unique constraint of any kind, so K rows are written per message. They are **not** identical and no digest inflates — each carries its writer's own `personaId`, and `listReviewQueueEntriesSince` plus the whole 3.5 sweep are persona-scoped, so Sarah's digest (the only one run today) still shows exactly one row. The real cost is K-fold table growth plus K−1 rows stranded in digests nobody runs. Per-message cost is also higher than first stated: a High-band message costs **three** billed calls per persona (Haiku classify, Haiku situational-appropriateness, Sonnet compose), not two.
  **Scoped to fix (1) only; the claim-first fix is split out as 5.2b** (Alex settled 2026-07-26, once recon showed fix (2) is a schema change rather than a reorder — see 5.2b). Fix (1) alone closes the live problem completely: if only one process classifies, nothing downstream runs on the others, so the duplication disappears at **every** band rather than only the High one. Gate ambient handling on a designated intake persona — VISION §5.3 already settles who ("Sarah is the canonical intake listener for shared/ambient channels"), so this is wiring a settled decision, not reopening it; a non-intake persona drops an ambient message before any billed call.

  **Where the gate goes, and what it must not gate.** Beside `isSurfaceInScope` in `handleAmbientChannelMessage`, and **before** it — the cheaper check, and the one that is true for seven of the eight processes. Deliberately _not_ folded into `isSurfaceInScope` itself: that function's `dm` arm returns `true` unconditionally and BUILD_PLAN 3.7's DM cascade depends on it, so a persona check inside would silently kill DM intake for all seven non-Sarah personas. It gates **only the ambient intake cascade** — not channel membership, not the Socket Mode listener, and not any other reason a persona posts into a work channel. VISION §6.5 has Nia posting EOD digests daily and §6.1's channel table puts those in `#moe-team` and gives `#moe-research` to Theo; gating membership or the listener would foreclose 6.7a/6.7c/6.10/7.6. It also leaves room beside it for a future @-mention branch (5.6's "addressed-only?" question), which must be able to reach a named persona in a channel it is not the intake listener for.

  **Designation mechanism (Alex settled 2026-07-26):** a constant in `packages/core` (`AMBIENT_INTAKE_PERSONA_ID`), not an env var. VISION §5.3 makes this a product fact, not a deployment knob, and eight independently-deployed Fly Apps each carrying their own copy of the value is eight chances to drift — with a silent failure mode in both directions (two classifiers, or none).
  **Operational consequence — the interim restriction is RETIRED.** While this was unbuilt, only Sarah could be a member of `#moe-team`/`#moe-incidents`/`#moe-research`; the other seven were safe in DMs (each app receives only its own) and in any channel outside `MOE_WORK_RELEVANT_CHANNEL_IDS`, since Stage 0 drops those before the classifier. That restriction lifted once this chunk was _deployed to all eight Fly Apps_ — not when it merged (deploys are deliberately not CI-automated, so a process on an older image still had no gate); both happened on 2026-07-27, verified live by a canary in which Marcus received a work-shaped `#moe-team` message and classified nothing. **The rule now in force is "all eight may be members; only the intake persona runs the ambient cascade."** **Needed to land before 5.5** (the welcome ritual posts to `#moe-team`, which implies the team is _in_ `#moe-team`) and before Stage 6's cross-persona handoffs. Note 5.6's own hard dependency is **5.3**, not this — its text says "Also worth landing after **5.2a**," a soft preference; `PROGRESS.md` had flattened that into "prerequisite."

- [x] **5.2b — Claim-first draft/question posting (defence-in-depth for 5.2a).** **Merged 2026-07-28** ([PR #79](https://github.com/Pushedskydiver/moe/pull/79)), **deployed 2026-07-28**: migrations `0020`/`0021` applied to production Neon (verified — 21 rows in `schema_migrations`, both new `UNIQUE (channel_id, source_message_ts)` constraints present with the correct auto-generated names, `message_ts` nullable on both tables, and the one pre-existing `pending_ticket_drafts` row survived intact with `source_message_ts` correctly `NULL`) and all eight personas redeployed on the fixed image, each confirmed single-Machine/healthy (`1 total, 1 passing`). _Split out of 5.2a on 2026-07-26, once recon showed this is a schema change rather than the two-statement reorder that chunk's text described._ 5.2a stops the duplication by designating one ambient intake listener. This makes the duplicate-post class **structurally impossible** rather than configuration-dependent — worth having, because 5.2a's guarantee is only as good as one constant being right in one place, and its failure mode is silent.

  **Why it is not a reorder.** "Insert the row first, post only if the insert won" cannot key the claim on `posted.ts`: that value does not exist before the post, and it is distinct per persona, which is exactly why the existing `UNIQUE (channel_id, message_ts)` never arbitrates anything. A claim needs a natural key every process computes identically — the **source** message's `(channel_id, ts)`. The precedent is one table over: `pending_confirming_questions` (migration `0008`) already carries both `message_ts` and `source_message_ts`.

  **What it therefore requires:** a migration adding `source_message_ts` to `pending_ticket_drafts` plus `UNIQUE (channel_id, source_message_ts)` as the claim key; `message_ts` made nullable until the post succeeds, then filled in by a second `resolve`-style UPDATE. **Postgres detail that decides the design:** leaving the claim on `UNIQUE (channel_id, message_ts)` with a NULL `message_ts` would _not_ dedupe, since NULLs are distinct in a Postgres unique index **by default** (PG15+ can opt out with `UNIQUE NULLS NOT DISTINCT`, and production is PG18 — but `0006`'s constraint is declared plainly, so the default applies) — the claim uniqueness has to come from the new constraint. The reaction lookup must keep querying by the **posted** message's ts (`handle-reaction-added.ts` → `getPendingTicketDraftByMessage`); if the row were ever keyed on the source ts instead, a reaction on the _source_ message would resolve as a draft outcome.

  **Known test impact, from recon:** three tests assert `messageTs` equals the posted message's ts specifically; one (the Slack-post-failure test asserting `draftStore.create` was never called) breaks outright and needs rewriting rather than adjusting; three more keep passing but their names/comments become false. The repository-level error path for a losing claim is currently **unpinned** — `migrate.test.ts` covers the DB constraint, but no repository test exercises the duplicate-insert path, so that needs adding as part of this.

  **Scope note:** apply the same treatment to `postAndPersistConfirmingQuestion` (identical shape, same posted-ts key) or state explicitly why not. `review_queue` has no unique constraint at all; decide here whether Low-band duplication needs one, given 5.2a already prevents it in practice.

  **Both scope-note questions settled with Alex via `AskUserQuestion` before any code moved**, matching this file's own precedent for genuinely open design decisions: apply the identical claim-first treatment to `pending_confirming_questions` in this same chunk (recommended and confirmed — same vulnerability, and that table already carries `source_message_ts` as a plain column, just not yet as its claim key, so it was the smaller lift of the two); leave `review_queue` unconstrained (recommended and confirmed — a review-queue row is a passive log entry nothing else claims or acts on, unlike a draft/question's claim-then-act shape, so a duplicate there costs a human seeing one event logged twice, not two conflicting Slack posts).

  **Built.** Two migrations (`0020`/`0021`), checked directly against production before writing either: one existing `pending_ticket_drafts` row (already resolved, from chunk 3.7's own live verification) with no way to backfill a `source_message_ts` it never had, so that column is DB-nullable despite being required on every new write; zero existing `pending_confirming_questions` rows, so no backfill question there at all. Both migrations drop the old `UNIQUE (channel_id, message_ts)` (verified against the real constraint names first, not guessed), make `message_ts` nullable, and add `UNIQUE (channel_id, source_message_ts)` as the real claim key. `pendingTicketDraftSchema`/`pendingConfirmingQuestionSchema` (`packages/core`) both take `messageTs` nullable at the round-trip level — including `pending_confirming_questions`' own `sourceMessageTs`, which stays required there, unlike the drafts side, since that table never had a legacy-row gap to tolerate. Each repository gained a `markPosted`-style CAS update (`WHERE messageTs IS NULL`, same double-fire guard shape as the existing `resolve` functions) alongside the existing `create`, which is now the claim-time insert (`sourceMessageTs` required, no `messageTs` accepted as input at all). `postAndPersistDraft`/`postAndPersistConfirmingQuestion` (`apps/server`) both reordered to claim → post → mark-posted, each split into its own small `claimDraft`/`claimQuestion` helper purely to stay under eslint's `max-lines-per-function` once the extra step landed — composition code extracts aggressively (`docs/CONVENTIONS.md` §Code Style). All four of the recon's known test-impact predictions held: the three `messageTs`-asserting tests were rewritten (not just adjusted) to assert `sourceMessageTs` on the claim call plus a new `markPosted` call; the Slack-post-failure test's `not.toHaveBeenCalled()` flipped to `toHaveBeenCalled()` since the claim now precedes the post; and a new repository-level duplicate-claim test was added for `pending_ticket_drafts`, mirroring `pending_confirming_questions_repository.test.ts`'s own pre-existing template for the equivalent case.

  **DA review found two Material gaps in the first pass, both folded.** (1) The first draft of this chunk copied only half of `createGithubIssue`'s own claim-first precedent (BUILD_PLAN 4.4b): that guard actually distinguishes a **definitive** failure (GitHub confirmed nothing was created — release the claim, allow a clean retry) from an **ambiguous** one (a crash/timeout — leave the claim in place, surfaced for manual reconciliation), but this chunk's first pass orphaned on every failure. A Slack `postMessage` returning `{ ok: false }` is exactly the definitive case, and DA proved the consequence concretely: `getDraftOutcomeCounts` (the VISION §5.4 acceptance-rate signal a prior DA pass, chunk 3.6, already once found miscounted) would silently bucket a draft that was **never posted** as `'ignored'` 24h later, since an orphaned `messageTs`-null row satisfies the exact same predicate as a real ignored draft. Fixed with a new `releasePendingTicketDraftClaim`/`releasePendingConfirmingQuestionClaim` pair (`DELETE ... WHERE id = $1 AND messageTs IS NULL`, mirroring `releaseTicketGithubIssueClaim` exactly), called on a definitive post failure only — a failure of the later mark-posted step still orphans, since by then a real Slack message exists and deleting the tracking row would make it strictly worse, unchanged from the equivalent pre-5.2b failure window. (2) `pendingTicketDraftSchema`'s own `.nullable()` on `sourceMessageTs` — added for the one legacy row — meant `createPendingTicketDraft`'s validation would silently accept `sourceMessageTs: null` at the Zod boundary despite `NewPendingTicketDraft`'s TypeScript type forbidding it, an erased-at-runtime guarantee only. Fixed with a separate, stricter `newPendingTicketDraftSchema` (non-nullable `sourceMessageTs`) that `createPendingTicketDraft` validates its input through before ever touching the nullable round-trip schema. A fresh R2 pass confirmed both folds but caught a third, smaller gap: the new regression test for (2) passed a blank string, not `null` — the actual reported bug — so it didn't reproduce the defect it claimed to pin (a blank string was already rejected pre-fix). Corrected with a real `null`-input test, verified by manual mutation testing (temporarily reverting the fix, confirming the corrected test fails, restoring, confirming it passes).

  **`copilot-surrogate` (two scoped passes, since the full 25-file diff exceeded its 20-file ceiling) found two more Material issues, both folded.** `packages/core` came back clean. `apps/server`: (1) the claim-first extraction in both `postAndPersistDraft` and `postAndPersistConfirmingQuestion` had left an explanatory comment block sitting between its own doc-comment marker and the newly-extracted `claimDraft`/`claimQuestion` helper, rather than above the function it actually describes — a real doc-comment misattachment in the confirming-question file (a `/** */` JSDoc block, so tooling-attached to the wrong function) and an equally misleading, if not tooling-attached, instance of the same shape in the drafts file (`//` comments); both moved back to sit directly above the functions they document. (2) `run-dm-intake-cascade.test.ts`'s Mid-band confirming-question test kept its pre-5.2b title ("persisting... fails after a successful post") after the reorder made `create` the pre-post claim step — the sibling High-band test in the same file was correctly relabeled during the original pass, this one was missed; retitled, and both it and its High-band sibling gained the `postMessage`-not-called assertion their own ordering claims weren't actually checking.

- [x] **5.3 — Persona prompts, one at a time.** One chunk _per persona_: prompt drafted with Alex (do-not-touch surface — co-authored, not submitted for rubber-stamp), anti-sycophancy patterns layered per VISION Appendix C's table, adversarial-conversation validation, replay recording (from the second persona on — 5.3a ships before the 5.4 harness exists; 5.4 backfills its recording). **Do not batch these.** Expect iteration; pace per Alex's appetite (VISION §13.1). This line expands into 8 sub-chunks (5.3a, 5.3b, …) now that the roster is fixed (2.1/5.0). **Settled at 5.0 (2026-07-24):** each sub-chunk also gets its own deep-research pass (surveying that role's real-world domain/tooling landscape) feeding prompt drafting, plus a genuine adversarial "grill" round before the prompt is considered done — extending the existing two-phase `spec-grill` discipline (`docs/DEVELOPMENT.md`) to persona prompts, not just specs. Designer's research pass is already banked ahead of its own sub-chunk: `.claude/research/designer-persona-landscape/landscape-survey.md`.
      **Prompt-template requirements this chunk owes, added 2026-07-25.** Three VISION commitments have no home other than these prompts, and would be silently dropped if each sub-chunk only layers Appendix C's anti-sycophancy table. (1) §7.6 layer 1's **evidence-before-verdict ordering**, with "not yet verified" as an explicitly acceptable, non-penalized answer — this is also where 2.5's deferred free-prose-claim bypass was sent, so nothing else schedules it. (2) §8.2's **ambiguity escalates the tier, not the eagerness** — "clean this up" on a Tier 2/3-adjacent path must make the persona restate a concrete plan and get it confirmed rather than act. (3) Per-persona **model selection** (§10: "Sonnet-by-default per persona with per-persona model tuning"), which today is a hardcoded model id per call site, not a per-persona config value — Riley's 6.2 agentic work is already flagged as wanting Opus-tier. Build the shared template requirements once, at 5.3a, rather than rediscovering them eight times.
      **5.3a's own code-level shared template requirements: merged, migrated, and deployed 2026-07-30** ([PR #82](https://github.com/Pushedskydiver/moe/pull/82)). Requirement (3), per-persona model selection, landed as `resolvePersonaModel` (`packages/agents`) — all 8 personas currently resolve to `claude-sonnet-5`, no override configured yet (assigning Riley's own Opus-tier override is 6.2's decision, not this chunk's). A fourth item, not originally listed above, was folded in: a **squeaky-wheel guard** (`evaluateSenderFrequencyGuard`, `apps/server`) suppressing a second High/Mid-band trigger from the same sender in the same channel within a 15-minute cooldown window, logged to `review_queue` rather than silently dropped (migration `0024`) — surfaced by Sarah's own deep-research pass (below) and settled with Alex via `AskUserQuestion` as a real code-level guard, not a prompt-only mitigation. Migration `0024` applied to production Neon and all eight personas redeployed on one built-once-reused image, both independently verified (schema_migrations row count + constraint definition; `fly status`/`fly checks list` per persona). Sarah's own deep-research pass (PM/intake-triage persona landscape, mirroring Maya's own banked methodology) is done and banked at `.claude/research/pm-persona-landscape/landscape-survey.md` (gitignored), including a set of concrete open questions for Alex to weigh in on while drafting.
      **5.3a complete, 2026-07-30: Sarah's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/sarah/prompt.md`), closing both remaining prompt-template requirements — (1) §7.6's evidence-before-verdict ordering (with "not yet verified" explicitly non-penalized) and (2) §8.2's ambiguity-escalates-the-tier, reframed from code-tier language to Sarah's own PM domain ("sort out the backlog" gets a stated plan, not a guess) — plus Appendix C's full layering for Sarah (Patterns 1, 2, 5, 6 priority-ordered, 7 universal). Five of the research doc's own open questions were settled live with Alex across two sessions (decline-sequencing acknowledge-first for actual declines only, question-first for Mid-band confirming questions, generic Mid-band wording pending real traffic, Low-band evidence-citing despite thin `review_queue` visibility today, no PM jargon by default, and 5.3a's own pacing budgeted for extra iteration given no reference implementation exists anywhere). Validated via nine live adversarial-conversation rounds against the real model (authority/rank-pulling pressure, evidence-before-verdict under deadline pressure, ambiguity-escalates, a banter-to-serious pivot, equal treatment of Alex specifically, a jargon-leak check, competing-priorities judgment, and two rounds grounded in real external repos — chief-clancy-adjacent framing plus two genuine open issues from `Pushedskydiver/Colour-Contrast-Checker`) — one live-tested gap (deferring to a confident, technical-sounding critique of a teammate's work without the same evidence scrutiny applied elsewhere) found, fixed, and reverified. Closed with a full two-phase `spec-grill` (`docs/DEVELOPMENT.md`): R1 discovery found 2 MATERIAL (an internal ambiguity over whether Sarah re-derives an already-decided band; a wiring-scope factual error in this session's own record, corrected in 5.3a-ii above) + several LOW/nit, all addressed; R2 verification caught a residual contradiction left by the first fix and a fresh factual error in the fix's own added text (the exact same failure class as the finding it was fixing), both corrected and manually re-verified clean. Replay recording is explicitly not required yet — 5.3a ships before 5.4's harness exists; 5.4 backfills Sarah's own recording once built.
      **5.3b complete, 2026-08-09 — Maya's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/maya/prompt.md`, [PR #89](https://github.com/Pushedskydiver/moe/pull/89)). **Output-mode decision:** Alex settled that Maya's target capability is real design creation, not critique/spec alone — but actual artifact-generation tooling isn't wired yet (no code touches this chunk; a dedicated design-tooling-mechanism research pass, banked at `.claude/research/designer-persona-landscape/design-tooling-mechanism.md`, found a real non-Agent-SDK path — v0's Platform API + a scoped render/screenshot tool + Impeccable's `detectText` — but flagged it would jump `BUILD_PLAN` chunk 6.0's own unbuilt tool-allowlist gate, deliberately left as a separate, later decision). The shipped prompt is honest about the gap: she specs and hands off to Riley today, and explicitly does not claim to generate or deliver a rendered artifact. Appendix C layering settled at 1, 2, 4, 5, 6, priority-ordered, universal 7 (`docs/VISION.md`'s own table updated to match — [PR #88](https://github.com/Pushedskydiver/moe/pull/88), merged). Two real-person voice references — Adam Silver (evidence-based "boring beats fancy" conviction) and Romina Kavcic (design-tokens/agentic-design-systems expertise, conversational register) — were independently researched and verified against their real public work before being used as inspiration; banked at `.claude/research/designer-persona-landscape/voice-references.md`.
      **Design philosophy resolved a real tension Alex raised directly, via a dedicated two-phase adversarial grill:** does "boring beats fancy" suppress legitimate creativity and become its own AI-slop risk? Landed on grounded/intentional vs. reached-for-because-comfortable as the actual axis, not boring-vs-bold — the banked landscape-survey's own definition of AI slop is specifically about defaulting absent explicit direction, which cuts both ways. A bold call is licensed when a brief genuinely earns it; generic-by-default is named as slop too, closing the door back to a creativity excuse. **A second, full-file two-phase grill** (4 parallel discovery angles — internal-consistency/absolute-quantifier sweep, Appendix C pattern-grounding, codebase claim-extraction, multi-mode scenario simulation — then a fold, then a distinct verification round) found and fixed 13 real issues, the sharpest: an overclaimed enforcement power ("you do not let it through" implied a merge-veto she doesn't have), a copy-drift bug that had silently dropped content while adapting Sarah's prompt structurally, a description of a genuinely dead code path (`isAmbientIntakeListener` is hardcoded to Sarah), and a header overclaiming the Sarah/Riley handoff as a present-tense wired mechanism rather than the shape the team's built for. The verification round itself caught the fold's own fix briefly overclaiming a screenshot/image input channel that doesn't exist (`InboundMessage` is text-only) — corrected before it shipped, not after. Closed with a 7-scenario live-model-harness validation against the real Claude API, run against the actual folded prompt — including an adversarial "let's be bold and creative, skip contrast checks, fix accessibility later" attempt, the exact failure mode the design-philosophy tension was worried about. Held cleanly.
      **Review saga on the PR itself:** `da-review` and `copilot-surrogate` dispatched together from the start (not sequentially after being asked, per the BUILD_PLAN 3.12 lesson) — both R1 passes independently converged on the same real gap (the voice-reference research had genuinely been done but never banked anywhere checkable), plus two commit-message overclaims and a missing paper trail for the chunk. Fixed; fresh-context R2 passes confirmed every fix, DA's own R2 catching one more small nesting issue in this very entry's first draft, also fixed.
      **Deployed 2026-08-09.** 5.3a-ii's own generic wiring meant no further code change was needed — `fetchPersonaPromptContent` picked her prompt up automatically. Built once and reused across all 8 Apps, every persona independently verified (single Machine, health passing, correct `personaId`); Maya's real voice confirmed live via a real Slack DM. That same verification surfaced a live-only bug — Slack's App Home Messages tab defaults new apps to read-only, blocking the DM with a real, visible Slack UI error — fixed in `build-persona-slack-manifest.ts` ([PR #90](https://github.com/Pushedskydiver/moe/pull/90), merged), with live remediation (a manual App Home toggle) confirmed on all 8 apps; full detail in `PROGRESS.md`.
      **5.3c complete, 2026-08-09 — Marcus's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/marcus/prompt.md`, [PR #92](https://github.com/Pushedskydiver/moe/pull/92), merged). A dedicated deep-research pass (five parallel angles — AI planning-tool precedent including Aider's literal "architect" mode and Claude Code's own Plan Mode, the broader dev-tools landscape, real-world architecture/RFC craft, existing open-source Architect-persona prompts, anti-sycophancy research plus VISION's own flagged-open interjection-bar question) is banked at `.claude/research/architect-persona-landscape/landscape-survey.md`. Its single strongest finding: BMAD-METHOD's "Winston" names "Rule of Three before abstraction" as an explicit principle — verified directly against the real repo file — a near-verbatim match for Marcus's only established voice example (VISION §1.3), meaning moe's existing canon already _is_ the Rule of Three, just unnamed until now. A second, targeted pass verified voice references (`.claude/research/architect-persona-landscape/voice-references.md`): Will Larson (staffeng.com, the literal source of the "Architect" archetype Marcus is named after) as primary anchor, Martin Fowler as secondary (reasoning stance, not brevity); Kelsey Hightower was considered and explicitly set aside — his convictions match but his real public register is confident/declarative, the opposite of "not blocking, just noticing." Neither reference name appears in the shipped prompt, same convention as Sarah's and Maya's.
      **Six open questions settled live with Alex before drafting:** the interjection-bar gap is named honestly as not-yet-actionable (Maya's 5.3b precedent) rather than omitted or overclaimed; the plan-output artifact stays agnostic (chunk 6.1c's own future decision, not presupposed here); the Dom/da-review boundary is stated explicitly; "Rule of Three" is named as a literal threshold; self-review anchoring resolves to Dom's review adjudicating a plan-vs-build disagreement, not Marcus judging his own work; ambiguity handling is calibrated (block-and-ask only when a wrong guess would invalidate the plan's core approach, name-and-proceed for smaller gaps) rather than absolute.
      **Closed with a full two-phase `spec-grill`** (four parallel R1 discovery angles: internal-consistency/quantifier sweep, Appendix C pattern-grounding, codebase claim-extraction, multi-mode scenario simulation). Real, cross-validated findings: zero `report_status` routing (found independently by two angles — a functional regression against VISION §7.6, since a real `prompt.md` fully replaces the generic fallback that does carry the instruction); the Triage-voice "drafting a ticket" text was unadapted Sarah/Maya boilerplate contradicting Marcus's own stated role (found by three angles — resolved by explaining the real dual-role distinction: a direct DM triggers the same generic per-persona intake every persona has, separate from his usual Sarah-triages/Marcus-plans flow); and a direct contradiction between "don't verify a build against your plan yourself" and "weigh in when your plan and Riley's build disagree" (resolved via a proactive-audit-vs-reactive-read distinction). A Pattern-4 (citation-required disagreement) paragraph had ridden along from cloning Maya's template without being in Marcus's assigned Appendix C set — resolved by amending VISION's layering table to give Marcus his own row (patterns 1, 2, 6, 5, 4 + universal 7), mirroring exactly how Maya's own row was settled in [PR #88](https://github.com/Pushedskydiver/moe/pull/88). R2 verification confirmed all eight R1 findings genuinely fixed, and caught two new small issues in the fix pass itself — including the same class of mistake twice (an ambiguous cross-reference direction), both tightened.
      **Live-tested against the real Claude API across ten scenarios** (Rule of Three at two and three occurrences, architecture-astronaut resistance under explicit "be thorough" pressure, calibrated ambiguity, self-review anchoring on a real plan-vs-build disagreement, `report_status` routing with and without real context, casual banter, deadline-pressure to approve an over-built plan, direct-DM ticket drafting) — all held. The self-review-anchoring scenario produced a clean, unprompted demonstration of the fix: asked directly why a build diverged from his plan, Marcus explicitly deferred the adjudication to Dom rather than judging his own work. The `report_status` scenario with real context went further than designed — Marcus caught that his own prior turn had asserted an unverified dependency as fact, and folded that correction into the status claim itself.
      **`da-review` + `copilot-surrogate` dispatched together from the start.** `copilot-surrogate` found two real issues the spec-grill's own file-scoped passes hadn't caught: the prompt's intro lifecycle description dropped Priya from the "Riley builds" stage (VISION's own authoritative string is "Riley builds (with Priya) → Dom reviews (with Priya)"; the drafted prompt only paired her with review) — fixed to match; and this file had no 5.3c entry at all, unlike Sarah's and Maya's own PRs which both updated it in the same commit — this paragraph closes that gap. `da-review` separately found `docs/PERSONAS.md`'s roster-status column had gone stale for all three drafted personas (Sarah, Maya, Marcus) across both prior PRs, never caught before — fixed for all three in this PR rather than patching only Marcus's own row. **Merged. Not yet deployed.**
      **Carried forward, not resolved here:** chunk 5.4 (persona-replay harness) is now two personas overdue against its own "from the second persona on" commitment (line above) — mitigated by the documented interim substitute (`docs/REVIEW-PATTERNS.md`'s own tracked live-model-harness testing, which both Maya's and this chunk's live-scenario validation followed), but worth an explicit Alex decision on when 5.4 itself gets scheduled rather than letting it keep sliding past its own trigger silently.
      **5.3d complete, 2026-08-09 — Riley's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/riley/prompt.md`, [PR #94](https://github.com/Pushedskydiver/moe/pull/94), merged). A dedicated deep-research pass is banked at `.claude/research/engineer-persona-landscape/landscape-survey.md`/`voice-references.md`. Its single strongest finding: BMAD-METHOD's "Amelia" (Dev agent, sibling to Winston/Marcus's own anchor) gives a clean axis for how an Engineer persona should differ from an Architect — Winston trades in "trade-offs, no verdicts," Amelia in "verdicts in file paths and AC IDs" — used to shape Riley's terse, evidence-grounded register rather than a softened copy of Marcus's. A second finding shaped Building philosophy directly: VISION §7.6's grounded-claim schema only catches _ungrounded_ fabrication (zero evidence), not a claim backed by a genuinely gamed tool call (a hardcoded/special-cased test, an assertion weakened before a failure resolves) — a gap only Riley's own prompt voice can close, not the schema. Voice references verified (`voice-references.md`): Kent Beck and Gary Bernhardt as primary anchors, both independently verified against Riley's own established register (VISION §1.3's "should ship by EOD" line); Sandi Metz as secondary (design-taste-while-implementing, a different axis, not independently verified against the terse register itself). Neither name appears in the shipped prompt, same convention as Sarah's, Maya's, and Marcus's. Four judgment calls settled live with Alex before drafting: register calibrated to Bernhardt-tight (dense, brief, concrete); the anti-fabrication discipline named explicitly in Riley's own voice, not left to the schema gate alone; Marcus's own "not blocking, just noticing" pattern-flagging move extended to Riley in his own verdict-shaped register; the voice-reference mix confirmed as researched rather than assumed.
      **Closed with a full two-phase `spec-grill`** (four parallel R1 discovery angles: internal-consistency/quantifier sweep, Appendix C pattern-grounding, codebase claim-extraction, multi-mode scenario simulation). One BLOCKING finding, converged on independently by two angles: "Disagreement and declining" had been templated from Marcus's shipped section rather than assembled from Riley's own narrower assigned pattern set (VISION's layering table scopes Riley/Dom to Patterns 1+4 only, not Marcus's 1,2,6,5,4) — leaked Pattern 2 (rephrase-as-question) and Pattern 6 ("not engagement, noise") were removed and the section rewritten from Riley's actual assignment, including a Pattern-7 drift-check rewritten in his own voice rather than a trimmed copy of Marcus's. Six further MATERIAL findings, all fixed: a real scope-line contradiction between two Building-philosophy bullets (extracting a helper physically inside an already-touched diff vs. "that's Marcus's call to schedule"); the non-negotiable evidence clause missing the one evidence channel Riley actually has today (been shown a result, vs. run/seen it himself); "What you can do today" missing the sibling-established "ask for what's missing" instruction; a casual-banter license that, combined with unusually dense evidence-discipline language elsewhere, risked collapsing ordinary banter into a work-mode disclaimer; no precedence between Triage voice's Mid-band question-only rule and Building philosophy's ambiguous-criterion rule when the two overlap; and no decision procedure for a wholly open-ended request ("make this more robust") landing directly on Riley. R2 verification confirmed all thirteen R1 findings (1 BLOCKING, 6 MATERIAL, 6 LOW) genuinely fixed, zero new findings.
      **Replay-recorded against the real Claude API, eight scenarios** (`packages/agents/src/personas/riley/replay/`) — six `dmReply` plus one `ticketDraft` and one `confirmingQuestion`, per `docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9, each grounded in a specific `prompt.md` commitment (test-weakening pushback, an ambiguous acceptance criterion, noticing-but-not-building a Rule-of-Three pattern, declining certainty with no live tool access, `report_status` routing on a genuinely-shown result, casual banter staying in character). Recording surfaced a build-tooling gotcha rather than a new persona-voice finding: `record-persona-replay.ts` imports each persona's `scenarios.ts` from its own compiled `dist/` output, not live `src/` — editing a scenario without rebuilding silently re-records the _previous_ version against the real API on every run, which happened here for several rounds before being caught. The same mechanism (a `dist/`-consuming script silently reusing stale compiled output after a source edit) as PROGRESS.md's own Session 28 finding (BUILD_PLAN 5.3a-ii, `prompt.md` never reaching `dist/` at all until the build step was fixed) — not the identical bug, but the same failure shape, now confirmed for scenario-file edits specifically, not just the build step itself. Once genuinely rebuilt, the `report_status`-routing scenario passed on the first real attempt.
      **`da-review` + `copilot-surrogate` dispatched together from the start on [PR #94](https://github.com/Pushedskydiver/moe/pull/94).** `copilot-surrogate` found one real MATERIAL finding: this very BUILD_PLAN entry's own first draft cited a dist-rebuild-gotcha lesson as something `docs/history/` "names" — that label only exists in the session's personal, cross-repo memory file, not anywhere in this repo; reworded to cite the real in-repo precedent (PROGRESS.md's Session 28 finding) instead. `da-review` found 3 MATERIAL findings: `docs/REVIEW-PATTERNS.md`'s own persona-replay-coverage line had gone stale the moment this PR shipped Riley's scenarios (still listed him among "no scenarios yet") — fixed, along with correcting the per-persona scenario count now that Riley's 8 differs from the others' 7; the `ticketDraft`/`confirmingQuestion` assertion predicates in `scenarios.ts` were byte-for-byte duplicated a 4th time across Sarah's/Maya's/Marcus's/Riley's own files, past `docs/CONVENTIONS.md`'s 2+-sibling-consumer extraction trigger the same file family already applied once for `dmReplyText`/`usedTool` — extracted to `ticketDraftBody`/`confirmingQuestionLeadIn` helpers (with their own unit tests) and all four scenario files updated; and a maximally permissive `/\?/` regex on the ambiguous-criterion scenario would have passed a rhetorical tag question on a reply that actually commits to a silent guess — the identical defect class BUILD_PLAN's own chunk-5.4 history already named needing seven rounds to properly bound on a sibling scenario — tightened to require a genuine interrogative clause before merging, not just punctuation presence. Two LOW findings (a dead regex branch spelling "thrice" as "thirce"; a non-blocking VISION §8.2 scope-note) — the typo fixed; the scope note left as-is after substantively checking (not just citing) that it carries the same calibrated escalate-or-proceed structure as Marcus's own already-reviewed §8.2 handling, and Riley's own entry above already names this as a deliberate, reviewed extension rather than an unreviewed copy. **A fresh R2 verification round (both `da-review` and `copilot-surrogate`, dispatched independently) confirmed all fixes genuinely landed and found zero new issues** — `da-review`'s own R2 flagged that this scope-note's justification initially read as the "existing code does it this way" anti-pattern even though the underlying substantive check was sound, prompting this reworded version. Full quality suite re-verified green after every fix. **Merged. Not yet deployed.**
      **5.3e complete, 2026-08-10 — Priya's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/priya/prompt.md`, [PR #95](https://github.com/Pushedskydiver/moe/pull/95), merged as `f5ce08e`). A dedicated deep-research pass is banked at `.claude/research/qa-persona-landscape/landscape-survey.md`/`voice-references.md` (four parallel angles: real-world QA/testing craft, competitor AI QA-persona precedent, anti-pattern/failure-mode research specific to AI-assisted testing and review, and bug-advocacy/disagreement craft). Its single strongest finding: the testing-vs-checking distinction (Bach/Bolton) gives Priya a precise, citable answer to "what do you add beyond Riley's own tests" — checking confirms what's already believed, testing looks for what nobody thought to assert. A second finding shaped her non-gatekeeper framing directly: Lisa Crispin's own words — "We're not gatekeepers. We're not the quality police" — plus her account of once holding real go/no-go authority ("That's not my decision. That's a business decision... it's not to me to say [go/no-go]") gave a real-world precedent for severity-vs-priority as her actual disagreement boundary, sourced independently to Michael Bolton's identical distinction. Voice references verified (`voice-references.md`): James Bach (strong on substance — testing vs. checking, bug advocacy — but flagged with a real, documented 2016 controversy over his combative public register, so used for content only, not tone), Lisa Crispin (strongest match for the "additive, not gatekeeping" design goal, used as the register anchor), Elisabeth Hendrickson (concrete exploratory-testing heuristics — Boundaries, CRUD, Goldilocks, Follow the Data — grounding "what to actually probe" in real technique). Neither name appears in the shipped prompt, same convention as Sarah's, Maya's, Marcus's, and Riley's. Four judgment calls settled live with Alex before drafting, via `AskUserQuestion`: register calibrated Crispin-led (collaborative-first, rigor high underneath) over a Bach-substance-dialed-down or situational-blend alternative; the Priya/Dom review-lane split resolved as Priya owning testing-specific concerns (edge cases, whether evidence proves what it claims) and Dom owning code quality/convention; a handful of Hendrickson's named heuristics included literally rather than left fully absorbed; and an explicit, literal "that's a real problem, and whether it blocks isn't my call" line rather than leaving the severity/priority boundary implicit.
      **Closed with a full two-phase `spec-grill`** (three parallel R1 discovery angles: Appendix-C pattern-grounding/VISION-consistency, internal self-consistency/codebase-claim verification, research-grounding fidelity). Five MATERIAL findings, all fixed: a Pattern-6-shaped clause ("'great question'... is noise") had leaked into the Disagreement-and-declining opening despite Priya's VISION Appendix C row assigning her only Patterns 1, 2, 4 — stripped to match Riley's own precedent for a 6-less persona; no explicit deadline-pressure resistance line despite BUILD_PLAN 5.3's own shared prompt-template requirement and three of four siblings already having one — added; execution-flavored language ("the one edge case you happened to try") contradicted the prompt's own "no standing way to run anything yourself today" constraint — reworded to a reasoning-based frame; the "calibrate how hard you push" section inverted the cited research's actual trade-off (richer scrutiny genuinely catches more real problems, at a real cost of blocking more good work, not "no benefit at all") — corrected to state both directions; and the severity language overclaimed objectivity ("say exactly how bad something is," "real confidence"), dropping Michael Bolton's own cited caution that severity itself is relational, not a fixed property — softened while keeping the "not my call" boundary intact. One LOW fixed for sibling consistency (Pattern 7's drift-check bullet moved to the tail of Disagreement-and-declining, matching Sarah/Marcus/Riley's placement); three further LOW findings left as-is, each matching an already-merged sibling's own precedent (a paraphrased rather than verbatim VISION §1.3 quote; a triage-voice line attributing the confidence band to the wrong file, shared verbatim across all five prompts; a dropped grounding clause in the high-band drafting guidance). **R2 verification confirmed 8 of the 9 cited items genuinely resolved and caught one incomplete fix**: the execution-flavored-language fix addressed only the cited phrase, leaving the identical contradiction one bullet later ("I can't get this to happen consistently yet," "say exactly what you tried and what you saw") — fixed in the required manual pass per the two-phase grill discipline's 2-round cap, reframing the whole bullet around evaluating given information rather than personally attempting reproduction.
      **Replay-recorded against the real Claude API, eight scenarios** (`packages/agents/src/personas/priya/replay/`) — six `dmReply` plus one `ticketDraft` and one `confirmingQuestion`, per `docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9, each grounded in a specific `prompt.md` commitment (treating a confident explanation as a claim to check, a passing test not being evidence unless it would've failed pre-fix, the severity-not-priority boundary, raising an uncertain finding without claiming personal reproduction, keeping disagreement aimed at the thing not the person, casual banter staying in character). Recorded twice — once before running `pnpm format`, once after (`prettier` rewrapped `prompt.md`'s lines, which changed its content hash and correctly failed all eight fixtures via the staleness gate, per decision 3/7) — with zero truncations or API failures across either pass. Three of the eight scenarios' own regex assertions false-failed against real (correct, in-character) model output during offline verification, across both recordings — not a persona-voice defect, a too-narrow-pattern defect each time: an early assertion only matched phrases like "would the tests have caught/failed," missing the real reply's actual "green before the fix" framing; another only matched "isn't my call," missing the real reply's "isn't mine to call"; a third, on the second recording, only matched a fixed list of check-oriented phrases, missing the real reply's genuine interrogative ("What was the actual root cause of the original timeout?"). All three broadened to match the underlying semantic signal — a genuine question or the actual before/after-the-fix framing — rather than one predicted phrasing, then reverified green.
      **`da-review` + `copilot-surrogate` dispatched together from the start on [PR #95](https://github.com/Pushedskydiver/moe/pull/95).** `copilot-surrogate` found 3 real MATERIAL findings, all in this very BUILD_PLAN entry's own first-draft prose describing this chunk — the review-saga's own self-documentation being a factual-claim surface in its own right, same as branch PP's finding on Riley's PR: a "9 of 10" arithmetic error against a paragraph that only names 9 distinct items; a Crispin quote that blended a genuine verbatim fragment with a paraphrase from the research doc's own analysis prose, presented as one continuous quotation; and a replay-fixture excerpt quoted as a full invented sentence that never appears in the actual recorded JSON. All three fixed against their real source (the paragraph's own enumeration, `voice-references.md`'s actual sourced quote, and the fixture file itself) and posted as a PR comment, per `docs/DA-REVIEW.md`'s own reporting-channel split. `da-review` found zero BLOCKING/MATERIAL findings on its own R1 pass (2 LOW/FYI, both pre-existing precedent shared across sibling prompts, deliberately left as-is). **A fresh R2 round (both agents, dispatched independently again) confirmed all three `copilot-surrogate` fixes genuinely landed and found zero new issues from that agent** — `da-review`'s own R2 pass, run as a genuine fresh full review rather than a check of the cited fixes alone, caught one real cross-cutting finding neither R1 pass surfaced: three TSDoc comments (`placeholder-system-prompt.ts`, `compose-ticket-draft.ts`, `fetch-persona-prompt-content.ts`) hardcoded "the 7 personas without a `prompt.md` yet," accurate only at Sarah's original 5.3a-ii PR and silently wrong through Maya's, Marcus's, and Riley's PRs before finally being caught here — reworded to count-agnostic phrasing and documented as a new `docs/REVIEW-PATTERNS.md` entry given the four-PR miss streak. Full quality suite re-verified green after every fix. **Merged.**
      **Deployed 2026-08-10** (`fly deploy -c fly.priya.toml --ha=false`, `registry.fly.io/moe-priya:deployment-01KZMYN7V0CQ8MDAXGXGHN9MK7`) — single App, no fleet-wide build-once-reuse pass needed since no other persona had pending changes. Verified: single Machine, `fly checks list` health passing with `{"status":"ok","personaId":"priya"}`, clean boot logs (`server started` → `github app credentials validated` → `slack socket mode connected`, no errors beyond the expected few-second health-check gap during the Machine restart itself). **Priya's real voice is now live in Slack.**
      **5.3f complete, 2026-08-15 — Dom's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/dom/prompt.md`, [PR #96](https://github.com/Pushedskydiver/moe/pull/96)). A dedicated deep-research pass (three parallel angles — real-world code-review craft/culture, competitor/AI reviewer-persona precedent, and failure modes specific to AI-assisted code review moe's existing generic mechanisms wouldn't catch) is banked at `.claude/research/reviewer-persona-landscape/landscape-survey.md`/`voice-references.md`. Its single strongest finding: **no competitor gives code review a standalone named character** — BMAD-METHOD (the same source that gave Marcus his Winston and Riley his Amelia) folds review into the Dev persona's own menu, and that review skill deliberately strips out severity/character voice; every product surveyed (CodeRabbit, Greptile, Qodo, GitHub Copilot review, Graphite) is either impersonal or carries a marketing-only mascot. Dom had more genuine design latitude than any prior persona — the real-world craft research (Google's own eng-practices guide, the Conventional Comments spec, the classic SmartBear/Cisco review-pace study, Bacchelli & Bird's Microsoft study) carried more weight than any AI-persona analog. The anti-pattern angle surfaced Dom's specific, non-obvious risk: reviewer sycophancy is measurably worse against _preemptive_ rebuttals (2.3× the regressive-sycophancy rate of in-context pushback, per SycEval) — exactly the "here's why this is fine" shape an implementer would front-load — and independently confirmed the plan-vs-build adjudication call (already pointed at Dom by Marcus's and Riley's shipped prompts, with no procedure yet given) is genuinely hard: a production drift-detection system scores near-human at the extremes but drops sharply on the ambiguous middle, the exact case Dom actually faces most. Voice references verified (`voice-references.md`): Michael Feathers (rigorous, mechanical design diagnosis — "look for the design problem" — paired with his own explicit, self-aware caution that "one of the worst things you can do as a developer is try to lecture your other developers on the team"), Michael Lynch (his widely-cited two-part essay "How to Do Code Reviews Like a Human" — reviewer-as-collaborator-not-gatekeeper, feedback as requests not commands), Paul Slaughter/Conventional Comments (a real, named spec for labeling blocking vs. non-blocking feedback — independently converged on by Google's own lighter "Nit:" convention). Neither name appears in the shipped prompt, same convention as every prior persona. Four judgment calls settled live with Alex via `AskUserQuestion` before drafting: register landed Feathers-led/Lynch-tempered (mechanical diagnosis as the primary voice, collaborative request-framing tempering delivery); the Conventional Comments label taxonomy stays absorbed judgment, not a named system (Alex deferred to evidence — sibling precedent never names a borrowed framework literally, and a documented practitioner critique found the full formal label set reads as cold to junior engineers); the plan-vs-build adjudication test combines a recorded-vs-silent filter with a blast-radius calibration, reusing Marcus's own established core-approach-vs-detail shape (Riley's own prompt hands this exact adjudication to Dom without establishing a version of the test himself — Alex deferred to evidence here too); and the reviewer-sycophancy risk is named explicitly in Dom's own voice, not left to Appendix C's generic patterns alone.
      **Closed with a full two-phase `spec-grill`** (four parallel R1 discovery angles: internal-consistency/quantifier sweep, cross-file grounding against VISION/sibling prompts/BUILD_PLAN, adversarial-scenario simulation, codebase claim-extraction). **Four BLOCKING findings, all fixed:** Personality's own first "don't-flex" invariant (approving only "real text" he'd actually seen) directly contradicted "What you can do today," which explicitly listed a mere description as one of three legitimate things reviewed — reworded around real text vs. a vague gloss, not narrowly diff-vs-description; the phrase "a real approve or request-changes" was used in Disagreement and declining as though it were a live capability, then flatly declared not to exist two sections later — reworded to stop implying a wired mechanism; Dom's prompt had imported Priya's Pattern-2 ("rephrase as a question") language wholesale in three places, despite Appendix C assigning his row (shared with Riley) only Patterns 1 and 4 plus universal 7 — removed, matching Riley's own Pattern-2-free precedent exactly; and, caught by the scenario-simulation angle, the plan-vs-build test's own core disambiguator cross-referenced "the same test Marcus and Riley already use" — but each persona's `prompt.md` is its _entire_ runtime system prompt (confirmed via `placeholder-system-prompt.ts`), so Dom never actually has access to that text — fixed by inlining a fully self-contained core-approach-vs-detail test directly into Dom's own prompt, with an explicit guard against treating "passes the plan's stated acceptance criteria" as proof of mere detail-level drift, the specific loophole the simulation round named. **Eleven further MATERIAL findings, all fixed:** (1) a "blocking" comment was asserted to carry real weight the rest of the prompt conceded didn't exist — reworded to name it as a communication signal, not an enforcement mechanism; (2) the scope of Dom's own deferred-to authority read differently across three separate passages — resolved by dropping the intro's governance-flavored "you decide, you don't defer" entirely; (3) a confident-explanation invariant escalated scrutiny from polish in one place while a separate "calibrate" paragraph said polish should never move scrutiny in _either_ direction — merged into one non-contradictory principle; (4) Voice's brevity rule read as conflicting with Disagreement and declining's required acknowledge-first structure — closed with an explicit cross-reference; (5) a "With Riley, during Build" subsection gave Dom a standing engagement stage neither VISION §3.3's lifecycle string nor Riley's/Marcus's own prompts corroborate — removed; (6) a deadline-pressure scenario was left un-operationalized despite the prompt's own stated deadline-invariance — closed with new, explicit language; (7) a bare-authority-claim scenario (someone asserting something's fine with no reason attached) was similarly un-operationalized — closed the same way; (8) the same-model-blind-spot guard named a real trigger but no concrete corrective action — given one; (9) a casual-banter-to-serious pivot wasn't bridged to the "needs real text" review discipline — closed with a connecting clause; (10) Triage voice was missing the "evidence-before-verdict works differently here" bridging paragraph four of five already-shipped sibling prompts carry; and (11), caught only after this PR was opened, Triage voice's own "the same discipline applies" referent turned out not to be uniform shared boilerplate at all — verified directly against all five already-shipped sibling prompts, it was byte-identical across three (Marcus/Priya/Maya) but already a shorter, more-ambiguous variant in a fourth (Riley), with Dom's own first-draft text a fifth distinct variant — realigned to the clearer three-sibling majority wording rather than left as an assumed-uniform convention nobody should touch unilaterally. **R2 verification, dispatched fresh, found the merged explanation-scrutiny paragraph still directly contradicted Personality's own invariant** (one said polish should trigger _more_ scrutiny, the other said polish shouldn't move scrutiny in _either_ direction) — precisely the kind of "claimed fixed, actually wasn't" gap this project's own history keeps naming. Fixed in the required manual pass (softened the invariant to a floor — "not a reason to look any less closely" — rather than a directional escalation) and confirmed consistent by one further narrow, independent dispatch rather than self-verified.
      **Replay-recorded against the real Claude API, eight scenarios** (`packages/agents/src/personas/dom/replay/`) — six `dmReply` plus one `ticketDraft` and one `confirmingQuestion`, per `docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9, several guarding fixes that landed specifically during this chunk's own spec-grill (the plan-vs-build acceptance-criteria loophole, deadline-invariance, the bare-authority-claim gap) rather than only the first-draft text. Two false-fails against genuinely correct model output, both fixed by broadening the assertion rather than the persona's own text (branch TT, now recurring a second time): one scenario's input naturally triggered `report_status` routing on a status-claim-shaped ask, leaving `dmReplyText` empty even though the tool call's own claim text was exactly the intended behavior (a plain declining-to-approve statement) — assertions rewritten to read whichever channel the model actually used; a second was a genuine assertion bug, not a model issue — a negation-blind regex flagged "not worth blocking on" as if it demanded blocking, because it only checked for "naming" and "blocking" co-occurring nearby rather than an actual withholding directive. The live-recorded transcripts held up well beyond what the assertions alone verify: asked about an unflagged memory-behavior swap that still passed every acceptance criterion, Dom independently reproduced the exact core-approach-vs-detail test from his own prompt, explained the concrete failure risk the swap introduced that the criteria wouldn't have caught (an OOM under load, not just a slower path to the same result), and wrote up a complete, decisive finding — sending it back rather than waving it through on passing criteria alone.
      **The first review round also found a third, more substantive replay-assertion gap**: the `favors-approving-not-a-gatekeeper` scenario's own input was a confident _description_ of a change rather than real code, which meant Dom correctly invoked his own real-text invariant and asked for the actual diff instead of rendering the "don't hold a good change hostage to a nitpick" verdict the scenario was built to guard — so the scenario had zero real coverage of the behavior its own name claimed, even though its permissive assertion happened to pass on an unrelated substring match. Fixed by giving the scenario real pasted code to evaluate (isolating the behavior from the real-text invariant it was accidentally triggering) and re-recording all eight scenarios fresh; two of the other seven, now recorded against genuinely different phrasing than the first pass, produced two more of the same false-fail-against-correct-output pattern (branch TT, now confirmed a third time) — broadened rather than the persona's own text touched. The two report-status-routing helper functions (`reportStatusClaim`, `replyOrStatusClaimText`) were extracted to their own file (`report-status-claim.ts`, with its own unit tests, closing a separate `da-review` R2 gap — the file had shipped with no coverage of its own single differentiating branch) to keep `scenarios.ts` under the repo's 300-line file cap — the negation-blind-regex fix itself stayed inline in its own scenario's assertion. **Promoted to `packages/agents/src/persona-replay/reply-or-status-claim-text.ts` in 5.3g below**, once Priya's own scenarios needed the identical fallback — this paragraph's file name is what shipped at the time, not where the helper lives today. **A third re-recording pass, triggered by an unrelated fix, surfaced branch TT a fourth time**: aligning Dom's own Triage-voice Low-band wording to the sibling majority (below) required a full re-record, which this time routed the deadline-pressure scenario through `report_status` rather than free prose — assertions broadened to the same reply-or-status-claim helper already used for the earlier report_status case. **A further review round, both agents dispatched fresh again, found two more real findings in this very paragraph — a quote that had been accurate when an earlier fix wrote it and had since gone stale after this section's own re-recording pass, and two sibling-attribution overclaims (Riley's prompt hands the plan-vs-build adjudication to Dom without establishing his own version of the core-approach-vs-detail test; Priya's prompt has no plan-vs-build adjudication language at all) — fixed without further re-recording, and one final fresh round of both agents came back clean on the whole PR. All four review rounds ran before the PR was ever opened, catching every finding above pre-merge.** **Merged as [PR #96](https://github.com/Pushedskydiver/moe/pull/96).**
      **Deployed 2026-08-15** (`fly deploy -c fly.dom.toml --ha=false`, `registry.fly.io/moe-dom:deployment-01M01SN394RJPME6BRAQGE9BP5`) — single App, no fleet-wide build-once-reuse pass needed since no other persona had pending changes. Verified: single Machine, `fly checks list` health passing with `{"status":"ok","personaId":"dom"}`, clean boot logs (`server started` → `github app credentials validated` → `slack socket mode connected`, no errors beyond the expected few-second health-check gap during the Machine restart itself). **Dom's real voice is now live in Slack.**
      **5.3g complete, 2026-08-15 — Theo's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/theo/prompt.md`). Alex chose Theo over Nia when asked which 5.3 sub-chunk to pick up next — nothing pins the order. A dedicated deep-research pass (four parallel angles — real-world research/analyst craft, competitor/AI research-persona precedent, failure modes specific to AI-assisted research and citation work, and voice-reference verification) is banked at `.claude/research/researcher-persona-landscape/landscape-survey.md`/`voice-references.md`. Its single strongest finding: **a named Researcher persona in a PM/Architect/Engineer-shaped team is precedented, unlike Dom's own research** — MetaGPT ships a standalone, hardcoded "David" (Researcher) in a cast structurally identical to moe's, and BMAD-METHOD's Mary/Deep Recon split gives Theo's actual differentiator its sharpest anchor: per-claim confidence tiers (`high`/`medium`/`low`/`unverified`) and an explicit "never conclude from training data alone" discipline, more sophisticated than any AI-persona confidence framework found for any prior persona. The anti-pattern angle grounded Theo's Appendix C layering directly — he's assigned only Patterns 5 (calibrated confidence) and 6 (intellectual integrity), the lightest of any shipped persona, because his whole domain is already citation-native; a 2025 _Science_ paper (Cheng et al.) gave the sharpest evidence for why Pattern 6 matters here specifically (sycophantic AI responses measurably increase a user's own conviction while decreasing responsibility-taking, even as users rate the sycophantic response higher quality). Voice references verified (`voice-references.md`): Philip Tetlock (calibration discipline, the Good Judgment Project's own measured accuracy of superforecasters' stated confidence), Tim Harford (a direct primary-source fetch — hedged, plain-language statistical reasoning), Bethany McLean (bounded skepticism, verified against her own 2001 Enron reporting), and Julia Galef considered alongside them; Zeynep Tufekci researched and explicitly excluded after finding a real, documented citation-accuracy dispute in her own 2023 reporting. Neither name appears in the shipped prompt, same convention as every prior persona. Four judgment calls settled live with Alex via `AskUserQuestion` before drafting: register lands as a blend (Tetlock's calibration discipline as the backbone, Harford's plain language for delivery, McLean's bounded skepticism for handling what he can't verify); confidence format is a named tier plus a rough number in parenthesis, not a bare percentage or tier-only; the independent-corroboration test is literal and named in his own voice, disputed-source handling stays absorbed judgment; and the capability gap (no live web-search/citation-fetch tool exists yet — chunk 6.0's tool-allowlist gate is unbuilt) is named plainly alongside the aspiration for what closes it once that gate lands, mirroring Maya's own honest-gap precedent.
      **Closed with a full two-phase `spec-grill`** — four parallel R1 discovery angles (internal-consistency/quantifier sweep, cross-file grounding against VISION/sibling prompts/BUILD_PLAN, adversarial-scenario simulation, codebase claim-extraction) converged independently on the same two BLOCKING findings: Theo's Disagreement section had leaked Pattern 2's ("rephrase as a question") and Pattern 4's ("citation-required disagreement") canonical phrasing wholesale from sibling prompts, despite his own Appendix C row assigning him neither — removed and reworded to derive visibly from his own Research-philosophy language instead. Four further MATERIAL findings, all fixed: "low confidence" was doing double duty as both his own defined tag and informal shorthand for the classifier's Low band; the "open-ended research ask" rule and the "High band, draft immediately" rule had no reconciling sentence for a message that's both; two codebase-claim inaccuracies traced to shared Triage-voice boilerplate rather than anything specific to Theo's own draft (see the fleet-wide fix, below). **R2 verification, dispatched fresh, confirmed all fixes landed with zero new findings** — a genuine, bounded-fold convergence in exactly two rounds.
      **A spec-grill finding turned into a fleet-wide fix, authorized separately by Alex.** The codebase-claim-extraction angle found two real factual inaccuracies in the shared "Triage voice" boilerplate paragraph — copied near-verbatim into all six already-shipped sibling prompts, not something Theo's own draft introduced: a wrong file citation for where the confidence _band_ (as opposed to the raw score) is actually computed, and a High-band "lead with the action, cite the specific line(s)" description that `compose-ticket-draft.ts`'s own fixed system prompt structurally cannot produce (its output is a plain `{title, body}` restatement, no field for narration). Initially flagged as a follow-up task rather than fixed inline; **Alex asked for it to be folded into this same PR instead.** Fixed identically across all seven prompts — Sarah's own copy of the second fix was a removal rather than a duplicated sentence, since her paragraph already described plain restatement in different words. Both fixes triggered a full fleet-wide replay re-recording (Sarah, Maya, Marcus, Riley, Priya, Dom, Theo — the staleness gate correctly failed all six previously-shipped personas' replay tests immediately on the hash mismatch, the first real trigger of that gate against an edit to already-shipped prompts, not just a fresh persona addition — see `docs/REVIEW-PATTERNS.md`).
      **Replay-recorded against the real Claude API, eight scenarios** (`packages/agents/src/personas/theo/replay/`) — six `dmReply` plus one `ticketDraft` and one `confirmingQuestion`, per `docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9. The fleet-wide re-recording surfaced branch TT repeatedly, across Theo's own new scenarios and several already-shipped siblings' pre-existing ones (Marcus's, Riley's, Dom's) — genuinely correct, differently-phrased model output false-failing narrow assertions, broadened against the real transcripts each time, never the persona text.
      **The review gate became this chunk's own dominant story: eight full rounds of `da-review` + `copilot-surrogate`, the deepest review saga in this project's history.** Rounds 1–4 found and fixed substantive issues: an overclaim in this same `docs/REVIEW-PATTERNS.md` write-up, real test-permissiveness regressions in the fleet-wide re-recording's own assertion fixes, and — the architecturally significant finding — the negation-guard technique used to fix those regressions was itself unscoped, letting an unrelated hedge anywhere in a reply mask a genuine claim. Closed by extracting a shared `hasSentenceScopedMatch` helper (`packages/agents/src/persona-replay/sentence-scoped-match.ts`, now used across all seven personas' own scenario files — Riley's, Marcus's, Dom's ×2, Sarah's, Maya's, Priya's, and five of Theo's own assertions) and explicitly documenting one genuinely irreducible compound-sentence edge case as an accepted, tested limitation rather than chasing it with more regex machinery. Rounds 5–7 kept finding narrower instances of the identical class recurring in the fix's own follow-on rounds — most sharply in two of Priya's own assertions, where successive fold rounds each added a new regex alternative that closed one adversarial construction while reopening a different one nearby — until round 7 switched philosophy from patching to subtracting the alternatives that weren't actually load-bearing for the real recorded transcripts, which held. **Asked directly after round 7 whether to keep dispatching until genuinely clean or treat the current (green, narrowing) state as sufficient, Alex chose one final round regardless of outcome.** Round 8 found two more narrow, real, test-only-code gaps; one was fixed cleanly, the other — a residual gap in Priya's own "declines the call" check that would need a second signal (whether she _also_ renders a verdict elsewhere) to close properly, not another regex alternative — was deliberately documented in place rather than risking a ninth patch-introduces-new-gap cycle, per da-review's own read that it doesn't block the diff from being a genuine net improvement.
      **`docs/REVIEW-PATTERNS.md` gained a new entry** ("A factual claim in shared persona-prompt boilerplate goes stale silently across the whole fleet") documenting the Triage-voice inaccuracy as its own pattern, and the "Recorded-transcript drift" entry recorded its first real trigger against a post-5.4 edit to already-shipped prompts, working exactly as designed. `docs/PERSONAS.md`'s Marcus and Riley rows, found stale during the review saga (still marked "not yet deployed" five days after they were), were corrected in the same pass. A pre-existing, unrelated stale section citation in Marcus's own scenario file (from PR #93, chunk 5.4, not this chunk) was flagged as a follow-up rather than fixed inline. **Merged as [PR #97](https://github.com/Pushedskydiver/moe/pull/97).**
      **Deployed 2026-08-18** (`fly deploy -c fly.theo.toml --ha=false`, `registry.fly.io/moe-theo:deployment-01M0AC7DG40FE5FA5EAPG4BTSC`) — verified: single Machine, `fly checks list` health passing with `{"status":"ok","personaId":"theo"}`, clean boot logs (`server started` → `github app credentials validated` → `slack socket mode connected`, no errors beyond the expected few-second health-check gap during the Machine restart itself). **Unlike every prior 5.3 sub-chunk, Theo wasn't the only persona with a pending prompt change this time** — the fleet-wide Triage-voice fix above edited Sarah's, Maya's, Marcus's, Riley's, Priya's, and Dom's already-shipped `prompt.md` too, and all six were still serving pre-merge images (`fly apps list`'s own `LATEST DEPLOY` column predated the merge for each). Caught before treating the deploy as done, not after. Built once (`fly deploy -c fly.sarah.toml --build-only --push`) and redeployed that same tag to all seven Apps — the first time the build-once-reuse pattern has redeployed an already-live fleet rather than rolled out a first-time one. All seven independently verified: exactly one Machine each, no standby, health passing with the correct `personaId`. **Theo's real voice is now live in Slack, and the whole fleet is back in sync with `main`.**
      **5.3h complete, 2026-08-20 — Nia's `prompt.md` co-drafted live with Alex** (`packages/agents/src/personas/nia/prompt.md`). A dedicated deep-research pass (four parallel angles — real-world Scrum Master/Agile-facilitation craft, competitor/AI facilitator-persona precedent, failure modes specific to AI facilitation/status-reporting/authority-exercising work, and voice-reference verification) is banked at `.claude/research/scrum-master-persona-landscape/landscape-survey.md`/`voice-references.md`. Its most distinctive finding, unlike any prior persona's competitor research: BMAD-METHOD shipped a literal, standalone "Bob, Scrum Master" agent in an Agile-flavored AI dev-team cast — then removed it in a later major version, folding sprint-planning into the Developer role instead. A real precedent, but a self-reversed one, not an industry pattern to follow. The failure-mode angle anchored two real, load-bearing findings: BetterUp/Stanford's "workslop" research (_HBR_, Sept 2025 — AI-generated content that "masquerades as good work" but lacks substance, directly grounding moe's own "felt scripted" complaint about the previous ceremony attempt) and a controlled study finding managers followed an algorithm's harsh rating only ~42% of the time versus ~60% for a lenient one — the sharpest available evidence for how Nia's real demotion lever could fail even once built. Voice references verified (`voice-references.md`): Lyssa Adkins (soft authority carried through stated belief, not command — the strongest single match), Esther Derby & Diana Larsen (the real source of moe's own "five-round retro" language, plain and dryly deflating of false ease), with Henrik Kniberg and Linda Rising kept as thinner secondary texture rather than primary anchors. Neither name appears in the shipped prompt, same convention as every prior persona. Four judgment calls settled live with Alex via `AskUserQuestion` before drafting, all recommended options chosen: voice leads with Adkins, Derby/Larsen for structural plainness; the capability gap — by far the largest of any persona, since every one of Nia's signature ceremonial functions (retro, EOD digest, monthly review, demotion lever, stall detection, unactioned-message flagging) is still unbuilt (BUILD_PLAN 6.5b/6.6/6.7c/7.4a-i/7.4a-ii/7.5a/7.5b/7.6 all unchecked) — gets full, plain naming in a dedicated "What you can do today" section rather than a lighter touch; the demotion-lever's voice is literal and specific (name the fact pattern, once, no drama), not left as absorbed judgment; and the anti-ceremony-theater discipline (guarding against hollow, smoothed-over retro/digest synthesis) is named explicitly rather than left to the universal Pattern 6 alone.
      **Closed a three-round `spec-grill`** — four parallel R1 discovery angles (internal-consistency/quantifier sweep, cross-file grounding against VISION/sibling prompts/BUILD_PLAN, adversarial-scenario simulation, codebase claim-extraction) converged on real findings, most sharply two independent angles finding the same BLOCKING issue: the tier-drop lever's Voice-section worked example read as a live, present-tense enactment of an authority Nia doesn't have. Fixed by making the example hypothetical throughout ("I'd drop him to Tier 2," not "dropping it to Tier 2") and reframing the whole passage around a stated opinion rather than a performed action. **R2 verification, dispatched fresh, caught a real over-correction the R1 fold had introduced** — a claim that "an automatic, directory-scoped version already runs in code today" for the demotion mechanism, which R2 verified is false: `classifyRiskTier` (`packages/core/src/risk-tier.ts`) has zero real call sites anywhere outside its own test file, and the chunks that would wire it (6.5a-i, 6.5b) are both unbuilt. Fixed by describing the classifier as real but inert ("nothing calls it yet") and switching the flagship worked example from directory-scoped to persona-scoped, matching VISION §8.2's actual "drop a persona a tier" framing. **R3, a narrower targeted re-verification of just that fix, came back clean.**
      **Replay-recorded against the real Claude API, eight scenarios** (`packages/agents/src/personas/nia/replay/`) — six `dmReply` plus one `ticketDraft` and one `confirmingQuestion`, per `docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9. All eight passed on the first recording — the tier-drop and anti-hollow-synthesis transcripts in particular came back as genuinely strong, in-character output (the shipped tier-drop transcript states the hypothetical plainly — "I'd make that call... consider this me agreeing with the call, not me executing it" — and the shipped mixed-feedback transcript correctly refuses to manufacture fake friction or smooth over Priya's real one). Re-recorded twice more after later prompt edits, per the staleness gate; all eight stayed green each time — this description reflects the fixtures actually at HEAD, not an earlier, superseded recording.
      **The review gate ran four rounds, all `da-review` + `copilot-surrogate` dispatched together, worktree-isolated, via the `Workflow` tool.** Round 1: `copilot-surrogate` came back clean; `da-review` found a real MATERIAL bug in the replay harness itself — the `noticing-names-fact-not-diagnosis` scenario's negation regex used a bare, unbounded `no` that silently matched inside ordinary words ("know," "cannot"), verified empirically to defeat the assertion, fixed by bounding it — plus two LOW findings (a stale `'nia'` placeholder-persona id left in `placeholder-system-prompt.test.ts`'s own fix, inconsistent with the other two touched test files; and the prompt's honest-gap section naming four of Nia's five unbuilt ceremonial functions but omitting unactioned-message flagging). Round 2: both agents dispatched fresh **independently converged on the same MATERIAL finding** — the prompt's "What you can do today" section named the monthly review as unbuilt in one paragraph but silently dropped it from the very next paragraph's concrete decline-instruction list, an intra-file schema-pair drift present since the original draft that R1's fold hadn't caught. `copilot-surrogate` additionally found two real documentation-completeness gaps: this BUILD_PLAN entry didn't exist yet, breaking every prior 5.3 sub-chunk's own precedent of landing in the same PR as the prompt; and `docs/REVIEW-PATTERNS.md`'s "Persona-prompt drift" pattern still asserted "Nia has no scenarios yet," now false. Round 3, explicitly briefed to also audit this same BUILD_PLAN entry's own prose (not just the code) per this project's own "review-saga self-documentation is itself a factual-claim surface" discipline: `da-review` came back clean, with two explicitly non-blocking LOW notes (the casual-banter scenario's assertion is more permissive than its stated intent — matching every sibling persona's identical assertion shape, not a Nia-specific deviation, left as-is; and an untested-but-not-proven-broken ambiguity between the acknowledge-first disagreement rule and the capability-gap decline framing — the real recorded transcript resolves it gracefully). `copilot-surrogate` found one real MATERIAL citation error in this very paragraph's first draft — it cited chunk 7.3b (Sarah's own replenishment ceremony) as one of Nia's unbuilt functions; the correct citation is 7.5a (monthly-review section builders). Fixed. Round 4, dispatched fresh once more, came back genuinely clean on both agents — `da-review` found zero findings; `copilot-surrogate` found one further non-blocking LOW (an illustrative quote in this very paragraph's own earlier draft described a detail from the first replay recording that a later re-recording, triggered by the R1/R2 prompt edits, had since superseded — the underlying claim was real but no longer matched the fixture actually at HEAD, fixed by quoting the current transcripts instead). **Merged as [PR #98](https://github.com/Pushedskydiver/moe/pull/98).**
      **Deployed 2026-08-28** (`fly deploy -c fly.nia.toml --ha=false`, `registry.fly.io/moe-nia:deployment-01M14EFWFP8NZ61P15G8KZE38J`) — single App, no fleet-wide build-once-reuse pass needed since this chunk didn't touch any sibling prompt.md. Verified: single Machine, `fly checks list` health passing with `{"status":"ok","personaId":"nia"}`, clean boot logs (`server started` → `github app credentials validated` → `slack socket mode connected`, no errors beyond the expected few-second health-check gap during the Machine restart itself). **Nia's real voice is now live in Slack — all 8 roster personas have their real `prompt.md` deployed.**
      **5.3 itself is now complete, and with it Stage 5's own exit criterion** ("all 8 personas from the cast redline run as separate processes with separate Slack apps; each has a distinct, Alex-approved voice; a DM to any of them gets an in-character response") — verified directly: `fly apps list` shows all 8 Apps deployed, every persona has a real, Alex-approved `prompt.md`, and the generic 5.3a-ii wiring (`buildPersonaSystemPrompt`/`composeTicketDraft`/the confirming-question path) already gives every persona the same in-character DM response, no per-persona code needed beyond the prompt itself. Per the gating rule (line 25), Stage 6 can now formally begin.
- [x] **5.3a-ii — Wire real persona prompts into all three cascade call sites, plus prompt caching.** Filed 2026-07-30 during Sarah's own prompt-drafting session. **Corrected same day, caught by `spec-grill`'s R1 discovery pass, which checked the actual wiring code rather than trusting this session's own earlier claim:** the original version of this bullet said `prompt.md` has one live consumer today (`buildPersonaSystemPrompt`, `generate-and-post-reply.ts`'s DM chat-reply path). That's wrong — `buildPersonaSystemPrompt` (`packages/agents/src/placeholder-system-prompt.ts`) is a real, live call site, but it's still the unchanged Stage-2 placeholder: a hardcoded, persona-agnostic string that never reads `prompt.md` at all, exactly as its own TSDoc says ("Still deliberately withholds actual character... since that's Stage 5 behind the do-not-touch gate"). **All three call sites are unwired, not one-of-three** — `composeTicketDraft` (`packages/agents`) uses its own fixed, persona-agnostic `DRAFT_SYSTEM_PROMPT`, and the Mid-band confirming question (`compose-and-post-confirming-question.ts`) is a hardcoded template string, not an LLM call at all (a deliberate 3.4b-i decision to avoid a new billed call site). BUILD_PLAN 3.7's own note already commits to the intent ("voice affects how a draft is phrased"); this chunk is what actually wires it — loading each persona's real `prompt.md` into `buildPersonaSystemPrompt` and `composeTicketDraft`'s system prompt, and converting the confirming-question path from a fixed template to a real, persona-voiced LLM call. Sequence after every persona whose voice should land this way is drafted, or wire incrementally per persona — Alex's call when this chunk is picked up.
      **VISION §6.7 (AI-transparency/external-facing posture) is deliberately out of scope for Sarah's `prompt.md` itself**, noted here rather than left silent per `spec-grill`'s own LOW finding — Sarah has no external-facing posting surface wired to her today. **Corrected during `spec-grill`'s own R2 verification pass, same day:** the first version of this note claimed `composeExternalPostBody` (BUILD_PLAN 4.4a) "has no live consumer yet" — wrong, and wrong the same way the call-site-count claim above was, by trusting 4.4a's own now-stale text instead of checking 4.4b immediately below it. `composeExternalPostBody` has had a real consumer since 4.4b landed: `create-github-issues-for-tickets.ts`, a manually-triggered CLI script (`create:github-issues`). The actual reason this is out of scope for Sarah's prompt stands regardless — that CLI script isn't invoked by any persona, so no persona has an external-facing posting surface wired to it yet. Revisit when a persona's own external-posting tool is actually wired, whichever chunk that turns out to be. (4.4a's own entry text is separately stale on this same point — noticed, not fixed here, since it's a different, already-shipped chunk's documentation, out of scope for this correction.)
      **Prompt-caching gap, folded in 2026-07-30.** Checked during the same session: no call site anywhere in `packages/agents`/`apps/server` sets `cache_control` on a system-prompt block, so every persona prompt is reprocessed at full token cost on every single turn. Anthropic's own current docs confirm `cache_control` cuts both cost (cache reads run at 0.1× base input price) and latency (reduces time-to-first-token), and actively encourage large static system-prompt content used _with_ caching — not a design smell to avoid. Wire `cache_control: {type: 'ephemeral'}` onto each persona's system-prompt block across all live call sites (`generate-and-post-reply.ts` today; `composeTicketDraft`/the confirming-question path once this chunk's own first half wires them) — a single, once-per-persona-prompt-block change, not per-call-site-specific logic.
      **5.3a-ii complete, 2026-08-01** — shipped as three PRs ([#84](https://github.com/Pushedskydiver/moe/pull/84), [#85](https://github.com/Pushedskydiver/moe/pull/85), [#86](https://github.com/Pushedskydiver/moe/pull/86)), split per `docs/DEVELOPMENT.md`'s own ~300-LOC Task Sizing signal rather than landing as one diff. All three cascade call sites now load a persona's real `prompt.md` when one exists (`fetchPersonaPromptContent`), falling back unchanged to the pre-existing generic template for the 7 personas without one — each future 5.3 sub-chunk lights up automatically the day its own prompt merges, no further wiring. Prompt caching wired via a shared `buildCachedSystemBlocks` primitive, with `cache_control` on the **last** static block rather than the persona block as this entry's own text above suggests: both segments are static per (persona, call-type), so marking the last one caches the whole prefix instead of leaving task instructions reprocessed at full price forever. The Mid-band confirming question became a real, persona-voiced LLM call (`composeConfirmingQuestionLeadIn`) with the reaction mechanic kept as a fixed, code-controlled trailer, so the 👍/👎 affordance can never drift from what the model happened to say; it falls back to the exact pre-5.3a-ii fixed template on any compose failure or cost-cap halt.
      **Two real defects found that this chunk's own filed scope never anticipated.** (1) A **cost-accounting gap**: the SDK returns cache spend on `cache_creation_input_tokens`/`cache_read_input_tokens`, separate from `input_tokens` — every usage-mapping site and `sonnetCostUsdMicros` read only the latter, so wiring `cache_control` would have made the persona-prompt tokens (the majority of every call's input, and the whole point of caching) silently vanish from the monthly cost cap. Fixed in the same PR that added the first `cache_control`, not deferred. (2) A **build/deploy gap**: `packages/agents` builds with plain `tsc`, which never copies `.md` into `dist/` — `prompt.md` only reached the deployed image because the root `Dockerfile` happens to `COPY` all of `packages/`, contradicting that package's own `"files": ["dist"]`. A future Dockerfile slimming would have broken persona loading in production with no test or CI signal. Fixed by copying `src/personas` into `dist/personas` at build time, with a `dist/`-importing integration test as the CI-visible tripwire (verified to actually fail when the copy step is removed).
      **The chunk's sharpest lesson was a misdiagnosis, not a feature.** DA review found the new confirming-question call site broke the model's structured output near-100% of the time on a message combining a quote character with classifier-field-label-shaped text. Three passes — DA, its own fresh-context R2, and Claude — independently confirmed the _symptom_ and its failure rate, and all three converged on the same wrong _cause_ (JSON escaping / delimiter spoofing), concluding it was unfixable by prompt construction and shipping it as a documented accepted risk. Alex asked whether it was worth fixing; testing four alternative constructions (two made it _worse_) forced capturing `stop_reason`/`output_tokens`, which answered it instantly: **`MAX_TOKENS` truncation**. Adversarial input provokes heavy extended thinking, thinking counts against `max_tokens`, and at 256 the entire budget went to a `thinking` block with no JSON emitted at all. Raising the ceiling fixed it completely (12/12 across four input shapes, driven through the real exported function from built `dist/`). Sibling exposure was live-checked rather than assumed — `composeTicketDraft`'s 512 sits comfortably clear — so this is a per-task ceiling, not a repo-wide constant. Durable lesson, recorded at the call site: `stop_reason`/`output_tokens` distinguish truncation from malformed output in one call, so check them before theorising about escaping — and adversarial-robustness testing that only ever asserts "it failed safely" can confirm a symptom indefinitely without ever finding its cause.
- [x] **5.4 — Persona-replay test harness.** Record/replay infrastructure for persona prompts (the v2 lesson: replays catch prompt↔schema drift that synthetic tests can't). Built once, used by every 5.3 sub-chunk from the second persona on. **Scope deliberately widened from this line's own literal "backfill the first persona's recording"**: by the time this chunk was picked up, three personas (Sarah, Maya, Marcus) all had real prompts with zero persisted replay coverage (`docs/decisions/PERSONA-REPLAY-HARNESS.md` decision 9 records the reasoning) — backfilled all three in the same chunk instead of only gating new ones.
      **Built:** a committed-JSON-fixture harness (`packages/agents/src/persona-replay/`), a manual recording script hitting the real Anthropic API (`pnpm --filter @moe/agents record:replay -- <personaId>`), and a network-free `persona-replay.test.ts` per persona wired into `pnpm test` (CI-blocking, no live-network job needed). A fixture's stored content-hashes of `prompt.md` and the scenario's own input, plus its resolved model id, must all still match at verification time — any drift fails the replay test by name, not silently. Full design (and two real defects found during implementation — a `cp -r` build-script bug that silently broke every persona's `prompt.md` path once the harness's own `.ts` files started compiling under `dist/personas/`, and `createAnthropicClient`'s 20s live-reply timeout genuinely timing out a batch recording) in `docs/decisions/PERSONA-REPLAY-HARNESS.md`.
      21 scenarios recorded live against the real API (7 per persona: 5–6 covering that persona's load-bearing behavioral commitments plus one `ticketDraft` and one `confirmingQuestion` for structured-output schema-drift coverage) — including a permanent regression fixture for Maya's own PR #91 banter-honesty fix. `da-review` + `copilot-surrogate` dispatched together found 2 + 4 MATERIAL respectively, all folded and R2-confirmed; one scenario (Marcus's `calibrated-ambiguity-names-and-proceeds`) went through seven total review rounds, each finding a real, progressively narrower false-pass in its assertion logic, converging on a bounded, principled opening-scoped design rather than continued keyword patching — full saga in that scenario file's own header comment. A third `copilot-surrogate` dispatch covered the PR's own final doc-update commits (a gap the first two dispatches, run before those commits existed, couldn't have covered) and found 2 more Material + 4 Low, folded. **Merged 2026-08-09** ([PR #93](https://github.com/Pushedskydiver/moe/pull/93)). No deploy action needed — nothing in `apps/server`'s runtime path changed; `createAnthropicClient`'s new `timeoutMs` param defaults to the prior behavior for every production call site.
- [ ] **5.5 — Welcome ritual.** How the team welcomes each newly-activated persona in `#moe-team` — an open question in VISION §4.1, not a settled mechanic, so this chunk opens by settling the format with Alex (its own mini-gate; default proposal: one thread per join), then implements it. Small, pure team-feel, cheap.
- [ ] **5.6 — [GATE] Conversational channel presence (banter).** VISION promises this and no chunk builds it. §6.1 lists `#moe-random` as a real channel ("Non-work banter") and `#moe-team` as carrying "banter" alongside digests and work updates; §13.1 item 3 names banter as one of the cross-persona dynamics the whole cast was stood up together to test ("standing the cast up together for the dynamics (banter, disagreement, handoffs) that actually need more than one persona to test"). But **chunk 3.3 made conversational replies DMs-only** — an ambient channel message is classified and may yield a ticket draft, never a chat reply — and every channel-posting chunk on the plan (5.5 welcome ritual, 6.7a incident posts, 7.6 EOD digest) posts something _structured and proactive_, not conversation. So a persona currently cannot talk in a channel at all. Discovered 2026-07-25 when Alex asked whether the team would respond in `#moe-random`.
      **This is a [GATE] because the naive implementation is actively bad.** Eight personas each replying to every message in a shared channel is eight bots talking over each other and over Alex — worse than silence, and it would land squarely on VISION §5.4's trust-erosion rule. Opens by settling with Alex, its own mini-gate like 5.5's: what triggers a conversational reply in a channel at all (addressed-only? a probability? one designated responder per thread?), how many personas may respond to one message, and what rate limit or cooldown applies. Options exist but this is an Alex call, not something to invent.
      **Do not implement by adding `#moe-random` to `MOE_WORK_RELEVANT_CHANNEL_IDS`.** That variable gates the _work-intake_ pipeline (Stage 0 → classifier → draft); putting the banter channel in it would feed jokes to the ticket classifier, which is exactly what VISION §5.2's Stage 0 exists to prevent. A social path bypasses that pipeline rather than joining it.
      **Sequencing: after 5.3.** Banter from eight identical placeholder prompts proves nothing about whether the dynamic works — the behaviour only becomes evaluable once personas have real, distinct voices. Also worth landing after **5.2a**, which settles the adjacent question of what a persona does with a channel message it _isn't_ the intake listener for.

## Stage 6 — Team behaviour: handoffs, review, merge

**Exit criterion:** a ticket flows Brief → Plan → Build → Review → merged-to-chief-clancy across at least three different personas, with the risk-tier gate deciding the merge path, without Alex driving. The proving ticket is expected to take the Tier 1 path — Dom's 6.3b approval satisfies the fast single-approve, then the merge is automatic; Alex having flipped the 6.5a-ii enforcement switch beforehand doesn't count as driving.

- [x] **6.0 — [GATE] Per-persona tool allowlist grid.** Write the grid VISION §11 delegates here ("re-specified in BUILD_PLAN.md rather than reproduced here") with Alex, as config + a doc: each persona's tool allowlist, the CLI-vs-MCP decision rule applied per tool, and the curated-allowlist supply-chain hygiene rules. Enforced via each persona's model-client tool config (inline Messages-API tool definitions for chat turns; the Agent SDK's tool allowlist for Riley's 6.2 agentic sessions), and by 6.2's sandbox for Riley specifically. This chunk is the delegation target of VISION §11's pointer. **This gate also settles who can satisfy Tier 3's named-owner review:** VISION §8.1 excludes both the requesting persona and the human who dispatched the work, and with one human on the team who dispatches most of it, that exclusion needs an explicit Alex answer (see 6.5a-i).
      **Settled 2026-08-28 — `docs/decisions/TOOL-ALLOWLIST-GRID.md`.** A codebase audit before drafting found this chunk is prospective, not a retrofit: no MCP server exists anywhere in the repo today, and exactly one tool (`report_status`) is ever exposed to any persona's own LLM calls — every other integration (Slack, GitHub, the DB) is direct code the model never invokes. Four positions settled live with Alex via `AskUserQuestion`, all recommended options chosen: **CLI/direct-SDK integration is the default, MCP only when a future service exposes no direct API** — grounded in a real finding (Stacklok's own security analysis) that MCP's actual advantage is crossing a multi-user trust boundary, which doesn't exist on moe's one-operator, one-shared-GitHub-App shape; **Riley's Agent SDK sandbox (6.2) gets no network access** — hermetic, dependencies pre-installed before a session starts, matching OWASP's "Excessive Agency" mitigation and Claude Code's own default-deny network-access model most directly; **Tier 3's named-owner review can be satisfied by any persona other than the requester, plus Alex's own sign-off in addition whenever he's the one who dispatched the work** — resolving 6.5a-i's own explicit dependency on this gate; and **a single, explicit, committed allowlist table is the source of truth for every persona's tool grants**, changed only via a PR touching that table directly. The grid itself specifies tools that don't exist in code yet — Theo's web-search/citation-fetch (likely Anthropic's own native web-search tool type, the lightest possible integration), Dom's PR-diff-fetch/review-post (`octokit`, matching every existing GitHub capability's own pattern), Maya's render/screenshot tool (direct — v0's Platform API, a plain bearer-token REST call with no OAuth broker, per the already-banked `design-tooling-mechanism.md` research; only whether v0 remains the right/available vendor choice by her own tooling chunk is open, not the integration shape), and a generic cross-persona "react" tool flagged at 6.1d — plus the supply-chain hygiene rules (scope minimization, no wildcard grants, tool responses treated as untrusted input the same way Slack/GitHub content already is). `docs/GLOSSARY.md`'s MCP entry and `docs/decisions/README.md`'s document table updated to point at the new ADR.
- [ ] **6.1a-i — Pull loop + atomic claim.** Per-persona poll scheduler, persona→stage eligibility mapping, atomic claim (1.3), with a stub no-op work step that releases the ticket back — the loop wakes, claims, "works", releases, and is integration-testable with fake work. Respects the 2.7a operating-rhythm guard. **Still owes:** `docs/decisions/BOARD-AND-CAPACITY-MODEL.md`'s Decision 2 promises Expedite "jumps ahead of Standard work within its board status" — a queue-_ordering_ behavior, distinct from 4.5's WIP-_cap_ guard (`evaluateWipLimit`). Flagged during 4.5's own architectural pass: no chunk currently claims this candidate-selection logic explicitly. This is the chunk that picks which ticket to claim next, so it's the natural (though not yet built) home for it.
      **Also owes the _assignment_ half, added 2026-07-25.** This chunk already flags Expedite's queue-_ordering_ as unclaimed, but nothing anywhere assigns Expedite in the first place: `reaction-outcome-actions.ts` hardcodes `DEFAULT_SEVERITY = 'Medium'` and `DEFAULT_CLASS_OF_SERVICE = 'Standard'`, and no code path sets any other value. So `docs/decisions/BOARD-AND-CAPACITY-MODEL.md`'s rule ("Expedite = anything from `#moe-incidents` or `severity: 'Critical'`") has no producer, migration `0016`'s column is permanently single-valued, and 4.5's shipped `evaluateWipLimit` model is inert. Both halves belong together — ordering has nothing to order until assignment exists. Note VISION §5.4's trust-erosion rule keeps severity assignment off the LLM layer, so this wants a deterministic source-channel/severity rule, not a prompt.
- [ ] **6.1a-ii — Stage transitions + WIP gate.** The transition + clear-ownership state machine — who moves a ticket forward, when ownership clears, what happens on failure mid-stage — plus WIP-gate wiring (4.5's guard gets its call site here). The Sarah-handoff mechanism from VISION §5.3 (DB-write, poll-claim) falls out of this for free.
- [ ] **6.1b — Brief-stage handler.** What Sarah actually does on claiming a Brief ticket: the LLM triage/brief work and its output artifact, replacing the 6.1a-i stub for that stage. Includes converting 4.2's triage-queue entries into board tickets — the persona half Stage 4's exit criterion deliberately left here.
- [ ] **6.1c — Plan-stage handler.** Marcus's plan work and artifact on claiming a Plan ticket. (Build and Review handlers are 6.2/6.3.)
- [ ] **6.1d — Reaction semantics on briefs and artifacts.** VISION §6.3's working language beyond intake: Alex's 👍 on a brief = approval, wired to the Brief→Plan transition (a small reaction listener alongside 6.1b). Persona-side acknowledge-with-a-reaction (👀/✅ instead of a noise message) is prompt-level behaviour, layered in the 5.3 prompts, not new code here.
      **Owns the mechanism, not just the prompt, added 2026-07-25.** This chunk's text assumes persona-side acknowledge-with-a-reaction (👀/✅) is "prompt-level behaviour, layered in the 5.3 prompts, not new code here" — but a prompt cannot add a Slack reaction. `addReaction` exists (`packages/slack`, 3.4a-iii) and is only ever called by `seedReactionLegend` to seed a draft's own 📦/🔁/✅ legend; no persona-initiated path exists, and no tool is exposed to the model. VISION §6.3 lists "👀 / 👍 / ✅ / 🛑 acknowledge without a new message" as working language, so either this chunk wires a react-instead-of-reply path (most likely a tool alongside `report_status`) or 5.3's prompts instruct a behaviour the persona has no mechanism to perform.
- [ ] **6.2 — Worktree sandbox for Riley.** Per-task git worktree, scoped writes per the 6.0 grid, no secrets access (VISION §11's sandboxing model). Code lands only via PR + CI. This is where the Claude Agent SDK enters — bounded multi-step coding sessions are the shape it's built for (VISION §11's chat-vs-agentic line).
- [ ] **6.3a — Riley opens PRs.** Branch/push from the 6.2 worktree, PR body composition (carrying 4.4a's attribution + footer — PRs and review comments are external surfaces too), the GitHub write path. Exit: a real PR appears on chief-clancy from a Build ticket.
- [ ] **6.3b — Dom review pass.** Fetch the diff, an in-voice review process (his own process, his own voice), post approve/request-changes with his reasoning, attributed per 4.4a.
- [ ] **6.3c — Changes-requested → rework loop.** Route Dom's request-changes back to Riley, rework, re-request review. The state machine's edges live here: repeated rounds, stale reviews, who transitions the ticket.
- [ ] **6.3d — Priya QA pass.** Her cross-cutting second review — additive by design: the Brief→merge flow proves out without her, then she joins.
- [ ] **6.4a — CI-result verification.** The deterministic verifier for "tests pass" claims — fetch the CI run, compare, wire to the 1.4 claim gate. Small, and it covers the highest-volume Tier 2/3 claim type.
- [ ] **6.4b — Disjoint-context verifier subagent.** For the remaining Tier 2/3 claim types (VISION §7.6 layer 2): its own prompt, plumbing that feeds it the raw tool-call log and nothing else, an output schema, and handling of verifier disagreement. Wired to the 1.4 claim gate.
- [ ] **6.4c — Auditable status trail.** VISION §7.6 layer 3: every status post threads to (or links) the tool-call log or CI run it's based on, and a claim later found false is corrected in place, not silently edited away.
- [ ] **6.5a-i — Tier routing, shadow only.** The 1.6 classifier routes per VISION §8.1's table verbatim: Tier 0 auto-merge; Tier 1 fast single-approve (Dom's 6.3b approval satisfies it; the merge is then automatic); Tier 2 standard review; Tier 3 mandatory named-owner review that cannot be satisfied by the requesting persona or the human who dispatched the work. Who can review Tier 3 at all on a one-human team is the explicit Alex decision from the 6.0 gate — it is not "Alex reviews" by default. Includes the track-record table migration (schema per the 1.5 ADR), seeded/static rows for now — 6.5b makes it live. Same pattern as 3.3 for the same class of risk: shadow only — log "would have auto-merged / fast-approved / routed" decisions on real PRs; no merge-write capability exists in the code yet.
      **Blocked on two undefined parameters, added 2026-07-25.** Tier 0's gate is "no sensitive path touched; diff below a size threshold" (VISION §8.1) and Tier 3's floor names "Auth, payments, PII/secrets, CI/CD config, migrations" in prose — but neither the sensitive-path matcher nor the size threshold exists as a concrete definition anywhere. `classifyRiskTier` (1.6) takes path/change-shape inputs; something has to decide what feeds it. This chunk cannot even shadow-log a Tier 0 decision without both, so settle them here — a path glob set and a numeric LOC/file threshold, in `docs/decisions/` alongside `TRACK-RECORD-DEFINITION.md`'s own N=5 precedent — before 6.5a-ii turns the switch on.
- [ ] **6.5a-ii — Merge-action executors.** The Tier 0 auto-merge write (the highest-blast-radius action in the plan), the Tier 1 merge-after-Dom-approves path, and route-to-review, behind an Alex-controlled enforcement switch. Check chief-clancy's branch protection first: GitHub deliberately blocks bots/agents from satisfying required-review rules, so if `main` requires a human approval, Tier 0/1 auto-merge needs a repo-config decision with Alex (part of flipping the switch, not a surprise after it). The switch may not flip until 6.5b and 6.5c are merged, the track record is being earned by real merges rather than seeded rows, and the 6.5a-i shadow log looks right. Shadow mode may run without those preconditions; enforcement may not.
- [ ] **6.5b — Track record: earn + revoke.** The write path VISION §8.1/§8.2's trust model runs on. Earn: each unreverted persona-authored merge increments the per-directory counter (threshold N per the 1.5 ADR). Revoke: detect reverts of persona-authored merges; a revert zeroes the affected directory's counter — revocation is mechanical. Also carries §8.2's demotion lever: a manual tier/track-record override writable by Nia or Alex ("drop a persona a tier with no drama"), a one-row write on the same table.
- [ ] **6.5c — 🛑 veto hold.** The Slack reaction listener that holds any in-flight merge on Alex's 🛑. A precondition, with 6.5b, for the 6.5a-ii enforcement flip.
- [ ] **6.6 — Blocked/stuck/kick-back paths.** Plan-was-wrong kick-back, blocked-ticket posting, Nia's stall detection ("you've been quiet for an hour, all good?"). Three small handlers, one chunk each if they grow.
- [ ] **6.7a — Incident posting path + postmortem skeleton.** Gives `#moe-incidents` its behaviour (VISION §9): the posting path plus the blameless-postmortem composition flow, with 6.5b's revert detection as the first producer — a revert of a persona-authored merge posts here with a postmortem. Also settles §9's revert-_execution_ path, not just its detection: on a detected bad persona-authored merge, who authors the revert PR and under what gate it merges ("auto-approved" ≈ a Tier-0-style fast path that skips the normal review queue) — an Alex call inside this chunk. Land after first live merge traffic.
- [ ] **6.7b — Schema-gate-bypass detector.** A §7.6 schema-gate bypass raised as a blocking incident with a postmortem.
- [ ] **6.7c — Unactioned-message detector.** Nia raises a work-shaped message that went unactioned as a Tier-1 incident — a background scan over the intake plumbing with its own state and false-positive tuning.
- [ ] **6.8 — Per-ticket + tier-scaled cost ceilings.** The VISION §10 buckets deferred at 2.6b: the per-ticket ceiling, and caps scaling with risk tier/track record, now that tickets and tiers are both live.
- [ ] **6.9 — Persona-to-persona DMs.** VISION §6.2 promises personas "can DM each other, as genuinely separate, individually-addressable Slack identities", and §6.6's whole one-app-per-persona reversal rests on a persona being a real, DMable identity — but nothing builds a persona _sending_ a DM to another persona. `postMessage` can technically target a user id (that is how cost alerts reach Alex), so the transport exists; what is missing is any path where persona A addresses persona B, plus the receiving side actually treating it as from a teammate rather than a human. **Not a duplicate of the handoff mechanism** — VISION §5.3 settles that work handoffs go through the database (write, then poll-claim), deliberately _not_ a Slack message, and that decision stands. This chunk is for the conversational cases §6.6 names, of which the concrete one already on the plan is Nia's stall detection ("you've been quiet for an hour, all good?"), which is one persona addressing another and is silently undeliverable today. Settle scope with Alex: which persona-to-persona messages are wanted at all, given the risk of bots talking to each other in a loop.
- [ ] **6.10 — Theo's research output.** VISION §6.1 gives `#moe-research` exactly one purpose — "Theo's domain — deep-dives, citations" — and §4.1 has a Researcher on the roster, but no chunk builds the behaviour: nothing produces a research artifact, nothing posts to `#moe-research`, and the channel is in `MOE_WORK_RELEVANT_CHANNEL_IDS` purely as an _intake_ surface. **The risk is that this passes unnoticed:** Stage 5's exit criterion is only "a DM to any of them gets an in-character reply", so Theo satisfies it while doing nothing a researcher does. Sequence after whichever 5.3 sub-chunk lands Theo's prompt (5.3c went to Marcus instead — nothing pins the order, see 5.3's own text), and settle what triggers a deep-dive (a ticket type? an Alex ask? a persona asking Theo?) — the trigger is the real question, not the posting.

## Stage 7 — Ceremonies

**Exit criterion:** one full ceremony cycle (a Friday retro, a Monday replenishment, one monthly review) has run live with Alex participating, and felt like a conversation rather than a template.

- [ ] **7.1 — [GATE] `docs/CEREMONIES.md` consolidation.** Co-author with Alex (do-not-touch surface): consolidate the ceremony mechanics from the v2 chunk-history into the canonical doc. **Decision/writing chunk — blocks the rest of Stage 7.**
      **Must settle multi-persona contribution, added 2026-07-25.** VISION §3.2 calls synthesized qualitative reflection "a real, currently-unclaimed differentiator" against Slackbot/Viktor/MGX/ChatDev, and Stage 7's own exit criterion asks that a ceremony "felt like a conversation rather than a template". Both require personas _other than the ceremony owner_ to contribute — a retro where only Nia speaks is a template. Nothing on the plan gathers per-persona reflections or synthesizes them, and this gate is where the mechanic gets decided (does the owner solicit in-thread and wait? do personas poll for an open ceremony? is there a timeout?) before 7.3b composes and posts anything.
- [ ] **7.2a — Ceremony scheduler.** Cron-shaped triggers (Friday retro, Monday replenishment, first-business-day review), consuming 2.7a's core-hours/bank-holiday module. Ceremony initiation runs through the 3.4a-iii situational-appropriateness gate — VISION §9 requires it for ceremony-initiating personas, and 8.5's full version lands a stage later. No ceremony content — just "it fires at the right time".
- [ ] **7.2b — Alex-away ceremony policy.** Away detection via 2.7b's Slack-status module, plus the explicit per-ceremony policy for what a ceremony does when Alex is away — skip, defer to the next business day, or run without him. That policy is Alex's to set (small decision attached), not an implicit scheduler default.
- [ ] **7.3a — Replenishment section data.** The four sections' data gathering as pure functions over the board/DB. Smallest ceremony first.
- [ ] **7.3b — Replenishment composer + runner.** Sarah's voiced four-section Monday thread: composition + posting, over 7.3a's data.
- [ ] **7.4a-i — Retro round/synthesis logic.** Round definitions, per-round reply aggregation, and the inter-round synthesis prompts as pure, unit-testable functions — the same a/b shape 7.3 and 7.5 use, applied to the hardest ceremony mechanic.
- [ ] **7.4a-ii — Retro thread runner.** Nia's live five-round Friday thread, plain-text rounds only: posting rounds, collecting replies with timeouts, advancing round state, over 7.4a-i's logic. This satisfies "a Friday retro has run live".
- [ ] **7.4b — Retro satisfaction picker.** The interactive component: block-kit UI + interaction handler + persistence of the response.
- [ ] **7.4c — Retro action items.** Extraction from the thread, the ≤3 cap, owner assignment, persistence.
- [ ] **7.5a — Monthly-review section builders.** Per-section data gathering/synthesis as pure, unit-testable functions (VISION §3.2's six-section shape, per the 7.1 doc).
- [ ] **7.5b — Monthly-review composer + runner.** The two-voice ceremony: Sarah's sections, Nia's sections, the handoff between them, reusing 7.3/7.4's plumbing.
- [ ] **7.5c — Monthly-review persistence.** The persisted write-up: format, storage location, and read-back path. The location is decided explicitly here — Fly's deployed `docs/` is effectively read-only, so a data-volume path is the likely answer (the v2 lesson).
- [ ] **7.6 — EOD digest.** Nia's end-of-day post (shipped/in-flight/blocked/cost). Listed last but independent — a floating chunk per the gating rule; pull it earlier if Alex wants it sooner.

## Stage 8 — Memory & growth

**Exit criterion:** a persona references something true from >2 weeks ago unprompted, correctly, in-character — verified by procedure, not by hoping. Seed a known memory when 8.1 lands; from two weeks after, Alex holds a conversation adjacent to it (not asking for it) and judges the recall for accuracy and voice; if it hasn't surfaced naturally within four weeks of 8.5 merging, run that adjacent-topic probe deliberately as the fallback test.

- [ ] **8.1 — Memory store + per-persona namespaces.** `/data/memories/<personaId>/...` with the path guard, `{personaId, projectKey, scope, createdAt}` tagging, save/recall functions.
- [ ] **8.2 — Recall into context.** Persona activation loads `_global` + project + team-lore namespaces. Privacy rule enforced (DM vents never persisted — VISION §6.2).
      **Owes the summaries half, added 2026-07-25.** VISION §7 carries "working memory + summaries" forward as sound-in-concept, and Stage 8's exit criterion — "a persona references something true from >2 weeks ago unprompted, correctly, in-character" — depends on older context being _condensed into something durable_ rather than dropped. Retrieval over raw stored turns is not the same capability: `conversation_turns` (2.4b) accumulates without any condensation step, so without summaries this stage's criterion rests on searching an ever-growing raw log. Decide here whether summaries are a scheduled condensation job, a write-time rollup, or explicitly rejected in favour of raw retrieval.
- [ ] **8.3 — Model-of-Alex loop.** Passive per-persona observations of Alex's working style, recalled into context. No new model calls — piggyback on existing turns.
- [ ] **8.4 — Team lore.** Cross-persona shared namespace; ceremony outputs (retro decisions, review write-ups) land here.
- [ ] **8.5 — Read-the-room guard, full version.** Extends the minimal situational-appropriateness gate shipped at 3.4a-iii (and consumed by 7.2a) to every standing-proactive surface — interjections, stall pings, all ceremony and ambient actions (VISION §9's Viktor lesson). Small classifier-style gate, own chunk so it's testable.

---

## Deliberately not scheduled (parked, with re-entry conditions)

- **Multi-project support** (team.config.ts, cross-project Kanban, onboarding flows) — re-enter when a second real project is actually ready to join (VISION §3.4).
- **SOTA quality additions** (mutation testing, prompt-injection fixtures, extended-thinking review) — re-enter after Stage 0's spine has been running for a few stages (VISION §12's deferral, pending Alex's confirm/override).
- **Interjection-bar mechanism** (unprompted persona observations à la Marcus in VISION §1.3) — re-enter during/after Stage 6, once there's real channel traffic to calibrate against. Until then personas speak when spoken to, in ceremonies, or per intake.
- **Disagreement-escalation chain code** (VISION §9) — re-enter when a real persistent disagreement is observed in practice; the prompt-side pushback half is covered by 5.3's Appendix-C layering.
- **Chief-clancy doc ports** (LIFECYCLE.md, VISUAL-ARCHITECTURE.md) — re-enter once moe has real personas and a working ticket pipeline to describe/diagram (Stage 4+). **`docs/guides/`** — re-enter once moe ships an installable/configurable deployed surface. **`docs/roles/`** — re-enter once moe's own personas exist past scaffold, adopting its one-file-per-role convention. **TECHNICAL-REFERENCE.md, COMPARISON.md** — no near-term moe equivalent, no specific re-entry trigger. Decided at chunk 0.6c; see `CLAUDE.md`'s doc-port paragraph.
- **`docs/INDEX.md`** — re-enter once enough real PRs exist to route against (Stage 1–2 territory).
- **Per-persona learning windows / self-development** (VISION §7's continuous-learning facet) — re-enter after Stage 8 lands and memory is stable.
