# BUILD_PLAN.md — moe v3

> **Sizing discipline (Alex's explicit rule):** every chunk below is small, safe, and simple — roughly one PR, one sitting, one reviewable idea. Anything that turned out bigger during drafting got split. If a chunk turns out to be complex _during implementation_, stop and split it rather than pushing through — a chunk that grows past ~300 changed LOC of real logic (excluding lockfiles/scaffolding boilerplate) is a signal to split, not a target to squeeze under.
>
> **Reading order:** `docs/VISION.md` (product truth) → `CLAUDE.md` (engineering rules) → this file (sequence). Where this file conflicts with VISION.md, VISION.md wins.
>
> **Chunk states:** `[ ]` not started · `[~]` in progress · `[x]` merged · `[GATE]` needs an Alex conversation/decision before starting — not buildable until the gate clears.

---

## How the stages ladder up

```
Stage 0  Repo skeleton + engineering spine        (nothing moe-specific yet)
Stage 1  Core domain: tickets, claims, database    (pure logic + one DB table at a time)
Stage 2  One persona process, one Slack app        (prove the loop with N=1 before N=7)
Stage 3  Intake: chat → ticket                     (the #1 failure-mode fix, on N=1)
Stage 4  GitHub + the board                        (tickets become real work on chief-clancy)
Stage 5  The full cast                             (N=1 → N=7, gated on the cast redline)
Stage 6  Team behaviour: handoffs, review, merge   (personas working together)
Stage 7  Ceremonies                                (retro, replenishment, monthly review)
Stage 8  Memory & growth                           (voice consistency over months)
```

Each stage ends with something observable working end-to-end. **The gating rule, precisely:** a stage starts when the previous stage's _exit criterion_ is met — not when every chunk in it is checked. Chunks an exit criterion doesn't cover may float across the boundary, with these hard orderings: 0.6a–0.6c/0.7 land before Stage 1's first PR; 1.5/1.6 land before Stage 5's first 5.3 sub-chunk (VISION §13.1's item 1 puts the risk-tier gate in the foundation, before any persona exists in prose); 3.6 may trail into Stage 4; 7.6 is independent and may be pulled earlier. That's the anti-scope-spiral mechanism, per VISION §13.

**On VISION §13.1's "the full cast stands up together":** this plan runs N=1 through Stages 2–4 because the plumbing those stages build (Slack transport, intake cascade, GitHub + board) is persona-independent and cheaper to debug on one process than seven — none of it is cross-persona dynamics. The cast then stands up together as one stage (Stage 5), not one-persona-at-a-time rollout. **Resolved at chunk 2.1:** this ordering is confirmed, not amended away — VISION §13.1's item 3 now says so explicitly.

---

## Stage 0 — Repo skeleton + engineering spine

**Exit criterion:** `pnpm test && pnpm lint && pnpm typecheck && pnpm format:check && pnpm knip` green in CI on a hello-world monorepo; the review-gate agents exist and have been exercised once.

- [x] **0.1 — git init + pnpm workspace skeleton.** `git init`, root `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, Volta/engines pin (Node 24), `.gitignore`, empty `packages/core` with one exported function and one passing test. Nothing else.
- [x] **0.2 — Lint/format config to CONVENTIONS.md spec.** ESLint (complexity caps, functional rules, unicorn, sonarjs, n), Prettier + import-ordering plugin, knip (its own config — the exit criterion requires it green), lint-staged + husky pre-commit. Copy chief-clancy's configs, apply the documented deltas (no path aliases, full Zod). One deliberately-failing fixture per rule-family proves the config bites, then delete the fixtures.
- [x] **0.3 — eslint-plugin-boundaries + the settled package graph.** Decide the real package graph (confirm or amend the first-cut table in `docs/CONVENTIONS.md` §Architecture Enforcement), scaffold the empty packages (`core`, `memory`, `agents`, `slack`, `github`, `apps/server`), wire boundaries enforcement. Update CONVENTIONS.md + GIT.md scope labels from "first-cut" to settled in the same PR.
- [x] **0.4 — CI pipeline.** GitHub Actions: the quality suite on every PR, PR-title gitmoji/type check, branch protection on `main`. No deploy automation (per GIT.md §Deploy Flow — deliberate).
- [x] **0.5 — Review-gate agents ported.** Adapt chief-clancy's `da-review.md`, `spec-grill.md`, `copilot-surrogate.md` into `.claude/agents/`, retargeted at moe's doc set. Port `docs/DA-REVIEW.md` + `docs/SELF-REVIEW.md` (the checklists they cite) in the same chunk — the agents are useless without their checklists.
- [x] **0.6a — Process docs.** `docs/DEVELOPMENT.md` (review gate, session handoff incl. moe's PROGRESS.md/SESSIONS.md equivalents — resolves the open question in CLAUDE.md) + `docs/TESTING.md`.
- [x] **0.6b — Review canon.** `docs/RATIONALIZATIONS.md` + `docs/REVIEW-PATTERNS.md`, pre-seeded per VISION §12. Its own PR because 0.5's review-gate agents cite these as authority — the highest-stakes content in the chunk-0 doc set deserves its own review.
- [x] **0.6c — Reference docs + port decision.** `docs/ARCHITECTURE.md`, `docs/GLOSSARY.md`, `docs/decisions/` with README. Also decide here which of chief-clancy's other docs moe ports (CLAUDE.md's open question — see the parked list). `docs/INDEX.md` is deliberately **deferred past Stage 0** — it needs real PRs to route against (CLAUDE.md already says this).
- [x] **0.7 — `generate:agents-md` script.** Port the AGENTS.md generation script (source-only + literal markers) into `scripts/`, wire as `pnpm generate:agents-md`, add a CI check that AGENTS.md is up-to-date with CLAUDE.md.

## Stage 1 — Core domain: tickets, claims, database

**Exit criterion:** two concurrent fake persona processes race to claim the same ticket in an integration test; exactly one wins, every status message about it carries evidence.

- [x] **1.1 — Ticket types + Zod schemas.** `Ticket`, board statuses (`Backlog → Brief → Plan → Build → Review → Done` + `Cancelled`), severity classes, `projectKey`. Pure types + schemas + unit tests. No DB yet.
- [x] **1.2a — [GATE] Process topology × DB access ADR.** **Decided (2026-07-11): N machines, one per persona, backed by Neon Postgres.** Rejects the single-machine + SQLite-on-volume + Litestream shape this chunk originally scoped as the cheaper default — a live-verified deep-research pass found that framing wrong on three fronts: Litestream has an ongoing pattern of silent-replication-failure bugs (one fix merged then reverted, unfixed for 4+ months since, plus two more open issues in the same class — the worst failure mode for claim-critical state); Fly's own platform incident cadence makes host-level blast radius a real, not just theoretical, risk; and the real cost gap is much narrower than assumed once Neon (not Fly Managed Postgres's $38/mo floor) is the comparison point. Full reasoning, rejected alternatives (including Fly's native multi-container Machines API as a better-than-`overmind` single-machine option — still rejected), and evidence: `docs/decisions/TOPOLOGY-AND-DATABASE.md`. Written ADR with Alex. **Decision-only chunk.**
- [x] **1.2b — Database layer + tickets table.** The DB per the 1.2a ADR (Neon Postgres, N machines — one per persona), migrations tooling, the tickets table, plain CRUD. No claiming logic yet.
- [x] **1.3 — Atomic claim.** Compare-and-set claim/release on a ticket (`claimedBy` + version column). Property/concurrency tests: N racing claimants, exactly one winner, no lost updates. This is the orchestrator's load-bearing primitive (VISION §4.5) — it gets its own chunk so it's tested in isolation.
- [x] **1.4 — StatusClaim schema + composer gate.** The `{claim, toolCallId, toolOutputSnippet, timestamp}` type (VISION §7.6), plus the composer function that refuses to emit a status claim without populated evidence and falls back to "not yet verified". Pure logic, heavily unit-tested. **This is failure-mode-#2's fix and it ships before any persona exists.**
- [x] **1.5 — [GATE] Track-record definition ADR.** **Decided (2026-07-12):** minimum track record across directories for a multi-directory diff; preserve on git-detected rename; no transfer to a brand-new directory (Tier 2 floor); threshold **N = 5** consecutive unreverted merges. Full reasoning and rejected alternatives: `docs/decisions/TRACK-RECORD-DEFINITION.md`. Written ADR with Alex. **Decision-only chunk.**
- [x] **1.6 — Risk-tier classifier.** `classifyRiskTier(diffMeta) → Tier 0–3` as a pure function per the ADR from 1.5 + VISION §8.1's table. Property-based tests. No enforcement wiring yet — just the classifier.

## Stage 2 — One persona process, one Slack app

**Exit criterion:** one persona (Sarah, confirmed at 2.1 as front-door) runs on Fly, has her own Slack App, responds to a DM, and every work-status statement she makes carries §7.6 evidence.

- [x] **2.1 — [GATE] Cast redline conversation.** **Decided (2026-07-15):** the roster itself (not just the first persona) is settled — keep the previous 7-role roster (Sarah/PM, Marcus/Architect, Riley/Engineer, Priya/QA, Dom/Reviewer, Theo/Researcher, Nia/Scrum Master) as-is (the research found no evidence for or against reworking it, a symmetric silence, not a confirmation) and confirm **Sarah** as the first/front-door persona for Stage 2. Only the 8th role (Designer) takes the deferred branch — pushed to the 5.0 gate, since no evidence supports adding it now and chief-clancy has no real UI/UX surface yet. Also added a new VISION §8.2 safeguard against a real, research-confirmed risk of named-persona framing (reduced human review scrutiny, blame-shifting onto the AI) the research surfaced along the way. Full reasoning and evidence: `docs/decisions/CAST-ROSTER.md`. VISION §4.1 filled in + `docs/PERSONAS.md` skeleton. Written with Alex.
- [x] **2.2 — Persona process skeleton.** `apps/server` boots, loads a persona config (ID, Slack credentials), connects nothing, health-check endpoint, structured logging with secret redaction. Deployable to Fly as one machine.
- [x] **2.3 — Slack app + inbound events.** One Slack App (the first persona's), **Socket Mode** (settled by a 2026-07-04 docs verification: no public endpoint per app, event retries work the same over Socket Mode as HTTP (~6-min retry window either way), Block Kit interactivity fully supported, 10 connections per app with no workspace cap, and Slack's "HTTP for production" advice targets horizontally-scaled apps — not 7 single-instance always-on processes, which is Socket Mode's stated sweet spot). Receives DMs and channel messages, Zod-validates payloads, normalizes to an internal `InboundMessage`. Replies with a hardcoded acknowledgment — no LLM yet.
- [x] **2.4a — LLM reply loop (stateless).** Wire the **raw Anthropic Messages API** (per VISION §11's verified model-client choice — the Agent SDK's per-query subprocess spawn (~12s) and session-file accumulation make it the wrong layer for chat turns; it returns for Riley's agentic work at 6.2): a single-turn DM produces a coherent reply in a deliberately generic, non-persona placeholder voice — explicitly **not** the persona's character, which is Stage 5 behind the do-not-touch gate. The placeholder prompt lives outside `packages/agents/src/personas/*/prompt.md` so no gated path is touched. This chunk proves the client wiring end-to-end (config, turn invocation with inline tool definitions, reply extraction, error/retry/timeout, post back to Slack).
- [x] **2.4b — Thread-scoped conversation state.** Store and rehydrate per-thread history into the next turn. Keying: a DM channel is one continuous conversation; a channel/group message only accumulates history inside an explicit Slack thread, keeping unrelated people/topics in a busy channel from bleeding together — an un-threaded channel/group message stays fully stateless, same as 2.4a. Trimming: last 20 stored turns (~10 exchanges) rehydrated per call, full history retained in Postgres regardless. New `conversation_turns` table + repository in `packages/core`; `apps/server` gains its first `@moe/core`/DB dependency (an edge already legal in the dependency-direction table, now exercised). Also closes a gap Slack's own event shape creates: the message that opens a channel/group thread never carries `thread_ts` itself (only the replies that follow do), so a small in-memory root-candidate buffer backfills the opening message (and the bot's own reply to it) into a newly-formed thread's history on its first reply. **Removed at chunk 3.3**: once ambient channel/group messages stopped going through the chat-reply path at all (DMs-only), this buffer became unreachable and was deleted rather than left as dead code. A per-thread-key serialization queue prevents two concurrent messages in the same thread from racing on history fetch/persist. Migrations stay a manual pre-deploy step, not run at boot.
- [x] **2.5 — Status messages through the claim gate.** Any status claim now routes through a `report_status` tool call rather than free prose, per VISION §7.6's typed-object requirement — resolved as the correct reading of this line's own acceptance test. Composed via the 1.4 gate. Integration test: persona calls `report_status` with a claim but no backing evidence (no real work tool exists yet at Stage 2) → the message that actually reaches Slack, and gets persisted to conversation history, says "Not yet verified." Known limitation of this chunk's own enforcement, not a VISION-sanctioned exception (VISION §7.6 only names the _misgrounded_-claim gap as an accepted Tier 0/1 tradeoff, not this one): a bare free-prose claim with zero tool call at all still bypasses the gate entirely, since enforcement is a system-prompt instruction, not a mechanical block — closing that fully is Stage 5/6 territory (persona prompting, independent verifiers), not this chunk's job.
- [x] **2.6a — Cost metering substrate.** `persona_cost_daily` table + migration (`packages/core`), accumulated atomically via a real `INSERT ... ON CONFLICT DO UPDATE`, not a read-modify-write from application code — race-tested with concurrent `recordUsage` calls, not just sequential ones. `generateReply` surfaces each turn's real token usage (`packages/agents`), priced against VISION §10's dated Sonnet-5 rates ($2/$10 per MTok through 2026-08-31, then $3/$15) as an integer micro-USD value, not a float. `apps/server` wires an accounting hook into the reply path, mirroring the existing `HistoryStore` DI seam. Accumulates and exposes the numbers only — no caps, no alerts (2.6b). Live-verified against Sarah's real Slack app: a real turn recorded `persona_id=sarah, day=2026-07-17, input_tokens=783, output_tokens=17, cost_usd_micros=1736` — an exact match for introductory pricing.
- [x] **2.6b — Caps + alert ladder.** `MOE_COST_CAP_MONTHLY` (a dollar amount, converted once at the config boundary to micro-USD) + `MOE_COST_ALERT_SLACK_USER_ID` (`packages/agents`'s `parseCostCapConfig`), consuming 2.6a's `persona_cost_daily` via a new `getPersonaCostForMonth` monthly-sum query (`packages/core`). The VISION §10 alert ladder (50/80/100%) is a pure `evaluateCostCap` decision (`packages/agents`) — exact integer-arithmetic threshold comparisons, no float percentage division — with threshold-crossing state persisted in a new `persona_cost_alerts` table via `claimAlertThreshold` — an atomic conditional-update claim (`WHERE highestThresholdAlerted < excluded.highestThresholdAlerted`, same mechanism as `ticket-lifecycle/claim.ts`'s `claimTicket`), not a blind `GREATEST` upsert: the Slack DM only sends when a claim actually wins, so two concurrent turns for the same persona racing on the same newly-crossed threshold can't both send it — race-tested with concurrent `claimAlertThreshold` calls, not just sequential ones. Alex confirmed the two open design points before code moved: hard-halt (not alert-only) at 100%, and a visible halt message to the user (not silence) — both per BUILD_PLAN's own note that this needed confirming inside this chunk; the alert ladder DMs Alex directly rather than a shared channel, also confirmed. `apps/server`'s `generateAndPost` checks the cap before calling `generateReply` at all, so a halted turn never reaches the Anthropic API. The remaining §10 buckets — the per-ticket ceiling and tier/track-record-scaled caps — land at 6.8 once tickets and tiers are live: deferred with a home, not dropped. Live-verified against Sarah's real Slack app: seeded spend at 95% of a $0.01 test cap, one real turn crossed 50% and 80% together (both alert DMs landed with correct text), a second real turn crossed 100% (alert DM landed) and hard-halted — the visible halt reply posted with zero new token usage recorded, confirming `generateReply` was genuinely skipped, not just its output discarded.
- [x] **2.7a — Core-hours + bank-holiday guard.** The shared core-hours/weekend/UK-bank-holiday module every persona-initiated behavior consults — VISION §6.4 delegates the concrete parameters here, and §14 makes weekend/bank-holiday rest a hard rule. Parameters settled (Alex confirmed via `AskUserQuestion`, not inferred): core hours 08:30–17:00 Europe/London Mon–Fri (`isWithinCoreHoursWindow`, DST-safe via `Intl`, `packages/core`); holiday source = the GOV.UK bank-holidays API (england-and-wales), cached in-process with a serve-stale-on-failure fallback (`Cached<T,E>`, `createBankHolidaysCache`) — a cold-boot cache with no successful fetch yet fails CLOSED (treats the day as a rest day) rather than guessing, matching §14's hard rule; off-hours policy — proactive sends and intake drafts defer to the next core-hours window (`evaluateOperatingRhythm`), direct DM replies are exempt and always proceed (a DM is reactive engagement, not Moe acting unprompted, so §14's rest rule doesn't reach it) — no wiring needed on the existing reply path for this. No consumer wired yet; this chunk builds the reusable primitive only. Consumed by intake (3.4a-i, wired for real as of that chunk — see its own entry), the pull loop (6.1a-i), and the ceremony scheduler (7.2a).
- [x] **2.7b — Away-detection.** Alex's Slack status text/emoji against a keyword list — settled (Alex confirmed via `AskUserQuestion`, not inferred): text keywords "away"/"holiday"/"OOO" (whole-word, case-insensitive, `isAway` in `packages/slack`); emoji shortcodes widened past BUILD_PLAN's own two named examples (🌴/🏖️) to `palm_tree`/`beach_with_umbrella`/`airplane`/`face_with_thermometer`/`thermometer`/`hospital` — each verified against the real `iamcal/emoji-data` short-name database, not guessed. `fetchSlackStatus` (`users.profile.get`) is a separate Slack API read from 2.7a's pure calendar logic — required a new `users.profile:read` OAuth scope Sarah's app didn't have (discovered live: the first real call returned `missing_scope`). Alex is adding the scope and reinstalling separately from this code change; live verification against the real API is pending that, not yet confirmed as of this commit. No consumer wired yet; first real consumer is 7.2b.

## Stage 3 — Intake: chat → ticket (failure-mode #1's fix)

**Exit criterion:** VISION §0's failure #1, inverted, now passes as a live test — paste a GitHub issue link (or a plain "this needs looking into") in a DM or scoped channel, no @-mention, no command, and a ticket draft appears with a reaction gate. At this stage the issue-link case produces a **link-only draft** (the URL plus whatever the message said) — issue title/body enrichment arrives with 4.1/4.4b, since Stage 3 has no GitHub client.

- [x] **3.1 — [GATE] Stage-1-classifier spike.** Resolved VISION §5.2's open question (`docs/decisions/STAGE-1-CLASSIFIER.md`): scoring method is a single bundled structured-output call (one 0–100 integer confidence score) on Claude Haiku 4.5 — log-probs don't exist on the Claude API at all (on any model), which alone rules that candidate out regardless of prefill's own model-by-model restrictions, so this was really a choice between "model-reported probability" and "a separate scoring call," which are the same mechanism at different points in the pipeline; bundled wins on VISION §5.2's own "fast, cheap" framing. Thresholds — High ≥ 70, Mid 35–69, Low < 35 — calibrated against a live 24-message synthetic eval (8 work requests, 8 banter, 8 ambiguous) run against the real Claude API on both Haiku 4.5 and Sonnet 5; Haiku showed a cleaner separation gap (scores 36–71 entirely unoccupied) at a fraction of Sonnet's cost, the deciding evidence. Spike chunk — the eval script itself was throwaway (not committed); the ADR + VISION.md edit are the permanent, reviewed output.
- [x] **3.2 — Channel scoping (Stage 0 of the cascade).** Config for which channels/DMs are work-relevant surfaces; messages outside scope never reach the classifier. Trivial on purpose. Built as `isSurfaceInScope`/`MessageSurface`/`ChannelScopeConfig` (`packages/core`): a DM to a named persona is always in scope (VISION §5.3 — already unambiguous, handled without Sarah in the loop; this chunk's own design choice is to short-circuit Stage 0 for that case rather than gate on it, since §5.3 doesn't speak to Stage 0 specifically); a channel is in scope only if its ID is in the configured work-relevant set. No open parameter to confirm with Alex here — VISION §6.1's channel roster (`#moe-team`/`#moe-incidents`/`#moe-research` in scope, `#moe-random` out) and §5.3's DM-bypass rule are both already settled, not open questions this chunk needed to resolve. No default config constant, unlike 2.7a/2.7b's `DEFAULT_*` values — real Slack channel IDs are workspace-specific artifacts with no meaningful code-level default; that resolution happens whenever a real listener is wired against this. No consumer wired yet at this chunk, same as 2.7a/2.7b — this chunk builds the reusable primitive only (consumer wired at 3.3).
- [x] **3.3 — Classifier gate (Stage 1 of the cascade).** The cheap per-message call per the 3.1 ADR, returning a confidence score. Logged, not yet acted on — run it silently in prod for a few days of real traffic to sanity-check the thresholds before 3.4a-i. Built as `classifyMessageConfidence` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`) wired into `apps/server/src/handle-inbound-message.ts`. A real design question surfaced mid-chunk, resolved with Alex before code moved: the existing reply handler generated a full conversational LLM reply to every inbound message, DM or ambient channel/group alike, with no Stage-0/1 distinction — a gap from before Stage 3 existed. Resolved as **DMs-only**: an ambient channel/group message now runs Stage 0 (3.2's `isSurfaceInScope`) then, if in scope, Stage 1 (this chunk) and logs the score — no reply posted, ever, for that surface; a DM is unaffected, unchanged from 2.4a/2.4b. Direct, confirmed consequence: `root-candidate-buffer.ts` (2.4b, backfilling an unthreaded channel message's root once a reply first threaded it) and `handleUnthreadedMessage` became entirely unreachable once ambient messages never reach the chat-reply path at all — removed rather than left as dead code (Alex confirmed before deleting). DA review caught 1 Critical (the classifier's own real, billed Anthropic call shipped completely uncapped and unaccounted-for — bypassing 2.6a/2.6b's cost-cap/hard-halt mechanism entirely, on a call site VISION §5.2 itself notes fires on every in-scope ambient message, not just addressed ones; fixed by adding `usage` to `classifyMessageConfidence`'s result, a new `haikuCostUsdMicros` pricing function, and wiring `checkCostCapAndAlert`/`recordUsageLogged` into the ambient path exactly as the DM path already does) + 1 Material (a TSDoc claim about when the classifier's `parsed_output` comes back `null` was wrong per the installed SDK's actual source — a schema/JSON-parse failure throws a bare `AnthropicError`, not `null`; fixed with a corrected TSDoc and a new `invalid-classification-output` error kind distinguishing that case from a genuine `APIError` network/rate-limit failure) + 1 Low (ambient classification isn't yet scoped to Sarah's process specifically — harmless at N=1 persona, a forward-reference for whichever Stage 5+ chunk adds a second persona sharing a channel).
- [x] **3.4a-i — High-band auto-draft.** High → LLM-composed ticket draft from the thread, posted in-thread with the 📦/🔁/✅ reaction legend (✅ commit the draft as a ticket; 🔁 redo — regenerate from the thread; 📦 park it to Backlog untriaged). Reactions are inert here — logged, not yet acted on — and auto-posting stays in log-only mode until 3.4a-iii's gate is wired in front of it. Respects the 2.7a operating-rhythm guard. Built as `classifyConfidenceBand` (`packages/core`, a pure `number → ConfidenceBand` function, thresholds from `docs/decisions/STAGE-1-CLASSIFIER.md`) and `composeTicketDraft` (`packages/agents`, Claude Sonnet 5, `zodOutputFormat` + `.messages.parse()`, a link-only draft — no enrichment beyond the source message, since Stage 3 has no GitHub client yet), wired into `handleAmbientChannelMessage` (`apps/server`, extracted from `handle-inbound-message.ts` to stay under `max-lines` once this chunk's logic landed). A genuine scope ambiguity surfaced mid-chunk, resolved with Alex before code moved: this chunk's own text reads like a real Slack post ("posted in-thread with... reaction legend"), but no reaction-listener or ticket-commit path exists yet (that's 3.4a-ii) and VISION §9's situational-appropriateness gate doesn't exist yet either (3.4a-iii) — resolved as **shadow/log-only**, same precedent as chunk 3.3 for the same class of risk (chunk 6.5a-i's own plan later follows this same pattern too, per its own text): the draft and its intended reaction legend are logged (`'would post high-band ticket draft'`), never actually posted to Slack. A second design gap surfaced during docs sync, after the shadow-mode implementation otherwise looked complete: this chunk's own text also names the 2.7a operating-rhythm guard as a requirement, which the first implementation pass had not wired in. Fixed before opening a PR, not left for review to catch: `evaluateOperatingRhythm` now gates `composeAndLogDraft` specifically (not the classification step above it, which 2.7a's own settled off-hours policy doesn't name), consuming a `bankHolidaysCache` constructed once via `createBankHolidaysCache()` at process startup (`start-slack-listener.ts`) and threaded through `HandlerDeps`. Gated by its own fresh `checkCostCapAndAlert` call and accounted via `sonnetCostUsdMicros`, same "every real, billed call site needs this from the start" lesson chunk 3.3's own DA review taught, applied proactively here rather than caught in review. DA review caught 2 Material (both R2-confirmed): a chronology-inverted precedent citation — three sites said "same precedent as chunk 6.5a-i," but 6.5a-i is itself unbuilt and its own text cites chunk 3.3 as _its_ precedent, not the other way around; fixed to cite 3.3 directly. And `docs/REVIEW-PATTERNS.md`'s pre-seeded Core-hours-guard pattern went un-graduated despite this chunk being the exact predicted failure firing for real (the first wired consumer's initial pass omitted the guard) — fixed with a real `Caught:` citation. Plus 2 Low, both deferred rather than fixed: `composeAndLogDraft`'s cost-cap check runs before the cheaper operating-rhythm check (a minor DB-round-trip cost during off-hours, not a correctness issue — reordering would also require pinning wall-clock time into an existing cost-cap-only test that currently doesn't need to); and `handleAmbientChannelMessage` (its own new file this chunk) has no co-located test file, tested only via `handle-inbound-message.test.ts` — a file-organization gap, not a coverage gap, flagged as a follow-up rather than restructured mid-review.
- [x] **3.4a-ii — Reaction listener + outcome paths.** The Slack reaction-event handler (short-name mapping, parent-message state) and the three outcomes: ✅'s ticket-commit write path, 🔁's regeneration loop, 📦's park-to-Backlog transition. A genuine chunk-sequencing conflict surfaced before any code moved, resolved with Alex via `AskUserQuestion`: this chunk's own text describes a real reaction listener reacting to a real posted message, but 3.4a-i shipped shadow/log-only (no real post) and VISION §9 requires 3.4a-iii's situational-appropriateness gate to exist before the first real standing-proactive action — resolved as **build the primitive, no live consumer yet**, same shape as 2.7a/2.7b/3.2's own precedent. Built as `classifyReactionOutcome` (`packages/slack`, a pure reaction-short-name → outcome mapping — ✅/🔁/📦 short-names verified against the real `iamcal/emoji-data` database, not guessed: `white_check_mark`/`repeat`/`package`, all three unambiguous, no aliasing gotcha this time), `rawSlackReactionEventSchema`/`isProcessableReactionEvent`/`normalizeInboundReaction` (`packages/slack`, mirroring `raw-message-event.ts`'s own raw-schema/type-guard/normalize-function shape for the `reaction_added` event type), a new `pending_ticket_drafts` table + repository (`packages/core`'s `create`/`getByMessage`/`resolve`/`updateContent`, the "parent-message state" this chunk's own text names — `resolve`'s atomic CAS mirrors chunk 1.3's `claimTicket` exactly, guarding the ✅/📦 outcomes against a double-fired reaction), and three outcome-path functions plus a dispatching `handleReactionAdded` (`apps/server`'s `reaction-outcome-actions.ts`/`handle-reaction-added.ts`): ✅ commits a real `Brief` ticket, 📦 parks a real `Backlog` ticket (both `projectKey: 'chief-clancy'`, VISION §3.4's settled single-project scope — not an open parameter; `severity: 'Medium'`, a fixed placeholder Alex confirmed via `AskUserQuestion` since VISION §5.4's trust-erosion rule keeps severity assignment off the LLM layer too, same reasoning that already keeps `composeTicketDraft` from producing one), and 🔁 recomposes from the _original_ source message (not the previous draft's own text) via `composeTicketDraft`, with its own fresh `checkCostCapAndAlert` check and `sonnetCostUsdMicros` accounting — the fourth real, billed call site this cascade has needed this treatment for, applied proactively from the start. `handleReactionAdded` also ignores any reaction (including 🔁) on an already-resolved draft, a defense-in-depth check in front of `resolve`'s own CAS. No Socket Mode `reaction_added` listener is registered against a real process — `socket-mode-listener.ts` itself is untouched, though `start-slack-listener.ts` DOES gain this chunk's `ticketStore`/`draftStore` construction and threading into `HandlerDeps`, it just never calls `handleReactionAdded` — and 3.4a-i's own draft composition still doesn't persist a `pending_ticket_drafts` row, so both halves of the real end-to-end loop wait for 3.4a-iii, per that chunk's own text ("wiring it in front of 3.4a-i completes the acceptance-test path"). DA review caught 3 Material (all R2-confirmed): (1) `classifyReactionOutcome`'s lookup was a plain object literal keyed by an external Slack reaction short-name — a custom workspace emoji named `constructor`/`__proto__`/`toString`/`hasOwnProperty` would have resolved to a truthy `Object.prototype` member instead of `undefined`, misdispatching as a real, billed 🔁 redo; fixed by switching to a `Map`, with regression tests for all four names. (2) A stale "untouched" claim about `start-slack-listener.ts` repeated verbatim in this bullet, `handle-reaction-added.ts`'s own comment, and `docs/GLOSSARY.md`'s Reaction-gate-outcomes entry (R2 independently found the third site) — corrected in all three. (3) No self-authored-reaction filter exists anywhere in the pipeline, unlike `raw-message-event.ts`'s sibling `bot_id` filter — once 3.4a-iii's own `reactions.add` call seeds the legend onto the persona's own posted message, that action itself emits a `reaction_added` event this handler would (mis)dispatch against; no `botUserId`-equivalent field exists in `PersonaConfig`/`HandlerDeps` to filter it with. Documented as an explicit known gap 3.4a-iii must close (a forward-reference, not a fix, since building it now would mean guessing at a shape 3.4a-iii's own real wiring might not need) rather than built this chunk — DA's own suggested fix offered this as an acceptable resolution alongside building it early. Plus 2 Low: `updatePendingTicketDraftContent`'s redo write has no `resolvedAt` guard, left as a documented trade-off (a narrow, already-bounded-by-the-cost-cap race with a concurrent commit, not a correctness risk); and a redundant type intersection in `handle-reaction-added.ts`, fixed since it was trivial.
- [x] **3.4a-iii — Minimal situational-appropriateness gate.** Its own testable module, consulted before any auto-post — VISION §9 requires the check before the _first_ standing-proactive action, not at Stage 8; 8.5 extends this gate, it doesn't introduce it. Wiring it in front of 3.4a-i completes the acceptance-test path. Alex confirmed via `AskUserQuestion` on two genuinely open parameters BUILD_PLAN's own text left unspecified: the gate mechanism (an LLM content classifier, not a keyword list or external toggle) and its scope (gates 3.4a-i's own draft-posting only, not 3.4a-ii's reaction-outcome dispatch — a human's own reaction is a response to the bot, not the bot acting unprompted, same reactive/proactive distinction 2.7a's core-hours guard already draws for DM replies). A third question — how much of this chunk's terse text to build in one PR — was also put to Alex rather than guessed or silently split: confirmed doing all of it in one chunk (the gate itself, wiring it into draft composition, flipping 3.4a-i's shadow-mode to a real Slack post, persisting the `pending_ticket_drafts` row on the real posted message, seeding real `reactions.add` calls, the self-authored-reaction filter 3.4a-ii's own DA review flagged as a known gap, and registering the real Socket Mode `reaction_added` listener) — completing both halves of the acceptance-test path 3.4a-i/3.4a-ii separately deferred. Built as `evaluateSituationalAppropriateness` (`packages/agents`, Claude Haiku 4.5, `zodOutputFormat` + `.messages.parse()`, `{appropriate: boolean, reasoning: string}`) — **fails CLOSED** on any gate failure (an API error, not just `appropriate: false`), the opposite of `checkCostCapAndAlert`'s fail-open design, since an uncertain safety signal should block the risky action, same "better to wrongly rest once" reasoning as 2.7a's bank-holiday guard. `postMessage`'s own success result (`packages/slack`) was extended to require `ts` on success — a genuinely necessary, backward-compatible API change (verified its only other caller, the DM-reply path, never reads it) needed to key the persisted `pending_ticket_drafts` row and seed reactions against the real posted message's own identity, not the source message's. `addReaction` (`packages/slack`, mirroring `postMessage`'s own wrapper pattern) seeds the 📦/🔁/✅ legend sequentially via a promise-chain reduce, logging (not aborting) a single failed reaction so the rest still get seeded. The self-authored-reaction filter is built one layer down from `apps/server`'s own dispatcher, in `@moe/slack`: `fetchBotUserId` (a thin `auth.test` wrapper) is called once at process startup, and `handleSocketModeReactionEvent` (mirroring `handle-socket-mode-event.ts`'s own `message`-event shape) compares each `reaction_added` event's `user` against it before ever calling `onReactionAdded` — `apps/server`'s `handleReactionAdded`/`reaction-outcome-actions.ts` never need to know about bot identity at all. `createSocketModeListener` gained a real `reaction_added` registration alongside `message`, and `start-slack-listener.ts` sequences `fetchBotUserId` before constructing the listener (an unrecoverable-class failure here — same as a bad Socket Mode token — exits the process rather than starting with no self-filter).
- [ ] **3.4b — Mid-band confirming question.** One short confirming question plus handling the answer: yes → draft via the 3.4a-i path; no or silence → log via the 3.4c path.
- [ ] **3.4c — Review-queue table + Low-band silent log.** The review-queue table (new table + migration, shared with 3.5) and the Low-band silent write. Natural prerequisite for 3.5.
- [ ] **3.5 — Review-queue sweep.** A small on-demand (or daily) job listing low-confidence messages for human eyeballing — the "nothing is silently eaten" backstop. Deliberately boring: a formatted Slack post, not a dashboard.
- [ ] **3.6 — Draft-outcome tracking.** Count High-band drafts by outcome — ✅-committed vs 🔁-redone vs ignored — because the ignored/rejected-draft rate is VISION §5.4's named production metric for this whole cascade, not an academic precision score. Surface the counts in the 3.5 sweep post (and later the 7.6 digest). Small; may trail into Stage 4, but starts accumulating early so calibration has data.

## Stage 4 — GitHub + the board

**Exit criterion:** a ticket created from chat exists as a tracked item against chief-clancy, and an externally-opened GitHub issue appears in the triage queue and can be linked to a ticket. Persona-driven conversion of a triage-queue entry into a board ticket is deliberately not a Stage-4 capability — it's Sarah's Brief-stage triage behaviour and lands at 6.1b, where the pull loop lives.

- [ ] **4.1 — GitHub App auth + client.** App credentials, installation token flow, boot-time key-validation guard (the v2 outage lesson — fail loudly at startup on a bad key, naming the fix). Read-only endpoints first.
- [ ] **4.2 — Issue discovery (poll).** Poll chief-clancy's open issues into a triage queue table. No webhook dependency (optional fast-path later).
- [ ] **4.3 — [GATE] Board home + capacity-model decision.** Where does the board live — GitHub Projects v2 (old VISION's answer), or just moe's own DB with GitHub issues as the mirror? One-conversation decision with Alex; the answer shapes 4.4b. **This gate's required output also includes single-project WIP limits + classes of service** — VISION §3.1/§3.4 say that question closes before board code is written, so it's settled here, not optionally folded in later. If the chosen classes of service need a ticket-schema field, that's a named follow-up migration to 1.1/1.2b, decided here rather than silently absorbed. **Decision-only chunk.**
- [ ] **4.4a — External-post attribution (AI transparency).** Every externally-visible artifact moe writes — GitHub issues now, comments/PRs/reviews at Stage 6 — carries VISION §6.7's three layers: bot identity, persona attribution in the message body, and a footer escape-hatch to Alex. §6.7 is non-negotiable for external humans, so this lands **before the first GitHub write** — the write path never exists without it.
- [ ] **4.4b — Outbound ticket → issue creation/link.** Create/link a GitHub issue for a chat-born ticket, per the 4.3 decision. This is the plan's first GitHub _write_: the App-permission/token-scope change from 4.1's read-only posture lands here, with its failure modes, not silently.
- [ ] **4.4c — Reconciliation poll.** Reflect external issue closure as `Cancelled` — an idempotent background job with its own state mapping. Edge cases decided here: reopened issues, closure of an issue whose ticket is mid-Build.
- [ ] **4.5 — WIP limits + classes of service enforcement.** Enforce the numbers decided at the 4.3 gate at pull time. Small config + one guard function — its call site is the 6.1a-ii transition wiring, which wires it in.
- [ ] **4.6 — DB backup + tested restore.** Neon's own point-in-time-recovery/branching story (or logical `pg_dump` backups, per the 1.2a ADR's shape) for the production DB, plus a rehearsed restore path — 1.2b ships migrations with no rollback story, and by Stage 6 this data is load-bearing (track record, review queue, claims), so durability lands before then, not after the first loss. Extends to the `/data` memory files when Stage 8 adds them.

## Stage 5 — The full cast

**Exit criterion:** all personas from the cast redline (2.1, completed at 5.0 if deferred) run as separate processes with separate Slack apps; each has a distinct, Alex-approved voice; a DM to any of them gets an in-character reply.

- [ ] **5.0 — [GATE] Cast redline completion.** 2.1 settled the 7-role roster and confirmed Sarah as first persona, but deferred one thing here: whether to add the 8th role (Designer) — decide that now, since this stage's exit criterion and 5.3's expansion into sub-chunks both depend on the full roster existing. Not a no-op even though most of the roster is already settled.
- [ ] **5.1 — Multi-app credential config + provisioning script.** Persona-keyed `{botToken, appToken, signingSecret}` config schema, plus a Slack **App Manifest API** provisioning script (verified 2026-07-04): one manifest template looped over the persona configs — `apps.manifest.validate` → `create` (~1/min rate limit), capturing `signing_secret`/`client_id`/`oauth_authorize_url` straight into secrets, with manifests kept in-repo as config-as-code (`apps.manifest.update` makes later scope changes a loop, not 7 UI sessions). Alex's genuinely manual tail per app: click the OAuth install URL, generate the `xapp-` app-level token (UI-only, no API), upload the avatar — ~4–5 min/app, one ~45-min session for 7, plus the one-time app-configuration-token generation.
- [ ] **5.2 — N-process deployment.** The mechanical Fly config per the 1.2a ADR, shared DB access as decided there, per-persona health checks. Genuinely mechanical: the DB engine was chosen and built at 1.2b against the real topology, and 1.3's atomic-claim tests already ran against it — no engine work hides here.
- [ ] **5.3 — Persona prompts, one at a time.** One chunk _per persona_: prompt drafted with Alex (do-not-touch surface — co-authored, not submitted for rubber-stamp), anti-sycophancy patterns layered per VISION Appendix C's table, adversarial-conversation validation, replay recording (from the second persona on — 5.3a ships before the 5.4 harness exists; 5.4 backfills its recording). **Do not batch these.** Expect iteration; pace per Alex's appetite (VISION §13.1). This line expands into N sub-chunks (5.3a, 5.3b, …) once the roster is fixed (2.1/5.0).
- [ ] **5.4 — Persona-replay test harness.** Record/replay infrastructure for persona prompts (the v2 lesson: replays catch prompt↔schema drift that synthetic tests can't). Built once, used by every 5.3 sub-chunk from the second persona on — build it after the first persona proves the prompt shape, not before, and backfill the first persona's recording as part of this chunk.
- [ ] **5.5 — Welcome ritual.** How the team welcomes each newly-activated persona in `#moe-team` — an open question in VISION §4.1, not a settled mechanic, so this chunk opens by settling the format with Alex (its own mini-gate; default proposal: one thread per join), then implements it. Small, pure team-feel, cheap.

## Stage 6 — Team behaviour: handoffs, review, merge

**Exit criterion:** a ticket flows Brief → Plan → Build → Review → merged-to-chief-clancy across at least three different personas, with the risk-tier gate deciding the merge path, without Alex driving. The proving ticket is expected to take the Tier 1 path — Dom's 6.3b approval satisfies the fast single-approve, then the merge is automatic; Alex having flipped the 6.5a-ii enforcement switch beforehand doesn't count as driving.

- [ ] **6.0 — [GATE] Per-persona tool allowlist grid.** Write the grid VISION §11 delegates here ("re-specified in BUILD_PLAN.md rather than reproduced here") with Alex, as config + a doc: each persona's tool allowlist, the CLI-vs-MCP decision rule applied per tool, and the curated-allowlist supply-chain hygiene rules. Enforced via each persona's model-client tool config (inline Messages-API tool definitions for chat turns; the Agent SDK's tool allowlist for Riley's 6.2 agentic sessions), and by 6.2's sandbox for Riley specifically. This chunk is the delegation target of VISION §11's pointer. **This gate also settles who can satisfy Tier 3's named-owner review:** VISION §8.1 excludes both the requesting persona and the human who dispatched the work, and with one human on the team who dispatches most of it, that exclusion needs an explicit Alex answer (see 6.5a-i).
- [ ] **6.1a-i — Pull loop + atomic claim.** Per-persona poll scheduler, persona→stage eligibility mapping, atomic claim (1.3), with a stub no-op work step that releases the ticket back — the loop wakes, claims, "works", releases, and is integration-testable with fake work. Respects the 2.7a operating-rhythm guard.
- [ ] **6.1a-ii — Stage transitions + WIP gate.** The transition + clear-ownership state machine — who moves a ticket forward, when ownership clears, what happens on failure mid-stage — plus WIP-gate wiring (4.5's guard gets its call site here). The Sarah-handoff mechanism from VISION §5.3 (DB-write, poll-claim) falls out of this for free.
- [ ] **6.1b — Brief-stage handler.** What Sarah actually does on claiming a Brief ticket: the LLM triage/brief work and its output artifact, replacing the 6.1a-i stub for that stage. Includes converting 4.2's triage-queue entries into board tickets — the persona half Stage 4's exit criterion deliberately left here.
- [ ] **6.1c — Plan-stage handler.** Marcus's plan work and artifact on claiming a Plan ticket. (Build and Review handlers are 6.2/6.3.)
- [ ] **6.1d — Reaction semantics on briefs and artifacts.** VISION §6.3's working language beyond intake: Alex's 👍 on a brief = approval, wired to the Brief→Plan transition (a small reaction listener alongside 6.1b). Persona-side acknowledge-with-a-reaction (👀/✅ instead of a noise message) is prompt-level behaviour, layered in the 5.3 prompts, not new code here.
- [ ] **6.2 — Worktree sandbox for Riley.** Per-task git worktree, scoped writes per the 6.0 grid, no secrets access (VISION §11's sandboxing model). Code lands only via PR + CI. This is where the Claude Agent SDK enters — bounded multi-step coding sessions are the shape it's built for (VISION §11's chat-vs-agentic line).
- [ ] **6.3a — Riley opens PRs.** Branch/push from the 6.2 worktree, PR body composition (carrying 4.4a's attribution + footer — PRs and review comments are external surfaces too), the GitHub write path. Exit: a real PR appears on chief-clancy from a Build ticket.
- [ ] **6.3b — Dom review pass.** Fetch the diff, an in-voice review process (his own process, his own voice), post approve/request-changes with his reasoning, attributed per 4.4a.
- [ ] **6.3c — Changes-requested → rework loop.** Route Dom's request-changes back to Riley, rework, re-request review. The state machine's edges live here: repeated rounds, stale reviews, who transitions the ticket.
- [ ] **6.3d — Priya QA pass.** Her cross-cutting second review — additive by design: the Brief→merge flow proves out without her, then she joins.
- [ ] **6.4a — CI-result verification.** The deterministic verifier for "tests pass" claims — fetch the CI run, compare, wire to the 1.4 claim gate. Small, and it covers the highest-volume Tier 2/3 claim type.
- [ ] **6.4b — Disjoint-context verifier subagent.** For the remaining Tier 2/3 claim types (VISION §7.6 layer 2): its own prompt, plumbing that feeds it the raw tool-call log and nothing else, an output schema, and handling of verifier disagreement. Wired to the 1.4 claim gate.
- [ ] **6.4c — Auditable status trail.** VISION §7.6 layer 3: every status post threads to (or links) the tool-call log or CI run it's based on, and a claim later found false is corrected in place, not silently edited away.
- [ ] **6.5a-i — Tier routing, shadow only.** The 1.6 classifier routes per VISION §8.1's table verbatim: Tier 0 auto-merge; Tier 1 fast single-approve (Dom's 6.3b approval satisfies it; the merge is then automatic); Tier 2 standard review; Tier 3 mandatory named-owner review that cannot be satisfied by the requesting persona or the human who dispatched the work. Who can review Tier 3 at all on a one-human team is the explicit Alex decision from the 6.0 gate — it is not "Alex reviews" by default. Includes the track-record table migration (schema per the 1.5 ADR), seeded/static rows for now — 6.5b makes it live. Same pattern as 3.3 for the same class of risk: shadow only — log "would have auto-merged / fast-approved / routed" decisions on real PRs; no merge-write capability exists in the code yet.
- [ ] **6.5a-ii — Merge-action executors.** The Tier 0 auto-merge write (the highest-blast-radius action in the plan), the Tier 1 merge-after-Dom-approves path, and route-to-review, behind an Alex-controlled enforcement switch. Check chief-clancy's branch protection first: GitHub deliberately blocks bots/agents from satisfying required-review rules, so if `main` requires a human approval, Tier 0/1 auto-merge needs a repo-config decision with Alex (part of flipping the switch, not a surprise after it). The switch may not flip until 6.5b and 6.5c are merged, the track record is being earned by real merges rather than seeded rows, and the 6.5a-i shadow log looks right. Shadow mode may run without those preconditions; enforcement may not.
- [ ] **6.5b — Track record: earn + revoke.** The write path VISION §8.1/§8.2's trust model runs on. Earn: each unreverted persona-authored merge increments the per-directory counter (threshold N per the 1.5 ADR). Revoke: detect reverts of persona-authored merges; a revert zeroes the affected directory's counter — revocation is mechanical. Also carries §8.2's demotion lever: a manual tier/track-record override writable by Nia or Alex ("drop a persona a tier with no drama"), a one-row write on the same table.
- [ ] **6.5c — 🛑 veto hold.** The Slack reaction listener that holds any in-flight merge on Alex's 🛑. A precondition, with 6.5b, for the 6.5a-ii enforcement flip.
- [ ] **6.6 — Blocked/stuck/kick-back paths.** Plan-was-wrong kick-back, blocked-ticket posting, Nia's stall detection ("you've been quiet for an hour, all good?"). Three small handlers, one chunk each if they grow.
- [ ] **6.7a — Incident posting path + postmortem skeleton.** Gives `#moe-incidents` its behaviour (VISION §9): the posting path plus the blameless-postmortem composition flow, with 6.5b's revert detection as the first producer — a revert of a persona-authored merge posts here with a postmortem. Also settles §9's revert-_execution_ path, not just its detection: on a detected bad persona-authored merge, who authors the revert PR and under what gate it merges ("auto-approved" ≈ a Tier-0-style fast path that skips the normal review queue) — an Alex call inside this chunk. Land after first live merge traffic.
- [ ] **6.7b — Schema-gate-bypass detector.** A §7.6 schema-gate bypass raised as a blocking incident with a postmortem.
- [ ] **6.7c — Unactioned-message detector.** Nia raises a work-shaped message that went unactioned as a Tier-1 incident — a background scan over the intake plumbing with its own state and false-positive tuning.
- [ ] **6.8 — Per-ticket + tier-scaled cost ceilings.** The VISION §10 buckets deferred at 2.6b: the per-ticket ceiling, and caps scaling with risk tier/track record, now that tickets and tiers are both live.

## Stage 7 — Ceremonies

**Exit criterion:** one full ceremony cycle (a Friday retro, a Monday replenishment, one monthly review) has run live with Alex participating, and felt like a conversation rather than a template.

- [ ] **7.1 — [GATE] `docs/CEREMONIES.md` consolidation.** Co-author with Alex (do-not-touch surface): consolidate the ceremony mechanics from the v2 chunk-history into the canonical doc. **Decision/writing chunk — blocks the rest of Stage 7.**
- [ ] **7.2a — Ceremony scheduler.** Cron-shaped triggers (Friday retro, Monday replenishment, first-business-day review), consuming 2.7a's core-hours/bank-holiday module. Ceremony initiation runs through the 3.4a-iii situational-appropriateness gate — VISION §9 requires it for ceremony-initiating personas, and 8.5's full version lands a stage later. No ceremony content — just "it fires at the right time".
- [ ] **7.2b — Alex-away ceremony policy.** Away detection via 2.7b's Slack-status module, plus the explicit per-ceremony policy for what a ceremony does when Alex is away — skip, defer to the next business day, or run without him. That policy is Alex's to set (small decision attached), not an implicit scheduler default.
- [ ] **7.3a — Replenishment section data.** The four sections' data gathering as pure functions over the board/DB. Smallest ceremony first.
- [ ] **7.3b — Replenishment composer + runner.** Sarah's voiced four-section Monday thread: composition + posting, over 7.3a's data.
- [ ] **7.4a-i — Retro round/synthesis logic.** Round definitions, per-round reply aggregation, and the inter-round synthesis prompts as pure, unit-testable functions — the same a/b shape 7.3 and 7.5 use, applied to the hardest ceremony mechanic.
- [ ] **7.4a-ii — Retro thread runner.** Nia's live five-round Friday thread, plain-text rounds only: posting rounds, collecting replies with timeouts, advancing round state, over 7.4a-i's logic. This satisfies "a Friday retro has run live".
- [ ] **7.4b — Retro satisfaction picker.** The interactive component: block-kit UI + interaction handler + persistence of the response.
- [ ] **7.4c — Retro action items.** Extraction from the thread, the ≤3 cap, owner assignment, persistence.
- [ ] **7.5a — Monthly-review section builders.** Per-section data gathering/synthesis as pure, unit-testable functions (VISION §3.2's six-section shape, per the 7.1 doc).
- [ ] **7.5b — Monthly-review composer + runner.** The two-voice ceremony: Sarah's sections, Nia's sections, the handoff between them, reusing 7.3/7.4's plumbing.
- [ ] **7.5c — Monthly-review persistence.** The persisted write-up: format, storage location, and read-back path. The location is decided explicitly here — Fly's deployed `docs/` is effectively read-only, so a data-volume path is the likely answer (the v2 lesson).
- [ ] **7.6 — EOD digest.** Nia's end-of-day post (shipped/in-flight/blocked/cost). Listed last but independent — a floating chunk per the gating rule; pull it earlier if Alex wants it sooner.

## Stage 8 — Memory & growth

**Exit criterion:** a persona references something true from >2 weeks ago unprompted, correctly, in-character — verified by procedure, not by hoping. Seed a known memory when 8.1 lands; from two weeks after, Alex holds a conversation adjacent to it (not asking for it) and judges the recall for accuracy and voice; if it hasn't surfaced naturally within four weeks of 8.5 merging, run that adjacent-topic probe deliberately as the fallback test.

- [ ] **8.1 — Memory store + per-persona namespaces.** `/data/memories/<personaId>/...` with the path guard, `{personaId, projectKey, scope, createdAt}` tagging, save/recall functions.
- [ ] **8.2 — Recall into context.** Persona activation loads `_global` + project + team-lore namespaces. Privacy rule enforced (DM vents never persisted — VISION §6.2).
- [ ] **8.3 — Model-of-Alex loop.** Passive per-persona observations of Alex's working style, recalled into context. No new model calls — piggyback on existing turns.
- [ ] **8.4 — Team lore.** Cross-persona shared namespace; ceremony outputs (retro decisions, review write-ups) land here.
- [ ] **8.5 — Read-the-room guard, full version.** Extends the minimal situational-appropriateness gate shipped at 3.4a-iii (and consumed by 7.2a) to every standing-proactive surface — interjections, stall pings, all ceremony and ambient actions (VISION §9's Viktor lesson). Small classifier-style gate, own chunk so it's testable.

---

## Deliberately not scheduled (parked, with re-entry conditions)

- **Multi-project support** (team.config.ts, cross-project Kanban, onboarding flows) — re-enter when a second real project is actually ready to join (VISION §3.4).
- **Maya (designer persona)** — re-enter when a project with real UI/UX surface joins.
- **SOTA quality additions** (mutation testing, prompt-injection fixtures, extended-thinking review) — re-enter after Stage 0's spine has been running for a few stages (VISION §12's deferral, pending Alex's confirm/override).
- **Interjection-bar mechanism** (unprompted persona observations à la Marcus in VISION §1.3) — re-enter during/after Stage 6, once there's real channel traffic to calibrate against. Until then personas speak when spoken to, in ceremonies, or per intake.
- **Disagreement-escalation chain code** (VISION §9) — re-enter when a real persistent disagreement is observed in practice; the prompt-side pushback half is covered by 5.3's Appendix-C layering.
- **Chief-clancy doc ports** (LIFECYCLE.md, VISUAL-ARCHITECTURE.md) — re-enter once moe has real personas and a working ticket pipeline to describe/diagram (Stage 4+). **`docs/guides/`** — re-enter once moe ships an installable/configurable deployed surface. **`docs/roles/`** — re-enter once moe's own personas exist past scaffold, adopting its one-file-per-role convention. **TECHNICAL-REFERENCE.md, COMPARISON.md** — no near-term moe equivalent, no specific re-entry trigger. Decided at chunk 0.6c; see `CLAUDE.md`'s doc-port paragraph.
- **`docs/INDEX.md`** — re-enter once enough real PRs exist to route against (Stage 1–2 territory).
- **Per-persona learning windows / self-development** (VISION §7's continuous-learning facet) — re-enter after Stage 8 lands and memory is stable.
