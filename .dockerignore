node_modules
dist
*.tsbuildinfo
coverage
.git
.github
.vscode
.claude
.env*
!.env.example
# Deploy-time config read by flyctl on the host, never needed inside the image — and excluding it
# keeps a regenerated config from invalidating the build cache.
fly.*.toml
